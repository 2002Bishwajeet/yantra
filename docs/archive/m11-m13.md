# Archive — M11, M12 and M13

Closed tasks, moved out of [`tracker.md`](../../tracker.md) on 2026-09-03 (Y-323) so the live file is
about live work. Nothing here is current.

These 37 rows carry 22,649 bytes, an average of 612 each —
they were written after `CLAUDE.md` §B5 required a row to say what to do and what *done* means and
stop. [m6-m7.md](m6-m7.md) holds the 36 pre-rule rows, which average
5,644.

**The invariants these tasks produced did not move here** — they live with the crate they bind.
Decisions stay in [`docs/adr/`](../adr/), and the designs these rows were minted from are
[D3](../design/03-dashboard-surface.md), [D5](../design/05-workspace-page.md) and
[D6](../design/06-sessions-attention-spend.md).

**What deliberately stayed behind in `tracker.md §3`:** every row that is not `✅ done` — Y-117,
Y-143, Y-144, Y-150, Y-159, Y-325, and the nine landing rows — plus Y-328, the row that records the
second move.

---

## M11 — The work inbox, M12 — Sessions and cost, M13 — The dashboard you can work in *(the rows that closed)*

All three closed on 2026-09-04 (Y-328) in [`tracker.md §2`](../../tracker.md#2-milestones). M13 closed
as *buildable work done*: its phone clause has been measured on nobody's phone.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-161 | A router: `/`, `/m/{machine}`, `/w/{name}` | ✅ done | claude | — | **Done:** each URL reloads into the same view and no daemon changed ([D1](../../docs/design/01-dashboard.md) §1, D1.2). Opened by the owner on 2026-08-09, and it is the precondition [D1.3](../../docs/design/01-dashboard.md), D1.5 and D2.3 all wait on. Why the History API rather than a dependency: [`web/README.md`](../../web/README.md). |
| Y-162 | TanStack Router replaces the hand-rolled one, and the terminal leaves the first load | ✅ done | claude | Y-161 | **Done:** the router is a maintained package, `/w/$name` is code-split, and the first load is **111 kB gzip against 170** ([`web/README.md`](../../web/README.md)). Owner's instruction, 2026-08-09: battle-tested packages in `web/`, custom code only where a package is not worth it — recorded in [CLAUDE.md](../../CLAUDE.md) §B1 and as an amendment on [ADR-0014](../../docs/adr/0014-react-with-the-compiler-for-the-web-ui.md). |
| Y-163 | `yantra doctor`, and `--json` for an installer and an agent to read | ✅ done | claude | — | **Done:** the nine checks in [D2](../../docs/design/02-setup.md) §3.1 report present/absent/unknown per machine, **an *unknown* never reads as an *absent*** (R-23), nothing is written (§3.2), and a test pins the JSON. D2.1 and D2.2; [Y-160](../../tracker.md#3-task-board) is the consumer. `heartbeat` answers *unknown* from any caller that is not the daemon; why, and what running it corrected in D2 itself: [#200](https://github.com/2002Bishwajeet/yantra/pull/200). |
| Y-164 | T3 Code's UI primitives, and the attribution the MIT licence requires | ✅ done | claude | Y-162 | **Done:** fifteen primitives mount under this repo's React, and [`THIRD-PARTY.md`](../../web/src/components/ui/THIRD-PARTY.md) names the commit they came from ([D1](../../docs/design/01-dashboard.md) §7, D1.1). Seven of §7's take list import `lucide-react` and wait on it. |
| Y-165 | TanStack Query replaces the hand-rolled poll in `useLooked.ts` | ✅ done | claude | Y-162 | **Done:** `useQuery`/`useQueries`, the timer and the `AbortController` deleted, and `useLooked` still answers a `Looked<T>` so no call site moved. The envelope stays the daemon's and Query wraps it — `isError` is unreachable, one failure path ([`web/README.md`](../../web/README.md)). [CLAUDE.md](../../CLAUDE.md) §B1. |
| Y-166 | `lucide-react`, and the seven overlay primitives [Y-164](../../tracker.md#3-task-board) could not take | ✅ done | claude | Y-164 | **Done:** D1 §7's take list is complete at the same pinned commit, and the glass surfaces the overlays name are in `index.css` ([D1](../../docs/design/01-dashboard.md) §7, D1.1). Named imports keep `lucide-react` at 0.77 kB gzip; what the overlays cost is Base UI's ([`web/README.md`](../../web/README.md)). |
| Y-167 | One computed verb per workspace card, and the overflow behind it | ✅ done | claude | Y-164, Y-166 | **Done:** each of [D1](../../docs/design/01-dashboard.md) §2's four states renders one primary button and the rest is an overflow (D1.3). The primitives are in ([Y-164](../../tracker.md#3-task-board), [Y-166](../../tracker.md#3-task-board)); this is the first thing to import them, so the React Compiler bail-out count rises here — [`web/README.md`](../../web/README.md) says by how much. Owner's ask, 2026-08-09.<br>**Built 2026-08-09.** `chosen()` in [`columns.tsx`](../../web/src/columns.tsx) reads the agent state and the row draws the one button it names; TERMINAL and EDIT stopped being columns. Where the labels depart from D1 §2, and why, is the dated note on [that section](../../docs/design/01-dashboard.md). **The overflow is `lazy`** — Base UI's menu is 29 kB gzip, a quarter of the fleet's first load, so [`Overflow.tsx`](../../web/src/components/Overflow.tsx) is fetched on the first tap and the trigger is anchored to rather than wrapped. Why: [#207](https://github.com/2002Bishwajeet/yantra/pull/207). |
| Y-168 | Readiness cards on `/` and `/m/$machine`, over a route the daemon does not serve yet | ✅ done | claude | Y-163 | **Done:** every [D2](../../docs/design/02-setup.md) §3.1 check is visible without running a command (D2.3). **Needs a `yantrad` route first** — [Y-163](../../tracker.md#3-task-board) shipped `doctor` as library and CLI only, deliberately, because a route with no consumer is this row built early; the types already derive `Serialize`. `heartbeat` is the one check a browser can answer and the CLI cannot (D2 §3.1, dated note). Owner's ask, 2026-08-09. **Daemon half landed** ([#208](https://github.com/2002Bishwajeet/yantra/pull/208)): `GET /api/readiness` and `GET /api/machines/{name}/readiness`, off the refresh sweep. **Left:** the cards, and the `contract.rs` entry and `contract.gen.ts` types that go with them. |<br>**Built 2026-08-09.** The route is [#208](https://github.com/2002Bishwajeet/yantra/pull/208), the cards [#210](https://github.com/2002Bishwajeet/yantra/pull/210). `heartbeat` is answered from the daemon's beats and then **overruled by the card** against the machines reading — a check is a verdict where an age is not, and [`web/README.md`](../../web/README.md) says why that is the page's job. A machine no workspace names is *not asked* rather than a failed look.
| Y-170 | A `gh` seam in `yantra-core`, proved against the real binary | ✅ done | claude | — | **Done:** `attention.rs` puts `Forge`/`Gh` beside `Inventory`/`Tailscale` — a local CLI behind a trait, faked above and proved below by an `#[ignore]`d test against the real binary, because GitHub does not go in the podman fixture. Five fields requested and no more. The three failures were **measured** against `gh` 2.96.0, none being specified: logged out exits 4; unreachable and 401 both exit 1 and separate on stderr. |
| Y-171 | `yantra ls attention` | ✅ done | claude | Y-170 | **Done:** one table for reviews and issues, since they are one queue to a reader, with the unread count under it — a count is not something to open and every column above is. Titles truncate by **character**: these carry em dashes, and byte slicing panics mid-codepoint. Exit 1 for each of absent, logged out and unreachable, and **0 for an empty inbox** — nothing waiting is an answer, the reading that makes `down` succeed on something already stopped. |
| Y-172 | `refresh.rs` polls the feed and stamps its age | ✅ done | claude | Y-170 | **Done:** a sixth task, and **the only one not on `EVERY`**. `/notifications` answers `X-Poll-Interval: 60`, so the 30 s sweep would poll GitHub at twice the rate its own server asks for; the feed runs at **300 s**. The sweep pays for itself and this one cannot — `ControlPersist=300` means an ssh poll keeps the fleet warm, while a `gh` poll warms nothing and spends the owner's own quota, which their `gh` and `git push` draw on too. |
| Y-173 | `GET /api/attention` reads the snapshot | ✅ done | claude | Y-172 | **Done:** the handler clones the snapshot and returns, touching no network. A missing `gh` surfaces as `looked: "failed"` with the reason rather than an empty-but-ok reading, proved by running the daemon with `gh` off the PATH while `/api/machines` kept answering. **One gap left open on purpose**: no `contract.rs` entry, so nothing machine-checks the shape against `web/src/api.ts` until Y-174 — it has to land with the types, in one change rather than two. |
| Y-175 | Readiness says whether this daemon can reach GitHub | ✅ done | claude | Y-170 | **Done:** its own route, `GET /api/readiness/github`, and **not** a tenth entry in `doctor::Report` — that would draw an answer about *this* host on *every* machine's card, an unearned claim about boxes no ssh session asked, which is exactly the R-23 failure. D2 §3.1 still says nine, with a dated note saying why. The measurement that shapes it: **`gh auth status` cannot tell a token GitHub refused from a GitHub it could not reach** — both print `The token in keyring is invalid` and exit 1, and neither names `gh auth login`. So only the two failures `gh` states outright are `absent`; everything else is `unknown`. Proved in a private netns across five cases, including a valid credential with the network down, which reads `unknown` and never `absent`. Every detail string is written here, never quoted from `gh`, because its output names the account (§B4). |
| Y-176 | `sessions::list` reaches a machine no workspace names | ✅ done | claude | — | **Done:** the module header said this was forbidden and was **wrong about its own citation** — ADR-0009 forbids Yantra *resolving* a name, and passing a tailnet label to `ssh` verbatim resolves nothing. `worth_asking` is the cost control, not a permission rule: `refresh.rs` polls this every 30 s, so a phone (no sshd, ever) and an offline peer (a negative already known) are not paid for repeatedly. Measured on the real tailnet: **6 peers → 2 asked, 0.335 s**, and with no workspaces defined it now finds the fleet where it previously returned nothing. |
| Y-177 | `yantra rm <workspace>`, and the route behind it | ✅ done | claude | — | **Done:** `remove.rs` is [`edit.rs`](../../crates/yantra-core/src/edit.rs)'s refusal with the field removed rather than moved, worded differently because a move can be undone and a delete cannot. `--force` **skips asking** the machine rather than ignoring its answer, so deleting a workspace whose box is off does not wait out a `ConnectTimeout`. A file that will not parse is still deleted and loses only its description — it is the file most wanted gone. Absence is success in both callers: exit 0, and `200 {"removed": false}` rather than a 404, so two tabs deleting one workspace do not both see a failure. |
| Y-178 | Close a session that belongs to no workspace | ✅ done | claude | Y-176 | **Done:** `yantra kill <machine> <session>` and `DELETE /api/machines/{machine}/sessions/{session}`. **Not `down`** — that reads how the agent ended before destroying the pane holding it, and a session Yantra did not start has no ending to read. The part worth testing was the sentence, not the kill: `Tmux::kill` treats absence as success and therefore cannot say which happened, so this asks first. Proved end to end against the MacBook over the tailnet — a session no workspace named, found by [Y-176](../../tracker.md#3-task-board), killed, and absent on the second run — and against a real tmux in podman (§B3). |
| Y-181 | What a session spent, in tokens | ✅ done | claude | — | **Done:** `yantra tokens <workspace>`, summed on the far side and shipped back as **numbers rather than records**, so no conversation crosses the wire. The finding that makes it correct is [**I-61**](../../crates/yantra-core/tracker.md): one API response is written as several records carrying the *same* usage, so summing per record over-counts — **1.80×** on the largest transcript here, a figure plausible enough that nothing would have looked wrong. Dedupe is by `requestId`. `logs::probe`'s find-the-file half became `logs::locate`, so both verbs open one file by one set of rules rather than two that can drift. No total and no dollars: the transcript holds no rate, and that is [Y-182](../../tracker.md#3-task-board). |
| Y-182 | A price table, and the date it was true | ✅ done | claude | Y-181 | **Done:** [`price.rs`](../../crates/yantra-core/src/price.rs) carries two numbers per model — the three cache prices are multipliers of base input, which is how the published table is built — and `AS_OF` prints on the same line as the figure. Two things had to be measured before a rate could be applied, both now [**I-62**](../../crates/yantra-core/tracker.md): a record names `model` again in a tool call's arguments, and `cache_creation_input_tokens` alone cannot be priced. On the largest transcript on the MacBook the whole write was hour-long, so the five-minute rate reports **$64.65** against a true **$69.44**. What the table cannot price it declines to price: an unknown model shows unpriced rather than free, and a fast-mode response withholds the money entirely. **Sonnet 5's introductory rate expires 2026-08-31** — the first thing to re-read here. |
| Y-184 | Ask a machine about a path before a workspace names it | ✅ done | claude | [ADR-0019](../../docs/adr/0019-a-probe-that-asks-a-machine-is-a-post.md) | **Done:** `yantra probe <machine> <path>` and `POST /api/machines/{machine}/probe`, answering *is it there* and *what origin does it hold* in **one round trip** — the second question only matters when the first says yes, and a person waits on both. The blocker was a decision rather than code, and the owner took it on 2026-08-11: [ADR-0019](../../docs/adr/0019-a-probe-that-asks-a-machine-is-a-post.md). Folding it into `refresh.rs` was never available — no snapshot holds an answer about a path nobody has typed. `git`'s own failure is swallowed: *not a repository* and *no origin* are one answer, and neither fails a probe whose first question succeeded. Proved against the MacBook (`/Users/biswa/.config/nvim` → its real `origin`) and against a real `git` in podman; a `GET` on that path is a **405**, so no read-shaped route exists. |
| Y-185 | The new-workspace form selects rather than fills | ✅ done | — | Y-184 | **Done** when machine, directory and agent are each chosen from what Yantra already knows, and the directory is confirmed present on that machine before the file is written. Owner's instruction, 2026-08-11: **less filling, more selecting.** [`NewWorkspace.tsx`](../../web/src/components/NewWorkspace.tsx) is three free-text fields today and checks none of them against the machine — `up` is what discovers the path was wrong, after the workspace exists. Agent is a list of one (Claude Code), which is the one-agent guardrail rendered rather than relaxed. It becomes the `/new` route rather than a card parked on `/` ([D3](../../docs/design/03-dashboard-surface.md) §3). **Built**: `/new` is a route, which also took Base UI's `field` off the first paint ([Y-194](../../tracker.md#3-task-board)). **Left**: `ui/select`/`ui/combobox` and the directory confirmed through `yantra probe`. **Left is now [D4](../../docs/design/04-workspace-creation.md)**, and it is [Y-300](../../tracker.md#3-task-board)..[Y-303](../../tracker.md#3-task-board) rather than this row: the directory cannot be *selected* until a machine can be asked what it holds. |
| Y-186 | `attention::classify` misses a dead connection | ✅ done | claude | — | **Done:** [Y-170](../../tracker.md#3-task-board) matched only `error connecting to`, which `gh` prints for an *unresolvable host*. A host that resolves and a connection that fails produce Go's `url.Error` instead — `Post "https://api.github.com/graphql": … dial tcp …: connect: connection refused` — and fell through to `Command`, so a network outage read as *`gh` did something unrecognised*. Found by the [Y-175](../../tracker.md#3-task-board) agent against `gh search`; it is worse than reported, since `gh api` does it too. |
| Y-187 | The shell gets navigation and a heading outline | ✅ done | — | — | **Done** when three nav items reach `fleet`/`machines`/`usage`, every route has one `h1`, every group an `h2`, and `<title>` names the route. [D3](../../docs/design/03-dashboard-surface.md) §3, §5.2. |
| Y-188 | `/` groups by who must act next | ✅ done | — | Y-187 | **Done** when the page draws *needs you*, *running* and *idle* from the reads it already takes, an unreachable machine is one row rather than one per workspace, idle collapses past a threshold, and nothing reorders without a tap. [D3](../../docs/design/03-dashboard-surface.md) §4.1, §4.4, §4.6. **Built**: the fourth band D3 §4.1 has no row for is drawn as *Not read yet* and the held order never keeps a row in it — see the §4.1 amendment. |
| Y-189 | `/machines` takes three cards off `/` | ✅ done | — | Y-188 | **Done** when machines, readiness and unclaimed sessions live on `/machines`, `/m/{name}` holds no comparison table, and nothing reachable before is unreachable after. [D3](../../docs/design/03-dashboard-surface.md) §3.1. **Built.** `/m/{name}` draws its machine as a definition list — the shape `DataTable` already gives a row on a phone. Every column keeps a home; `LAST SEEN` is dropped rather than drawn empty where the column was blank anyway (I-39). |
| Y-190 | The states a surface owes a reader, and the one it cannot fix | ✅ done | — | [ADR-0020](../../docs/adr/0020-a-raw-write-only-from-broken-to-valid.md) | **Done** when a pending read draws a skeleton instead of claiming nobody looked, the terminal names *connecting* and *reconnecting*, a page whose every read failed says so once without drawing stale data, and `/w/{name}/repair` edits a workspace file that will not load. [D3](../../docs/design/03-dashboard-surface.md) §7.1–§7.3, §7.5. The last clause is [ADR-0020](../../docs/adr/0020-a-raw-write-only-from-broken-to-valid.md) — a raw write only from broken to valid — with `yantra repair` beside it, spelled `repair` rather than §12.1's `put`. |
| Y-191 | One freshness line, and a footer that counts | ✅ done | — | Y-188 | **Done** when the oldest of the five reads sets one age, a read 30 s behind the rest is named, and the footer counts machines, unreachable machines and unclaimed sessions. [D3](../../docs/design/03-dashboard-surface.md) §4.3. |
| Y-192 | One type, density and clock system | ✅ done | — | — | **Done** when four type sizes and two row heights hold everywhere, numeric cells are monospaced, and every time reads as an age under 24 h and a date over it. **Geist has no `tnum`** — measured, [D3](../../docs/design/03-dashboard-surface.md) §5.5 — so `tabular-nums` is not the fix. §5.3, §5.4, §5.7. |
| Y-193 | Four marks and five colour roles | ✅ done | — | Y-192 | **Done** when a greyscale render still separates *needs you*, *running*, *idle* and *unknown*, and no state uses the accent. [D3](../../docs/design/03-dashboard-surface.md) §6. Two of the three roles are the page foreground until the design system grounds them — D3 §6.2 amendment. |
| Y-194 | Weight and motion | ✅ done | — | — | **Done** when fonts are under 30 kB, first-load JS and CSS have not grown, nothing in app code animates beyond overlays and disclosures, and `prefers-reduced-motion` holds one frame rather than blanking. [D3](../../docs/design/03-dashboard-surface.md) §9 and its 2026-08-22 amendment. Fonts 76,420 B to 29,400 B. **First load 149.73 kB gzip to 144.38 kB against the 145 kB ceiling** — the CSS carried 4.3 kB in rules for files no route imports, and `/usage` is split. `tailwind-merge` was measured as load-bearing and stays. `motion.test.ts` asserts both motion conditions and recomputes the reachable set from the import graph. |
| Y-195 | The forms use the ported primitives, and destructive verbs ask | ✅ done | — | — | **Done** when no hand-rolled input, label or submit button is left in app code, and delete and kill confirm while stop and resume do not. [D3](../../docs/design/03-dashboard-surface.md) §4.7, §7.4. Neither delete nor kill existed in the dashboard; the row built both verbs, not two dialogs. |
| Y-196 | `⌘K` finds workspaces, machines and routes | ✅ done | — | Y-187 | **Done** when the ported `command` primitive navigates and runs no verb. [D3](../../docs/design/03-dashboard-surface.md) §3.2. |
| Y-197 | `/` is the setup checklist when no workspace exists | ✅ done | — | Y-188 | **Done** when a fresh install sees [D2](../../docs/design/02-setup.md) §3.1's checks as the page, readiness re-checks on request over a POST ([ADR-0019](../../docs/adr/0019-a-probe-that-asks-a-machine-is-a-post.md)'s precedent), and the work page returns on the first workspace. [D3](../../docs/design/03-dashboard-surface.md) §4.8. **The sweep cannot supply this page**: `doctor::fleet` reads its machine list from `workspace::list()`, so a fresh install answers `[]` and 404s per machine. The daemon POST landed with no 404 for that reason; the page takes its list from `/api/machines`. [D3](../../docs/design/03-dashboard-surface.md) §4.8 amendment. |
| Y-300 | A machine can be asked what one directory holds | ✅ done | — | — | **Done** when `yantra ls dirs <machine> [path]` lists one level with repositories marked and their origin, `POST /api/machines/{machine}/dirs` publishes it on the same gate as every other write, and a path holding a quote or a newline comes back whole or not at all. [D4](../../docs/design/04-workspace-creation.md) §3, §7.1. **No ADR**: [ADR-0019](../../docs/adr/0019-a-probe-that-asks-a-machine-is-a-post.md) classified it, and D4 §2 measures it at a probe's price. **Built.** Five things in [D4](../../docs/design/04-workspace-creation.md) §3's snippet were wrong and each would have shipped a bug — see its amendment. Records are NUL-separated because a path may hold a newline, and a path that is not there is a **409** rather than an empty list. |
| Y-301 | The directory is walked rather than typed | ✅ done | — | Y-300 | **Done** when each step is one request, typing filters what is listed, a full path typed is probed directly, changing the machine clears it, and no hand-rolled control is left in the form. [D4](../../docs/design/04-workspace-creation.md) §4.1, §4.2. **Built**, then rebuilt as one box by [Y-304](../../tracker.md#3-task-board). Both constraints this row first reported about `ui/combobox` were misreadings of one `Omit<…>` and neither is real. [D4](../../docs/design/04-workspace-creation.md) §8 amendments. |
| Y-302 | Only a proven absence blocks the create | ✅ done | — | Y-301 | **Done** when a directory that is not there blocks and names the machine and the path, a machine that could not be asked allows and says the path is unchecked, and a directory with no git origin blocks nothing. [D4](../../docs/design/04-workspace-creation.md) §5. **Built**, and taking a directory always probes it: a listing says it is there and not what origin it holds. |
| Y-303 | The name is derived and startup is a choice | ✅ done | — | Y-301 | **Done** when the name follows the chosen directory until it is edited, a name `validate_name` would refuse is said before the button, and startup offers claude, a plain shell and a command — with the secrets sentence unchanged. [D4](../../docs/design/04-workspace-creation.md) §4.3, §4.4. **Built**, and §4.4 lost a third of itself to the schema: *claude* and *a plain shell* are the same file, so the form offers claude and a command. |
| Y-304 | The directory is one box holding a path | ✅ done | — | Y-301 | **Done** when the box is the path, typing filters the level it names, `/` walks in and `..` walks up, only crossing a `/` asks the machine, and no second field or up button is left. Owner's instruction, 2026-08-11: **do like what vscode does properly.** [D4](../../docs/design/04-workspace-creation.md) §4.2 amendment. **Built.** Five controls became two and first load fell 2 kB. §8's report of two `ui/combobox` constraints was wrong; see its second amendment for the four that are real. **A real browser then found two faults 238 jsdom tests could not**: the page hung on the first hover (React #185 out of `onItemHighlighted`), and an open list made the confirm unclickable. §4.2 third amendment; [D4](../../docs/design/04-workspace-creation.md) §8 proposes **D4.8** for making that repeatable. |

## Moved 2026-09-04 (Y-328) — the rows that closed M11, M12 and M13

The 30 rows that closed between 2026-09-03 and 2026-09-04, over PRs #226 to #243. Y-300…Y-304 were
already above; the copies Y-323 left on the board were removed rather than moved.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-169 | An unknown `/api` path falls through to the SPA and answers `200 text/html` | ✅ done | — | — | PR #235. `api::router()` answers its own misses; axum hands a nested router's miss to the **outer** fallback, which was the SPA. Reuses `api.rs`'s existing `Missing` shape. **I-64** in [crates/yantrad/tracker.md](../../crates/yantrad/tracker.md). |
| Y-174 | The dashboard draws what needs attention | ✅ done | — | Y-173 | PR #227 built the units. `AttentionBand` is a block inside `Needs you` under its own `h3`, three kinds, each linking to GitHub. Units **Y-314**…**Y-316**. |
| Y-179 | Attach the browser to any session, not only a workspace's | ✅ done | — | Y-320 | PR #239. `/m/$machine/s/$session`, sharing `/w/{name}`'s `Terminal-*` chunk so xterm.js stays off the first load. `Terminal` takes a `Target` mirroring the daemon's enum. [ADR-0022](../../docs/adr/0022-a-socket-may-address-a-session-rather-than-a-workspace.md) settled it; **D6 §8 never named the browser's route** and now carries a dated note saying so. |
| Y-180 | The dashboard draws a session as something to act on | ✅ done | — | Y-177, Y-178 | PR #226 built the unit. `sessionColumns` carries an ACT column; **Y-320** added Terminal beside Kill. Unit **Y-317**. |
| Y-183 | The usage view | ✅ done | — | Y-182 | PR #237. `Answer` and `Figure` in `components/Spend.tsx`, the read in `lib/spend.ts`, the picker left in `Usage.tsx`. The answer names its session, since a transcript's basename **is** the session id. No fleet total, and the refusal is in the page's own copy. MIT attribution in [THIRD-PARTY.md](../../web/src/components/ui/THIRD-PARTY.md). |
| Y-198 | `/w/{name}`: three tabs, the transcript, and the trust prompt in place | ✅ done | — | Y-187 | All nine units closed — **Y-305**…**Y-313**, over PRs #229, #230, #232, #238 and #240. |
| Y-199 | `/usage`, `/settings`, and the push the open page suppresses | ✅ done | — | Y-183 | [ADR-0021](../../docs/adr/0021-the-relay-is-written-to-an-environment-file.md) closed D3 §12.2 on 2026-08-22, and the row still said blocked. `/settings` and `yantra relay` write the relay; `/usage` reads one workspace on request with the price table's date beside the figure; the beacon in [`notify.rs`](../../crates/yantrad/src/notify.rs) keeps a change seen on an open page out of the push, and a restart forgets it. **D3 §11.4 is wrong about the picker** — spend is per workspace, since `tokens` loads a workspace to find its transcript. Building `/usage` found a **priced zero** in shipped code, fixed with it. |
| Y-305 | A tool call carries what it acted on | ✅ done | — | — | **Done** when `Entry.tools` is `Vec<Call>`, the target is the first key present of `command`, `file_path`, `path`, `pattern`, `url`, `query`, `description`, `prompt`, it is capped at 120 characters and a cut one says so, `yantra logs` prints it, and a call with no target prints its name alone. Measured: the input already crosses the wire and the parse discards it. [D5](../../docs/design/05-workspace-page.md) §2.1, §4.2. **Built** in [`logs.rs`](../../crates/yantra-core/src/logs.rs) and [`main.rs`](../../crates/yantra/src/main.rs). The list is ordered, so a `Grep` is named by its `path` and not its `pattern`. |
| Y-306 | The transcript read takes a window and returns a count | ✅ done | — | — | **Done** when the probe takes `before` as `tail -n {lines + before} \| head -n {lines}`, returns `total` from `grep -c` on the same pipeline, and a repo path or session id holding a quote still cannot reach the remote shell. [D5](../../docs/design/05-workspace-page.md) §2.2, §4.4. **Built** in [`logs.rs`](../../crates/yantra-core/src/logs.rs), proved against a real sshd in [`tests/logs.rs`](../../crates/yantra-core/tests/logs.rs). **Only the window is free**: re-measured over 17.8 MB where `grep` is busybox's, the count is a second pass and costs 245 ms. Past the start of the file the window stops rather than emptying, so `total` is how a caller knows to stop. |
| Y-307 | `POST /api/workspaces/{name}/logs` | ✅ done | claude | Y-305, Y-306 | **Done:** `said` in [`write.rs`](../../crates/yantrad/src/write.rs) on `allowed()`, sharing `tokens`'s `from_logs`. The body is `{lines, before}` and absent is the first 50 records. `Transcript` is in [`api.ts`](../../web/src/api.ts) and in [`contract.gen.ts`](../../web/src/contract.gen.ts), so the shape is machine-checked from the first commit. |
| Y-308 | Three tabs, and the URL carries which | ✅ done | — | — | **Done** when `terminal · transcript · spend` are three `Link`s, an explicit `?view=` always wins, the width decides once at 768 px, a resize moves nothing, an unknown value falls back rather than 404s, and a press replaces the history entry. There is no `ui/tabs` and ADR-0014's first rule forbids editing one in. [D5](../../docs/design/05-workspace-page.md) §3. **Built 2026-09-03.** The bar is three `Link`s in [`OneWorkspace.tsx`](../../web/src/routes/OneWorkspace.tsx), over a `?view=` that [`router.ts`](../../web/src/router.ts) validates from [`views.ts`](../../web/src/views.ts) — its own module, so the router does not pull xterm.js into the first load. The width is read in a state initialiser, so nothing subscribes to it. [`workspace.test.tsx`](../../web/src/workspace.test.tsx) asserts each clause; the two unbuilt tabs say they are not built. |
| Y-309 | The transcript view | ✅ done | — | Y-307, Y-308 | PR #238. Text renders as text; no Markdown parser. State is held above the tab, since only the open tab is mounted. Found and fixed a **dead clock on `/usage`**: `useTick` returned nothing, so the compiler reused a `<Stamp>` whose props never changed. |
| Y-310 | A conversation that moved while you paged back | ✅ done | — | Y-309 | PR #238. A grown `total` offers a re-read; nothing is stitched. Asserted against a real JSONL file that gains a record between two reads. |
| Y-311 | The spend tab | ✅ done | — | Y-308 | PR #240. No picker; `Answer`/`Figure` shared with `/usage` via one `useSpend`. **D5 §6.2's premise was false** — `Spend::of` nulls `cost` only when *every* model is unpriced, so a partial sum shipped. Both sections carry dated amendments. **`/usage` changed with it**, and reverses in one predicate. |
| Y-312 | Three tabs against a machine that is not there | ✅ done | — | Y-308, Y-309, Y-311 | PR #240. `components/Machine.tsx` is the machine as a link to `/m/{machine}`, used in each tab's own refusal. No page-level banner. |
| Y-313 | `Terminal` takes a height | ✅ done | — | — | **Done** when the prop defaults to `60vh` and `/w/{name}` passes nothing — which is what gives [D3](../../docs/design/03-dashboard-surface.md) §4.5's twelve rows somewhere to come from. [D5](../../docs/design/05-workspace-page.md) §5.1. **Built 2026-09-03** in [`Terminal.tsx`](../../web/src/components/Terminal.tsx). The default sits at the use site rather than in the signature: the React Compiler declines a whole component over a default in a destructured parameter, measured. |
| Y-314 | The attention band draws | ✅ done | claude | Y-173 | **Done:** [`Attention.tsx`](../../web/src/components/Attention.tsx) under `Needs you`'s rows, fetched by [`Fleet.tsx`](../../web/src/routes/Fleet.tsx); `speaks()` in [`work.ts`](../../web/src/work.ts) keeps an empty queue out. Types in [`api.ts`](../../web/src/api.ts), tests in [`attention.test.tsx`](../../web/src/attention.test.tsx). The contract fixture Y-173 deferred is [Y-322](../../tracker.md#3-task-board). |
| Y-315 | The band carries its own age | ✅ done | claude | Y-314 | **Done:** the band stamps `/api/attention`'s own age and names its 300 s clock. It does not use `Age`, whose stale thresholds are the 30 s sweep's. |
| Y-316 | `gh` that cannot answer says which | ✅ done | claude | Y-314 | **Done:** a failed look draws the daemon's sentence verbatim, a pending one a skeleton, and `never` says nobody has looked. |
| Y-317 | An unclaimed session can be killed | ✅ done | claude | Y-177, Y-178 | **Done** when `sessionColumns` gains an ACT column holding `Kill`, it confirms first, and an already-gone session reads as nothing to kill rather than as a failure. `Act.tsx` exports `Kill` today and nothing renders it. Adoption is refused, not deferred. [D6](../../docs/design/06-sessions-attention-spend.md) §4.2, §4.4.<br>**Built 2026-09-03**: the ACT column in [`columns.tsx`](../../web/src/columns.tsx), tested in [`dashboard.test.tsx`](../../web/src/dashboard.test.tsx). The daemon needed nothing — `DELETE /api/machines/{machine}/sessions/{session}` already answers 200 with `killed: false` for a session that is gone. |
| Y-318 | ADR: a socket may address a session rather than a workspace | ✅ done | — | — | [ADR-0022](../../docs/adr/0022-a-socket-may-address-a-session-rather-than-a-workspace.md), accepted by the owner on 2026-09-03. It unblocks [Y-179](../../tracker.md#3-task-board); the work is Y-319 and Y-320. |
| Y-319 | A session-addressed `plan` | ✅ done | — | Y-318 | PR #236. `plan_on(machine, session, term)`; `plan` loads the workspace and calls it. `remote_command` and `Tmux::pane` are untouched — `tmux.rs` is not in the diff. [ADR-0022](../../docs/adr/0022-a-socket-may-address-a-session-rather-than-a-workspace.md). |
| Y-320 | `GET /api/machines/{machine}/sessions/{session}/terminal` | ✅ done | — | Y-319 | PR #236. A `Target` enum picks the plan; `allowed()`, `bridge` and the ping are the workspace socket's, unchanged. The ACT column's Terminal verb shipped disabled and **Y-179** wired it. |
| Y-321 | The first load is still under budget | ✅ done | — | Y-314, Y-317 | PR #241. **148,096 B of 148,480 — 384 bytes of headroom.** Recorded in [D3](../../docs/design/03-dashboard-surface.md) §9.1, whose *Now* column was three amendments stale. Fonts **29,400 B** against §16's D3.12 target of 30 kB, so that budget is met. **Y-325** is the row that prices the script growth. |
| Y-322 | The attention contract is machine-checked | ✅ done | claude | Y-314 | **Done:** [`contract.rs`](../../crates/yantrad/src/contract.rs)'s fixture holds an `attention` reading, and `attention` in [`contract.gen.ts`](../../web/src/contract.gen.ts) `satisfies Looked<Attention>` — a renamed field on either side is now a red build. `github: None` stays: nothing in `web/src` reads `/readiness/github`. |
| Y-323 | The task board is pruned into the archive | ✅ done | claude | — | **Done:** 73 closed rows moved to [m6-m7.md](../../docs/archive/m6-m7.md) and [m11-m13.md](../../docs/archive/m11-m13.md). §3 went from 268 kB to 42 kB, the file from 342 kB to 114 kB. The pre-rule rows average 5.6 kB and the post-§B5 ones 612 B, which is the case for the rule. |
| Y-324 | §5's ADR index is two rows behind `docs/adr/` | ✅ done | — | — | [ADR-0020](../../docs/adr/0020-a-raw-write-only-from-broken-to-valid.md) and [ADR-0021](../../docs/adr/0021-the-relay-is-written-to-an-environment-file.md) have rows. **It was behind by more than two**: [ADR-0017](../../docs/adr/0017-the-forwarded-address-is-the-caller-when-the-hop-is-ours.md) and [ADR-0022](../../docs/adr/0022-a-socket-may-address-a-session-rather-than-a-workspace.md) still read `proposed` after the owner accepted both. Every row now matches its file. |
| Y-326 | A shared `CARGO_TARGET_DIR` serves one worktree's test binary to another | ✅ done | claude | — | **Done:** the [`justfile`](../../justfile) exports `CARGO_TARGET_DIR` as `target/` beside itself, so the profile's shared value reaches no recipe. Cargo names a workspace unit by its path relative to the workspace root, so two checkouts wrote one binary and shared one fingerprint, and mtimes then chose whose build was fresh. Reproduced with a copy of this checkout carrying one extra test; after the fix `just test` lists only this checkout's tests. |
| Y-327 | `crates/yantrad/llms.txt` lists `src/api.rs` twice | ✅ done | claude | — | **Done:** [the file](../../crates/yantrad/llms.txt) names `refresh.rs`, `notify.rs` and `api.rs` once each. Y-172 and Y-173 had appended to one copy of the block and Y-175 to the other; the tails are joined. |
| Y-329 | A machine name beginning with `-` reaches `ssh`'s argv as an option | ✅ done | claude | — | **Done:** `--` precedes the destination in every `ssh` argv. [`ssh.rs`](../../crates/yantra-core/src/ssh.rs)'s `destination_args` serves `exec` and the pty, and `yantra attach` puts the same `--` before its own. Proved in [`tests/pty.rs`](../../crates/yantra-core/tests/pty.rs): a name of `-V` is refused as a host and no longer prints a version. Closes **I-63** in [crates/yantrad/tracker.md](../../crates/yantrad/tracker.md). |
