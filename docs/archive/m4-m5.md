# Archive — M4 and M5

Closed tasks, moved out of [`tracker.md`](../../tracker.md) on 2026-08-03 so the live file is about
live work. Nothing here is current.

**The invariants these tasks produced did not move here** — they still bind code, so they live with
the crate they bind: [`yantra-core`](../../crates/yantra-core/tracker.md),
[`yantrad`](../../crates/yantrad/tracker.md), [`yantra-agent`](../../crates/yantra-agent/tracker.md).
Decisions stay in [`docs/adr/`](../adr/). Earlier milestones are in [m0-m1.md](m0-m1.md) and
[m2-m3.md](m2-m3.md).

**Tasks still open when this was written did not move:** Y-117, Y-118, Y-120, Y-122 and the eight
landing-site rows live in `tracker.md §3`, which also keeps a one-line stub for every task below that
deferred something, dropped something, or left the plan somewhere other than where it started.

---

## Carried forward from M0–M3 *(the ones that closed here)*

The four rows [m2-m3.md](m2-m3.md) left behind because they were still open on 2026-07-31. Three
closed; the fourth was dropped without ever being built.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-020 | Synthesise research → architecture decisions | ✅ done | claude | Y-010..Y-015 | ADR-0004, 0005, 0006, 0007, 0009, 0010, 0011, 0012 ✅ — and the last one, the telemetry ADR, is now **[ADR-0013](../../docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md)** (left `proposed`, because accepting it was the owner's; accepted 2026-08-03 — Y-103). **Seven payload fields, chosen by the reverse of I-10** — a signal that cannot change a placement decision has no reason to be collected — which cut GPU metrics, uptime, load averages, core count, temperatures and WoL readiness, all of which R5's own fact table collected and R5's own scorer never reads. **I-9 is enforced by the type, not by a rule**: `enum Power { Ac, Battery { percent } }` makes unknown power *unrepresentable*, so the bug that scores every always-on desktop down cannot be written. `os` is excluded because Tailscale already reports it and two sources can disagree. **Identity is the source address**, not a body field, which honours I-5 and I-33 for free and is address→identity rather than name→address — so it does not reopen ADR-0009. Produced **I-50**, which sharpens I-38 and removes the special case the ADR had been braced for. |
| Y-037 | Release rehearsal via `workflow_dispatch` | ✅ done | claude | Y-034 | **Green on the first run** ([30652293147](https://github.com/2002Bishwajeet/yantra/actions/runs/30652293147)), 2026-07-31 — five targets, five archives, `publish release` correctly **skipped** because `if: github.ref_type == 'tag'` and a dispatch is a branch. Version resolved to `0.0.0-dev.<sha>`, so a rehearsal cannot be mistaken for a release. **This is the first evidence the macOS and Windows halves of the matrix work at all** — they had never been built, and [R-19](../../tracker.md#7-risk-register) said cross-compiling to Apple targets from Linux is genuinely hard. The workflow sidesteps R-19 rather than solving it: both Apple triples build on a **macOS runner** with Apple's own toolchain, one runner covering both slices. **R-19 downgraded** accordingly — the risk is real and simply not on Yantra's path. **R-7 is untouched**: what builds for Windows is `yantra-agent`, which is still a stub that prints its version, and Windows' actual problem is that it has no tmux and cannot serve Tailscale SSH. A compiling stub refutes nothing. Archive sizes recorded for M7's budget: **963 KB** aarch64-musl and **998 KB** x86_64-musl for all three binaries, against 72–161 KB for the single-stub platforms. **Still no release**, deliberately — publishing needs a tag, and a version worth tagging. |
| Y-044 | Session state store (`rusqlite`) | 🚫 dropped | claude | Y-043 | **Dropped 2026-08-02 without being built, after going looking for the consumer that would force it and not finding one.** The row's condition — *only if state genuinely can't be derived from tmux* — is met, and the **title** is the part that turned out to be wrong: what this store would have held is already held by three stores Yantra did not write. **Declared state is the workspace TOML** (`~/.config/yantra/workspaces/*.toml`) — the operator's file, on disk, outliving everything. **Live state is tmux**: `pane_start_command` is the exact command Yantra asked the pane to run — repo, agent binary and `--session-id <uuid>` — and tmux updates it on respawn, so it survives `resume` (Y-091, measured on 3.7b and on the 3.5a fixture; `agent::session_id_in` reads it back). **History across a `down` is the agent's transcript JSONL**, written by Claude Code at the path [ADR-0011](../../docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md) made predictable and read by `logs` with no session in the way — the one place Yantra shows the past is a file it neither writes nor owns. Declared, live, historical: each already has an owner and none of them is Yantra.<br>**Five candidate consumers, and what answers each instead of a store.** (1) **History across a `down` — nothing asks Yantra to remember any.** `down` reads the ending off the pane *before* the `SIGTERM`, because that is the only moment it exists (I-4), renders it once, and no verb ever asks again. The one verb that does show the past is `logs`, and it reads a file the agent wrote. (2) **The daemon while tmux is not running** — the snapshot is in memory and dies with the process on purpose. It is rebuilt within `EVERY = 30s`, and Y-071 already decided the harder half of this the other way: **a failed look replaces a good one** rather than being covered by a stale reading, so persisting the snapshot would build precisely the confident lie R-23 names. Nothing survives a daemon restart today and nothing needs to. (3) **The scheduler** — R5's `place()` takes a `hist` and uses it three times, and none of the three needs disk: the **live-session short-circuit** is `up`'s idempotency and is read from tmux; **session affinity within 7 days** is [Q11](../../tracker.md#6-open-questions), whose prior is to drop it as re-solving what the short-circuit already solves; and the **−20 penalty for a failure in the last hour** costs an ordering rather than an answer when it is forgotten. (4) **`yantra-agent`** — push-only by decision, not by omission: [ADR-0013](../../docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) §6.3 writes one row per machine to memory, overwritten every beat; its non-goals rule out metrics history and time series outright; and §7 has the agent **drop** an undeliverable beat rather than queue it, because a heartbeat delivered forty seconds late is a false statement with a timestamp attached. (5) **The repo's own references** — `rusqlite` appears in **no `Cargo.toml` and no `Cargo.lock`**; every mention is prose. I-12, I-13 and I-14 stay parked exactly where [`crates/yantrad/tracker.md`](../../crates/yantrad/tracker.md) already says they are unexercised and two of them unmeasured against the crate they name.<br>**The strongest case for keeping it open is ADR-0013's own *not decided here*, and read to the end it argues the other way**: *"the in-memory row is enough for M4 and M5; it is not enough to answer a question about yesterday, and nobody has asked one."*<br>**What brings it back — the first question about the *past*.** Three are visible from here. **M5's `yantra why`, if it must explain a placement that already happened** rather than the one it would make now: R5's decision record carries a facts snapshot precisely because the telemetry that drove it was ten seconds old and ADR-0013 keeps no history of it, so that answer can only be *kept*, never re-derived — while a `why` that is a dry run over current facts needs nothing. **[Q11](../../tracker.md#6-open-questions) answered "keep session affinity"**, which is the one score term that must know where a workspace ran after its session is gone. **M7's notifications**, if *did I already send this* has to survive a restart. On any of those the task that comes back is **not this one**: it is a placement or notification record rather than session state, and at roughly five placements a day, append-only and read newest-first, the shape to argue for is a file — I-14 already refuses an ORM for four small tables.<br>**Left untouched and reported rather than fixed**: root [`CLAUDE.md`](../../CLAUDE.md) §B1, [`README.md`](../../README.md)'s Stack line and [ADR-0004](../../docs/adr/0004-rust-for-the-daemon.md) all still name `rusqlite`/SQLite as part of the stack. ADR-0004 is immutable (§B5) and the other two summarise it, so correcting two of the three trades one stale claim for a three-way disagreement. Whether ADR-0004 gets an amendment recording that the daemon writes no SQLite is the owner's. |
| Y-066 | Say when an agent is waiting at the trust prompt | ✅ done | claude | Y-059 | **I-49 named, without Yantra ever answering the dialog** (ADR-0011 intact). The hard part was not the message but the discriminator: at the trust prompt the pane is alive and the registry is empty, which is **exactly** what R-2's silent death looks like, so the two sources cannot settle it. The third piece of evidence is the dialog still drawn on the pane — `capture-pane | grep -qF` run **on the far side**, so the pane's contents never cross the wire and one bit comes back — and it is asked **only** in the branch that was already `Unclear`, so the extra round trip buys the one verdict that had no action in it. **The failure direction is the design**: a reworded dialog, a pane too narrow, or an ssh that does not answer all fall back to `Unclear`, never to a wrong name; proven by mutation — breaking the match string made only the new test fail, and it failed *back to `Unclear`*. A registered agent outranks whatever is left on its screen, and the dead-pane path is untouched, so an agent **killed** at the dialog is still `Killed` rather than waiting (its own container test). **Rejected: reading `hasTrustDialogAccepted` from `~/.claude.json`** — it is the cause rather than a symptom, but it fails toward a *false positive*, needs JSON parsing on a fleet without `jq`, and that file holds an account email and prompt history that must never cross the wire. `status` exits **1** and `is_running()` stays false: no work has been done and none will be until a human answers. `logs` now says a transcript appears on the agent's **first message**, not at launch, and points at `status`. Premise re-measured against real Claude Code 2.1.220 by hand — dialog on screen, absent from `agents --json`, no project directory — but the container proves it with a stub, so **that half is `[D]`, not `[V]`**. **151 tests**, 4 skipped. |

## Found by the scope audit, 2026-07-31

An audit against [`docs/brainstorm.md`](../brainstorm.md) — the founding intent, archived unedited
— asking what has drifted versus what was dropped on purpose. **The deliberate half came back clean**:
every major omission it found (Aider, Wake-on-LAN, node_exporter, per-machine `user`/`port`/`identity`,
sending input to an agent, ten heartbeat fields) traces to a written reason in an ADR or a closed
question. These four are what did **not** have one.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-077 | `yantra resume <workspace>` | ✅ done | claude | — | **Found by the scope audit, 2026-07-31, and it was drift rather than a deferral.** [`docs/brainstorm.md`](../../docs/brainstorm.md) names four agent verbs: *Launch. Resume. Stop. View logs.* Yantra shipped three. **Q9 killed Aider on exactly this ground** — *"Yantra's core promise is 'continue where you left off'."*<br>**The premise in the scope-audit note was wrong, and finding that out was most of the task.** It said the machinery already exists because `agent.rs` presets the session UUID — but **nothing persists that UUID**, so `--resume <id>` has no id to pass, and Y-044 is deliberately unbuilt. Measured against the installed 2.1.220, not inferred: a bare `--resume` opens an **interactive picker**, which resumes nothing until a human attaches; `--continue` resolves the last conversation **from the cwd**, which the launch command already `cd`s into; `--continue` alone reuses the *original* session id, costing the predictable transcript path ADR-0011 built `logs` on; and `--continue --session-id <uuid>` is **refused outright** — *"--session-id can only be used with --continue or --resume if --fork-session is also specified."* **So the command is `cd <repo> && exec claude --continue --fork-session --session-id <uuid>`**, which continues the conversation *and* keeps the id Yantra's. Verified end to end: the forked transcript carries the earlier turns, and `logs` follows it because it reads the newest file in the project directory rather than a remembered id.<br>**The loud negative finding: no flag can tell Yantra there was nothing to resume.** `--continue` in a directory with no earlier conversation starts a fresh one and exits **0**. So `resume` on a never-used workspace is `up --agent claude` under another name, and that cannot be detected without the session store this project is trying not to build.<br>**What it refuses rather than guesses**: an agent already running is left alone (exit 0, §B4); one holding at the trust dialog is refused, because it has no conversation to continue and ADR-0011 says Yantra never answers the dialog; a live pane the registry does not know about is refused, because respawning would destroy R-2's evidence to find out what it was; and a workspace with its own `startup` is refused for ADR-0007's reason, as `up --agent` already does. The four *endings* — finished, stopped, crashed, killed — all resume into the **same pane**, via `respawn-pane` (`tmux::respawn_with` extracted as the public `tmux::respawn`), because `remain-on-exit` means the pane outlived its process and I-29 rules out a second session.<br>**Rebasing onto Y-081 opened a gap underneath that respawn, found in review and closed here.** Y-081's far-side `test -d` lives in `up::open`, which the `Plan::Open` path inherits for free — but a respawn reaches tmux directly and skipped it, so a workspace whose session was alive and whose `repo` had since been deleted got `cd '<gone>' && exec claude …`, the `cd` failed, the pane died again, and `resume` reported **`Resumed`**. The same bug Y-081 had just fixed, one verb along, and worse for being *inconsistent between two verbs*. `ensure_repo` is now `pub(crate)` and called from the respawn arm — not from before the match, which would have cost the open path a second round trip for a check it already makes. **The test asserts the pane is still dead afterwards**, not merely that an error came back: an implementation that refused *after* respawning would pass on the error type alone. Confirmed to fail without the fix, with `Resumed(Launch { command: "cd '/tmp/resumerepo' && …" })`. **170 tests**, 4 skipped: 6 unit tests on the pure verdict→action map, and 7 against real sshd + tmux in podman, including a real `kill -9` — the one state a hand-built pane cannot produce. **This probably warrants an ADR amending ADR-0011**, whose *Launch* bullet now describes only half the command surface. |
| Y-078 | `yantra attach <workspace>` | ✅ done | claude | — | **Found by the scope audit.** Attaching is a **printed hint you copy-paste** (`attach_hint` in `crates/yantra/src/main.rs`), not an action. That may well be right — the hint carries the resolved tmux path, the machine and the `TERM` that Y-052/Y-053/Y-058 fought for, and running `ssh -t` from inside the CLI means handing over the terminal — but **the reasoning is written down nowhere**, which makes it indistinguishable from an omission. Decide it, then record it either way. **Decided 2026-08-02: build it**, and the deciding argument was not ergonomics. [Y-081](#) made `up` refuse a workspace whose `repo` has since been deleted — deliberately, and its own row records the cost: *a live session whose repo has since gone cannot be reattached through `up`*. That left a running session with **no way back into it**, and `attach` is the way back, so it deliberately does **not** inherit Y-081's check. A container test deletes the repo under a live session and proves it is still reachable. **It never creates**: `up` is the idempotent open, and a second verb that quietly opened would be a worse `up`. A workspace with no session is `attach::Error::NoSession`, which names the machine as well as the workspace, and a test asserts the asking created nothing. **`exec`, not spawn-and-wait.** The process is replaced by `ssh`, so there is no parent to forward `SIGWINCH`, relay signals or reap a child — the things that would have made this complicated are avoided by not having them. The consequence is that `attach` is **outside the exit-code contract**, which [the crate's notes](../../crates/yantra/CLAUDE.md) now say explicitly; everything it can decide it decides before handing over. **stdin is checked for a terminal first**, because `ssh -t` without one degrades into a session attached to nothing and explains itself in a way nobody reads. Windows gets a printed command and exit 1 — it has no `exec`, and no tmux to reach either (R-7), so Q4 is not foreclosed. **The remote command moved into the library and is shared with the hint**, so what a user copies and what `attach` runs cannot drift; the pre-existing hint test still passes unchanged, which is what proves the strings are identical. Verified on the real fleet: refusals for a missing workspace and a missing session, the terminal guard firing, and — under a pty — `yantra ls sessions` reporting **`ATTACHED 1`**, which is the only proof the `exec` actually handed over. |
| Y-099 | `down` describes a shell session in agent vocabulary | ✅ done | claude | Y-091 | Found while tearing down Y-078's test session: `yantra down` on a workspace opened as a plain shell prints **`agent: killed by SIGTERM, so it ran no shutdown of its own`**. There is no agent, and nothing failed to shut down. Same family as Y-091 — a verb built for M3's agent lifecycle describing M1's shell-only session, which is still the default `up` — and the same fix is available: `down` renders a `Verdict`, and `Verdict::NoAgent` now exists to be rendered. Smaller than Y-091 was, because the discriminator is already built. **`Report.ending` is now `Option<Verdict>`**, `None` when there was no agent to have an ending — the invalid state made unrepresentable rather than guarded by a bool, so the CLI cannot print an ending that does not exist. **The question has to be asked before the `SIGTERM`**: afterwards a shell and an agent that ignored its own shutdown are the same dead pane, which is the whole reason the wording was wrong in the first place. `down` already reads `status` twice for exactly this class of reason (I-4), so this costs no round trip. **Mutation-proved**: dropping the check makes the new container test fail with `Some(NoAgent)` — which `describe` renders as *"agent: no agent — the session was opened as a shell"*, so the naive path was awkward in a second way as well. The four existing `down` tests keep their meaning because the fixture's stub registry claims an agent in `REPO`; the new test opens a session **elsewhere**, which is what makes its pane read as `NoAgent`. Verified on the fleet: a shell session stops with no agent line, an agent session still reports `stopped cleanly (exit 143)`. **185 tests**, 4 skipped. |
| Y-079 | A stale `ControlMaster` socket is never unlinked | ✅ done | claude | — | **Found by the scope audit, and it is a hole in the transport everything runs on.** [ADR-0006](../../docs/adr/0006-ssh-exec-transport.md) defers R7's explicit-master recommendation (`-M -N -f` plus a per-host mutex, `ControlMaster=no` for clients) — **it records the deferral but not the cost.** R7 documents it: a stale control socket is never unlinked, so *"every subsequent exec repeats the message, forever."* Nobody has hit it because `ControlMaster=auto` self-heals, so this is latent rather than urgent. The fix is an unlink-on-stale check, not a redesign. At minimum, amend ADR-0006 so the cost is recorded beside the deferral. **There is no bug, and finding that out inverted the row.** R7's hole belongs to `ControlMaster=no`, which is the *client* half of the explicit-master design ADR-0006 deferred — not to `ControlMaster=auto`, which is what `ssh.rs:179` actually sets. **Measured rather than read**: a live master was `kill -9`ed, the stale socket confirmed on disk and confirmed refusing `-O check`, and the next Yantra command against it exited **0 with zero bytes on stderr** while a new master took the same path over. OpenSSH guards the unlink with `options.control_master != SSHCTL_MASTER_NO`, which is exactly the asymmetry R7 documented. So the deferral is not merely cheaper, it is **safer**, and that is now recorded in [ADR-0006](../../docs/adr/0006-ssh-exec-transport.md) beside the deferral it qualifies — with the warning that adopting R7's recommendation later is not a drop-in, because it must bring `-O check`-then-unlink with it or ship a transport that complains on every command for the life of the socket. **Note the convention this bends**: §B5 calls ADRs immutable once accepted. What was added records a *consequence* of a decision that is unchanged, dated and attributed, rather than altering it. |
| Y-080 | The research index has drifted from the ADRs | ✅ done | claude | — | **Found by the scope audit.** [`docs/research/README.md`](../../docs/research/README.md) omits R7 entirely and cross-references two ADR numbers since taken by different decisions (→ *ADR-0007 telemetry source*, → *ADR-0006 agent plugin interface*); [`04-workspace-prior-art.md`](../../docs/research/04-workspace-prior-art.md) points at ADR-0005/0006 for the workspace schema, which is **ADR-0007**. Exactly the defect §5 already caught and fixed once on 2026-07-30 — **the index drifts, the ADRs do not** — so the second occurrence argues for a link check in CI rather than a third manual sweep. **A link check was written and then not kept**: it found **0 broken relative links across 53 markdown files**, and neither drift occurrence would have tripped it — both times the reference resolved to a *real* ADR that was about something else. It was the wrong tool for the failure that keeps happening. **The fix is to delete the duplicated fact instead.** The index's `Blocks` column now links to ADRs and never names them, because an ADR's subject is written down in exactly one place — its filename — and a number with no label cannot say the wrong thing. R1 → ADR-0013, R3 → ADR-0011, R4 → ADR-0007. R4's own body carried **four** more stale pointers, not the one this row claimed; the three that were *recommendations* now name the subject rather than a number, because the note predates the numbering and inventing one would be manufacturing history. **The worst of it was in this file.** Three places claimed provisioning-as-a-non-goal lived in an ADR — the decisions table said ADR-0007 carries it, R-6's mitigation column said ADR-0005 does, and the guardrail said it was still to be written into ADR-0005. **ADR-0007 contains the word zero times**, ADR-0005 is the library-crate decision, and the non-goal is actually recorded in §1 here and in CONTRIBUTING.md. A risk register asserting a mitigation that is not where it says is worse than the index drift that started this. All three now point at where it really is. |

## Found by verifying the CLI, 2026-07-31

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-081 | `up` reports success when the repo path does not exist | ✅ done | claude | — | **Measured against `bishwajeets-macbook-pro`, not inferred**, and it reproduces on Linux too — `tmux new-session -c <a path that is not there>` exits **0** and puts the pane in `$HOME` on 3.7b (Arch) and on 3.5a (the Alpine fixture) exactly as it did on the MacBook, so this was never a macOS quirk. `up::open` now runs `test -d {repo}` on the far side (ADR-0009 — the path is a path on *that* machine) before tmux is touched, and returns `up::Error::NoRepo`, which names the workspace, the path **and** the machine because any one of the three can be the thing that is wrong. Exit 1 needed no change: `crates/yantra/src/main.rs` already maps every `up::Error` to `ExitCode::FAILURE`, and 2 stays clap's. **Three decisions.** *Where*: in `open<E: Exec>` rather than `up(name, …)`, because `open` is the half the container tests drive, so the refusal is provable against a real sshd. *Round trips*: **one extra**, declared. It does not fold into the `new-session` exec — that would put `up`'s concern inside `tmux.rs` and make a missing directory indistinguishable from a tmux failure. *The already-open case*: **refused too**. Checking only when creating needs a `has-session` probe first, which is I-1's TOCTOU race in a new place, and `up`'s attach hint promises a session **at the repo** — a promise a deleted repo breaks in both branches. The cost is that a live session whose repo has since gone cannot be reattached through `up`; `yantra ls sessions` still lists it and `yantra down` still stops it. **The test asserts the absence of the session on the far side**, not merely the error type — an implementation that opens first and complains second passes the second and fails the first. It then creates the directory and checks the same workspace opens and that a second `up` still attaches (§B4). A companion container test proves a `repo` carrying `'; touch …; '` never executes against a real `/bin/sh` (I-26), with the exact-string unit test beside it in `up.rs`, the shape `tests/agent.rs` uses. **154 tests**, 4 skipped. |
| Y-082 | One example workspace name, and it is `yantra` | ✅ done | claude | — | The docs used `nexus` as the placeholder workspace while the tracker's own milestone definitions used `demo` — two invented names for the same slot, and the user-facing one read like a Yantra concept rather than an example. It came from the original README's diagram and spread to the crate READMEs and [architecture.md](../../docs/architecture.md) with Y-067 and Y-074. **Now `yantra`**, which is the project's name and an accurate example: the first workspace anyone here opens is this repo. The word survives in exactly three places, all of them the record of the rename itself — [ADR-0002](../../docs/adr/0002-project-name.md) and the archive banners on [brainstorm.md](../../docs/brainstorm.md) and [vision.md](../../docs/vision.md). |
| Y-083 | Remove the superseded runtime decision | ✅ done | claude | — | Owner's call, 2026-07-31: the project started in Rust, so the runtime it briefly considered before writing any code should not appear anywhere. ADR-0003 and research notes 06a/06b/06c existed only to justify a runtime no line of Yantra has ever run on, and every reader had to work out that they were dead before ignoring them. **Two PRs, because it was well past 400 lines**: #53 deleted ADR-0003 and the three sub-notes and repointed every citation; #54 stripped note 06 to the findings that are language-independent, which is where I-12, I-14, I-18, I-19 and I-20 come from and the reason it could not simply be deleted with the others. **The retired numbers survive as numbers** — R-0, R-4, R-9, R-11, R-14…R-18, I-15…I-17 and ADR-0003 are all recorded as not reused, so the arithmetic in this file still adds up and nobody re-derives a decision that was already made. Note 06 went 305 lines → 201 and kept all five of its invariants. **Note the convention this bends** — §B5 calls ADRs immutable once accepted. ADR-0003 was superseded the same day it was written and never governed shipped code, so what is being deleted is a decision that never took effect; ADR-0004 keeps its number and the 0003 slot stays empty. |

## Found by exercising the CLI against the real fleet, 2026-08-02

Every command was run end to end against `bishwajeets-macbook-pro` and `cachyos-g14` with a real
Claude Code, not a stub: launch → trust prompt → running → transcript → `down` → `resume`, and the
forked agent recalled the earlier turn. The exit-code contract held everywhere (0/1/2, nothing
leaked), `up` was idempotent, and I-44's keychain guard fired correctly on macOS. What follows is
what did **not** hold.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-091 | `status` has no verdict for a workspace that never had an agent | ✅ done | claude | — | **`yantra up <ws>` with no `--agent` — the default path — then `yantra status <ws>` answers `unclear — the pane is alive but claude knows of no agent in that directory`, and exits 1.** Nothing is wrong: there is no agent because none was ever asked for. All seven `Verdict` variants assume an agent was launched, so `status` was built for M3 and never taught about M1's shell-only sessions, which are still the default. Exit 1 says *something is broken* about a healthy session, and `resume` inherits the same confusion — it refuses with R-2's reasoning (*"respawning would destroy the evidence"*) when there is no evidence and nothing to resume. **The obvious discriminator does not work**: `pane_current_command` cannot separate "never had an agent" from R-2's silent death, because both leave a shell in a live pane — that conflation is exactly why `Unclear` is honest and it must not be papered over. **`pane_start_command` can, and costs nothing.** `tmux.rs` creates every session with the default shell and *then* respawns the pane with the startup or agent command (a one-shot `new-session` takes the server down before `remain-on-exit` is set), so the field is precisely *what Yantra asked this pane to run* — empty for a plain shell, and holding `cd <repo> && exec claude … --session-id <uuid>` for an agent. It is one more field in the existing `PANE` format string at `crates/yantra-core/src/tmux.rs:170`, so **no extra round trip**. Measured on tmux **3.7b** (both fleet machines) and **3.5a** (the Alpine fixture), and it **updates on respawn**, so `resume` keeps it current. Fail safe: a start command that is neither empty nor recognisably an agent stays `Unclear`. **Landed as `Verdict::NoAgent`** — *no agent — the session was opened as a shell* — with `agent::session_id_in` reading the launch back out of the pane. **The exit code did not change and should not have**: this crate's contract makes 1 mean *nothing is running*, which is true of a shell, so the defect was the wording claiming a contradiction between two sources that never disagreed. `resume` gets its own refusal rather than inheriting `Unclear`'s — it will not start a *first* agent, because that is `up --agent` and the pane may have someone working in it — and names that command in the note. **`resume.rs`'s exhaustive `match` caught the new variant at compile time**, which is exactly what its comment said it was for. **Two container tests had been simulating R-2 with a bare `sleep 300`**, which this change correctly re-reads as `NoAgent` — so both now start the pane with `sh -c 'exec sleep 300' --session-id '<uuid>'` (the trailing arguments land in `$0`/`$1` and are ignored, so the pane stays alive while carrying an agent's command line) and a new test covers the shell case on each path. Verified on the real fleet: a shell session reports `no agent`, an agent session still reports `running`, and the pane's start command reads back the session id `up` printed. **181 tests**, 4 skipped.<br>**Corrected by Y-094, 2026-08-02: it does not read back *exactly*.** tmux wraps any multi-word start command in double quotes of its own, so `session_id_in` returned `<uuid>'"` and this row's original *"the exact session id"* was false. It survived here because `status` only asks `is_some()`, and a corrupt id is still `Some`; the first caller that needed the value found it. Now **I-51**. |
| Y-092 | `yantra logs` prints every timestamp in UTC, unlabelled | ✅ done | claude | — | `time_of_day` (`crates/yantra/src/main.rs:285`) slices bytes `11..19` out of the transcript's `2026-08-01T21:52:48.600Z` and prints `21:52:48`. Local time was **23:52 CEST**, so every turn reads two hours in the past with nothing saying which zone it is — directly beneath `last write: 28s ago`, which is correct, so one header carries a right time and a wrong one. **The instant is the agent's, not the reader's**: the transcript is written in UTC on the remote machine, and rendering it as local time is a *choice*, not a correction. Cheapest honest fix is one character — render `21:52:48Z` — because there is no date crate in the tree and adding `jiff` or `chrono` to relabel a log viewer is a dependency for cosmetics. Decide which; do not leave it unlabelled. **Settled as the label**: `time_of_day` now renders `21:52:48Z`, the column widens by one and continuations indent to match. The `Z` is **appended rather than sliced** — the zone designator sits after a fractional-second field the writer may or may not emit, so a wider slice picks up `21:52:48.` on exactly the transcripts that have one. |
| Y-093 | A machine cannot ssh to itself over Tailscale SSH, and nothing says so | ✅ done | claude | — | `ssh cachyos-g14` **from** `cachyos-g14` is refused on port 22 even with `RunSSH: true`, because Tailscale SSH is peer-to-peer and does not intercept a machine reaching itself; there is no sshd behind it. **Owner's answer, 2026-08-02: this is the design, not a bug — ssh into the MacBook and come back out.** That is what the `g14-via-mac` stanza (`ProxyJump bishwajeets-macbook-pro`) has been doing all along, and it works: `hostname` returns `CachyOs-G14` and tmux resolves. **The stanza is in `~/.ssh/config` and written down in the repo nowhere**, so the first workspace pointed at the local box gets a bare `Connection refused` and no hint that this case is special. ADR-0009 already says a `machine` is an ssh destination and `~/.ssh/config` decides what it means — this is that ADR's most useful consequence and it belongs in [docs/machines.md](../../docs/machines.md) and the README's workspace section. Documentation only; no code. **Owner's wording, recorded as the rule**: go out to the MacBook and come back. [docs/machines.md](../../docs/machines.md#a-machine-cannot-reach-itself) now carries the measurement, the stanza and the cost — the local box is reachable only while the MacBook is — and the README says it where someone writes their first workspace. **No code, deliberately**: a local-machine special case would be a second transport for one host, and ADR-0009 already put the answer in `~/.ssh/config`. |
| Y-094 | `logs` shows the newest transcript rather than the running session's | ✅ done | claude | Y-091 | **`logs` now opens `$HOME/.claude/projects/<slug>/<session-id>.jsonl` by name**; `ls -t | head -n 1` survives only on the path where there is no id to name. The id comes from `pane_start_command` through `agent::session_id_in` (Y-091), which is the whole reason this depended on that task rather than standing alone.<br>**The premise it was built on was half wrong, and finding that out was the task.** Y-091 recorded that the pane *"reads back the exact session id `up` printed"*. It does not — tmux re-quotes a multi-word start command in double quotes of its own, so the id arrived as `<uuid>'"` and `trim_matches('\'')` stopped at the `"`. Measured on tmux **3.7b** on both fleet machines and **3.5a** in the Alpine fixture, byte-identical, so unlike I-42/I-43/I-45/I-48 this is not a platform split. **`status` never saw it because it only asks `is_some()`** — a corrupt id is still `Some` — and Y-094 is the first caller that needs the value, where a trailing quote builds a filename that cannot exist and `logs` answers *no turn yet* forever. Now **I-51**.<br>**Filename = session id was verified rather than assumed**: every `*.jsonl` under `~/.claude/projects/*/` on this machine carries an internal `"sessionId"` equal to its basename, the same holds on the MacBook over ssh, and Y-077's own fork test directory holds the original beside the fork named by the id Yantra passed. Incidental: those directories also hold `memory/` and `<uuid>/tool-results/`, which the `*.jsonl` glob already excluded.<br>**A named session with no file yet is `Error::NoTurnYet`, never a fall back to the newest.** That is the decision with the most in it: the fallback is wrong *precisely* in this state, which every fresh launch and every fresh `resume` passes through (I-49 — a launch writes no transcript), so it would render another conversation under this session's header, with a header line naming someone else's file. The guess being removed would have survived exactly where it does the most damage. Exit code unchanged (1).<br>**A second code-execution boundary, not in the brief.** A workspace's own `startup` decides what follows `--session-id`, so the id is `sq()`-quoted like the repo path, with the escaped form asserted as an exact string — searching for the payload proves nothing, since correctly-escaped text still contains it.<br>**Cost: 1 round trip → 3**, all on the existing multiplexed socket (I-20). No call path had a `Pane` in hand — `status::of` does, but `logs::logs` does not call it and nothing in `yantrad` calls `logs` at all — and folding `Tmux::resolve` into the probe would mean reimplementing it inside `logs.rs` (§B2). No agent, no session, no tmux server, or any tmux failure yields no id and today's behaviour exactly; `session_of` swallows its own errors because `logs` never needed tmux and must not start needing it. **191 tests**, 4 skipped. |

## M4 — Web UI *(closed 2026-08-02)*

Broken down 2026-07-31 — the plan is [docs/plans/m4-web-ui.md](../plans/m4-web-ui.md), and **it is
a proposal awaiting review, not a decision.** Two milestones wearing one name: `yantrad` must serve
`yantra-core` over HTTP before a web UI has anything to read. **Q6 binds the scope** — personal-first,
so single-tenant, no auth beyond Tailscale, no settings screen. **Q13 and Q14 block the back and
front halves respectively** and both are the owner's.

The library needs no new orchestration: everything the dashboard shows already exists as a function
returning a `Result`, which is [ADR-0005](../../docs/adr/0005-core-logic-in-a-library-crate.md) collecting
on an argument it made two milestones before there was a second caller. `yantrad` is still the
15-line M0 skeleton, so nothing has to be undone.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-068 | Split the tracker per crate | ✅ done | claude | Y-067 | Owner's ask, 2026-07-31. `tracker.md` had grown to **115 KB** — 94 KB at the start of the previous session alone — while being the file every contributor is told to read first. Split three ways, by what each part *is* rather than uniformly: the **invariants** are per-crate, because they bind code and the crate file is what is open when one is about to be broken; the **closed tasks** went to [docs/archive/m2-m3.md](../../docs/archive/m2-m3.md), following M0/M1's precedent; the **session log** went to [docs/session-log.md](../../docs/session-log.md), 40 KB of append-only history that is read approximately never. **34 KB left**, and every one of the 49 invariant rows and 23 task rows verified to appear exactly once across the repo, with all relative links re-rooted and resolved. `yantra` ended up with **zero** invariants, which is a finding rather than an omission: it talks to no one else's program, so it has nothing to be surprised by. |
| Y-069 | `yantrad serve` — bind to the tailnet or refuse | ✅ done | claude | ADR-0012 | **The daemon exists.** `axum` + `tokio`, one route (`/healthz`), a `SIGTERM` shutdown for M7's supervisor, and `tracing` — which had been a workspace dependency with no user since M0. **No clap**: the CLI's own threshold was three commands (Y-056) and this has one, so `yantrad` serves when run. The whole point is the bind address. Q6 removed authentication, so **where it listens is the entire security model** (R-22), and `listen_on` fails closed — every branch that cannot prove an address belongs to this machine returns an error, because the only default available listens to the world. Addresses come from `Self.TailscaleIPs` via a new `Inventory::addresses`, which is **`Self` only**: a peer's address would be name resolution, and [ADR-0009](../../docs/adr/0009-machine-names-are-ssh-destinations.md) declined that — the trait method carries the boundary so the next person cannot widen it by accident. **No `--bind` and no `--port`**; a flag that can expose the API is a flag someone eventually passes, and a test asserts the port is a constant. **Verified against the real tailnet, not just the fake**: it bound both the v4 and the v6 address, `/healthz` answered over the tailnet, `curl 127.0.0.1:7717` was **refused**, `ss -ltnp` showed the two tailnet addresses and no `0.0.0.0`, and `SIGTERM` exited **0** with the port released. 4 unit tests, and the one that matters asserts the **refusal** — a test that asserts it binds passes just as well when the fallback is `0.0.0.0`, which is R-22's stated retire condition. 136 tests, 4 skipped. |
| Y-070 | The read model — refresh in the background, never on the request path | ✅ done | claude | Y-069 | `yantra_core::snapshot` holds the **shape** and `yantrad::refresh` holds the **clock** — split that way because [ADR-0012](../../docs/adr/0012-the-cli-and-the-daemon-are-two-callers-of-one-library.md) gives the daemon no logic of its own, `inventory::Fake` lives in core so the honesty rules are testable there, and anything defined in a binary crate that only tests use trips `dead_code` under `-D warnings`. One task per class: a fleet-wide session query costs a full `ConnectTimeout` per sleeping machine and the two cheap classes must not queue behind it. `EVERY = 30s`, a constant, **below `ControlPersist=300` so the poll keeps every ssh master warm rather than taxing the fleet** — and the `ControlPath` is per-user, so it speeds the CLI up too. Age leaves as a `Duration` from a monotonic `Instant`, never a wall-clock stamp, so a reading cannot get *younger* when the clock moves. **The plan named three honesty rules and there are four.** A look that *failed* is not "nobody has looked yet": if the snapshot stored only successes, a tailnet down since boot would be indistinguishable from a daemon that started a second ago — I-47 one layer up. Each class therefore stores the whole `Result` inside the reading. 6 unit tests, including one the plan never asked for. **Two things the plan got wrong**, both recorded rather than papered over: §3 says workspaces refresh *on request* while §5.2 says one task per class (§5.2 was followed), and it does not settle what a **failed** look should do to a **previous good** one — today the failure replaces it, so the API will show the failure rather than an ageing stale answer. **That is Y-071's to decide.** **144 tests**, 4 skipped. |
| Y-071 | The HTTP API, and `yantra ls workspaces` to keep the CLI honest | ✅ done | claude | Y-070 | **Three routes, not the plan's four.** `GET /api/{machines,workspaces,sessions}`, each mirroring an `ls` verb. `/api/workspaces/:name/status` is **dropped from M4**: `status::status` is not in the read model, and serving it would mean awaiting ssh in a handler — the one thing the whole design exists to prevent. It needs a fourth refresh class, which is **Y-084**, not a handler. **The judgement Y-070 left open — a failed look versus an ageing good one — is settled as: the failure wins, and it says so.** The plan's R-23 framing pushes the other way, so the deciding argument is what these errors actually *are*: every class-level failure the daemon can raise is **local and persistent**, not a transient network blip — `tailscale` missing, no config directory, a malformed workspace file. A retained stale reading would therefore not tide the page over a hiccup; it would show two workspaces that no longer parse, indefinitely, while the operator never learns the file is broken. **Measured, not argued**: dropping one bad `.toml` into the workspaces directory turned `/api/workspaces` *and* `/api/sessions` to `looked: failed` carrying the parse error and the line number — a page that can be acted on. The transient case that *would* justify staleness is a **machine** that did not answer, and Y-054 already keeps that inside a successful reading, so nothing is lost by discarding at the class level. **The distinction is visible rather than implied.** Every answer is tagged: `{"looked":"ok","age_seconds":6,"data":[…]}`, `{"looked":"failed","age_seconds":3,"error":"…"}`, `{"looked":"never"}` — and per machine, `"reached":"yes"` with `sessions` or `"reached":"no"` with `error`. No state is inferable only from a **missing** field, because that is the inference a client gets wrong. `error` carries the whole `source()` chain, like the CLI's `report_error`: `sessions::Error::Workspace` is `#[transparent]`, so the headline alone would say nothing. All three states answer **200** — the resource is *what the daemon currently knows*, and it always knows something, even when that is "I could not find out"; `/healthz` is the liveness signal, and a 503 for the first 30 seconds of every boot would be a lie. **DTOs live in `yantrad`** (ADR-0012): six small structs, `id` deliberately absent — a read-only page does not need a node key. **7 tests, and the ones that matter are the bad states**: never-looked, failed-look, the cause under the headline, a 1.1 s-old reading served with its age, a machine that did not answer surviving into the array, and `POST`/`PUT`/`DELETE`/`PATCH` on `/api/machines` all **405** — R-22's shape applied to the API surface, since a write route is where Q6's absent auth stops being free. `tower` joins as a **dev-dependency only**, so a test can drive the router rather than assert on a handler's return type. **`yantra ls workspaces` calls `workspace::list()` in-process** and works with no daemon running (ADR-0012); an empty directory names the path a file goes in rather than printing a bare header, and exits **0** — absence is the state asked for. **Verified on the real tailnet** (I-50: the address, never the short name): all three routes, the `never` state caught 50 ms after start, a real per-machine failure (`connect to host cachyos-g14 port 22: Connection refused`) rendered as `reached: no`, and `curl 127.0.0.1:7717` still refused. **Two stacked PRs**, split at the daemon/CLI seam. **163 tests**, 4 skipped.
| Y-072 | The dashboard | ✅ done | claude | Y-071, Q14 | **Three sections, not four** — the agent-status section is still **Y-084** and no placeholder was built for it. One page, no navigation, no router, no state library, in [`web/`](../../web/). **Five things, written once each**: `useLooked` polls with `setTimeout` *after* the response and **never throws**, mapping a dead daemon into the daemon's own `failed` envelope so the page has one failure path; `Section` calls `children` only in the `ok` branch, so a section **cannot** draw a table for a failed look; `DataTable` takes `Column<T>[]`, so the three tables are three arrays and it owns the fourth state nobody lists — `ok` with zero rows; `Status` is the only file that maps a domain state to an appearance, and call sites pass a `tone`; `Age` asks the staleness question once. **The CLI is matched, not reinvented**: `STATUS` is a composed sentence (`offline, key expired`), `LAST SEEN` is blank whenever the machine is online (I-39 — the API does not blank it for you), a null `startup` is a blank cell rather than the word "none", `attached` is the client count, `created` is the remote machine's clock and is never parsed, and both CLI wordings carry over verbatim — `{machine} unreachable: {error}` and `no workspaces yet — make one at …/<name>.toml`, plus the `N sessions on A of B machines` admission without which an unreachable machine reads as a machine with no sessions. The three classes refresh independently, so each section stamps **its own** age; there is no shared "last updated". Polls every 5 s purely to keep that age ticking. **Three loud negatives, all measured.** (1) **`127.0.0.1:7717` is refused, by design** — `yantrad` binds only this machine's Tailscale addresses (R-22), so the dev proxy target is derived from `tailscale ip -4`, not loopback; the plan had this backwards. (2) **The React Compiler bails out of three of shadcn's own generated components** — `Card`, `Badge` and `EmptyMedia`, on `BuildHIR::lowerAssignment` over a destructuring default (`size = "default"`), and the rule against editing `components/ui/` means they cannot be fixed here. (3) **oxlint's `react/react-compiler` reported none of it** — R8 found that a lint error does not imply a bail-out; the converse is also false, so the lint is not a proxy for optimisation in either direction. Both R8 mechanisms are therefore wired: a `logger` that names the file and the reason, and `npm run compiled` grepping the bundle for `react.memo_cache_sentinel` inside `npm run build`. Every hand-written component compiles — `useLooked` only after a `throw` inside `try/catch` was removed, which R8's sketch of this exact hook did not have. **12 tests**, and the ones that matter are the dishonest states: `failed` renders the error and **no** table, a multi-line TOML error survives with its newlines, `reached: "no"` renders as unreachable rather than as zero sessions, `last_seen` is blank when online, a null `startup` is blank, and a rejected fetch becomes `looked: "failed"` rather than an exception. **Verified against the real daemon** through the proxy — all three routes, a `looked: failed` carrying a caret-underlined parse error with its line and column, and two `reached: "no"` machines. **Y-073 is untouched**: no `Cargo.toml` change, no `build.rs`, and the web CI is a **new** workflow filtered to `web/**`, so no Rust job acquires Node (R-24). **Two stacked PRs**, split at the toolchain/dashboard seam. |
| Y-084 | Agent status in the read model | ✅ done | claude | Y-071 | **A fourth refresh class and `GET /api/workspaces/:name/status`**, which Y-071 dropped from M4 because `status::status` opens ssh and a handler that awaits ssh is the failure mode M4 exists to prevent. Two stacked PRs, split at the refresh/route seam.<br>**Grouped by machine, and the decision was measured rather than argued.** `status::of` makes four round trips and only one is about the workspace: `Tmux::resolve` (~50 ms), `Claude::resolve` (~30 ms) and **`claude agents --json` (~330–420 ms warm, 1,230 ms cold — it is a Node process boot)** are machine facts, against ~40–70 ms for the pane query. So ~480 ms of a ~530 ms call is a machine fact every workspace would re-buy, and the registry is whole-machine anyway — `of` was already filtering it by `cwd == repo`. **The sharper number is the unreachable machine**: `ConnectTimeout=10` is spent per *ssh invocation* and `ControlMaster` caches no failure (three consecutive execs measured 10016/10009/10017 ms), so per-workspace would cost ten seconds *per workspace* every 30 s. End to end against the real daemon one agent-class look took **10,091 ms with one workspace on the sleeping machine and 10,088 ms with three** — N-independent. Mutation-proved: a container test drives `on_machine` through a counting `Exec` with two workspaces whose verdicts differ, read out of one registry, and reverting to per-workspace makes the count 3 and the test fail.<br>**[The plan](../../docs/plans/m4-dashboard-next.md) §1.1 was half wrong and the correction is worth keeping.** It said the pane query is *also* per machine. It is not — not for free. Batching needs `list-panes -a` with `#{session_name}`, and `tmux.rs` already has one field that must be last (`pane_start_command`, I-51). A foreign session name may contain `|`, so two fields would need to be last, and splitting from the left lets a session called `yantra|weird` shadow the workspace `yantra` — a **wrong** answer rather than a missed one. It stays per workspace *inside* the per-machine task: 50 ms against the registry's 400 and the connect's 10,000.<br>**It found a live bug it could not work around.** `tmux::pane` mapped `no server running` and `can't find session` to `Ok(None)`, and **every existing test that asked about an absent session had no tmux server running at all**. With a server up, tmux resolves `list-panes -s -t '=name'` as a *window* target and answers `can't find window: nosuch`, which reached the caller as a hard error — so `yantra status <ws>` fails today for any workspace not open on a machine holding any other session, the most common state a read model asks about. Reproduced on 3.7b (both fleet machines) and 3.5a, so fleet-wide rather than a version split like I-48. Fixed, with a test that produces the state rather than describing it.<br>**The verdict is a tagged object, not a string**: `{"state":"no_agent"}`, `{"state":"awaiting_trust"}`, `{"state":"unclear","because":…}`, `{"state":"crashed","exit_status":1}`, `{"state":"killed","signal":"KILL"}`. Y-091's `no_agent` sits beside `unclear`'s genuine contradiction rather than inside it, `awaiting_trust` has its own name (I-49), and every variant carries what told it apart — so **Y-096 gets cases rather than a rewrite**.<br>**404 is a deliberate deviation from Y-071's *every state answers 200*.** This is the only route naming a *resource* rather than a class, and a 200 with absent `data` would make non-existence inferable only from a missing field — the exact inference Y-071 banned. It is unreachable before the first look, and a test pins that.<br>**Verified on the real tailnet** (I-50; `127.0.0.1:7717` still refused): `looked:"never"` caught before the first look, `no_agent` from a real shell session, `no_session` from a defined-but-unopened workspace, **`awaiting_trust` from a real Claude Code agent really holding at the trust prompt**, then `running` with `session:{id,pid}` after answering it by hand — id and pid matching what `up --agent` printed. An unreachable node rendered `reached:"no"` with its `Connection timed out` chain; an unknown name 404s. **194 tests**, 4 skipped. |
| Y-095 | Plan the dashboard's next round | ✅ done | claude | Y-072 | [docs/plans/m4-dashboard-next.md](../../docs/plans/m4-dashboard-next.md). Splits the remaining dashboard work by whether it needs to know what the page **looks like**, because the design system is arriving from elsewhere and needs more direction first — and most of it does not. The ranked gap is **the state a human has to act on is invisible**: an agent at the trust prompt is the only place in the system where the machine has stopped and is waiting for a person (I-49), and it is the one thing the page cannot show. Names one shape to consider before building Y-084 the obvious way, and one collision (`--accent` means a muted hover surface to shadcn and a pigment to the incoming system; both are `:root`, so **the cascade decides and neither order errors**). |
| Y-096 | The dashboard's fourth section: agent status, including the trust prompt | ✅ done | claude | Y-084 | **Shipped as `85ac4f6`; this row read `⬜ todo` until 2026-08-02 because the task that closed it flipped its dependants' rows and not its own** — the drift §B5 exists to catch, found by an audit rather than by anything failing. `App.tsx:61` renders the section.<br> Y-072 shipped three sections and no placeholder for this one. **Y-091 is what makes it renderable**: before it, the most common case on the page — a session opened as a shell — reported `unclear`, so the section would have alarmed about the ordinary state. **Y-097 found a second thing that waits on it**: `yantra resume` is the command the plan used as its own example and the one verb the read model cannot choose honestly, because picking it requires knowing the agent crashed. Rows carry `attach` or `up` today; the third verb becomes derivable here. |
| Y-097 | Rows carry the command that acts on them | ✅ done | claude | Y-072 | Workspace and session rows gained a `COMMAND` column: a `<code>` holding `yantra attach <name>` or `yantra up <name>`, and a copy button. No write routes, so no auth surface (Q6) — the shape `up`'s attach hint already chose for this problem.<br>**The plan's own example command turned out to be the one that cannot be offered.** Both this row and [the plan](../../docs/plans/m4-dashboard-next.md) §1.2 used `yantra resume <name>`, and choosing it honestly requires knowing the agent **crashed**, which is not in the read model. Offering it off a live tmux session would be wrong in both states that session can be in: `resume` refuses when an agent is already running, and errors `NoAgent` when the session never held a conversation (Y-091). **Y-096 is where the third verb becomes derivable**, and nothing here anticipates that section.<br>**`navigator.clipboard` is `undefined` on the origin this page is served from, and it works in dev.** The Clipboard API is `[SecureContext]` and the potentially-trustworthy set is loopback only; Tailscale is `100.64.0.0/10`, and `yantrad` binds exactly those and refuses loopback (R-22). But `npm run dev` serves from `localhost`, so a clipboard-only button looks right on the machine it was written on and is dead on every machine it is used from — a trap rather than a nuisance. The button therefore **selects first** (a `Range`, plus `select-all` on the `<code>` so one click selects it with no JS), then tries `clipboard.writeText`, then `document.execCommand('copy')` — deprecated but not secure-context gated, and the path that actually runs in production. If both fail the label reads *selected — copy it yourself*; it cannot silently do nothing, because the selection already happened. jsdom has neither API, so the default test environment **is** the insecure-origin case.<br>**The name is re-validated in the browser rather than inherited.** `workspace::validate_name` is the intersection of I-24 and I-2 — non-empty, ASCII alphanumeric, `_` or `-`, and `list_in` validates every stem so a bad name fails the whole class — so the page *could* have relied on it. It does not: `columns.tsx` re-checks against `/^[A-Za-z0-9_-]+$/` and renders no command on a miss, because the browser is a separate program consuming JSON over HTTP and the daemon's charset is a promise it cannot see. **Escaping was rejected**: a name needing escapes is one the daemon would not have loaded, so there is nothing honest to escape it into. The tmux session name — a remote machine's output, and the field this would most plausibly have been built from — is a lookup key only and never reaches the string.<br>**Not knowing renders as `up`, not as blank**, because `up` is idempotent (§B4): a failed or absent sessions look costs the command precision, never honesty. That is what lets the workspaces section read the sessions class without inheriting its failure, and it sidesteps §1.4's merge problem rather than solving it. Machine rows carry no command — no verb takes a machine — and neither does a session with no matching workspace, since every verb takes a workspace name.<br>A plain `<button>` rather than shadcn's, deliberately: its source destructures `asChild = false`, the exact pattern the three known compiler bail-outs share, so importing it would have made the list four. **25 tests.** Browser-only with no API change, so unlike Y-072 it was **not** verified against a live `yantrad`. |
| Y-098 | Staleness is a reading, not a timestamp | ✅ done | claude | Y-072 | Past the threshold `age_seconds` stops meaning *slightly old* and starts meaning *the last refresh never finished*, so the page says so — as a `Status tone="bad"` reading *refresh stuck*, through the tone seam, rather than by shading the number. **`Age.tsx` no longer knows a colour exists**; `Status.tsx` stays the only file that maps a domain state to an appearance (ADR-0014).<br>**The threshold survived contact with the real numbers exactly, and the arithmetic is now in the file rather than in a review comment.** `refresh.rs` sleeps `EVERY = 30s` **after** the look and `Reading::new` stamps when the look *returns*, so an age climbs to `EVERY + the next look's duration` before it is replaced; `ssh.rs` sets `ConnectTimeout=10`; and `sessions::list` fans out one `tokio::spawn` per machine, so an unreachable machine costs that timeout **once per sweep, not once per machine** — which is what makes the ceiling a constant rather than a function of fleet size. `30 + 10 = 40`, and `age_seconds` is `Duration::as_secs()`, a floor, so a reported `40` is still explainable and `41` is not.<br>**Two things checked while deriving it, both arguing against padding the number.** `Age` does not interpolate — it renders the daemon's number and the 5 s poll only makes it move — so the displayed value is always **≤** the true age and poll lag can make the warning late but never false. And a host that freezes *after* connecting is bounded by `ServerAliveInterval=15 × ServerAliveCountMax=3 = 45s`, putting a legitimate age at 75 s: not a false positive, since the refresh genuinely has not completed and genuinely is blocked in ssh, but *over 40 s* does not mean *exactly one connect timeout*.<br>**`looked: "never"` is the third state the plan does not name, and it needed no change** — `Section` renders no `<Age>` at all in that branch, so the first ~30 s of every daemon boot reads *Not looked at yet.* rather than as a wedged refresh. Pinned by a test, because the obvious way to write this change — a threshold over a number defaulting to `0` — would have broken it.<br>**No `stale` tone key was added**: [the plan](../../docs/plans/m4-dashboard-next.md) §3 asks the design system for a staleness treatment, and a fifth tone aliasing `bad` would be a token with nothing behind it until that lands. `index.css` untouched. One file plus tests. **16 tests** at the time of merge. |
| Y-100 | Staleness names the machine it is waiting on, not the refresh | ✅ done | claude | Y-098, Y-084 | **Opened on a premise that was wrong twice over, and the corrections are the finding.** The row said *about two seconds of margin* and *this fleet always has one asleep machine*. Neither survived measurement.<br>**The margin is ~20 ms, not two seconds.** Y-084 saw `age_seconds` at 38 on a healthy reading and that read as slack; re-derived from Y-084's own numbers it is the healthy peak sampled two seconds early. An unreachable machine costs 10.016–10.017 s, `sessions::list` fans out concurrently so it is paid once per sweep, and `age_seconds` floors — so the peak is `30 + 10.02 = 40.02 → 40`, and Y-098's `> 40` was correct with almost nothing to spare.<br>**`cachyos-g14` costs no `ConnectTimeout` at all.** It is powered on with no sshd, so it sends a RST and ssh fails in **6 ms** with `Connection refused` — measured, after the row had already asserted the opposite. Only a machine that is *asleep*, dropping packets rather than refusing, pays the ten seconds. Sampled live for two minutes, both ssh classes peaked at **30–31**, not 38–40. **The consequence is what makes the fix work**: the sweep that pays the timeout is the sweep whose reading names that machine `reached: "no"`, so the excess and the evidence for it arrive together by construction rather than by inference.<br>**`Age` now takes the machines its class's look is known to be waiting on**; `Section` forwards them and `App` derives them from the sessions reading's per-machine `reached`. Four readings: at or under 40, the number alone; over 40 with **every machine answered**, *refresh stuck* (`bad`) — nothing is left for it to be waiting on; between 41 and 75 with a machine named, *waiting on `<machine>`* in `unknown`, the quietest tone; over 75, *refresh stuck* again, because `EVERY + ServerAliveInterval × ServerAliveCountMax = 30 + 45` is longer than ssh itself will wait.<br>**A class that opens no ssh passes nothing and has none by construction**, which is also the answer to whether one threshold belongs on all three sections: it specialises without a per-class table, because `machines` and `workspaces` run local commands and can never name anyone.<br>**The 75 s freeze case is deliberately not covered by a bigger constant.** Between 41 and 75 with every machine answered the badge still says *refresh stuck*, because a host that froze after connecting is not yet named unreachable in the reading being served and the next reading moves it into the named case. Y-098 already settled that as a true statement said early rather than a false positive, and raising 40 to 75 would delay every real wedge by 35 s to buy it. Still no `stale` tone key — Y-098's reason holds until the design system delivers a staleness treatment. |
| Y-101 | ADR-0004 still names SQLite as stack, and the daemon writes none | ✅ done | claude | Y-044 | **Shipped 2026-08-02 as `b7487a5`, and this row read `wip` until 2026-08-03** — the third row this week to close without flipping itself, after Y-096 and Y-073. The amendment is in ADR-0004, `CLAUDE.md` §B1 follows it, and `README.md`'s Stack line no longer names SQLite; `grep` finds the word in no summary. Recorded rather than quietly corrected because three is a pattern and the pattern is that the task which *closes* a row is not the task that flips it.<br>Owner's call, 2026-08-02: **a dated amendment to [ADR-0004](../../docs/adr/0004-rust-for-the-daemon.md)**, then `CLAUDE.md` §B1 and `README.md`'s Stack line follow it. Y-044's audit left this deliberately unfixed, and the reason it named is the reason it needs doing properly: ADR-0004 is immutable (§B5) and the other two summarise it, so correcting two of three would trade one stale claim for a three-way disagreement. **The amendment must not read as *ADR-0004 was wrong*.** It cited `rusqlite`'s real `busy_timeout` and `spawn_blocking` as an argument *for choosing Rust*, and that argument was true when made. What changed is that the daemon turned out to need no datastore at all — one premise, not the decision. The mechanism is the dated blockquote Y-079 used on [ADR-0006](../../docs/adr/0006-ssh-exec-transport.md). Research notes recording I-12/I-13/I-14 stay untouched: they record what was measured, not a plan to use it. |
| Y-102 | ADR-0015: why `resume` forks the conversation | ✅ done | claude | Y-077 | **Outstanding since 2026-07-31** — Y-077 recommended an ADR and deliberately did not write it, and the owner has now chosen **a standalone ADR-0015 rather than an amendment to ADR-0011**. Y-044 checked and did not take the number. The substance is already in the Y-077 row: the four flags measured against Claude Code 2.1.220, why `--resume <id>` is impossible (Yantra keeps no id — and **Y-044 being dropped makes that permanent rather than pending**), the loud negative that no flag can report *there was nothing to resume*, the four refusals, and the review gap where a respawn skipped Y-081's repo check. ADR-0011 is not superseded: its decision is untouched and correct, and what 0015 does is complete a *Launch* bullet that describes only half the command surface. Note `pane_start_command` (I-51) holds the id for a **live** pane, which is not a store and does not reopen this.<br>**Landed as [ADR-0015](../../docs/adr/0015-resume-forks-the-conversation.md), *extends* rather than amends or supersedes.** `Amends` is this repo's word for withdrawing a mechanism (0008) or dropping a field (0010), and nothing in ADR-0011 becomes untrue — so 0011 keeps a plain `accepted` and gains one dated blockquote under its *Launch* bullet, pointing at the second command. **Two things in the Y-077 row were already stale and the ADR wrote the current state instead**: that row justifies the fork by `logs` reading the newest file, which **Y-094 changed the next day** — the ADR argues that strengthens the case, since without the fork a resumed session has no id in its start command and `logs` falls back to exactly the guess Y-094 removed; and the row lists **four** refusals where the shipped code has **five**, `Verdict::NoAgent` having gained its own in Y-091. **`--resume <id>` is useless rather than impossible**, which is the sharper statement: `pane_start_command` does hold a readable id wherever a pane exists, dead ones included (I-4), but the id lives exactly as long as the pane — so the state `resume` most exists for, a workspace with no session at all, has nothing to read; where a pane does exist the id names the conversation `--continue` resolves to anyway; and it arrives wrapped in tmux's own quotes (I-51), needing a parser its alternative does not. **One consequence nobody had stated**: every resume mints a transcript, so *n* resumes leave *n*+1 files in the project directory, each older one a prefix of the next. `logs` is unaffected since Y-094 names the file it wants, but what eventually removes them is Claude Code's `cleanupPeriodDays` (default 30), not Yantra. |

## M5 — Control from the phone *(buildable work finished 2026-08-03)*

Headed *the heartbeat agent* in `tracker.md` to the end, because the milestone was re-scoped after
these rows were written and the header was left as the history it is — `tracker.md §2` carries the
re-scope. The rows are in the order the tracker held them.

[The plan](../../docs/plans/the-heartbeat-agent.md), 2026-08-02, measured on both machines before any code was written. It settled the two seams [ADR-0013](../../docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) deliberately left open — where the wire type lives, and how the agent speaks HTTP — and reproduced every probe on real hardware, which is where most of its value turned out to be.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-103 | Accept ADR-0013 | ✅ done | **biswa** | — | **Accepted by the owner 2026-08-03 — and it is two ADRs, not one.** The title names only ADR-0013 because the row predates ADR-0016 by two days; the decision taken was *"adr0013 approved and adr 0016 approved"*, so both flipped together and the title was left as the history it is. Fifteen ADRs, none now `proposed`. What was at stake is closed: the repo's record of *what was decided* had been disagreeing with the six tasks that implemented ADR-0013 and with the write routes Y-112 and Y-116 built on ADR-0016.<br>**Accepting ADR-0016 fixes the shape of [Y-118](../../tracker.md#3-task-board)'s fix, which is the substantive consequence.** ADR-0016 §2 resolves the caller's source address, `tailscale serve` makes that address the proxy's, and the ADR is now immutable — so the fix can no longer be an edit to ADR-0016 and must be an amending or superseding ADR. That ADR is not written here; Y-118 is still the owner's.<br>**ADR-0013 carries no live objection**, and the one thing near it is not one: the [heartbeat-agent plan](../../docs/plans/the-heartbeat-agent.md) §6 flagged that §2's *desktops have no `AC*` entry* premise is read rather than run, which Y-106 then recorded by regrading **I-9 `[V]` → `[D]`** in [`crates/yantra-agent/tracker.md`](../../crates/yantra-agent/tracker.md). It downgrades the evidence behind a premise, not the decision resting on it — `Power::Ac` was measured on both fleet machines through two live traps — and Y-110 is what closes it. |
| Y-104 | The heartbeat wire type | ✅ done | claude | Y-103 | `crates/yantra-core/src/heartbeat.rs` — `Heartbeat` (seven fields, `deny_unknown_fields`) and `Power`. **`Power` is external-tagged**, so `"ac"` round-trips ADR-0013 §1's example *character for character* while a battery is `{"battery":{"percent":42}}` — the two are different JSON **types**, a string and an object, so neither can be misread as the other however the keys are spelled. That is the property I-9's argument actually needs; an internal tag would have forced `Ac` into `{"state":"ac"}` and changed the ADR's payload for nothing.<br>**Three ambiguities it surfaced are worth more than the code**, and each landed on a task that had not started. (1) **§2's *"two positive readings … from that same device"* cannot be a schema constraint** — `Battery { percent }` is constructible from one reading and no serde shape prevents it, so the rule is **entirely the probes' obligation** and nothing in the loop or the daemon can catch a violation. (2) **RFC 3339 does not mean UTC and the ADR never says which it means**; the type accepts either and preserves the offset, so the daemon must compare **instants, not strings**, or a correctly-set machine in another timezone reads as skewed. (3) **`arch` has a hard filter and no agreed spelling** — the type takes a `String` and deliberately does not pick. First timestamp Yantra writes, so `time` joins the workspace with `serde-well-known`. |
| Y-105 | `MachineInfo` carries each peer's tailnet addresses | ✅ done | claude | — | ADR-0013 §5 attributes a heartbeat to the peer owning the **source address**, which needed one observed field on a struct that already existed. `Vec<IpAddr>`, not strings — `Node.tailscale_ips` was already that type, and it matters because `fd7a:115c:a1e0::2` and `fd7a:115c:a1e0:0:0:0:0:2` are **one address and two strings**: a text comparison fails *closed*, dropping a real heartbeat as unattributable, which looks exactly like a dead agent.<br>**One change beyond the estimate, and it is the leniency this crate is required to have**: the private mirror's field became `Option<Vec<IpAddr>>` collapsed with `unwrap_or_default()`, because a bare `Vec` turns an explicit `"TailscaleIPs": null` into a **whole-document** parse error — and the sibling `Addrs` is literally `null` on every peer in the live status, so nulling a key is demonstrably how this format moves. Survivable while only `Self`'s addresses were read; not once the whole fleet's attribution depends on the document parsing.<br>**Addresses are deliberately absent from the API's machine DTO**, matching Y-071's omission of `id` and pinned by an assertion with a non-empty fixture. I-50 is the argument the other way and does not reach: it binds a *program* opening a socket, and nobody dials from the dashboard — a human reaches a machine by name through `~/.ssh/config`, so an address beside the name is a second, competing answer to *where is this machine*, which is the confusion [ADR-0009](../../docs/adr/0009-machine-names-are-ssh-destinations.md) exists to prevent. |
| Y-106 | The probes, Linux and macOS | ✅ done | claude | Y-104 | `crates/yantra-agent/src/probes.rs`. Fixed facts at start, moving facts per beat, exactly as ADR-0013 §1 requires.<br>**`arch` is `std::env::consts::ARCH` — `x86_64`/`aarch64`, never `arm64`** — and that settles the spelling Y-104 refused to guess. **R5's own example machines file says `arm64` and is therefore wrong against hard filter 3**; M5 must compare against this spelling or a Pi is filtered out of its own fleet.<br>**The two-reading `Power` rule is enforced by counting, because it cannot be typed.** `power_from(mains_offline, percent)` is the only place either platform names the variant, and `battery_has_exactly_one_construction_site` asserts exactly one construction site outside `#[cfg(test)]` — **proven to fail** (`left: 2, right: 1`) by adding a second. `Battery` is produced for the first time in this project, from a fake sysfs tree and a recorded `pmset` string.<br>**Every reader fails toward the value that loses a placement**: a failed load-average read reports **100**, never 0, because `cpu_busy_pct: 0` is a *perfect* CPU-idle score and a broken reader would otherwise win every placement.<br>**Findings that would each have shipped as a bug**: `sysctl -n vm.loadavg` returns `{ 2.26 1.67 1.57 }` — **with braces**, so a `/proc/loadavg`-shaped parser yields 0 and ranks every Mac first forever; `command -v` finds **nothing** under a LaunchAgent's `PATH` of `/usr/bin:/bin:/usr/sbin:/sbin`, so labels are resolved through `yantra_core::agent::CANDIDATES` rather than a second list that could drift from I-34's; macOS free RAM has two answers differing by 2× (`vm_stat` 5,402 MB vs `memory_pressure` 11,141 MB), and the conservative, arithmetic-checkable one wins; macOS `/` is the sealed system snapshot, so `free_disk_mb` is right **by luck** via APFS container sharing and says so in a comment.<br>**Two corrections to the plan's own measurements**: `nvidia-smi --version` is **21 ms**, not 1.232 s — the 1.2 s belongs to *plain* `nvidia-smi`, which is what the probe runs, so the budget holds but the attribution was wrong; and the page size comes from `vm_stat`'s own header rather than a second `sysctl`, one process instead of two and self-consistent. `sent_at` truncated to **whole seconds**, the ADR's own shape, in a separate revertible commit. **466 KB musl, 26 KB under the plan's ceiling.** |
| Y-107 | The agent's beat, and the POST it writes by hand | ✅ done | claude | Y-104 | 10 s loop, hand-written HTTP/1.1 over `std::net::TcpStream`, **no async runtime and no HTTP crate**. The numbers, not the taste: `tokio` alone is **+28 %** for one timer, `ureq` +57 %, `hyper`+`tokio` +87 %, and **`reqwest` cannot cross-build to musl at all** — `native-tls` → `openssl-sys`, build script exits 101, which would have taken Y-037's five-target release pipeline down *while looking like a CI problem*. The module header carries the retire condition: if this ever needs redirects, retries, keep-alive, compression or TLS, the decision is wrong and the answer is `ureq` — and ADR-0013 §4 and §7 rule out every one of those by name.<br>**It refused a free win.** The binary measured 378 KB against the plan's predicted 492 KB and the row said plainly that this was **not** a saving — the plan's candidates all carried the probes, this branch had a stub, so 492 KB stayed the ceiling and Y-106 spent 88 KB of the remaining 114.<br>**Log-once-then-quiet is the only state in the process**, a `bool` behind a function returning `Option<String>` so a test can assert the log without capturing stderr: failure→failure→failure→success→success→failure gives `Some, None, None, Some, None, Some`. Proven end to end too — 25 s against a closed port produced exactly one line and did not exit.<br>**Configuration is one environment variable and it is an address**: `parse::<SocketAddr>` refuses a MagicDNS name *by construction*, which is I-50 enforced by the type rather than by a comment. An unusable value exits 1 at startup naming `127.0.1.1`; a well-formed but wrong one fails per beat, once, and the process keeps running. `anyhow` left the manifest — the failure vocabulary is a three-variant enum with a `Display`. |
| Y-108 | `POST /heartbeat`, attributed by source address | ✅ done | claude | Y-104, Y-105 | The first write into `yantrad`. 204, 4 KB body limit, strict types, 405 on other verbs — Y-071's conventions applied to a write route.<br>**ADR-0013 §5 says drop an unknown sender and never says what it sees; the answer is `403`.** A dropped beat answering **204** is indistinguishable from a recorded one — R-23's confident lie in a new place — and what is actually being diagnosed is nearly always a stale inventory or an agent pointed at the wrong daemon, i.e. a problem on the machine that is asking. It costs nothing because §7 already has the agent log once and go quiet.<br>**A third answer fell out that nobody specified: `503`.** Before the first inventory look, or after a failed one, the daemon does not know who *anyone* is — so a 403 there would send someone to the wrong machine. Y-070's four-states discipline arriving on the write path unprompted.<br>**It took Y-105's trap**: `to_canonical()` on **both** sides of the comparison, with its own test, because `::ffff:100.64.0.2` is not `IpAddr`-equal to `100.64.0.2` and the failure is silent and looks exactly like a dead agent.<br>**Out-of-range percentages rejected at the door, 422, `0..=100`**, and the deciding argument is consistency rather than caution: without it `300` is a 422 and `200` is a 204, a line drawn at `u8::MAX` instead of at what a percentage means — and R5 scores CPU *idle* as this number's complement, so `200` is a **negative score term**, not a bad reading. `100` stays accepted, because it is the pessimistic value Y-106's failed reads must produce.<br>**`sent_at` is stored and nothing else, but stored as an instant.** Computing skew would need a wall-clock arrival stamp, since `Reading` deliberately holds a monotonic `Instant`; what is pinned instead is the silent failure — a beat sent `+05:30` is the same instant as the ADR's `Z`, and nothing compares text.<br>**Verified live, including the parts easiest to fake**: real tailnet round trip from the MacBook (204, 4.0 ms connect), 204 over v6, 204 from the daemon's own machine to its own address, loopback refused, 413 on a 5,737-byte body, and the **403 checked with a genuinely non-peer source** (`curl --interface <lan-address>`) rather than only in a test. |
| Y-109 | Beat age, and ADR-0013 §7's four display states | ✅ done | claude | Y-108 | `GET /api/machines` carries a `heartbeat` object per machine — `age_seconds` beside the six facts — and **`null` is *never heard from***. The join is `api.rs`'s and it is on the **node id** (I-5), which is the one field the route still does not serve; `Fleet` grew a `FromRef` for each half so only this handler reads both and the other three routes still name the snapshot alone. The four states are `reporting()` in `columns.tsx`, rendered in a `HEARTBEAT` column beside the beat's age.<br>**Verified against the real fleet, 2026-08-03**, with `yantrad` and `yantra-agent` running on `cachyos-g14` and the daemon's own JSON rendered through the real component: **ready** (`beat 1s ago`, 7,866 MB free, 8 % busy, on AC), **up, but not reporting** (agent killed, `beat 51s ago`, `online: true`), and **never heard from** on the other five peers. **`asleep or off` was not produced** — it needs a machine that *has* beaten to then go `Online: false`, which is Y-110's unplugged laptop; it exists in tests only. **Half of that is now supplied: [Y-110](#m5--control-from-the-phone-buildable-work-finished-2026-08-03) made the MacBook beat on 2026-08-03**, so its row in `Beats` exists and closing the lid renders the state. The lid is the owner's, and the row does not survive a `yantrad` restart.<br>**Three things the brief and the record turned out to be wrong about.** (1) **The fleet does not beat.** The §0 status line says both machines report every 10 s; nothing runs the agent — there is no unit (install is M7's) and **the MacBook holds no checkout of this repo at all**, so the only beats that have ever existed are from a binary someone started by hand. (2) **Four states describe machines, and this tailnet is mostly not machines.** An iPad, an iPhone and two dead laptops read *never heard from* permanently and correctly, so five of six rows carry the state that is meant to be the alarming one — a finding for Y-113's picker rather than for this row. It does prove R-23 in production, though: `laptop-9ml3d644`, offline for 28 days, renders *never heard from* and **not** *asleep or off*. (3) **ADR-0013 §7's table says *what the daemon says*, and the daemon deliberately says none of it.** The wire carries `age_seconds` and `online`; the page names the state, because a server-side label would be a second source for one fact — §3's own argument against shipping `os`. The 30 s threshold therefore lives in `columns.tsx`, and the daemon filters nothing.<br>**`sent_at` is the one field not exposed**, deliberately: ADR-0013 §1 justifies it as the only way to tell slow delivery from a wrong clock, and it now reaches no consumer at all — the daemon stores it and serves everything else. The diagnostic exists nowhere until something asks for it.<br>**The seam, as briefed and as built.** The seam is `Beats = Arc<RwLock<BTreeMap<String, Reading<Heartbeat>>>>`, keyed on node id (I-5), beside `Model` rather than inside `Snapshot`. **A machine that has never beaten is absent from the map** — the `None` that *never heard from* needs, rather than an empty row (I-47's lesson).<br>The four states are ADR-0013 §7's, and the reason they are four rather than two is R-23: a dashboard that says **asleep** when it means **we have not heard from it** is the confident lie M4 exists not to tell. Beat within 30 s ⇒ **ready**; no beat but `Online: true` ⇒ **up, but not reporting**, an agent or install problem, a different thing to go and fix; no beat and `Online: false` ⇒ **asleep or off**; never ⇒ **never heard from**. `Online` sharpens the *explanation* and never the *feasibility* (R-8), which keeps it inside I-10. |
| Y-110 | Verify on the fleet, and produce a real `Battery` | ✅ done | claude | Y-106, Y-107, Y-108 | Y-106 already ran the whole path — real `yantrad`, both agents, several beats each, no failure line, so every POST was attributed. **What is left is the state nobody has produced: a machine actually on battery.** Both fleet machines stayed on mains throughout, so `Power::Battery` exists only from a fake sysfs tree and a recorded `pmset` string, and **I-9's desktop half cannot be closed at all until there is a desktop** — see the regrade in [`crates/yantra-agent/tracker.md`](../../crates/yantra-agent/tracker.md). Needs a human to unplug a laptop, which is why it is not an agent task.<br>**Closed 2026-08-03, the owner having unplugged the MacBook.** `yantra-agent` was built natively on `bishwajeets-macbook-pro` from a throwaway clone, pointed at `YANTRA_DAEMON=100.x.x.x:7717`, and `GET /api/machines` served its beat with `"power":{"battery":{"percent":100}}` — the first `Power::Battery` this project has produced from a machine rather than from a fixture. Every POST was accepted (204): the agent's log-once latch stayed silent across the whole run. The clone and its `target/` were removed and no process was left on the Mac.<br>**The hand-written `PMSET_BATTERY` fixture was wrong in both details it invented.** Reality reads **100 % while `discharging`** — the *same* percentage `PMSET_AC` carries, so the mains line is the only thing separating the two states and any reader tempted by the status word or the percentage would call an unplugged Mac plugged in. And macOS prints **`(no estimate)`** for the first minutes off mains before it prints a time, so `2:41 remaining` was not the only shape. Both real forms are fixtures now, and the test was proven non-vacuous twice — breaking the mains match and breaking the `%` split each turn it red.<br>**What this did *not* close.** The **Linux** battery path is still `[D]`: `cachyos-g14` held `AC0/online: 1` throughout and cannot be unplugged remotely. I-9's desktop half still has no desktop. And **`asleep or off` still needs the owner** — the Mac now has a row in `Beats`, so closing the lid renders it, but that row lives only in the running daemon's memory (nothing is persisted) and a `yantrad` restart puts the machine back to *never heard from*. See [Y-109](#m5--control-from-the-phone-buildable-work-finished-2026-08-03). |
| Y-111 | `yantrad` behind `tailscale serve`, so the dashboard is HTTPS | ✅ done | claude | Y-073 | **The PWA is blocked on this and the clipboard already is.** Service workers do not register outside a secure context, and `yantrad` serves plain HTTP on a `100.64.0.0/10` address — the same reason [`Command.tsx`](../../web/src/components/Command.tsx) cannot call `navigator.clipboard` and falls back to selecting text. **Verified 2026-08-02**: the tailnet already has HTTPS certificates enabled (`tailscale status --json` reports a non-empty `CertDomains`; `tailscale cert` exists in 1.98.9), so this needs no admin-console change. §B2 decides the shape — `tailscale serve https / proxy` is a config line, against terminating TLS in `axum` and owning cert renewal ourselves. |<br>**Shipped 2026-08-03, and it is one line plus documentation exactly as scoped**: `just https` runs `tailscale serve --bg --https=8443 "http://$(tailscale ip -4):7717"`, `just https-off` undoes it, and no Rust changed. `--bg` stores it in `tailscaled`, so it is set once per machine rather than once per shell. Verified against the **built** dashboard on the real tailnet, not the API-only placeholder: `https://cachyos-g14.<tailnet>.ts.net:8443/` answers **HTTP/2 200** with `index.html`, the app bundle is **200 `text/javascript`, 245 054 bytes**, a deep link falls back to the app, `/api/machines` returns the six-peer reading over TLS, and the certificate is real (`issuer: C=US; O=Let's Encrypt; CN=YE1`). code-server on `/` is untouched and still answers (302 to its login). **The operator prerequisite was `sudo tailscale set --operator=$USER`, done by the owner on 2026-07-31; no `sudo` was needed here.**<br>**Three things the plan and this row had wrong.** `tailscale serve` accepts **any** HTTPS port — 9443 configured without complaint — so 443/8443/10000 is a **Funnel** restriction, not a serve one, and 8443 was chosen for convention rather than necessity. It also accepts a port another process already holds and *reports success*: `--https=7717` printed its usual URL while `ss -lntp` still showed the socket belonging to `yantrad`, so a silently-dead endpoint is the failure shape to expect there. And the row's own framing of the daemon as read-only was already stale — Y-112 and Y-116 shipped writes before this ran.<br>**The finding that outranks the feature: the proxy defeats [ADR-0016](../../docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md).** A reverse proxy terminates the caller's connection and opens its own, so the daemon sees the proxy. Measured across the fleet with a listener standing in for `yantrad`: a request issued on `bishwajeets-macbook-pro` arrived from **this** node's address, with the caller's address in `X-Forwarded-For` and its identity in `Tailscale-User-Login`. Writes from the phone therefore work — the proxy runs on the owner's own untagged node — but the check "rejects nothing" for a *new* reason, and the day a tagged or shared-in node appears is exactly the day ADR-0016 was bought for. Recorded as a dated amendment there and carried by **[Y-118](../../tracker.md#3-task-board)**. R-22's outer boundary is unchanged: `serve` is tailnet-only, and `tailscale serve status` says `(tailnet only)` on both mappings. |
| Y-119 | `create` accepts an empty `startup`, and writes a command that cannot run | ✅ done | claude | — | **Found by Y-116's form agent while deciding what to send for a blank field, and it is a real gap in core rather than a UI concern.** `workspace::create` refuses an empty `machine` and an empty `repo` (`Error::Empty`) and does **not** check `startup`, so `Some("")` renders `startup = ""` into the TOML. That is not "just a shell" — absent is the state that means that (ADR-0007, and the dashboard renders a null `startup` as a blank cell rather than the word "none"). It is a workspace whose startup is a command that cannot run, and `up` will try to run it.<br>**The form is already safe and that is why this is not urgent**: it omits the key entirely rather than sending `""`, which is tested. **The CLI is not** — `yantra new x --machine m --repo /r --startup ""` writes the broken file today, and so does any hand-written POST, since the route passes `startup` through untouched.<br>**Where the fix goes is the whole question.** Refusing `Some("")` in `create` is one line and matches the two checks beside it; treating it as `None` is the friendlier reading and is the kind of quiet coercion that makes two callers disagree about what they wrote. Refusing is the shape the module already has.<br>**Done: refused, not coerced.** One check in `create_in` beside the other two, `Error::Empty { field: "startup" }`, using `.trim().is_empty()` so `"   "` goes the same way `machine` already does — a whitespace-only command is the same non-command, and `machine` is the neighbour to match because `repo` is a `Path` and tests emptiness differently. **Neither caller needed a line**: `from_create` matches `Error::Empty { .. }` on the field wildcard, so the new field is a **400** already, and the CLI's `report_error` prints *a workspace's startup cannot be empty* and exits 1 — verified with the real binary against a scratch `XDG_CONFIG_HOME`, which refused `--startup ""` and `--startup "   "` and left no file behind, while a normal create and a no-`--startup` create still wrote theirs.<br>**The loud negative: `load` still accepts `startup = ""` from a file on disk.** This closes the writing path only. A hand-edited workspace still carries a blank command into `up`, and refusing it at load is a different decision — it would make an existing file stop the whole of `list()`, which is the rule a malformed file already gets and a blank string may not deserve. |
| Y-121 | The workspaces table does not fit a phone, and the buttons are the part that falls off | ✅ done | claude | Y-113 | **Found by Y-113 by measuring the shipped page in a 390 px viewport, and it is the acceptance test's own last inch.** `DataTable` is one `<table>` in an `overflow-x-auto`, and shadcn's cells are `whitespace-nowrap`: the workspaces table is **749 px wide inside 295 px of card**, so `ACT` begins at **310 px** and the start button is off screen until the row is swiped. Y-113 spent what a column order can — `ACT` moved ahead of `REPO`, `STARTUP` and `COMMAND`, and the heartbeat badge stacked under the machine name for 120 px — and 15 px is what was left. **A table is the wrong shape here, not a table with better widths**: `bishwajeets-macbook-pro` is 23 characters and the workspace name is beside it. What this wants is the workspaces section rendering as one block per workspace below a breakpoint, which is a `DataTable` change and touches four tables, so it is its own row rather than Y-113's. **The other four tables have always overflowed** — this is not a regression, it is the first time something you have to *tap* was in the part that falls off. **Renumbered from Y-120 on 2026-08-03**: the row that had already taken that id proposes the count check, and two rows claiming one identifier is the cost §B5 names — a commit message stops saying which work it belongs to. Two changes ran in parallel and both reached for the next free number, which is the collision worth designing against rather than the mistake worth apologising for.<br>**Shipped 2026-08-03 in [`DataTable.tsx`](../../web/src/components/DataTable.tsx) and nowhere else.** Below `48rem` a row is drawn as a labelled block — every header a `<dt>`, every cell a `<dd>` — and above it the table is byte-for-byte what it was. The breakpoint is read with `matchMedia` through `useSyncExternalStore`, so narrowing a window swaps the shape without a reload (verified live: loaded at 1280 px, resized to 390 px, the table became blocks). All four `Column<T>[]` arrays are untouched and `DataTable` still owns the empty state.<br>**Measured in headless Chromium 151 over the real HTTPS URL**, `yantrad` serving `YANTRA_WEB`, two real workspaces on the fleet. **Before, at 390 px**: table **924 px** wide in a **310 px** visible box, `ACT` beginning **310 px** into the table, `Start claude` at **x 358–443** — its centre 10 px past the screen, and `document.elementFromPoint` there returns `null`. **At 320 px** the same button is at 358–443, entirely off a 320 px screen. **After, at 390 px and at 320 px alike**: no table exists at all, the button is at **x 140–225**, `elementFromPoint` at its centre returns the button, and `document.documentElement.scrollWidth` equals the viewport. The cost is vertical — the workspaces card grows 322 px → 527 px at 390 px and → 581 px at 320 px — which is the scroll a page already has.<br>**Dropping columns was rejected on a measurement, not on taste.** The first two cells alone are **127 + 184 = 311 px**, wider than the 310 px a 390 px phone shows and far wider than the 240 px a 320 px one does: a table cut down to `WORKSPACE`, `MACHINE` and `ACT` still puts the buttons off the screen. Hiding a fact would have bought nothing, so nothing is hidden (R-23).<br>**This row's own numbers were data, not constants, and did not reproduce.** 749 px in 295 px was one workspace's table; two workspaces measure 924 px in 310 px. `ACT`'s **310 px offset reproduced exactly**, which is why the conclusion held. **`48rem` rather than `40rem`** because 768 px is the narrowest width where the table's own `ACT` cell (x 358–443) is on screen without a sideways swipe — measured, not chosen. **jsdom implements no `matchMedia` whatsoever**, so every dashboard test now names the width it draws at and the stub evaluates the query the component really asks. **62 dashboard tests**; the two that matter were proven by disabling the branch and watching them fail. |
| Y-112 | ADR-0016, and write routes authorised by peer identity | ✅ done | claude | Y-105, Y-108 | **A readable API and a writable one are different objects.** R-22 says the bind address is the entire security model because Q6 removed auth; that is defensible for reads and not for *start a process on my Mac*. **Owner's decision, 2026-08-02: Tailscale identity.** The mechanism is already built — Y-105 put each peer's tailnet addresses on `MachineInfo` and Y-108 attributes a heartbeat by source address — so writes resolve the caller to a peer and refuse anyone they cannot name. The ADR comes first because this is a decision, not an implementation detail (§B5). Then `POST /api/workspaces/{name}/{up,down,resume}`.<br>**Shipped 2026-08-02 in three parts: the ADR, the core seam, then the routes.** **I-52 came out of the seam and is the finding that outranks the feature**: `tailscale whois` and `tailscale status` name the same node's identity under different keys — `StableID` vs `ID`, `User` vs `UserID` — and `whois` carries a *second*, numeric `ID` that deserialises into the same field without erroring. Either mistake yields an authoriser that refuses everyone, or defaults to zero and matches a node nobody owns; neither fails loudly. The documents also disagree on shape, so they need separate structs.<br>**Three refusals that are easy to get subtly wrong**, all tested: a `tailscale` that cannot answer is **503 and never 403**, because nothing was decided about the caller and blaming them is a lie about which thing broke; **tags are checked even when the owner matches**, since a tagged node *is* owned by the tailnet and the user check alone would admit a CI runner; and identity never comes from the body (ADR-0013 §5).<br>**`Caller` deliberately omits the login name** — nothing authorises on it, and reading it would put a person's identity into the daemon's memory for no decision, the same boundary `agent::Status` draws.<br>**Verified live**: authorised from this machine's own tailnet address, then 404 with the path it looked in; a body typo 422; an invalid name 400; reads unaffected. **The refusal paths are unit-tested rather than live, because ADR-0016's own measurement says this tailnet holds no node that should be refused.** **The machine is *not* in the body** — `up` has no override, which is [Y-117](../../tracker.md#3-task-board). |
| Y-113 | The dashboard acts: a machine picker and real buttons | ✅ done | claude | Y-112, Y-116 | **This is the milestone's acceptance test**: tap a workspace on a phone, choose the Mac, tap start, Claude Code is running in the right repo. Y-097 deliberately shipped the opposite — a row hands over a command to paste into a terminal, which from an iPad is worth nothing. `Command` stays where a write still does not exist (`attach` is a paste by design, Y-078). Y-109 is not a dependency but makes the picker far better: free RAM and power state beside each machine is what a person chooses on.<br>**Shipped 2026-08-03 as [`web/src/components/Act.tsx`](../../web/src/components/Act.tsx), one cell of every workspace row.** Start, stop and resume against `POST /api/workspaces/{name}/{up,down,resume}`. **The picker is not a machine argument and must not become one** — the target is `workspace.machine` and [Y-117](../../tracker.md#3-task-board) says why a transient override breaks three verbs; what is *shown* is the machine and Y-109's reading of it in the cell beside the buttons, before either is tapped. A machine the tailnet does not list gets **no** state rather than a guessed one, which is not hypothetical: `g14-via-mac` is an `~/.ssh/config` alias (I-38) and rendered exactly that way live.<br>**Verified against the real fleet from a real browser over the real HTTPS URL, 2026-08-03**, not by curl and not only by tests. Created a workspace, tapped start, and `claude 2.1.220` was really running in a tmux session in the named repo — the pane's own start command reading `cd '<repo>' && exec '<home>/.local/bin/claude' --session-id '<uuid>'`. Tapped start again: *that session was already open … so this attached to it rather than starting a second* (§B4, I-30). Tapped resume: a pane running `--continue --fork-session --session-id` (ADR-0015). Tapped stop: *Stopped … The agent ended: Crashed { status: 1 }*. Tapped stop again: *nothing was running … so there was nothing to stop*. A session was created and killed on **`bishwajeets-macbook-pro`** itself, and both machines were left with no tmux server. `404`, `400` and `422` were produced live; `403` and `503` stay unit-tested, because ADR-0016's own measurement says this tailnet holds no node that should be refused.<br>**The acceptance sentence still does not work on the Mac, and the reason is not in this repo.** Claude Code **is installed there** — `2.1.220` at `~/.local/bin/claude`, found by `Claude::resolve`'s I-34 search — and is **not logged in**: `claude auth status` answers `{"loggedIn": false, "authMethod": "none"}`. So `up --agent claude` is refused before a pane exists, and the row says so in the daemon's own words. **That reading was wrong, corrected 2026-08-03 when the owner said the Mac is logged in — and it is: this is [I-44](../../crates/yantra-core/tracker.md), tripped by the task that had the invariant available to it.** Claude Code keeps its token in the macOS **login keychain**, and an ssh session lands in launchd's `Background` domain rather than `gui/<uid>`, so the read fails and the CLI reports `loggedIn: false` with nothing explaining why. Re-measured on the Mac: `launchctl managername` answers **`Background`**, the keychain item's **metadata reads fine** — which is what makes an existence check pass — and `security find-generic-password -s "Claude Code-credentials" -w` exits **36 with zero bytes on stdout and stderr**, I-25's shape of silence. **The account is logged in; the token is simply unreadable from where Yantra stands.** So the blocker on the acceptance test's *on the Mac* clause is not a login and is carried by [Y-122](../../tracker.md#3-task-board). There is also no `personal-website` checkout on the Mac at all, which [Y-109](#m5--control-from-the-phone-buildable-work-finished-2026-08-03) had already found for this repo's own checkout. Neither is a code defect; both are the fleet.<br>**Three things found by building it.** (1) **`launched` reports an *agent*, and a workspace's own `startup` is not one** — `up::up` sets it from the agent alone, so a created session running `startup` answers `launched: false` and the first wording called that *a plain shell*, which was a lie about a session that really was running `sleep 600` on the Mac. Caught live, not in review. (2) **`resume` refusing at the trust prompt arrives as `500`** — it is an `up::Error` and not a `workspace::Error`, so `from_workspace` falls through to *the verb failed*; the sentence is right and the status says *server fault* about a state of the world. Left alone, since `write.rs` is Y-112's. (3) **The buttons are 15 px past the right edge of a 390 px phone** — measured: the table scrolls, `clientWidth` 295, `scrollWidth` 749, and `ACT` starts at 310 even after the heartbeat badge was stacked under the machine name to reclaim 120 px. `ACT` was moved third, ahead of `REPO`/`STARTUP`/`COMMAND`, which is as far as a column order goes; a phone wants a different shape, which is [Y-121](#m5--control-from-the-phone-buildable-work-finished-2026-08-03).<br>**What was deliberately not built.** No wake button (Q10, Y-115). No `up --machine` (Y-117). No plain-shell start for an agent workspace — one button, labelled `Start claude` or `Start`, which is what it will do. **The agents section still hands over `yantra up` and `yantra resume` as pastes**: that is Y-097's derivation off Y-084's verdict and folding it into buttons is its own change, not this one's. **59 dashboard tests**, and the three that matter were each proven by breaking the code: an idempotent `up` drawn as a failure, the five refusals collapsed into one, and the in-flight state skipped. |
| Y-116 | Create a workspace from the dashboard | ✅ done | claude | Y-112 | **Found by checking the plan against [`docs/brainstorm.md`](../../docs/brainstorm.md), 2026-08-02, and it is the one place M5 was out of sync with the vision.** UI Philosophy says it plainly: *"Everything should be configurable from the interface. No YAML editing. No configuration files. Configuration files are implementation details. The interface should generate them automatically."* Yantra requires hand-writing TOML, and this is not theoretical — **the fleet has zero workspaces**, and `yantra ls workspaces` answers *"make one at `~/.config/yantra/workspaces/<name>.toml`"*. **So M5's acceptance test cannot start**: there is no `personal-website` workspace, and creating one means finding a terminal, which is the thing the milestone exists to stop. The file stays the source of truth (Y-044 established it is the one store Yantra did not invent and should not replace) — the UI **generates** it, it does not supersede it. Schema is [ADR-0007](../../docs/adr/0007-workspace-schema-v1.md) plus [ADR-0010](../../docs/adr/0010-drop-branch-from-the-workspace-schema.md)'s amendment. **It needs a CLI verb first, which does not exist.** [`crates/yantrad/CLAUDE.md`](../../crates/yantrad/CLAUDE.md) binds it: *"anything the web UI can do must be expressible in `yantra` first"* — that is what stops the daemon growing a richer API the CLI cannot reach. `yantra` had eight verbs and **none created a workspace**, so this task is two: the verb, then the form. **The verb shipped 2026-08-02** — `yantra new <name> --machine <m> --repo <p> [--startup <c>]`, and `workspace::create` under it. It **refuses to overwrite**, because a `new` that replaced a workspace would lose the operator's own file to a typo in a name, and it validates **neither** `machine` nor `repo`: ADR-0009 has Yantra never resolving a machine, and `repo` is a path on the *far* side that `up` already refuses there before a session exists (Y-081) — checking locally would check the wrong filesystem. The TOML is written by hand rather than serialised, so the file keeps the shape the README shows and [`Workspace`] gains no wire format (ADR-0005); a quote or backslash in a path or startup command survives the round trip, tested by writing and reading back rather than by eye. **This also absorbs [Y-117](../../tracker.md#3-task-board)**: choosing the machine here is choosing it at all, with one source of truth. **Secrets stay references, never values** (§B4), which the form must enforce rather than merely document.<br>**The route shipped 2026-08-03; the form is what is left.** `POST /api/workspaces` takes `{name, machine, repo, startup?}` with `deny_unknown_fields`, authorised by the same ADR-0016 lookup as the other three, and answers **`201` with the created workspace**. It is the one write that touches no machine, so it is also the one that never waits on ssh. **A name already taken is `409`, not `400`** — the caller asked for something reasonable that the world already answers, and `400` would send them hunting a mistake they did not make; an unusable name and an empty field stay `400`, and no config directory is `500` because a user without one did not make a bad request.<br>**Two things measured live and worth keeping.** `GET /api/workspaces` did **not** contain the new workspace for 15 s after the `201`, because `refresh.rs` looks every 30 s and a create does not poke it — so the `201` returns the whole workspace and the form must render *that*, not re-read the list, or it will draw an empty one immediately after a success. And `GET` and `POST` on `/workspaces` live in two different routers, which reads like a conflict and is not: axum merges the method routers and would only panic on two handlers for the same method. There is a test holding both halves of that.<br>**§B4 cannot be *enforced* at this seam, and saying otherwise would be theatre.** ADR-0007's schema has three keys and none of them is a secret, so there is no reference field to validate — the only string that could carry a value is `startup`, which is an arbitrary shell command, and any check over it is a heuristic that either misses the real case or refuses a legitimate one. What actually holds the rule is that the form offers **nowhere to type a secret**: `deny_unknown_fields` refuses a `secrets` key outright rather than dropping it silently, which is tested. If §B4 is to become enforceable it needs a schema change, and that is an ADR, not a validator.<br>**The form shipped 2026-08-03 and closes this**: [`web/src/components/NewWorkspace.tsx`](../../web/src/components/NewWorkspace.tsx), one `<Section>` between the workspaces table and the sessions one, posting the three keys and the optional fourth. It is wrapped in the **machines** reading rather than its own, so the picker cannot draw a machine nobody looked for, and a failed inventory look renders as that look's error instead of an empty `<select>`. An **offline** machine is offered and choosable (ADR-0009), which is Y-117's absorption and `brainstorm.md`'s Manual mode. The **`201`'s own body is what is rendered**, with a line saying the table above is a 30 s reading and may not list it yet. Each status keeps its own sentence — `409` taken, `400` unusable, `422` a field the daemon does not know, `403` a node that is not the owner's, `503` a `tailscale` that could not answer — and the daemon's plain-text body is shown whole beneath it; the four that matter are tested against `App`.<br>**Three things the brief got wrong, all small and all worth knowing.** (1) **The `400` for an empty field is unreachable from this form** — `required` stops the submit before a POST exists — so `400` here means an unusable *name* in practice, and the empty-field branch is the CLI's and a hand-written request's. (2) **An empty `startup` must be omitted, not sent as `""`.** `create` checks `machine` and `repo` for emptiness and **not** `startup`, so `Some("")` renders `startup = ""` and produces a workspace whose startup is a command that cannot run — absent is the state that means *just a shell*, and the form sends no key at all. (3) **Editing did not ship and this row's title said it would.** There is no `PUT`, no `PATCH`, and no `yantra edit`, and `crates/yantrad/CLAUDE.md` binds the order — the verb first, then the route, then the form. The title is now what was built; editing needs its own row when it is wanted, and the file stays the operator's to edit meanwhile. |
| Y-114 | PWA shell, installable on iOS | ✅ done | claude | Y-111 | Manifest, icons, `apple-touch-icon`, `display: standalone`, and an offline shell. **The service worker must never cache a reading** — a cached dashboard is R-23's confident lie with a longer memory, and the whole point of the read model is that a failed look replaces a good one rather than hiding behind it. Cache the shell; never the data.<br>**Shipped 2026-08-03 as 45 lines of `web/public/sw.js` and a nine-line manifest — no `vite-plugin-pwa`.** §B2 applied to the toolchain: the only thing the plugin adds over this is a build-time precache manifest of Vite's hashed filenames, which `install` reads back out of `index.html` in four lines, and against that it brings a Workbox runtime, a config to audit, and defaults that cache more than the shell.<br>**A reading is not cached because it is never intercepted at all.** `/api`, `/healthz` and `/heartbeat` get no `respondWith`, so the browser issues them itself and a daemon it cannot reach lands in `useLooked`'s `failed` envelope exactly as with no worker installed — there is no cache to go stale rather than a cache that is kept fresh. `web/src/sw.test.ts` runs the **shipped** `sw.js` through `new Function` against a fake `caches`, and the test that matters plants a reading in the cache by hand and asserts the worker still will not serve it; deleting the guard fails three of the five.<br>**Verified in a real Chromium over the real HTTPS URL**, driven by CDP: registration `scope = https://cachyos-g14.<tailnet>.ts.net:8443/`, state `activated`; the `yantra-shell` cache held `/`, the hashed JS and CSS, one `woff2`, `/favicon.svg`, `/manifest.webmanifest`, `/apple-touch-icon.png`, `/icon-192.png` and **zero** `/api` entries while `/api/machines` was answering **200** through the same origin. With the network cut, a deep link to `/workspaces/yantra` drew the shell from cache and every section read *The look failed. TypeError: Failed to fetch* — offline presents as offline. `ServeDir` types the manifest **`application/manifest+json`** (`mime_guess` has the extension), `sw.js` `text/javascript`, the icon `image/png`; no daemon change was needed.<br>**The loud negative: the daemon being down is not the offline case, and the worker does not pretend it is.** `tailscale serve` answers **502 itself** when nothing is listening on 7717, so `fetch` *resolves* and the shell is not taken from cache — the browser's error page names the 502. Measured with the proxy up and `yantrad` stopped. Kept deliberately: nothing answering is offline, something answering badly is a broken daemon, and drawing the app over a 502 would hide which. The cost is that a `yantrad` restart shows an error page rather than the app.<br>**Icons are committed, because generating them at build time is a dependency for four files.** `favicon.svg` rasterised onto white — opaque because iOS composites a transparent home-screen icon onto black — with `rsvg-convert` and ImageMagick; the exact commands are in [`web/README.md`](../../web/README.md). 78 KB for three PNGs against a rasteriser in `package.json`. **The manifest names no colour**: `theme_color`/`background_color` take a literal and `index.css` is the swap point a design system replaces, and neither is needed for installability.<br>**What could not be verified, stated rather than implied: the iOS install itself.** No iPhone or iPad was available. Chromium proves the worker registers, its scope, and what is and is not in its cache; it cannot prove Safari's *Add to Home Screen* honours `display: standalone`, takes its icon from the `<link rel="apple-touch-icon">`, or that the launched window is not a Safari tab. Those three need a device.<br>**Two things this row's own framing had wrong.** The task was written when the dashboard was read-only, and Y-112/Y-116 shipped writes before it ran — the worker leaves every non-`GET` alone, which is what keeps `POST /api/workspaces` out of this entirely. And, noticed while reading the offline page: a `failed` look renders **`looked 0s ago`** above its error, because `useLooked` stamps `age_seconds: 0` on the envelope it invents for a dead fetch. Pre-existing and not touched here (§A3), but *0s ago* beside *the look failed* is the one sentence on that page that reads better than the truth. |
| Y-115 | An unreachable machine reads honestly and offers no wake | ✅ done | claude | Y-109 | **Q10 answered by the owner, 2026-08-02: there is no always-on machine on the Mac's LAN, so wake waits for M7.** Tailscale is L3 and magic packets are L2 broadcast; there is no `tailscale wake` and a sleeping NIC has no IP stack ([research 01 §6](../../docs/research/01-tailscale-inventory.md)). The note also warns the relay pattern wakes *powered-off* machines but often **not S3 sleep**, which is exactly a closed MacBook — so this is unproven work, not merely deferred work. What M5 owes is R-23's honesty: show *asleep or off* as that, and **ship no wake button that would not work**.<br>**Closed 2026-08-03 with no code, because nothing was missing.** The row was written before the three tasks that answered it, and each of its four clauses now has an owner. *Asleep or off must never read as we have not heard from it* is [Y-109](#m5--control-from-the-phone-buildable-work-finished-2026-08-03)'s `reporting()` in [`columns.tsx`](../../web/src/columns.tsx) — ADR-0013 §7's four states, threshold in the page rather than on the wire. *No wake button* is a thing nobody built: `wake` appears nowhere under `web/src` or `crates/`, and the shipped bundle's own DOM answers `/wake/i` **false**. *An unreachable machine must not read as a machine with zero sessions* is `Sessions` in [`App.tsx`](../../web/src/App.tsx) — the named alert plus `N sessions on A of B machines` — and predates this row. *The starting path must neither invent a refusal nor imply a success* is [Y-113](#m5--control-from-the-phone-buildable-work-finished-2026-08-03)'s [`Act.tsx`](../../web/src/components/Act.tsx), where the button stays live against a sleeping machine because ADR-0009 says the daemon decides. [`web/README.md`](../../web/README.md) already **names this row by number** and states its answer.<br>**Measured live at 390 px in a real Chromium over the real HTTPS URL, against a fleet that is mostly unreachable.** All six peers read **never heard from**, which is correct rather than alarming: `laptop-9ml3d644`, offline 28 days, draws `offline, key expired` · `never heard from` · `LAST SEEN 2026-07-05T18:46:07.1Z`, and **never** *asleep or off*. A throwaway workspace pointed at that laptop made the sections that need a target speak: `laptop-9ml3d644 unreachable: ssh to laptop-9ml3d644 failed before the command reported a status: ssh: connect to host laptop-9ml3d644 port 22: Connection timed out`, beside `0 sessions on 0 of 1 machines`, and an agents row reading *machine did not answer* — three ways of saying unreachable and not one of them saying empty. Tapping **Start claude** on it gave `starting…` disabled and *waiting on laptop-9ml3d644 — ssh gives it ten seconds*, then *The verb ran and failed. What it says below is the whole chain.* over that same ssh error. **The ten seconds is exact**: the same call timed **10.079 s**, so `ssh.rs`'s `ConnectTimeout=10` is what the sentence promises. The workspace was deleted and the fleet left as found.<br>**Three things this row was wrong about.** (1) **`Depends` names Y-109 and that is half of it** — the fourth clause is about buttons, and buttons are [Y-113](#m5--control-from-the-phone-buildable-work-finished-2026-08-03)'s, which did not exist when this was written. (2) **`asleep or off` still cannot be produced on this fleet**, confirming [Y-109](#m5--control-from-the-phone-buildable-work-finished-2026-08-03): the state needs a machine that *has* beaten and then goes `Online: false`, and nothing here has ever beaten, so it lives in tests until [Y-110](#m5--control-from-the-phone-buildable-work-finished-2026-08-03). (3) **No test was added, deliberately.** A test asserting the absence of a button nobody built asserts nothing, and the four states, the unreachable machine's sessions, the in-flight `up` and the start against a sleeping machine are already four tests in [`dashboard.test.tsx`](../../web/src/dashboard.test.tsx), each proven non-vacuous by the task that wrote it. **62 dashboard tests, unchanged.** |
| Y-073 | Asset serving: directory in dev, embedded at release | ✅ done | claude | Y-072 | **Moved into M5 on 2026-08-02 as its first task, having been an M4 leftover.** `yantrad` mounts `/api` and `/healthz` and nothing else, so the dashboard is served by **Vite's dev server** today — looking at it means running a development server on a laptop, which is not a thing to do from a phone. TLS (Y-111) has nothing to put behind it until this exists. It also settles the PWA's origin question the easy way: one process serving both app and API is same-origin, no CORS, service-worker scope `/`.<br>Vite with hot reload in development; assets embedded in the binary for release, so the M7 appliance stays one file to copy. **Embedding goes behind a cargo feature that is off by default** — the moment a Rust build needs a JavaScript toolchain, every `fmt`/`clippy`/`test` job and the musl cross-build grow a dependency on npm (R-24). Built assets are never committed.<br>**Shipped 2026-08-02 as the directory half only, and the embed is deliberately not here.** `YANTRA_WEB` names a directory of built assets, served as the router's fallback so `/api`, `/healthz` and `/heartbeat` keep winning. Unknown paths get `index.html`, which is what makes a deep link work. **Unset** serves the API alone and says so on `/` *and says how*; **set but wrong refuses at startup**, because a `ServeDir` over a missing directory answers 404 to everything and that reads as a broken dashboard rather than a typo in one variable. The cargo feature this row describes arrives with the M7 appliance that wants one file to copy — building it now would buy nothing and cost R-24 immediately. **Recorded because it is not obvious**: a path climbing out of the root answers **200 with the app**, not 403 — `ServeDir` refuses the climb and the SPA fallback then treats it as a route, so traversal and deep link are indistinguishable by status. Verified against the real built dashboard on the tailnet address, not only in tests. |
| Y-074 | README: install, usage, and a Status that is not two milestones stale | ✅ done | claude | — | Owner's ask, 2026-07-31: *"I know how to use it, but what after 1 month?"* **The answer was not an API docs site** — `cargo doc` already renders every `//!` header, and those headers are the best documentation in the repo, so a site would be a second copy with a build step for an audience [Q6](#) says does not exist yet. The real gaps were smaller and all in the README: its Status still described **M1** (*"no agent integration"* — that shipped in M3), it had never shown a single command, and the workspace schema was reachable only by reading [ADR-0007](../../docs/adr/0007-workspace-schema-v1.md) and then applying [ADR-0010](../../docs/adr/0010-drop-branch-from-the-workspace-schema.md)'s amendment yourself — a decision record standing in for a reference. Now: install from source (**verified** by installing to a throwaway `--root`, not asserted), what each target machine needs and why, the workspace file as a copy-pasteable block, all eight commands, the exit-code contract, and `cargo doc --open` named as the API reference. Layout table gained the per-crate files, `docs/plans/` and the session log. **No install instructions for a release** — Y-037 has never run, so there are no binaries to point at, and instructions for an unbuilt release are instructions that do not work. |

## §0's Status cell, as it read on 2026-08-03

The rolling narrative §0 carried until this archive was made. It was one 10 KB line that had only
ever been appended to, and it is the only running account of M4 and M5 as they happened, so it is
kept whole; §0 now holds a short current-state statement instead. Its `<br>` markers are rendered as
paragraph breaks and nothing else about it is changed.

✅ **M0–M3 closed. M4's back half is done.** `yantrad` binds only this machine's Tailscale addresses and refuses to start otherwise (Y-069), refreshes a snapshot in the background so no handler ever awaits ssh (Y-070), and now **serves that snapshot read-only at `/api`** (Y-071) — every answer saying whether the look succeeded, failed or has not happened yet, and how old it is. `yantra ls workspaces` renders the same `workspace::list()` **in-process**, which is [ADR-0012](../../docs/adr/0012-the-cli-and-the-daemon-are-two-callers-of-one-library.md) holding under its first real temptation. **276 Rust tests**, 5 skipped, and **62 in the dashboard**. 50 invariants, numbered to I-53.

**`yantra resume` ships (Y-077)** — the fourth verb `brainstorm.md` named and the promise Q9 used to kill Aider. Built on `claude --continue --fork-session --session-id <uuid>`, **not** `--resume`, because Yantra keeps no session id to resume by; the fork is what gives the predictable transcript path back. It refuses rather than guesses in every state where the pane holds something it did not start. **The loud negative: no flag can tell Yantra there was nothing to resume** — `--continue` in a directory with no earlier conversation starts a fresh one and exits 0.

**Y-081 closed**: a workspace whose `repo` is not on the machine is refused **before a session exists**, on the far side (ADR-0009). It was never a macOS quirk — `new-session -c <missing dir>` lands in `$HOME` and exits 0 on tmux 3.5a and 3.7b alike, so this was fleet-wide. `resume` is bound to the same check on its respawn path, because two verbs disagreeing about a missing repo is the confusing part.

**Q1, Q6 and Q13 answered by the owner. Q14 — what the web UI is built with — is the only thing blocking Y-072/Y-073**, and **Y-084** (agent status in the read model) stood between Y-071 and a complete dashboard until 2026-08-02.

**Y-082 and Y-083 closed**: one example workspace name (`yantra`), and the superseded runtime decision is gone from the repo entirely.

**2026-08-02, five tasks run in parallel worktrees.** **Y-044 is dropped without being built** — the audit found no consumer, because what the store would have held is already held by three stores Yantra did not write: the workspace TOML, tmux's `pane_start_command`, and the agent's own transcript. **Y-094 named the transcript** and, in doing so, **falsified a sentence in Y-091** — tmux wraps a multi-word start command in quotes of its own, which `status` never noticed because it only asks `is_some()`. That is now **I-51**. **Y-097 and Y-098** finished the design-independent half of [the dashboard plan](../../docs/plans/m4-dashboard-next.md): rows hand over a command, and staleness reads as a wedged refresh rather than an old number. Y-097 found that **`resume` is the one verb the read model cannot choose honestly**, which put a third thing behind Y-084 — and **Y-084 then closed**, per-machine on measurement, taking a live `tmux::pane` bug with it. **Y-096 is now unblocked and is the last of the dashboard's four sections**, and is in progress with Y-100. Two decisions the owner had been carrying are also settled and in flight — **Y-101** (ADR-0004's stale SQLite claim) and **Y-102** (the `resume` rationale becomes a standalone ADR-0015, not an amendment to ADR-0011). **Y-100 closed the thing that batch left behind**, and corrected the row that opened it: the margin was ~20 ms rather than two seconds, and `cachyos-g14` costs no `ConnectTimeout` at all — it refuses in **6 ms** because it is powered on with no sshd, so only a machine that is genuinely *asleep* pays the ten seconds. Staleness now names the machine its class is waiting on, which works because the sweep that pays the timeout is the sweep whose reading reports that machine unreachable. **Y-096 shipped the dashboard's fourth section**, so all four exist and the trust prompt — the one state where the machine has stopped and is waiting for a person (I-49) — is finally visible. Two owner decisions closed as **Y-101** (ADR-0004's stale SQLite claim, amended in place) and **Y-102** ([ADR-0015](../../docs/adr/0015-resume-forks-the-conversation.md)).

**`yantra-agent` is no longer a stub — the largest functional gap in the project is closed.** [The heartbeat-agent plan](../../docs/plans/the-heartbeat-agent.md) measured both seams ADR-0013 left open before any code was written, and five tasks then built it: the wire type (Y-104), peer addresses (Y-105), the probes (Y-106), the beat and a hand-written POST (Y-107), and the daemon's first **write** route (Y-108). Both machines now measure themselves and report every 10 s, attributed by source address, verified end to end across the real tailnet. **466 KB musl**, no async runtime, no HTTP crate. **Two corrections outrank the features**: **I-9 is regraded `[V]` → `[D]`**, because its *"desktops have no `AC*` entry"* half was never measured on a fleet that contains no desktop; and **R-22's blast radius is restated**, since the daemon is writable for the first time. **Y-103 is the owner's and is the one thing outstanding: ADR-0013 still reads `proposed` while six tasks have implemented it.** Y-109 makes a beat's age visible; Y-110 needs a laptop unplugged.

**M5 is re-scoped by the owner, 2026-08-02: `Placement` becomes `Control from the phone`, and automatic placement moves to M10.** The acceptance test is one sentence — *open the personal-website workspace in Claude on the Mac, from a phone, and start building* — and the audit it prompted found the gap is larger than it looked: **the dashboard is read-only by construction, not by omission.** Five `GET` routes plus `POST /heartbeat`, no write call anywhere in the web UI, and [`Command.tsx`](../../web/src/components/Command.tsx) saying so outright — *"the API answers 405 to every write, so the row hands over the command instead"*. Y-097's copy-a-command affordance is worth nothing on an iPad. Three constraints were measured before planning around them: the **PWA is blocked on TLS** (service workers need a secure context, which is also why the clipboard already fails) and the tailnet **already has HTTPS certs enabled**, so Y-111 needs no admin change; **waking a sleeping machine is impossible from the phone** until an always-on LAN peer exists, so Q10 is answered *defer* and Y-115 ships honesty instead of a dead button; and a **writable API is a different object from a readable one**, so writes are authorised by Tailscale identity (Y-112), reusing the peer lookup Y-105 and Y-108 already built. Nothing built for the old scope is wasted — ADR-0013's seven fields are exactly what a *person* reads when choosing a machine by hand. **Two pieces of drift closed with it**: Y-096 had read `todo` since it shipped, and the heartbeat plan is renamed off the `m5-` prefix now that M5 means something else. §0 gains a glossary, because the owner could not read his own tracker.

**2026-08-03 — M5 is one task from its acceptance test.** **Y-116 closed in two halves**: `POST /api/workspaces` (a name already taken is **409**, not 400) and the form that posts to it, which renders the **`201`'s own body** because `GET /api/workspaces` was measured *not* holding a new workspace for 15 s afterwards — `refresh.rs` looks every 30 s and a create does not poke it, so a form that re-reads to confirm draws an empty list right after a success. The row's title promised *and edit*; nothing edits, and it now says what was built. **Y-111 closed**: the dashboard is HTTPS at `https://cachyos-g14.<tailnet>.ts.net:8443/` via `tailscale serve`, one `just https` line and **no Rust** — §B2 holding under a milestone-blocking temptation. **The finding that outranks both is [Y-118](../../tracker.md#3-task-board): the proxy defeats [ADR-0016](../../docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md).** A reverse proxy terminates the caller's connection and opens its own, so every write through `:8443` is attributed to the machine running the proxy, measured across the fleet rather than reasoned about. Nothing in M5 is blocked — the proxy runs on the owner's own untagged node — but the authoriser now rejects nothing for a *second* reason, and the fix inverts ADR-0016 §4, so it is the owner's decision and not a quiet `if`. **Two process findings.** Two agents were run in one working tree and collided, one committing onto the other's branch; both recovered without loss, and parallel work now takes an isolated worktree. And **a fourth row was found closed-but-not-flipped** (Y-101, after Y-096 and Y-073), alongside §0 itself miscounting the invariants as 48 against an actual 49 — the pattern is that the task which *closes* a fact is not the task that *restates* it, which is exactly what §B5 exists to catch and is not catching. **M5’s buildable work is finished, 2026-08-03.** Y-073, Y-109, Y-111, Y-112, Y-113, Y-114, Y-116, Y-119 and Y-121 shipped; **Y-115 closed on evidence with no code written**, its four clauses already owned by Y-109, Y-113 and a sessions section that predates the row. The acceptance test runs end to end from a phone — create a workspace, start it, Claude Code really running in tmux in the named repo, start again and it attaches rather than duplicating, resume, stop — driven through the browser over HTTPS against the live fleet. **What it does not do is the Mac**, and [Y-122](../../tracker.md#3-task-board) is why: the token is in the login keychain and ssh lands in launchd’s `Background` domain (I-44), which [research 10](../../docs/research/10-claude-code-credentials-on-macos.md) then established is **architecture and not a setting** — no env var, flag or config moves the stored credential, and the only path that keeps the subscription is the one §B4 forbids. That research also produced **I-53**: `claude auth status` reports the credential it found and never that it works, so every environment-variable remedy trades an honest refusal for a healthy-looking useless session. **Everything still open is the owner’s**: Y-103, Y-110, Y-118, Y-120 and Y-122.
