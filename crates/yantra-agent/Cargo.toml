[package]
name = "yantra-agent"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
# `heartbeat`'s type and `agent::CANDIDATES` are the whole of what this binary
# takes from the library — neither links any code. The dependency edge costs
# 11 KB, and one call into ssh/tmux costs 319 KB.
yantra-core.workspace = true
serde.workspace = true
serde_json.workspace = true
time.workspace = true
