[package]
name = "yantra-agent"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
# `heartbeat` is the one module of the library this binary may call: the
# dependency edge costs 11 KB, and one call into ssh/tmux costs 319 KB.
yantra-core.workspace = true
serde.workspace = true
serde_json.workspace = true
time.workspace = true
