# Yantra — Tracker

**This file is the single source of truth for project state.**
Read it first. Update it last. If it disagrees with your memory, the file wins.

| | |
| --- | --- |
| **Project** | Yantra (यन्त्र) — personal developer control plane |
| **Current milestone** | `M5 — Control from the phone` (M4 closed 2026-08-02; M5's buildable work finished 2026-08-03) |
| **Status** | ✅ **M0–M4 closed. M5's buildable work is finished.** The acceptance test runs end to end from a phone, through the browser over HTTPS against the live fleet: create a workspace, start it, Claude Code really running in tmux in the named repo, start again and it attaches rather than duplicating, resume, stop. **What it does not do is the Mac** — the token is in the login keychain and ssh lands in launchd's `Background` domain (I-44), which [research 10](docs/research/10-claude-code-credentials-on-macos.md) established is architecture and not a setting. **Everything still open is the owner's**: Y-117, Y-118, Y-120 and Y-122, all in [§3](#3-task-board). How M4 and M5 got here, and this cell's own rolling narrative up to 2026-08-03, are in [docs/archive/m4-m5.md](docs/archive/m4-m5.md). |
| **Runtime** | **Rust** (daemon, CLI, per-machine agent) + **TypeScript** (web UI only) — see [ADR-0004](docs/adr/0004-rust-for-the-daemon.md) |
| **Top risk** | **R-7** — Windows is a second path with no tmux and no Tailscale SSH server, and Q4 is still deliberately open. *R-21 (macOS agents cannot authenticate over ssh, I-44) is **downgraded**: it is real and unfixed, but Y-059 found a machine that works without touching it, so it costs a target rather than the milestone. R-1 refuted on Linux, still untested on macOS; R-2 is now handled in code — see I-47 and `status.rs`.* |
| **Last updated** | 2026-08-03 |
| **Updated by** | Claude (session: `asleep or off` recorded as produced on real hardware, completing ADR-0013 §7's four display states; five malformed table rows repaired) |

---

## 0. How to use this file

**Rules for every contributor, human or agent:**

1. **Before starting work** — read §2 (milestones) and §3 (task board). Pick a task that is `todo` and whose dependencies are `done`.
2. **When starting** — set the task to `doing` and put your name in Owner.
3. **When finishing** — set to `done`, add the artifact path in Notes, and append a line to §8 (Session log).
4. **When you learn something that changes the plan** — do NOT silently change course. Add an entry to §5 (Decisions) or §6 (Open questions) and flag it.
5. **Never delete a task.** Move it to `dropped` with a one-line reason. The graveyard is evidence.
6. **One task = one reviewable change.** If a task cannot be finished in a single sitting, split it.

**Status vocabulary:** `todo` · `doing` · `blocked` · `review` · `done` · `dropped`

### What the prefixes mean

Added 2026-08-02, because this file opens by calling itself the source of truth and then uses seven
undefined prefixes. **Every identifier in the repo is one of these:**

| Prefix | Is a | Lives in | Example |
| --- | --- | --- | --- |
| `Y-123` | **Task.** A single reviewable change. | §3 below | Y-109 — show heartbeat age on the dashboard |
| `M0`–`M10` | **Milestone.** A stage of the project. | §2 | M5 — control from the phone |
| `ADR-0013` | **Architecture Decision Record.** A dated document saying *we decided X, and here is why* — written so the reasoning survives, not just the outcome. Immutable once accepted; if one is wrong you write a superseding one rather than editing it. | [`docs/adr/`](docs/adr/) | ADR-0013 — what a heartbeat carries |
| `Q4` | **Open question**, usually one only the owner can answer. | §6 | Q4 — Windows: first-class or WSL2-only? |
| `I-51` | **Invariant.** A rule learned by getting it wrong once; violating it produces a bug that looks like something else. | the **crate** trackers, §1b says which | I-51 — tmux re-quotes multi-word commands |
| `R-22` | **Risk.** Something that could go badly, tracked so it is not rediscovered. | §7 | R-22 — the bind address is the entire security model |
| `R1` *(no hyphen)* | **Research note.** Dated evidence gathered before deciding. | [`docs/research/`](docs/research/) | R1 — Tailscale inventory |

> **`R-1` and `R1` are different things** and always have been — risk one versus research note one,
> told apart by a hyphen. Nothing else in this scheme collides. Read `R-` as *risk* and a bare `R`
> as *research*; when writing new prose, prefer naming the research note by file rather than by
> number.

---

## 1. Guardrails (read before writing any code)

These exist because this project's failure mode is obvious: it is a nine-phase mega-vision, and
mega-visions die in Phase 4 with a beautiful architecture and nothing that runs.

- **Walking skeleton first.** M1 is one thin end-to-end slice, not a layer. No layer gets built to completion before the whole path works badly.
- **One machine before many.** Everything must work against a single machine before it works against the tailnet.
- **Localhost is not a special case.** Every machine — including the one the daemon runs on — is reached over SSH. One code path, forever. The tempting shortcut (spawn `tmux` directly when local) buys a little convenience now and costs two divergent code paths for the life of the project, where the well-tested one is the one that matters least. Decided 2026-07-28; goes into ADR-0005.
- **Orchestrate, don't reinvent.** If a task's description starts to look like "implement SSH/terminal multiplexer/scheduler", stop and re-read the brainstorm.
- **Provisioning is a permanent non-goal.** Yantra adopts machines that already exist; it never creates, images, or destroys them. This is precisely the line that separates it from Coder — and R4's verdict is blunt: *if Yantra ever grows a provisioning layer it becomes a worse Coder and should be deleted.* **This guardrail is where that is written down**, together with [CONTRIBUTING.md](CONTRIBUTING.md); no ADR carries it, and Y-080 found three places that claimed one did.
- **One agent first. Do not build the universal adapter.** The vision doc says every agent should behave identically; that is the *destination*, not the starting point. Yantra supports **Claude Code only** in M3 — the one actually installed, with the strongest resume story (`--session-id` presets the UUID, verified in Y-023). A second agent gets added when a second agent is genuinely wanted, and the shared interface is extracted **from two working implementations**, never guessed ahead of one. Owner's framing, 2026-07-28: *"use the cli that we want, not to join everything. If needed we can do it later."*
- **No UI until the CLI is good.** The CLI is the API's first client and its honesty check.
- **No hardware until the software is boring.** Phases 7–9 are a reward, not a shortcut.
- **Test hard.** Every orchestration primitive gets an integration test against a real sshd + real tmux. Mocks lie about SSH. **The fixture is a disposable podman container**, not the host — a separate filesystem, user and network hop make it a more honest stand-in for a remote machine than `ssh localhost`, and it changes nothing about the host's security posture.
- **Rust for the daemon, TypeScript for the web UI** — [ADR-0004](docs/adr/0004-rust-for-the-daemon.md). Chosen for quality and appliance fit over iteration speed, with that trade made explicitly. Rust punishes design churn, so **think before writing**: this is the milestone where the shape is still unknown.
- **Orchestration lives in `yantra-core`, a library crate** — [ADR-0005](docs/adr/0005-core-logic-in-a-library-crate.md). The binaries are thin. The library never prints and never exits, which forces a typed error from the first fallible call instead of an M2 cleanup; M2's daemon changes *where* the code is called from, not *what* it does.
- **Keep SSH, tmux, telemetry and hardware behind narrow traits.** Not to enable a runtime swap any more — to keep the four seams that touch an unreliable outside world fakeable in tests.

---

## 1b. Invariants

Non-obvious rules that research proved the hard way. Violating one produces a bug that looks like
something else entirely — which is why they are worth more than the code they constrain.

**They live with the crate they bind**, because that is the file open on the screen of whoever is
about to break one. Moved there on 2026-07-31 (Y-068).

| Where | What, and how load-bearing |
| --- | --- |
| [crates/yantra-core/tracker.md](crates/yantra-core/tracker.md) | I-1…I-6, I-8, I-20…I-30, I-33…I-37, I-39…I-49, I-51…I-54 — ssh, tmux, agent, terminfo, logs, inventory, workspace. Every one earned by execution against a real machine. [`CLAUDE.md`](crates/yantra-core/CLAUDE.md) maps them to modules. |
| [crates/yantrad/tracker.md](crates/yantrad/tracker.md) | I-7, I-10…I-14, I-18 — SQLite, scheduling, PTY. **None exercised by shipped code**, and two were measured against runtimes ADR-0004 removed. |
| [crates/yantra-agent/tracker.md](crates/yantra-agent/tracker.md) | I-9, I-19 — telemetry and hardware. Neither exercised by shipped code. |
| [crates/yantra/tracker.md](crates/yantra/tracker.md) | None, and that is the finding: the CLI talks to nobody else's program, so it has nothing to be surprised by. Its contract is its exit codes. |

The four below bind no crate. They are about how this project *works* — what counts as evidence —
rather than what it talks to, so they stay here where the guardrails are.

| # | Invariant | Why | Src |
| --- | --- | --- | --- |
| I-31 **[D]** | **A test suite of instantaneous commands proves nothing about duration.** Any primitive that wraps a remote process needs at least one test with a command that takes seconds. | Y-041 shipped a watchdog that killed everything slower than ~200 ms and passed CI, because every command in its suite finished in under a millisecond. Found only when Y-042 ran `tmux`, which is merely slow enough to notice. | Y-042 |
| I-32 **[V]** | **A green CI run is not evidence unless it ran on the environment that failed.** GitHub rolls runner images out gradually, so a fix can go green five times on machines that were never broken. Re-run until the job lands on the failing image, and print the versions that matter so you can tell which one you got. | Y-048's fix passed five consecutive runs on image `20260720.247`, which had the working podman. Only the sixth landed on `20260726.254` and actually tested anything. Without the version printout, all six looked identical. | Y-048 |
| I-38 **[V]** | **A machine's own MagicDNS short name is not its tailnet address.** | `getent hosts cachyos-g14` returns `127.0.1.1`: `/etc/hosts` shadows MagicDNS for the local hostname. Self-dialling also never traverses the WireGuard path, so `tailscaled`'s port-22 interception is never reached and a self-directed `ssh` gets `Connection refused`. A workspace that names the local machine therefore fails looking like a network problem. | Y-049 |
| I-50 **[V]** | **A host *can* reach a normally-bound listener on its own tailnet address — it is the MagicDNS *name* that fails, not the address.** Anything dialling this machine must use the address. | Sharpens **I-38**, which generalised too far. I-38's "self-directed traffic never traverses the WireGuard path" is about `tailscaled` **intercepting** port 22 — interception needs that path. A real listener bound to the tailnet address needs no interception, because the address is on a local interface. Measured against a running `yantrad` on `cachyos-g14`: `curl` to its own tailnet address returned `ok`, while the same request to its own short name failed, `getent hosts` giving `127.0.1.1`. So the machine running the daemon is **not** a special case for a heartbeat, and ADR-0013 no longer has to assume it might be. | Y-020 |

### Retired

| # | Invariant | Why | Src |
| --- | --- | --- | --- |
| ~~I-15~~, ~~I-16~~, ~~I-17~~ | **Retired by [ADR-0004](docs/adr/0004-rust-for-the-daemon.md).** All three constrained a runtime Yantra does not use, and nothing replaced them. The numbers are not reused. | | — |

---

## 2. Milestones

Renumbered from the original 9-phase roadmap in [docs/vision.md](docs/vision.md) to front-load a working
end-to-end path. The original phases still map: M1 covers old Phases 1–3, M2 = Phase 5, M3 = Phase 4, etc.

| ID | Milestone | Definition of done (the demo that proves it) | Status |
| --- | --- | --- | --- |
| **M0** | Foundations | Research notes landed, ADRs written, repo skeleton + test harness runs green. | ✅ **done**, [archived](docs/archive/m0-m1.md) — 9 research notes, 4 ADRs, cargo workspace, CI + release pipelines, repo scaffolding. 29 commits. |
| **M1** | Walking skeleton | `yantra up demo` on my Linux box: reads one workspace file → opens tmux session → cd's to repo → runs startup cmd → prints attach hint. Idempotent (running twice attaches, doesn't duplicate). Integration-tested against a real sshd + tmux in a disposable podman container (§B3 — stricter than the `ssh localhost` this originally said). | ✅ **done**, [archived](docs/archive/m0-m1.md) — PRs #6–#11. `yantra_core::{workspace,ssh,tmux,up}` + the CLI, 4 ADRs (0005–0008), 8 invariants (I-24…I-31), 33 tests. |
| **M2** | Real machines | Same command targets a *remote* tailnet machine chosen by name. Machine inventory from Tailscale. `yantra ls machines` / `yantra ls sessions` work. | ✅ **done** 2026-07-30 — Y-050..Y-058, 9 tasks. `up` opens a session on the MacBook by name and attaches on the second run, as a committed test; `ls machines` and `ls sessions` both work. 10 invariants came out of it (I-34…I-43). Q4 (Windows) stays open by choice and blocks nothing. |
| **M3** | Agent orchestration | `yantra up demo --agent claude` launches Claude Code in the session; `yantra logs demo` tails its transcript; `yantra down demo` stops cleanly. **Claude Code only** — no plugin abstraction until a second agent is actually wanted. | ✅ **done** 2026-07-31 — Y-059..Y-065. All four commands verified end to end against a **real** Claude Code 2.1.220, not a stub: the agent launched under the id Yantra chose, `status` read it back from `claude agents --json`, `logs` rendered its transcript, and `down` produced `stopped cleanly (exit 143)`. Six invariants, I-44…I-49. |
| **M4** | Web UI | Read-only dashboard over the same HTTP API the CLI uses: machines, workspaces, sessions, live status. Served over Tailscale. | ✅ **done** 2026-08-02, [archived](docs/archive/m4-m5.md) — [docs/plans/m4-web-ui.md](docs/plans/m4-web-ui.md) then [m4-dashboard-next.md](docs/plans/m4-dashboard-next.md), Y-068..Y-102. `yantrad` serves the read model at `/api` and the dashboard draws its four sections, each stamping its own age. Q13 and Q14 both answered before code started. |
| **M5** | Control from the phone | Start, stop and resume a session **from the dashboard**, choosing the machine from a list. Served over HTTPS and installable as a PWA. | ✅ **buildable work done** 2026-08-03, [archived](docs/archive/m4-m5.md) — [docs/plans/m5-control-from-the-phone.md](docs/plans/m5-control-from-the-phone.md), Y-103..Y-121. The acceptance test runs from a phone over HTTPS against the live fleet; **Y-118 and Y-122 stay open and are both the owner's**, and the *on the Mac* clause is blocked by Y-122. **Re-scoped by the owner on 2026-08-02**: `Placement` moves to M10. Two machines, one of which is usually the right answer, does not need a scheduler — it needs a list to tap. Nothing built for the old scope is wasted: ADR-0013's seven fields are what a *person* reads when choosing a machine by hand, so the heartbeat's consumer changed and its payload did not. |
| **M6** | Terminal in browser | Live interactive terminal attached to a session from the web UI. | 🟡 wip — **planned 2026-08-04**, [docs/plans/m6-terminal-in-browser.md](docs/plans/m6-terminal-in-browser.md), Y-127..Y-132. The architecture was settled in advance by [R2](docs/research/02-tmux-sessions.md) §3 — daemon-side PTY + xterm.js over WebSocket, control mode deferred to v2, ttyd rejected — so planning measured the repo against that note rather than re-opening it. Two things it found: `attach.rs` already assembles the exact remote command a browser terminal has to run, and `Exec` cannot carry it, being one-shot with `RequestTTY=no` and no stdin. **Y-118 became a dependency** rather than a neighbour. The pty (Y-128), the socket (Y-129) and the browser (Y-130) are in, and **every layer is proved against something real except the join between them**: a real tmux for the pty, a real listener for the authoriser, a real socket and the real xterm.js for the page — and nothing yet that has carried a session's bytes end to end, which needs the fleet and a browser rather than another test. **Y-118 closed on 2026-08-05** ([ADR-0017](docs/adr/0017-the-forwarded-address-is-the-caller-when-the-hop-is-ours.md)), so the socket is now authorised through `tailscale serve` on 8443 as it already was on 7717, and the last dependency this milestone was waiting on is gone. **What the acceptance test waits on now is nobody's code.** It is a real browser on the phone, against a real daemon on this machine, against a real tmux session on a real machine of the fleet — the join no layer's test can stand in for, and it is the **owner's to run**. M6 is not done until it has been. **Y-132 added reconnect and no buffer with it**: tmux draws the pane for whichever client attaches, so the screen comes back from the far side and the browser's only job is to open another socket — which leaves the daemon holding nothing across a closed one. |
| **M7** | Appliance | Runs 24/7 on the Pi 5 / N100. Survives reboot. Notifications via ntfy. | ⬜ todo |
| **M8** | Hardware prototype | Display + rotary encoder + LEDs driving real workspace actions. **Simpler than planned:** R6 found `dtoverlay=rotary-encoder` (evdev, IRQ-decoded in kernel) and `dtoverlay=ws2812-pio` (`/dev/leds0`, RP1 PIO) — **no Rust, no microcontroller**. Panel stays a second **Rust** process for lifecycle isolation only. | ⬜ todo |
| **M9** | Enclosure & PCB | University workshop build. | ⬜ todo |
| **M10** | Placement | `preferred` and `automatic` modes with an explainable decision record (`yantra why demo`). | ⬜ todo — **was M5 until 2026-08-02.** Deferred by the owner, not dropped: the telemetry it would score already arrives every 10 s, so what is missing is the scorer and the explanation, not the facts. **Q11** (is session-affinity a score term at all) belongs to this milestone and no longer blocks anything. |

---

## 3. Task board

Closed tasks live in the archive — [M0 and M1](docs/archive/m0-m1.md),
[M2 and M3](docs/archive/m2-m3.md), [M4 and M5](docs/archive/m4-m5.md). **This section is only what
is open**, plus one line per closed task that steered. A short section is the goal rather than a sign
of a slow week.

**Task numbers: `Y-0xx`/`Y-1xx` here, `Y-2xx` for the landing page.** The landing-page effort keeps
its own tracker on its own branch and took a number this file had already used **three times** —
Y-084, which it renumbered itself out of; Y-091, while a different Y-091 was merged here; and then
Y-100 and Y-101, taken while this file was allocating those same two numbers on the same day. Three
collisions from one source is a pattern rather than an accident, and the cost is not a merge
conflict: it is two rows in two files claiming the same identifier, so a commit message or a PR title
stops saying which piece of work it belongs to. **Y-200 upward is reserved for the landing page**,
and nothing in that range is ever allocated from this file. **Adopted 2026-08-02**: the landing rows
became Y-200..Y-207 and PR #80 merged renumbered.

### Open

**Y-134..Y-140 are the board**, opened 2026-08-05 by sweeping the *not built* clauses out of rows
that had already closed. Every one of them is real remaining work that a reader could find and nobody
could pick up, because a clause inside a `✅ done` row is evidence rather than a task — which is
§B0's rule failing in the direction it was written to prevent: *never start work that is not
represented there* stops binding when the representation is a sentence in the middle of something
finished. Most were named as deliberately deferred by the row that closed over them — Y-113 twice,
Y-119, Y-073. One was named by two rows at once and owned by neither: Y-129 and Y-132 both record
the missing reaper and both say it is not theirs. One is an open question with a machine that can
answer it. And one is a claim this file makes about `web/` that the code contradicts.

**None of them depends on another, and that is the point of opening them together** — an overnight
loop can take any of them in any order without ordering damage, so `Depends` is `—` throughout. The
interactions worth knowing are not dependencies: [Y-136](#3-task-board)'s buttons will say the wrong
thing about a refusal until [Y-135](#3-task-board) fixes the status, and [Y-139](#3-task-board)'s ssh
route is the one Y-122 blocks while its GUI route is not. Both are written into the rows rather than
into the column, because a `Depends` that is not a blocker is a loop stalled on nothing.

**What was the owner's still is.** Y-117 waits on M10, which is where the read model that surveys the
tailnet gets built, and the macOS credential blocker is Y-122's and no other row's. Everything M6
opened has closed, Y-118 with it, so **what M6 waits on now is a browser rather than a branch** — the
acceptance test in [§2](#2-milestones) is the owner's to run against the real fleet, and no code is
in the way of it.

**This paragraph does not say how many rows are open**, and that is [Y-120](#3-task-board)'s rule
applied to the file that wrote it. The count was here for a day and went stale three times in it —
once per merge — because three branches were closing rows at once and each had to guess what the
others would leave behind. A number restated above a table that already carries it is the drift
Y-120 deleted everywhere else; the table is the count.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-118 | The proxy hides the caller, so identity comes from the forwarded address | ✅ done | claude | Y-111 | **Found by Y-111 the moment TLS landed, and it is a hole in an authoriser rather than a refinement of one.** [ADR-0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md) §2 resolves the caller's **source address** through `whois`; `tailscale serve` makes that address the proxy's, so every write through `:8443` is attributed to the machine running the proxy and `allowed()` cannot refuse anything that reaches it. Nothing is broken today and nothing in M5 is blocked — the proxy is this owner's own untagged node, so the phone's writes are authorised, and `serve` is tailnet-only so R-22's boundary holds. What is lost is the **failure mode the ADR exists for**: a tagged CI runner or a node shared in from another tailnet would be authorised through the HTTPS port and refused on 7717, which is the worse of the two answers being the reachable one.<br>**The material is already there**: `serve` forwards `X-Forwarded-For` (the caller's tailnet address) and `Tailscale-User-Login`, both measured 2026-08-03. **It is a decision before it is a patch, which is why this is not folded into Y-111.** ADR-0016 §4 says identity never comes from something the caller can write, and a header is exactly that — trusting it is sound *only* because the connection came from `tailscaled` on this machine, so the rule becomes "trust the forwarded address if and only if the peer is loopback-equivalent, i.e. one of our own bind addresses". That is a new sentence in the security model and wants the ADR amended or superseded, not a quiet `if`. **Do not read the `Tailscale-User-*` headers instead** — they name a login, and ADR-0016 authorises on the numeric `Node.User` and `Node.Tags`, which only `whois` reports; swapping to a login string would drop the tag check that forecloses "CI starts a workspace". **The test to write is the refusal**, per this crate's own rule: a forwarded address that resolves to a tagged node must be refused even though the TCP peer is ours.<br>**The owner accepted ADR-0016 on 2026-08-03 ([Y-103](#3-task-board)) knowing this hole, which settles the *shape* of the fix without touching its content.** An accepted ADR is immutable, so the new sentence in the security model can no longer be an edit to ADR-0016 §2 or §4: it is an amending ADR — a dated blockquote appended, per §B5, if the premise is what changed — or a superseding one, if the decision itself is. Which of the two, and whether to trust the forwarded address at all, is still the owner's.<br>**Written 2026-08-03 as [ADR-0017](docs/adr/0017-the-forwarded-address-is-the-caller-when-the-hop-is-ours.md), status `proposed`. The row stays open because a proposal is not a decision** — nothing is built, no Rust changed, and ADR-0016's dated amendment now names where the decision went while saying it has not been taken. It **amends** rather than supersedes: ADR-0016's decision is right and four of its five parts are untouched and implemented, so what stopped holding is the premise that the TCP peer *is* the caller — the shape [0008](docs/adr/0008-withdraw-the-stdin-watchdog.md) and [0010](docs/adr/0010-drop-branch-from-the-workspace-schema.md) already use. **Two measurements against `tailscale` 1.98.9's `ipn/ipnlocal/serve.go` sharpen this row's own reasoning.** `serve` writes `X-Forwarded-For` with `Set` from the connection it terminated, so an inbound value is **replaced** and there is no comma list to mis-parse — the header carries one address or the request is refused. And `addTailscaleIdentityHeaders` returns early for a tagged node (*"2023-06-14: Not setting identity headers for tagged nodes"*), so the `Tailscale-User-*` route is worse than this row said: for the one node ADR-0016 exists to refuse the header is **absent**, indistinguishable from a Funnel request or an unproxied one, while the *address* still resolves through `whois` to `Node.Tags`. The acceptance criterion is unchanged and is the refusal this row already named.<br>**2026-08-04: this row now gates M6, having blocked nothing in M5.** [The M6 plan](docs/plans/m6-terminal-in-browser.md) §3.3 found that a browser terminal cannot be reached through `tailscale serve` until this is decided — `allowed()` would authorise every caller arriving at `:8443`, which is tolerable for `up` on the owner's own untagged node and is not tolerable for a shell, since `up` starts a process Yantra chose and a terminal runs whatever is typed. **And it cannot be reached around the proxy either**: refusing forwarded callers leaves a terminal that works only on `:7717` over plain HTTP, while the dashboard is served over HTTPS on `:8443`, and an HTTPS page cannot open a `ws://` connection. So M6's layers can be built while this is open and M6 cannot be *used from the phone* while it is.<br>**Accepted by the owner 2026-08-05 and built the same day. ADR-0017 is `accepted`, ADR-0016 carries a second dated amendment saying so, and the rule is one function.** [`write.rs`](crates/yantrad/src/write.rs)'s `Authoriser` holds the inventory *and* the set `listen_on` bound — passed in from `main.rs` rather than re-derived, so "our own bind addresses" cannot drift from the addresses actually bound — and `caller_address` is the whole decision: **the TCP peer, unless the peer is one of that set, in which case `X-Forwarded-For`**. One address or refuse: a comma list, a second header line, an empty value and anything unparseable are all refused rather than repaired, and **the leftmost entry is never taken**, which is how a caller-supplied entry would otherwise become an identity. An absent header is not a refusal — it says nothing proxied this, which is every call on 7717. Both callers go through it: the four write verbs and `PATCH`, and [`terminal.rs`](crates/yantrad/src/terminal.rs), which matters more because it is a shell rather than a button.<br>**One thing the ADR left to implementation, recorded because it was a choice.** A malformed forwarded header answers **503**, not 400 or 403, on `Refused::CannotAsk`'s own reasoning (R-23): the caller did not write that header, nothing was decided about them, and a 4xx would name the wrong party. It is a distinct `Refused::Forwarded` variant so the log says which of the two shapes arrived.<br>**Six tests, and every assertion was made to fail first** — the four breaks were: trust the header from any peer, trust it from none, take the leftmost of a comma list, and treat an absent header as a refusal; each failed exactly the assertion written for it and nothing else. A forwarded address from a peer that is *not* ours is ignored (a tagged node on 7717 cannot launder itself); a forwarded address from a peer that *is* ours **is** the caller, which is the ADR's own acceptance criterion — refused for being tagged even though the peer is this owner's untagged node — and a second case asserts the `NotAPeer` names the *forwarded* address, so the test cannot pass by refusing everything; an absent header behind our own hop is allowed as the peer; and four malformed shapes are each a 503. The terminal socket has the same pair against a **real `TcpListener`**, which Y-129 established is the only way to reach that handler at all — `WebSocketUpgrade` strips `OnUpgrade` during extraction, so a `tower::oneshot` refusal test asserts a status the authoriser never produced.<br>**What it still cannot catch.** `whois` resolves live against the tailnet, so no test here resolves an address — the fake *is* the tailnet, and what is proved is the **address selection**, which is the decision. Nothing has run behind a real `tailscale serve`: that a proxied request arrives with the peer equal to a bind address rests on Y-111's measurement and ADR-0017's reading of `serve.go` at 1.98.9, not on anything in CI, and it is **version-sensitive** (§B0.5) — decision 3 turns a future `Add`-instead-of-`Set` into a visible 503 rather than into silent trust, which is the loudest failure available but is still a failure. **Any process on this machine can connect to a bind address and name any caller it likes**, which ADR-0017 states as a consequence rather than an accident: no escalation under ADR-0012, since a local process can already call the library in-process, but the trust is now written down and a second local reverse proxy in front of 7717 inherits it. And the write handlers' success paths remain untested for Y-126's reason, which this row does not change. |
| Y-120 | The counts restated in the summaries — deleted, rather than checked by a `just` recipe | ✅ done | claude | — | **Proposed rather than built, because five instances is no longer bad luck.** §B5 says a summary is updated by the change that summarises it, and it is not holding: Y-096, Y-073 and Y-101 each shipped without flipping their own row; §0 has carried a stale test count twice and miscounted the invariants as 48 against 49; and root [`llms.txt`](llms.txt) said *48, numbered to I-51* while the trackers said I-52. Every one was found by a person going to look, which is exactly the mechanism §B5 was written to replace.<br>**The retire condition writes itself**: a restated number that is wrong should be a red build, not a reader's problem. The countable facts are the invariant total and highest number (across the four crate trackers), the ADR count in `llms.txt`, and the Rust and web test totals. `just check` already gates every commit, so the recipe has somewhere obvious to live.<br>**Why it is a proposal and not a task in flight**: a check that greps prose is a check that fails on a rewording, and the cure for stale counts must not be a recipe nobody can edit a sentence past. The honest alternative is to **delete the counts from the summaries entirely** and let the trackers be the only place they appear — §B5's own advice is *prefer a link over a restatement*. Which of the two, and whether the counts are worth keeping at all, is the owner's call.<br>**Sixth instance, 2026-08-03, and this one has a named cause — me.** §0's dashboard test count read 47 against an actual 62 within hours of Y-120 being opened. It drifted because parallel tasks are told *do not edit §0's Status cell*, since it is one 40 KB line and simultaneous edits to it guarantee a hand-resolved conflict. **That instruction is correct and it is also the drift's mechanism**: a cell no task may touch is a cell no task updates. So the count is not stale through carelessness, it is stale by construction — which settles the choice above more firmly than any of the five instances before it. A recipe that reds the build would fire on every parallel branch at once; **deleting the counts is the option that removes the conflict rather than policing it.** <br>**Half-done on 2026-08-03 by the archive trim, and only the half that needed no decision.** §0's Status cell no longer restates any count — it is four sentences and a link — so the surface this row is most about is gone rather than checked. What is left to decide about is §1b's *50 live, numbered to I-53* (corrected here from 48/I-52, with `yantra-core` corrected from 34 to 37) and root [`llms.txt`](llms.txt)'s invariant and ADR counts; those name a number the crate trackers own, which is the case for a check and the case for a link, and the choice is still the owner's.<br>**Closed 2026-08-03: the owner chose deletion over the recipe, so the counts are gone rather than checked.** Seven sites emptied. §1b lost *50 live, numbered to I-53* and the whole **Holds** column of its table — the invariant *ranges* stay, because they are the actual index and are the reason to open the table at all. Root [`llms.txt`](llms.txt) lost *50 non-obvious rules, numbered to I-53 … 37 of them in `yantra-core`* (the claim and the *earned by a bug that presented as something else* clause survive, now pointing at §1b) and *fifteen* ADRs. `CLAUDE.md` §B5 lost *37 of the 50*, keeping the link and the pointer to §1b; [`crates/yantra-core/CLAUDE.md`](crates/yantra-core/CLAUDE.md) lost *the 37 invariants*; and the per-crate `llms.txt` files lost *the two invariants* (`yantra-agent`), *seven invariants* (`yantrad`) and *34 of the project's 46* (`yantra-core`). **The last two were not in the swept list, and one is a seventh instance**: `yantra-core/llms.txt` read 34 of 46 against an actual 37 of 50 — drifted twice over, in a file nobody had reason to open, found only because this task went looking. §0 needed nothing, the archive trim having already cut its Status cell to four sentences and a link.<br>**What the repo gives up is the totals, and it should be said plainly rather than sold.** No file states how many invariants exist or how many ADRs there are. A reader who wants either opens §1b and the four crate trackers and counts; §1b's ranges say which crate holds which numbers, not how many. That is the price of the trade. **What this does not do is fix the mechanism.** Nothing prevents the next summary from restating a count tomorrow — §B5's *prefer a link over a restatement* is still a rule a person has to honour, with nothing failing when they do not. This removes today's drift surface, not the thing that generates it, and the recipe is dead by the owner's decision rather than by having been made unnecessary. |
| Y-122 | Yantra cannot start Claude Code on a Mac, because the token is in a keychain ssh cannot read | ⬜ todo | — | — | **The acceptance test's *on the Mac* clause is blocked, and not by anything the dashboard does.** [I-44](crates/yantra-core/tracker.md) recorded this and graded it `[V]`; Y-113 still read `loggedIn: false` over ssh and reported it as an account that needed logging in, which the owner corrected on 2026-08-03. **The invariant existing did not stop the mistake, and that is the part worth keeping**: I-44 lives in `yantra-core`'s tracker under `agent.rs`, and the task that tripped it was a web task that never read that file.<br>**Re-measured on `bishwajeets-macbook-pro`, 2026-08-03.** `launchctl managername` over ssh answers **`Background`**, not `gui/<uid>`. The keychain item `Claude Code-credentials` **exists and its metadata reads fine** — an existence check passes — while `security find-generic-password -w` exits **36 with zero bytes on stdout *and* stderr**. `launchctl asuser $(id -u)` answers *Could not switch to audit session: Operation not permitted*, because it needs root. `security unlock-keychain` needs a password and is therefore refused by §B4 outright. **The account is logged in; the token is unreadable from where Yantra stands.** Linux is unaffected — there the token is a plain `~/.claude/.credentials.json`, which is why Y-023's spike saw none of this.<br>**The promising answer is one Yantra is already half-way to, and it is an ADR rather than a patch.** `yantra-agent` runs *on* each machine, and M7 installs it as a service; a launchd **user agent** in the `gui/<uid>` domain is in the one place that can read the login keychain. Launching a Mac agent through `yantra-agent` rather than through raw ssh would close this — and it amends [ADR-0006](docs/adr/0006-ssh-exec-transport.md)'s reach and grows the heartbeat agent past *reporter*, which R-12 and [ADR-0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) §7 both guard deliberately. That is the owner's decision, not a quiet one.<br>**What must be checked first, and by research rather than memory (§B6)**: whether Claude Code 2.1.x can be told to keep credentials in a file on macOS as it does on Linux. If it can, this is a setting and not an architecture. Nobody has looked.<br>**Looked, 2026-08-03 — [R10](docs/research/10-claude-code-credentials-on-macos.md). It is an architecture, and the row above may now stop hedging.** No setting, env var or flag in Claude Code `2.1.220` moves the *stored* credential off the login keychain. The storage location is documented per OS and the one relocation knob, `CLAUDE_CONFIG_DIR`, is scoped by the docs to *"Linux or Windows"* — macOS excluded by name. What exists instead are ways to *supply* a credential from outside — five precedence levels sit above the keychain, four once the cloud providers this fleet does not use are set aside — and **every one is a secret value Yantra would have to hold or set, so §B4 disqualifies the lot**; three of the four also switch billing from the subscription to the API. The only subscription-compatible one is `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token`, which the docs say is printed once and *"does not save the token anywhere"* — an env var is the only place to put it.<br>**Two measurements changed the shape of the problem.** (1) **`~/.claude/.credentials.json` does exist on the Mac** — `0600`, 505 bytes, readable from the ssh session — and its only top-level key is `mcpOAuth`, one plugin's MCP state. There is **no `claudeAiOauth`**. So macOS does not write a Linux-shaped account token and then decline to read it; it never writes one, and the file-fallback everyone asks upstream for would have nothing to find. (2) **`claude auth status` validates nothing.** `CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-bogus` makes it answer `{"loggedIn": true, "authMethod": "oauth_token"}` **on the MacBook itself**. Yantra's gate reads that field, so any env-var remedy would turn the refusal back into the healthy-looking useless session [ADR-0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md) added the gate to prevent — the failure just moves to the first model request, where nothing is watching. `authMethod` also has more values than the two seen so far (`oauth_token`, `api_key`, `api_key_helper`) and a new `apiKeySource` field; `serde` drops what it does not name, so nothing breaks.<br>**Upstream has been asked four times and has never answered**: [#5515](https://github.com/anthropics/claude-code/issues/5515), [#5957](https://github.com/anthropics/claude-code/issues/5957), [#10158](https://github.com/anthropics/claude-code/issues/10158), [#29816](https://github.com/anthropics/claude-code/issues/29816) — 2025-08 to 2026-04, every one closed by the inactivity bot as `NOT_PLANNED` or `DUPLICATE`, then locked, with no maintainer comment on any of them. #5957 dates the break to the changelog line *"(Mac-only) API keys in macOS Keychain"*, so **ssh-to-Mac worked before Anthropic moved the store** — this is a regression nobody is triaging, not a macOS law. Nothing here should be planned on the assumption it changes. **So the launchd user agent in `gui/<uid>` is the remaining candidate, and the decision is the owner's.**<br>**Written 2026-08-03 as [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md), which is `proposed` and awaits the owner — this row stays open until it is accepted or rejected, and nothing was built.** It does **not** propose what the paragraph above expected. The constraint is that keychain reach is a property of a process's launchd session and is inherited from the parent, and [ADR-0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md)'s pane is forked by the **tmux server**, not by the ssh session — so today's `Background` domain was never chosen, it came from whichever ssh command first needed a server. The proposal therefore has the **user's own login session** start the tmux server on macOS, and Yantra refuse to create one there; ssh keeps its monopoly (**ADR-0006 is not amended**), `yantra-agent` is not touched (**R-12 and ADR-0013 §4/§7 hold unamended**), and no token crosses a Yantra boundary. Its costs are named rather than mentioned: the ADR-0011 gate currently runs over plain ssh, which on macOS is the *wrong process*, so on that platform it must run inside the server and may still only claim *a credential was found* (**I-53**); `down` killing the last session takes the `gui/<uid>` server with it (**I-30**), so the launchd job has to bring it back; and M7 owns the plist. **One premise is untested and the ADR says so in its own §8**: that a process forked by a `gui/<uid>` tmux server inherits keychain access, plus that the ssh and GUI sessions share a tmux socket. Neither can be measured over ssh — `launchctl asuser` needs root — so it takes the owner at the Mac's keyboard once, and if it comes back negative the ADR's own fallback is *do nothing and refuse honestly* (R-23), **not** the exec agent, which rests on the identical premise.<br>**Probed on 2026-08-05 at the Mac's keyboard, and the premise holds — the row stays open, because accepting the ADR is still the owner's.** `tmux new-session -d -s yantra-probe` in **Terminal.app** on `bishwajeets-macbook-pro`; everything else from `cachyos-g14` over ssh. The `$TMPDIR` reported in each context was **byte-identical** and `tmux ls` over ssh saw the session, so the ssh client reaches the GUI session's server on the default socket and ADR-0018 §1 needs no `-S` path. Then, same `$HOME`, same binary, minutes apart: over plain ssh `claude auth status` answers `{"loggedIn": false, "authMethod": "none"}` and `security find-generic-password -s "Claude Code-credentials" -w` exits **36**; in a window forked by that Terminal.app-started server it answers `{"loggedIn": true, "authMethod": "claude.ai"}` and exits **0**. **What is inherited is the keychain and not the file** — `~/.claude/.credentials.json` *is* present on that Mac, so a `claude` that preferred it would have answered `true` over ssh, which has the same `$HOME`; it answered `false`. **What did not survive is the discriminator §8 named**: `launchctl managername` prints `Background` in the ssh session *and* in a process forked by the GUI server, so nothing about keychain reach may be read off it, and the outcome table's *`Background` → the premise fails* row is the reading the measurement contradicts — recorded as a dated amendment on [ADR-0018 §8](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) and as a clause on **I-44**. Two clauses for the implementation: `tmux` is not on that Mac's ssh `PATH` (**I-34**), and macOS has no `timeout` — the first attempt wrapped both commands in it and got exit **127** twice, a symmetrical-looking measurement of nothing. Only `loggedIn` and `authMethod` were read (the two fields `agent.rs`'s `Status` names), the keychain secret was read only as an exit code with its value discarded, and the probe session and its scripts were removed afterwards. **Nothing was built, and the ADR's `Status` is untouched.** |
| Y-117 | `up --machine`, the override the picker needs | 🔵 deferred to M10 | — | — | **Found while building Y-112's routes, and it is a gap in M5's acceptance test rather than a refinement.** `up(name, term, agent)` takes no machine: the target comes from `workspace.machine`, a single required field, so *choose the Mac from a list* has nothing to send. [`docs/brainstorm.md`](docs/brainstorm.md)'s Scheduling Philosophy describes exactly this as **Manual** — *"I explicitly choose. Mac. Windows. Linux. Run there."* — and it is the first of its three modes, so the override is the vision's own starting point rather than an extra. **The CLI gets the flag first** ([`crates/yantrad/CLAUDE.md`](crates/yantrad/CLAUDE.md): anything the web UI can do must be expressible in `yantra`), then the route carries it in the body. **Deferred on 2026-08-02, the day after it was opened, because building it as scoped would break three verbs.** A session lives in tmux *on a machine*, and `down`, `resume`, `status` and `logs` all find it by reading `workspace.machine`. A **transient** override therefore places a session where nothing afterwards knows to look — `yantra up` on the Mac followed by `yantra down` searching the laptop, silently finding nothing, and reporting the absence as success (I-30). Three ways out, and the cheap two are wrong: persisting the override into the TOML makes a launch flag rewrite the operator's file, and searching every machine for the session needs the read model to survey the whole tailnet rather than *the machines your workspaces name*.<br>**What replaces it is smaller and already planned: the machine is chosen when the workspace is written, which is [Y-116](#3-task-board).** That keeps one source of truth, needs no new concept, and is what `brainstorm.md`'s UI Philosophy already describes — *the interface should generate them automatically*. Manual mode is satisfied by choosing the machine in the form rather than at the moment of launch.<br>**What brings it back** is the read model surveying the tailnet instead of the workspace list, which is the same change **Preferred** and **Automatic** modes need — so this belongs with them in M10, not ahead of them. |
| Y-124 | The dashboard is tested against a mock of the daemon, not the daemon | ✅ done | claude | — | **Found by the 2026-08-03 audit of the spec against the code, and it is the one finding here that can break silently.** All 62 dashboard tests assert against a hand-rolled `vi.stubGlobal('fetch', …)` returning literal JSON typed to match [`web/src/api.ts`](web/src/api.ts); not one touches a running `yantrad`. The five service-worker tests import the real shipped `public/sw.js` but run it against a fake `caches`/`fetch`, so they are a real file in a fake environment. **The two sides are kept in sync by convention.** Rename a field in [`crates/yantrad/src/api.rs`](crates/yantrad/src/api.rs) and CI stays green while the dashboard renders nothing — the Rust tests pass because they test Rust, and the TypeScript tests pass because they test a mock that was updated by hand or not at all.<br>**This is §B3's own sentence applied to the seam §B3 does not currently reach.** *Mocked SSH proves nothing; it tests your mock* was written about the transport, and the transport obeys it — a real sshd and a real tmux in a disposable container. The daemon-to-browser seam is the same shape and has none of that, which is not an oversight anyone recorded: M4's plans asked for the four sections and got them, and nobody asked what happens when the JSON moves. **The one time the write path met the real fleet was [Y-113](docs/archive/m4-m5.md), a manual one-off** — a person at a phone, not a check that runs again tomorrow.<br>**What makes this a row and not a patch is that the check crosses a language boundary**, and the cheap answers are each wrong in a different way. Generating TypeScript types from the Rust DTOs makes the daemon's shape authoritative and adds a codegen step to a two-language repo that has so far kept TypeScript in the browser and nowhere else (§B1). Asserting the two by hand is a third copy of the same fact, which is the drift [Y-120](#3-task-board) just spent a session deleting. Standing the daemon up in the podman harness and hitting `/api` is the option that matches §B3's existing shape and costs a fixture, and it is the recommendation rather than the decision. **The acceptance criterion is the failure, not the pass**: rename a field in `api.rs`, and the check must go red without anyone editing the test. |<br>**Closed 2026-08-03, and not by the podman harness this row recommended.** The daemon cannot be stood up in a container without a tailnet: `listen_on` refuses every address Tailscale did not hand it, and each write route resolves its caller through `whois` live — so the harness needs either a real tailnet or a stub `tailscale` on `PATH`, and a stub is the thing §B3 says proves nothing. [`crates/yantrad/src/contract.rs`](crates/yantrad/src/contract.rs) drives the real `api::router()` over the same fake snapshot `api.rs`'s own tests use, which renders **the same bytes** for none of that. The row's recommendation was the more expensive way to reach an identical fixture.<br>**What exists is one file per language and two checks that fail in opposite directions.** The Rust test renders every read route — `/machines`, `/workspaces`, `/sessions`, and `/workspaces/{name}/status` for all nine verdicts plus a machine that did not answer — and the four write DTOs, into [`web/src/contract.gen.ts`](web/src/contract.gen.ts): committed TypeScript in which every answer is followed by `satisfies` the type [`web/src/api.ts`](web/src/api.ts) names for it. Moving a DTO fails the Rust test, which says `just fixtures`; regenerating without moving `api.ts` fails `tsc`. **Proved rather than asserted**: renaming `free_ram_mb` to `ram_free_mb` in `api.rs`'s `Beat` gave `web/src/contract.gen.ts no longer matches what /api answers — run just fixtures`, and after regenerating, `src/contract.gen.ts(35,9): error TS2353: Object literal may only specify known properties, and '"ram_free_mb"' does not exist in type 'Beat'`. Restoring the field made both green. **The 62 dashboard tests are untouched** — this is a layer beside them, not a rewrite of them.<br>**TypeScript and not the JSON fixture, and the reason is a TypeScript rule rather than a taste.** An imported JSON module has its string literals widened to `string`, so `{"looked": "ok"}` is not assignable to `Looked<T>` and **no discriminated union in `api.ts` can be checked from a `.json` file at all** — every tag in that file is one. A contextual type is what stops the widening, so the assertion has to sit where the literal is written and the generator writes both. What that costs is six type names on the Rust side, which is the whole of what this crosses the language boundary with; it is not [Y-120](#3-task-board)'s third copy, because the shapes themselves are still stated once each.<br>**What it does not cover.** Status codes, headers and every refusal body — those are plain text, and the per-status sentences in `Act.tsx` and `NewWorkspace.tsx` are still mapped by hand against nothing. The write answers are serialised from their DTOs rather than fetched through the router, because those handlers authorise a live tailnet caller and then await ssh, so a handler that stopped returning the DTO it declares would pass. No browser meets a real daemon anywhere in CI; [Y-113](docs/archive/m4-m5.md) is still the only time that happened. And a field both sides rename together is green, which is the correct answer and worth saying out loud.<br>**One thing found while building it that outlives the row**: `.github/workflows/web.yml` fires only on `web/**`, so a rename under `crates/` never reaches `tsc` on its own. The Rust side therefore has to be the half that fails first, and the drift check is what makes it so — a design that had put the whole check in TypeScript would have been green on exactly the pull request this row was opened about. |
| Y-125 | Two documents promise a capability in the present tense that nothing implements | ✅ done | claude | — | **The audit's other finding, and it is §B5's failure mode running backwards.** That rule exists because a summary goes stale when the thing it summarises changes underneath it. These two went stale without anything changing at all: they described an intended capability in the present tense, the capability never arrived, and no count moved to give it away.<br>**Docker.** [`CLAUDE.md`](CLAUDE.md) §B2 and [`CONTRIBUTING.md`](CONTRIBUTING.md) both say Yantra coordinates *`ssh`, `tmux`, `tailscale`, `docker`, and agent CLIs*. The only Docker touchpoint in the tree is [`crates/yantra-agent/src/probes.rs`](crates/yantra-agent/src/probes.rs)'s label probe asking whether the binary exists, so a machine can carry a `docker` label for a placement filter that M10 has not built; everything else is test fixtures. No milestone lists Docker as a deliverable, no ADR defers it, and no row admits the gap. **The sentence is defensible as scope-setting** — §B2 is a design guardrail whose point is *if you start implementing a container runtime, stop* — and it is still the only place a governing document names a capability with nothing behind it. Naming `docker` as intended rather than current costs one clause.<br>**The workspace-centric layout.** [`docs/plans/m4-dashboard-next.md`](docs/plans/m4-dashboard-next.md) §1.4, *Three tables is the API's shape, not the operator's*, proposed replacing the separate tables with one card per workspace carrying reachability, session and agent together. M4 closed with the four-table shape unchanged, and the same plan goes on referencing §1.4 twice more as though it were happening. Nothing dropped it and nothing deferred it; it fell out. **A plan is what was known on its date** — the note ADR-0013's plan already carries — so the fix is a dated line saying it was not built, not a rewrite.<br>**A third, smaller, recorded here rather than given its own row.** [`docs/vision.md`](docs/vision.md) lists user-settable **tags** on a Machine for organising the fleet. Nothing carries them: `labels` on the heartbeat is auto-derived capability detection, and the Tailscale tags [`write.rs`](crates/yantrad/src/write.rs) reads are ACL tags used to refuse a write. Three different things share the word, and only the vision doc's meaning is missing. Every other dropped workspace field is properly recorded — [ADR-0007](docs/adr/0007-workspace-schema-v1.md) excludes agent selection, env vars, secret references, port forwards and machine preferences from v1, and [ADR-0010](docs/adr/0010-drop-branch-from-the-workspace-schema.md) dropped `branch` — which is what makes the unrecorded ones worth naming.<br>**Closed 2026-08-03: one clause each for Docker, a dated blockquote for the layout, nothing written for tags.** [`CLAUDE.md`](CLAUDE.md) §B2 and [`CONTRIBUTING.md`](CONTRIBUTING.md) each gained the same clause — *`docker` names intended scope, not a shipped capability (Y-125)* — leaving the guardrail's *if you start implementing a container runtime, stop* untouched, because that sentence still does its job. [`docs/plans/m4-dashboard-next.md`](docs/plans/m4-dashboard-next.md) §1.4 gained a dated blockquote in the shape [the-heartbeat-agent.md §6](docs/plans/the-heartbeat-agent.md#6-adr-0013-is-proposed-and-accepting-it-is-the-owners) already set: the section is left as written, because a plan is what was known on its date, and the note says M4 closed with the four-table shape unchanged and that §3 and §6 still cite §1.4 as pending. **`docs/vision.md` was not touched.** It has had exactly one commit since it was written and no task has ever edited it; the smaller move is to let this row be the only place recording that its **tags** meaning — user-settable, for organising the fleet — has no owner, rather than annotating a founding document for a gap nothing else in it has needed. **What this does not fix**: nothing stops the next sentence written in the present tense about a capability that has not arrived — this was a one-time audit finding, not a check that runs again. |
| Y-126 | Editing a workspace, which the dashboard promised and the spec requires | ✅ done | claude | — | **The gap is recorded and the work is not.** [`tracker.md` §3](#3-task-board) carries the stub — *editing did not ship and the row's title had promised it; there is no `PUT`, no `PATCH` and no `yantra edit`* — so a reader hits it where they should. What does not exist is a row to do it, which is why it is here rather than in the archive's evidence alone.<br>**The founding spec is unusually direct about this one.** [`docs/brainstorm.md`](docs/brainstorm.md)'s UI Philosophy bans hand-editing config outright — *the interface should generate them automatically* — and [`docs/vision.md`](docs/vision.md)'s UI section says the same. The dashboard currently agrees with the spec on creating and contradicts it on changing: [`web/src/App.tsx`](web/src/App.tsx)'s empty state offers the form first and the TOML path second, which is honest, and there is no path at all for a workspace that already exists. On a phone that means a typo in `repo` is unfixable without an ssh session.<br>**The CLI verb comes first**, per [`crates/yantrad/CLAUDE.md`](crates/yantrad/CLAUDE.md)'s rule that anything the web UI can do must be expressible in `yantra` — so this is `yantra edit` (or a narrower set of field verbs) before it is a route, and a route before it is a form.<br>**One question has to be answered before code, and it is not about HTTP.** A workspace's `machine` is where its tmux session lives, and `down`, `resume`, `status` and `logs` all find that session by reading the field. Editing `machine` while a session is running therefore strands it exactly as [Y-117](#3-task-board) described — the session stays on the old machine and every verb afterwards looks at the new one, finds nothing, and reports the absence as success (**I-30**). So editing is not uniformly safe field by field: `repo` and `startup` are inert until the next `up`, and `machine` is not. Refusing to edit `machine` while a session is live is the small answer; moving the session is a different task and probably M10's. **Nothing should be built until that is settled**, because the cheap version silently reintroduces the bug Y-117 was deferred to avoid. <br>**Settled by the owner on 2026-08-03, and built one layer deep.** **The decision: editing refuses to change `machine` while a session is running on it.** Moving a running session to another machine is *not* in scope and belongs to M10, which is where the read model that surveys the tailnet gets built — the same thing [Y-117](#3-task-board) waits on. `repo` and `startup` stay freely editable, being inert until the next `up`. The refusal is the task, not a guard bolted onto it: the cheap version silently reintroduces the bug Y-117 was deferred to avoid.<br>**Layer 1 shipped: [`crates/yantra-core/src/edit.rs`](crates/yantra-core/src/edit.rs) and `yantra edit`.** `workspace::update` rewrites only the fields named and knows nothing about sessions; `edit` owns the refusal and reaches the machine **only** when `machine` actually changes, so editing `repo` works with the fleet asleep and naming the machine a workspace already has is not a move (§B4). **What counts as occupied is a tmux *session*, not a running agent** — a session whose agent finished still holds a pane (I-4) and is still `down`'s to clean up on that machine, so it strands identically, and `Verdict::is_running` is deliberately false for `Unclear`, which would wave the move through in exactly the case nothing is sure about. Five container tests against a real sshd and tmux; three deliberate breaks of the guard each failed a different one of them.<br>**What the refusal cannot catch, recorded because R-23 says a check that cannot know must say so.** A session started between the check and the write — the window is one ssh round trip, and closing it needs a lock on the far side that nothing else in Yantra has. A session on the *destination* machine already using this workspace's name, which `up` would then attach to rather than create; the spec is about stranding, not collision, and this is a second round trip nobody asked for. A workspace edited by hand or by a second `yantra` between the two loads. And an unreachable machine, or one without tmux, is **refused rather than allowed** — over-refusing on purpose, so a typo in `machine` is unfixable through the CLI while its machine is asleep. There is no `--force`, and adding one is a decision rather than a patch.<br>**Layer 2 shipped: `PATCH /api/workspaces/{name}`** in [`write.rs`](crates/yantrad/src/write.rs), which is `yantra edit` on the wire and decides nothing the CLI does not. It rewrites only the fields the body names, and **absent and `null` are not the same word**: `"startup": null` clears the command, no `startup` key leaves it alone, and serde folds those together unless the field is read into an `Option<Option<_>>` by hand — the way a `PATCH` blanks a field nobody named. An unknown key is refused as a typo (ADR-0007's shape) and a body naming no field at all is a **400**, since `yantra edit` with no flags is a usage error rather than a no-op. It answers the workspace as it now reads, in the same shape `POST /api/workspaces` already answers — so `contract.gen.ts` needs no new type for it, and the form has one to redraw from while the read model is up to 30 s behind. **The refusal reaches the client as a `409`** carrying `edit.rs`'s own message, which names the workspace, the machine it may not leave and the `yantra down` that ends the refusal; **a machine that could not be asked is a `503`**, the same code a `tailscale` that cannot answer gets and for the same reason — nothing was decided, so blaming the request would name the wrong thing. Authorised exactly as the writes beside it are, which means it is correct on 7717 and **reachable-but-unauthorised through `tailscale serve`**: [Y-118](#3-task-board)'s hole, one route wider, deliberately neither fixed nor worked around here.<br>**What layer 2 cannot catch is everything layer 1 cannot, plus one that is the daemon's own: no test in this crate reaches the handler.** `edit::edit` reads the operator's real config directory and ssh's to the machine the file names; `yantrad` is a binary crate, so it has no integration tests, and `unsafe_code = "forbid"` rules out redirecting `XDG_CONFIG_HOME` from a unit test. What is proved here is the router — a `PATCH` reaching authorisation rather than a 405, and `{name}/status` still answering underneath it — the body's absent-versus-null, and each refusal's status *and* body; what produces those refusals is proved against a real sshd and tmux in [`yantra-core/tests/edit.rs`](crates/yantra-core/tests/edit.rs). **The success path has never run in a test**, and will not until something can stand the daemon up against a fixture (Y-124's reason: `listen_on` refuses a non-Tailscale address and `whois` resolves live).<br>**Layer 3 shipped: [`EditWorkspace.tsx`](web/src/components/EditWorkspace.tsx), and the sentence this row opened with stops being true.** *There is no path at all for a workspace that already exists* was [`App.tsx`](web/src/App.tsx)'s state; every workspace row now carries an **Edit** button, and it opens a form beside the create form rather than a control inside the cell — three fields and a picker do not fit a table column, and the row already opens a section that way for the terminal. **What it sends is only what differs**, which is the whole of why absent and `null` were separated a layer down: a form that PATCHed all three fields every time would turn fixing a typo in `repo` into a move of `machine`, and a move is the one edit a live session refuses. A form where nothing differs sends nothing at all, a body naming no field being a `400`. **Emptying `startup` sends `null`**, which is the failure the row was opened for — a startup command set once was unremovable from a phone. **The `409` is drawn as a refusal and not as a crash**: the plain alert rather than the destructive one, carrying `edit.rs`'s own sentence whole, since that sentence already names the workspace, the machine it may not leave and the `yantra down` that ends it; inventing wording here would be a second, worse copy of it. The `200` is rendered rather than re-read — the workspaces reading is up to 30 s behind its own write — and a second edit is measured against that answer rather than against the row the form opened from. **The picker keeps a machine the tailnet does not list** (ADR-0009's `~/.ssh/config` alias), because a `<select>` missing the workspace's own machine selects another and turns a repo fix into a move nobody asked for.<br>**What layer 3 cannot catch.** The row it opens from is up to 30 s old, so a workspace changed elsewhere in between is drawn as it was; it is not clobbered — an untouched field is compared against the stale value, matches, and is never sent — but the form shows what it replaced until the next look. **There is no rename**, because the route addresses a workspace by its name and renaming is a create and a delete, which neither the verb nor the route is. And every assertion here is over a stubbed `fetch`: [`contract.gen.ts`](web/src/contract.gen.ts) pins the shape the `200` answers and nothing pins a status code, so the `409` and the `503` are still mapped by hand against sentences no daemon was asked for. |
| Y-127 | Prove the PTY: does `^C` reach a process through `ssh` and tmux, or does it not | ✅ done | claude | — | **It does — and the invariant it was testing is right for the wrong reason.** [`crates/yantra-core/tests/pty.rs`](crates/yantra-core/tests/pty.rs) opens four connections against a real sshd and a real tmux in a disposable podman container. They differ only in how the local `ssh` is given a terminal: the session, the victim and the far-side command are the same in all four, and that command is [`attach::remote_command`](crates/yantra-core/src/attach.rs) rather than a hand-written `ssh … tmux attach` (I-34, I-35, I-36/I-43). **The proof is always that `sleep 300` died** — polled through `pgrep` and `#{pane_current_command}`, never by finding `^C` on the screen, which is the exact failure I-18 describes.<br>**(1) A pty whose child owns it.** Attaches; the far side reports the *local* window, `100x30` and not openssh's `80x24`; `0x03` kills the sleep; and the local `ssh` **survives**, because the byte is forwarded rather than signalled here. `master.resize()` reaches tmux. **(2) The negative control, which is `Exec`'s shape today** — plain pipes, `RequestTTY=no`. tmux does not fail to be interrupted; it refuses to start — `open terminal failed: not a terminal` — and the sleep outlives the whole test.<br>**(3) The finding worth reporting loudly.** `set_controlling_tty(false)` — `setsid()` without `TIOCSCTTY`, which is the implementation **I-18** was found in — **still interrupts.** What it loses is `SIGWINCH`: `resize()` reaches nobody and the far side silently keeps the size it was opened with. I-18 keeps its rule and changes its reason ([`crates/yantrad/tracker.md`](crates/yantrad/tracker.md)); breaking it ships a terminal that cannot be resized, not one that cannot be interrupted. **(4) R2's fallback, measured rather than inherited.** `ssh -tt` plus plain pipes *does* interrupt, so *"loses proper TTY signalling"* is wrong about signals — it loses the **window**, reporting an `80x24` nobody chose with nothing to resize afterwards. So the pty in Y-128 earns its place on resize, not on `^C`.<br>**What it cost.** `portable-pty` 0.9 in **`[dev-dependencies]` of `yantra-core`**, deliberately not `[dependencies]`: R-12 guards that crate's reach and where `pty.rs` lands is Y-128's to decide. Its tree is pure Rust — `filedescriptor`, `serial2`, `downcast-rs`, `shell-words`, `nix`, `anyhow`, `log` — every licence already allowed, `just deny` clean, and it type-checks for `aarch64-unknown-linux-musl` unchanged. Two things Y-128 will want: `CommandBuilder` **clears the environment** and resolves the program through its own `PATH`, so both must be set explicitly; and the reader and writer are blocking `std::io`, so the daemon reaches them through `spawn_blocking` rather than `await`.<br>**What it does not cover.** tmux **3.5a** on Alpine, where both real machines run **3.7b** (I-42), so this test can only ever see the 3.5a half. Linux only — none of it has been seen on macOS. And one client, so §3.2's two-viewer question is exactly as open as it was. |
| Y-128 | `pty.rs` — the attach command under a pseudo-terminal | ✅ done | claude | Y-127 | **`Exec` cannot express this, and that is the whole task.** [`ssh.rs`](crates/yantra-core/src/ssh.rs)'s trait is `exec(&self, command: &str) -> Result<Output, Error>` — one shot, no stdin, no incremental output — and its implementation sets `RequestTTY=no` with a written reason (a forced pty corrupts stdout with CRLF and merges stderr) and `Stdio::null()` for stdin per [ADR-0008](docs/adr/0008-withdraw-the-stdin-watchdog.md). **Those are the right choices for every caller that exists and exactly wrong for this one.** So M6 adds a *third* ssh call shape, beside the library's non-interactive `Exec` and the CLI's `execve` hand-over in [`crates/yantra/src/main.rs`](crates/yantra/src/main.rs) — the first that is neither. It should still ride the same `ControlMaster` socket, kept warm by `refresh.rs`'s 30-second loop, and it must respect I-28's `ControlPath` budget like everything else.<br>**What it must not do is re-derive the command.** [`attach::plan`](crates/yantra-core/src/attach.rs) already returns the machine, the per-host resolved tmux path (**I-34**), the session spelled so a login `zsh` cannot glob it (**I-35**) and a `TERM` the far side actually has (**I-36**, **I-43**); `attach::remote_command` renders them and is asserted. Four tasks paid for those and a hand-written `ssh … tmux attach` string would lose all four — **measurably**, because `tmux` is not on the Mac's PATH over ssh (verified 2026-08-04: a non-interactive `ssh bishwajeets-macbook-pro tmux -V` answers `command not found`, `/opt/homebrew/bin` being absent from a non-login `zsh`'s PATH). It would work on Linux and fail on the machine the acceptance test names. Adds `portable-pty`; **not `node-pty`**, which is a native addon and is R-24's line.<br>**Built 2026-08-04, and each of the three decisions the row left open was settled by a measurement rather than a preference.** [`crates/yantra-core/src/pty.rs`](crates/yantra-core/src/pty.rs) is `open(name, term, size)` — [`attach::plan`](crates/yantra-core/src/attach.rs) whole, then the pty — with `on(&Ssh, &Plan, Size)` as the half the container tests drive, and a `Terminal` that reads bytes out, writes bytes in, resizes, and ends in `Drop`. It renders no command of its own; `attach::remote_command` does, so I-34, I-35 and I-36/I-43 arrive already paid for.<br>**(1) `portable-pty` is a plain `[dependencies]`, because the number R-12 guards did not move.** `just appliance-size` on `aarch64-unknown-linux-musl`, before → after: `yantra-agent` **441,280 → 441,264 bytes**, sixteen bytes *smaller* and well inside build noise; `yantrad` 2,074,280 → 2,077,272 and `yantra` 1,244,152 → 1,246,424, both about +0.15 % for a module neither of them calls yet. A control build — module and dependency removed, tokio's new `sync` feature kept — put `yantra-agent` back at 441,280 exactly, so the tokio feature is free and the two-odd kilobytes is the unreached module. Against the **+319 KB** [this crate's CLAUDE.md](crates/yantra-core/CLAUDE.md) records for one *call* into ssh/tmux, that is the same lesson pointing the same way: the edge is nearly free and the call graph is not. **No cargo feature and no move to `yantrad`** — a feature buys an amount that does not show up in `ls -lh` and costs a build combination CI never compiles, and [ADR-0005](docs/adr/0005-core-logic-in-a-library-crate.md) puts a transport in the library where the CLI can reach it too.<br>**(2) Reads on a thread of their own; writes through `spawn_blocking`.** Y-127's note said `spawn_blocking` for both, and for the reader that is wrong in the way **I-13** names: this read blocks for as long as someone is looking at the terminal, and a blocking-pool thread held for the life of a browser tab is borrowed from every other blocking call the daemon makes. A thread of its own is borrowed from nothing. Writes stay in `spawn_blocking` because a pty write is bounded and not instant — `ssh` stops draining its stdin when the connection stalls. Output crosses into async through a **bounded** `tokio::sync::mpsc`, so a terminal nobody drains slows down rather than growing a buffer ([Q5](#6-open-questions) says the stream is never stored, and a buffer is storage). First use of tokio's `sync` feature in the workspace.<br>**(3) `ssh.rs` was factored, not duplicated.** `Machine::connection_args()` and `Machine::prepare_sockets()` are now shared by `Exec::exec` and a new `Ssh::tty_argv`, so the pty rides the same `ControlMaster` socket with the same `ControlPersist`, the same `UserKnownHostsFile` and the same I-28 check at construction. Two things stayed at `exec`'s call site with their reasons: **`RequestTTY=no`**, which is precisely what this caller must not have, and **`-E`**, because a pty's ssh diagnostics belong on the screen the user is looking at rather than in a temp file nothing will read. `Stdio::null()` never came near it.<br>**And one thing it must not inherit, stated because it looks like a rule being broken.** The remote command reaches the login shell as one argument, **not** through [ADR-0006](docs/adr/0006-ssh-exec-transport.md)'s base64 envelope: that envelope's `/bin/sh` reads its script from a pipe, so the tmux client inside it inherits a pipe for stdin, and Y-127's negative control already measured what that produces — `open terminal failed: not a terminal`. **I-35 is satisfied the other way**, by `tmux::sq` quoting `=name`, which is how `yantra attach`'s `execve` hand-over has always done it.<br>**Proved in [`tests/pty.rs`](crates/yantra-core/tests/pty.rs)**, beside Y-127's arms and in the same container: the terminal attaches, and the evidence is the far side's own screen arriving through `read` with tmux's status line naming the session; `list-clients` reports **100x30** rather than openssh's 80x24; a `write` of `0x03` kills `sleep 300`, polled through `pgrep` and `#{pane_current_command}`; `resize` reaches the far side. **Both were made to fail before being believed** — `set_controlling_tty(false)` breaks the resize and nothing else, I-18 landing exactly where Y-127 measured it, and `mem::forget` in place of the drop leaves the client attached.<br>**Teardown, where I-27 lands differently.** Killing the local `ssh` orphans a remote *command*, which reparents to PID 1; this one owns a remote **terminal**, so sshd hangs it up and tmux detaches — measured, with the session still running afterwards, which is the half that must survive: ending a session is `down`'s. `Drop` kills and **waits**, so nothing is left locally as a zombie either.<br>**What it does not cover.** No socket and no browser (Y-129, Y-130). `open`'s workspace-loading half is not exercised by a container test; only `on` is. Nothing has run on macOS, and the fixture is tmux **3.5a** against **3.7b** on both real machines (I-42). One client, so §3.2's two-viewer question is as open as Y-127 left it. And the window is whatever size the caller names — nothing yet tells the daemon how big a browser is. |
| Y-129 | `GET /api/workspaces/{name}/terminal` — the socket, and what it refuses | ✅ done | claude | Y-128 | **A WebSocket upgrade is a `GET`, so it does not get the write check for free — and it is the route that most needs one.** Every write in [`write.rs`](crates/yantrad/src/write.rs) resolves its caller through `allowed()`: `whois` on the socket peer, refuse a tagged node, refuse anyone who is not the owner ([ADR-0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md)). `main.rs` already wraps every listener in `into_make_service_with_connect_info::<SocketAddr>()`, so a new route inherits the peer address and `allowed()` is reusable unchanged. **`up` starts a process Yantra chose; a terminal runs whatever the person on the other end types**, which is why this row calls the authoriser explicitly and says so in a comment rather than relying on a reader noticing.<br>**It also has to be the second documented exception to the read path.** `refresh.rs` states the rule — handlers read memory, nothing awaits ssh on a read — and `write.rs` is the exception because a human tapped a button once. A terminal holds ssh open for as long as someone is looking at it, which is a third thing again, and it belongs in the comment beside the other two.<br>Adds axum's **`ws`** feature; the crate is currently `default-features = false, features = ["http1", "json", "tokio"]`, so `axum::extract::ws` does not compile today. `just deny` runs on the new transitive tree in the same PR. **`yantrad` has no `tests/` directory at all** — the in-process router tests in `api.rs` and `contract.rs` are the shape to copy.<br>**Built 2026-08-04 in [`crates/yantrad/src/terminal.rs`](crates/yantrad/src/terminal.rs), and the test shape this row prescribed is the one thing that did not survive contact.** The route calls [`write.rs`](crates/yantrad/src/write.rs)'s `allowed()` by name before the upgrade — `allowed`, `Refused` and `chain` became `pub(crate)` and nothing else about ADR-0016 moved. **The frames carry no envelope, because the protocol already carries two kinds.** Binary is terminal bytes in both directions; text is control. From the browser text is always `{"rows":…,"cols":…}`, `deny_unknown_fields`, and it must arrive **before anything else** — `pty::open` takes a window and nothing else tells the daemon how big a browser is, so the first control frame is what *starts* the terminal rather than what resizes it, and the same message does both jobs rather than a query string doing one and a frame the other. From the daemon text is the reason a terminal could not be opened, which had to be a frame rather than a close reason: **a WebSocket close reason is capped at 123 bytes and an ssh diagnosis is longer.** It is in [`contract.gen.ts`](web/src/contract.gen.ts) as `terminalSize`/`TerminalSize`, the first entry there the *browser* writes rather than reads — `satisfies` proves the two sides spell one message the same way and proves nothing about the daemon accepting it, which is why a Rust test asserts the literal and refuses both a missing side and a `term` field Y-130 has not added yet.<br>**The negative finding is about this repo's own test shape, and it would have shipped a green test over no authoriser at all.** This row said to copy the in-process router tests in `api.rs` and `contract.rs`. **That cannot test this route.** `axum::extract::WebSocketUpgrade` removes `hyper::upgrade::OnUpgrade` from the request extensions *during extraction*, and a request built by hand and driven through `tower::ServiceExt::oneshot` never has one — so every extractor-stage rejection fires before the handler body, `allowed()` is never reached, and a refusal test written that way asserts a status the authoriser did not produce. The tests therefore stand up a real `TcpListener` on loopback, serve the router through `axum::serve` with `into_make_service_with_connect_info`, and write the handshake bytes at a socket; the tailnet is faked at **127.0.0.1** because the peer address is the kernel's to report, not the test's. **Made to fail before being believed**: deleting the `allowed()` call turns the refusal red and leaves the 101 green, which is the pair that says the harness reaches the upgrade rather than failing short of it.<br>**A second thing Y-128 could not have known: `pty::Terminal::write(&self)` is unusable from this caller.** `Box<dyn MasterPty + Send>` is not `Sync`, so a future holding `&Terminal` across an await is not `Send`, and `on_upgrade` requires `Send` — the signature compiled and had no possible caller. It is now `&mut self`, with the reason on it so nobody narrows it back; [`tests/pty.rs`](crates/yantra-core/tests/pty.rs) needed no edit, having already bound `let mut terminal`. The bridge loop pays for that by having `select!`'s arms carry the event out instead of handling it, so the borrows end before either side is touched again.<br>**Q5 holds by there being nothing to remove**: no frame, in either direction, reaches a `tracing` call. Opening, ending, a refused caller and a resize that failed are logged; a byte never is. Adds axum's `ws` on the workspace line, `yantrad` being the only crate that names axum — `tokio-tungstenite`, `tungstenite`, `sha1` with its digest stack and a second `base64`. **`just deny` passes**, and says three more duplicate-version warnings than before (`base64`, `getrandom`, `r-efi`, against `multiple-versions = "warn"`). `serde_json` moved from `dev-dependencies` to `dependencies`, the size message being parsed at runtime now.<br>**Y-118 is untouched and this route is on the wrong side of it.** On **7717** the authorisation is correct. Through **8443** `tailscale serve` hands the daemon its own address, so `allowed()` authorises whoever runs the proxy and this route is reachable-but-unauthorised there — the same hole the three writes already have, on the one route where it buys a shell. Nothing here reads `Tailscale-User-*`, nothing trusts `X-Forwarded-For`, and no refusal of forwarded callers was added: the hole stays one hole in one place for the owner to close for every route at once.<br>**What this does not cover, and half of it is the half that matters.** **The handshake is proved and the stream is argued.** No test opens a pty, an ssh connection or a tmux client through this socket — the bridge loop, the resize path and the failure text frame have never run against anything. The daemon cannot be stood up in a container for the reason Y-124's row gives (`listen_on` refuses non-Tailscale addresses, `whois` resolves live), so proving the stream end to end needs the real fleet and a browser, which is Y-130. Authorisation runs in the handler body and extraction runs before it, so a **malformed** upgrade from a refused caller is answered by axum before `allowed()` sees it — nothing is granted, but that status describes the request rather than the caller. **Nothing limits sockets per workspace**: two tabs are two ssh connections and two tmux clients, which is §3.2's unanswered question and Y-131's. There is no idle timeout and the daemon sends no ping, so a socket dropped by something in the middle is discovered when a write fails. `TERM` is still `terminfo::FALLBACK` (Y-130), replay is not built (Y-132), and no line of the browser half exists. |
| Y-130 | xterm.js, and where a terminal lives in a dashboard that has no router | ✅ done | claude | Y-129 | **Three things about `web/` that are not obstacles but are decisions.** There is no router: [`App.tsx`](web/src/App.tsx) is one `<main>` with five hardcoded sections, no `react-router`, no hash routing, no `pushState` — while [`web.rs`](crates/yantrad/src/web.rs) *already* SPA-falls-back deep links to `index.html`, so the server is ready for `/workspaces/<name>/terminal` and the client would land on the same page. **A sixth section or an overlay is the smaller move; adding a router is a decision to take deliberately rather than because something needed one.** Only five shadcn primitives are vendored — `alert`, `badge`, `card`, `empty`, `table` — so whatever holds the terminal arrives with it. And the service worker caches what it is served: Y-114 was careful never to cache a *reading*, and a socket endpoint needs the same exclusion, asserted in `sw.test.ts`.<br>**Expect the React Compiler to bail out.** [`useLooked.ts`](web/src/useLooked.ts) carries a comment about a real bail-out it had to be restructured around, and `npm run compiled` greps the bundle for the compiler's sentinel as a build gate. A socket hook is the same shape — an effect with a cleanup, a ref and a conditional — so check the gate rather than assume it passed.<br>**This is also the first caller that can send a real `TERM`.** `write.rs`'s `term()` says *"a browser has none"* and falls back to `terminfo::FALLBACK` (I-36); a browser running xterm.js **is** a terminal and a well-known one, and `terminfo::choose` already knows how to negotiate against what the far side has (I-43). That comment stops being true in this row and should not be left standing.<br>**Built 2026-08-04 in [`web/src/components/Terminal.tsx`](web/src/components/Terminal.tsx), and each of the four decisions went the smaller way for a reason rather than by default.** **(1) A sixth section, not a route and not an overlay.** [`App.tsx`](web/src/App.tsx) holds one `string | null` and draws the terminal above the five sections when it is set; the workspace row's *Open terminal* button is what sets it. A route would have to mean something on a reload — a page that reopens a socket on load, which is Y-132 and is not built — and an overlay would be the first thing on this page that traps focus, over a screen a phone already gives the whole of. **(2) Nothing new is vendored to hold it.** It is the same `Card` the other five sections sit in, so the answer to *which primitive* turned out to be *none*: [`Section`](web/src/components/Section.tsx) takes a `Looked<T>` and a terminal is not a reading, so this composes `Card` directly instead. The one button style is exported from [`Act.tsx`](web/src/components/Act.tsx) rather than copied. **(3) The service worker needed no change and got a test anyway.** Y-114's `DATA` regex is `/^\/(api|healthz|heartbeat)(\/|$)/` and the socket lives under `/api`, so the exclusion already covered it — and a WebSocket handshake never reaches a worker's `fetch` handler at all. Both are why `sw.test.ts` asserts it now: moving this route out from under `/api` is what would put a terminal in the cache, and it would do it silently.<br>**(4) `TERM` is `xterm-256color`, on evidence, and the strongest evidence argues against the obvious answer first.** ncurses *does* ship an entry named for this — `vscode|xterm.js|Visual Studio Code terminal using xterm.js`, aliased in 2019 — and it is the wrong choice twice over: it lives in `ncurses-term`/`ncurses-terminfo` rather than the base package, it is **absent from Apple's ncurses** (checked against `apple-oss-distributions/ncurses` tag `ncurses-79`, whose `terminfo.src` is dated 2015-07-25), and it *understates* xterm.js 6.0.0 by disabling `initc` where OSC 4 is implemented. `xterm-direct` is absent from the same database and would misrender besides: it sets `colors#0x1000000` with `CO#8`, so `tput setaf 130` emits `\e[38:2::0:0:130m` — near-black navy — where the application meant palette entry 130. **I-36 is the rule and the Mac is the machine it names**, so the entry has to be one every database carries: `xterm-256color` is in Debian's `ncurses-base` and Alpine's `ncurses-terminfo-base`, and it is what every xterm.js consumer measured sends (VS Code, ttyd, wetty, terminado, GoTTY's maintained fork). **The value is `terminfo::FALLBACK`'s own, so `choose` short-circuits and the attach pays for no `infocmp` probe** — what changed is provenance rather than bytes. I-36's *"the client's terminal is unbounded, so it can never be trusted as an input"* still holds, because this is a constant in Yantra's own client code and not something read out of a user's environment. Two things `xterm-256color` gets wrong, neither actionable here: it advertises `Ms` (OSC 52) that xterm.js core does not implement, and truecolour wants `COLORTERM`, which tmux honours and **ssh does not forward**.<br>**Proved against a real socket and the real xterm.js, in [`web/src/terminal.test.tsx`](web/src/terminal.test.tsx).** A `ws` server on a fixed port and the page building its own URL from `location` at it: the first frame is text and is `{rows, cols, term}` before anything else; a binary frame from the server arrives in `.xterm-rows` as text on the screen; a **text** frame lands in an `alert` and provably not on the screen; a real `keydown` of ctrl-C on xterm's helper textarea leaves as a **binary** frame of `0x03`, which is Y-127's proof from the other end; unmounting closes the socket; and a socket that goes away having said nothing is not drawn as an error. **The negative finding is the harness itself: jsdom's own `WebSocket` cannot connect under vitest.** jsdom builds it on undici's, undici constructs the global `Event`, the jsdom environment has replaced that class — so the handshake dies inside `dispatchEvent` with *"must be an instance of Event. Received an instance of Event"* and the socket times out with nothing to read. `ws`'s client is stubbed in as a second RFC-6455 implementation, which is not a stand-in for the socket under test: it really connects to a real server. xterm.js also wants the **legacy `MediaQueryList.addListener`** on the device-pixel-ratio query, which the width stub `dashboard.test.tsx` already carries does not have.<br>**The React Compiler gate passed, and one bail-out had to be designed around.** `npm run build` reports `CompileError` for the same three `ui/` files it always has and **not** for `Terminal.tsx`; the sentinel count went 4 to 5. What it refused first was `EffectSetState` — resetting the ended-state at the top of the effect — so a different `name` is a remount (`key`) rather than a reset, and the component's own doc says so where a caller can see it. **The bundle is what this costs**: `dist/assets/index-*.js` **256.61 kB to 592.73 kB**, gzip **80.72 to 165.71 kB**, measured by building `main` and this branch. xterm.js is about as heavy as everything else on the page put together, and it is loaded on every visit because it is imported at the top; splitting it behind the button is real and is not this row's.<br>**Three changes outside `web/`.** [`terminal.rs`](crates/yantrad/src/terminal.rs)'s `Size` gains `term`, required, so the Rust test Y-129 left failing on purpose now asserts the whole message and refuses a caller that will not say what it is — one message still does both jobs, so `term` arrives on every resize and is not read there. The daemon names no terminal of its own any more and `terminfo::FALLBACK` left the module. `write.rs`'s `term()` comment — *"a browser has none"* — now says the thing that is true, which is that `up` and `resume` open a session nobody is yet sitting at. And the dev server needed `ws: true` on its `/api` proxy: the string form forwards only plain requests, so a terminal under `npm run dev` connected to nothing.<br>**What this does not cover, and the first half is the half that matters.** **Nothing here has run against a real daemon, a real ssh or a real tmux.** The socket half is proved in CI against a server that speaks the protocol and knows nothing about a pty, so what stays unproved is exactly what Y-129 could not prove either: that a session's bytes reach a browser and that a keystroke reaches the far side. The acceptance test — Claude Code's TUI drawing in a browser tab — needs the fleet and a browser, and through `tailscale serve` it needs **Y-118**, which is untouched here. Whether `serve` proxies an upgrade at all is unmeasured. Sizing is unproved by construction: `FitAddon.proposeDimensions()` answers `undefined` under jsdom because nothing has a width, so every frame in CI carries xterm's default 80x24 and the resize test proves the wiring rather than the arithmetic. **`onBinary` is not wired**, so whatever xterm.js emits down that path — some mouse-reporting encodings — is dropped rather than sent. **StrictMode opens two sockets in development**, one per double-invoked effect, the first closed immediately: dev-only, harmless, and the same shape as Y-131's question. Copy and paste belong to the browser and to tmux's copy-mode, and neither is tested. |
| Y-131 | Two clients, one window: what the phone does to the desktop | ✅ done | claude | Y-130 | **[R2](docs/research/02-tmux-sessions.md) named this as the one immediately user-visible unknown and proposed a setting that turns out to be the default.** Its §3 says *"two browser tabs = two tmux clients, so the smaller clamps pane size. Set `window-size latest` / `aggressive-resize`, accept it, or go to (c)."* **Measured 2026-08-04**, on a server started `-f /dev/null` with no user config (there is no `.tmux.conf` on `cachyos-g14`): the default `window-size` is **already `latest`** and `aggressive-resize` is **off** — on **3.7b**, which both real machines run, *and* on **3.5a** in the CI fixture, which is the one place I-42 would normally predict a difference and here does not.<br>**So the suggested mitigation is what is already in force, and R2's description of the failure points the wrong way.** With `latest` the window does not clamp to the *smallest* client; it follows the *most recent* one — which means **opening the dashboard terminal on a phone resizes the desktop tmux client someone is sitting in**. Same size of problem, opposite mechanism, and no setting avoids it: the real options are `window-size manual` (fix the window, letterbox every client) or R2's option (c), control mode, which is explicitly v2. **The plan deliberately does not choose**, because the choice has a visible cost either way and belongs to the row that first attaches a second client. **[V]** for the default's value; **[D]** for what it then does with two live clients — attaching two sized clients without a terminal did not work in the probe, and this must be measured against a real browser rather than inherited from R2's sentence.<br>**Y-130 left two things here.** The frame this row will argue about exists and is sent: [`Terminal.tsx`](web/src/components/Terminal.tsx) says `{rows, cols, term}` on open and on every `window` resize, and the daemon sets the window from it. And **React's StrictMode already opens two sockets** in development, one per double-invoked effect with the first closed immediately — dev-only and harmless, but it is this row's scenario arriving by accident, and it means *two clients* is reachable on one machine without a second device.<br>**Measured 2026-08-04 with two real clients, and the answer is `accept` — with no Rust changed and no tmux option set.** The clients are [`pty::Terminal`](crates/yantra-core/src/pty.rs)s, which is exactly what the daemon opens per socket and is what the earlier probe lacked, against the real sshd and real tmux in the fixture. A desktop client of 40×160 alone gives a window of 160×39. A phone client of 20×45 attaching takes it to **45×19** — the desktop shrinks, as this row expected. **Then one byte typed at the desktop, with no resize and the phone still attached, takes it back to 160×39** — and a byte typed at the phone takes it back to 45×19. `latest` is the client used **last**, not the client attached last, and that is the sentence neither R2 nor this row had. All **[V]**.<br>**R2's sentence is falsified outright, not just re-aimed.** If the smaller client clamped anything, a larger latecomer would change nothing: a 20×45 client alone gives 45×19, and a 40×160 client attaching second takes the window to **160×39**. Nothing is clamped by the smaller client in either direction, so `aggressive-resize` and a `window-size` setting are answers to a problem that does not exist. R2 §3 and [the plan](docs/plans/m6-terminal-in-browser.md) §3.2 each carry a dated blockquote saying so, and the rule is now **I-54**.<br>**The cost is bounded and it reverses itself, which is what decides this.** A 155-column line printed in the pane wraps to 45 columns while the phone is attached — wrapped, not truncated, its end marker still in `capture-pane` — and comes back **at 155 columns** when the phone leaves. The last client standing gets the window back without being touched. So accepting `latest` is not accepting damage: the person typing always has their own size, and the person who is idle has a reflow that undoes itself.<br>**The alternative was measured rather than argued, and it is worse in the ordinary case.** `window-size manual` does stop a second client moving the window — by stopping the *first* one moving it too: with the window fixed at 160×39 and the phone attached, the desktop resizing its own terminal to 170×44 left the window at 160×39. Manual means a size Yantra picks, holds and has to re-pick, and every client that does not match it sees a crop it cannot scroll. **Control mode (R2's option (c)) stays v2 and nothing here forces it** — it buys per-client sizing, and the measured price of not having it is a reflow that reverses.<br>**I-42's version hazard does not reach this, and that is worth stating because the default assumption in this repo has to be that it does.** The same sequence — attach, attach, type on each, resize, detach, and the larger-latecomer discriminator — was run on the fixture's **3.5a** and on `cachyos-g14`'s **3.7b** through a pty-driven probe, and the two agree step for step, including which client wins after a keystroke. `window-size` is `latest` and `aggressive-resize` is `off` on both.<br>**Y-129's missing ping turns out not to be a sizing problem.** A phone whose network drops without closing leaves a tmux client attached until TCP gives up — and the desktop still gets its size back on its next keystroke, so the bound on this is a keystroke rather than a timeout. Nothing about client limits or idle timeouts changed.<br>**What shipped is the measurement**: [`crates/yantra-core/tests/two_clients.rs`](crates/yantra-core/tests/two_clients.rs), against the real fixture — the window following whoever typed last, the larger latecomer taking it, the reflow undoing itself, and `window-size manual` measured beside them as the rejected option, so a future change of tmux default fails in CI rather than on someone's desk.<br>**What this does not cover.** **No browser was involved.** The clients are the layer the daemon really opens, but `FitAddon` chooses the numbers in production and nothing here has measured what it chooses on a phone. **The pane held a shell, and a shell reflows** — an agent TUI on the alternate screen does not: it is redrawn by the application on `SIGWINCH`, so what survives there is whatever Claude Code can redraw, and that is **[D]**. **No user `.tmux.conf` was involved**: both the fixture and the probe start a server from no config, so a machine that sets `window-size smallest` or `manual` behaves differently and Yantra neither checks nor overrides it — **whether Yantra should pin `window-size latest` on the sessions it creates, as it already pins `remain-on-exit` for I-4, is the one thing here left for the owner**, and it blocks nothing. **macOS was not measured**: it runs 3.7b too, and I-48 is the standing precedent for one tmux version behaving differently on either side of this fleet. |
| Y-132 | Reconnect without losing the screen | ✅ done | claude | Y-130 | R2's *What Yantra must build itself* names **reconnect/replay-last-N-bytes** beside the PTY↔WebSocket bridge, and it is what separates a terminal from a demo — a phone locks its screen, the socket closes, and re-opening to a blank pane means the session looks lost when it is not.<br>**The buffer is in memory and bounded, or it is not built.** [Q5](#6-open-questions) closed *reference-only, always* and its wording names this exact place: *"never writes it to SQLite, logs, the API, **or a terminal stream**."* A replay buffer is a terminal stream at rest, and R2 §7 measures why the size matters even setting secrets aside — the raw stream carries input echo, full SGR/CSI, OSC title sets and bracketed-paste toggles, so two trivial `echo`s are **966 bytes** and an agent redrawing a TUI produces MB/min. Cap it hard from the first commit rather than after the first agent fills it.<br>**Y-130 drew the state this row removes**: a socket that ends with no reason from the daemon leaves the screen where it stopped and says so, and closing plus reopening is the whole of reconnect today. It also decided *against* a route for the terminal on the grounds that a deep link would promise a socket reopened on load — so **if this row builds replay, the routing decision is worth reopening with it**, not before.<br>**Closed 2026-08-04, and it is not built, because the screen was never Yantra's to keep.** tmux draws the pane's current contents for every client that attaches — that is what an attach *is* — so a second socket is a second attach and the screen comes back from the far side. **Measured rather than assumed**, in [`tests/pty.rs`](crates/yantra-core/tests/pty.rs) against the fixture's real sshd and real tmux: a terminal is opened, `before-the-drop` is printed to the pane and reaches it, the terminal is **dropped** and the far side confirmed to hold no client, `while-nobody-looked` is printed to nobody, and a **second** `pty::Terminal` is then drawn *both* — the screen from before the drop and what arrived while nothing was attached. A second test does the same with a full-screen application holding the **alternate screen**, which is the acceptance test's actual case, an agent's TUI. Both were made to fail before being believed; the failure prints the container's own hostname and tmux status line, so the bytes asserted on are a real tmux's.<br>**So the cap is not a buffer cap, and saying what a buffer would have bought is the argument against building one.** The redraw is byte-exact and complete: it opens with **smcup** — `\e[?1049h`, asserted — taking the client's alternate screen, then clears and addresses every line, so the old screen is not something this end has to clear either. A whole attach on a near-empty 80×24 screen, teardown included, measured **1,326 bytes** on 3.7b here: about what R2 §7's two `echo`s cost as raw stream, for the *correct* screen instead of a window of it. A byte window is the worse copy twice over — it is cut at an arbitrary offset, which lands inside a CSI sequence and leaves the screen wrong, and every honest size for it is a fraction of a second of an agent redrawing a TUI at MB/min while still being large enough to hold a resolved secret from one `echo`. **Q5 forecloses the class, not the size.** `capture-pane` was rejected on R2 §7's three measured failures — `-S -N` is not last-N-lines, `-e` re-synthesises SGR rather than replaying bytes, and while a TUI holds the alternate screen the scrollback is unreachable — and control mode stays v2 per R2 §3.<br>**No daemon state was added, and no daemon code either.** [`terminal.rs`](crates/yantrad/src/terminal.rs) is untouched; [`pty.rs`](crates/yantra-core/src/pty.rs) gained three lines of comment saying why no buffer belongs there and nothing else. **The daemon persists nothing and still holds nothing across a closed socket** (`CLAUDE.md` §B1, Y-044): a reconnect is a new socket, a new pty, a new `ssh` and a new tmux client, and the durable thing was always the session. **The retrying is the browser's**, which is where Y-129 said the client work lives.<br>**What was capped is the reopening: five attempts, half a second apart** ([`Terminal.tsx`](web/src/components/Terminal.tsx)). The number is chosen on what an attempt *costs* rather than on network folklore — each one is an `ssh` connection and a tmux client on a machine that may be asleep, and `ssh.rs` sets `ConnectTimeout=10`, so an unbounded retry against a sleeping laptop is the storm [`refresh.rs`](crates/yantrad/src/refresh.rs)'s rule exists to prevent, arriving from the browser instead. Half a second is invisible on the case that matters — a phone waking, a network changing hands — and 2.5 seconds is enough for that and not enough to be a loop. **The budget refills on any frame received**, so it bounds an *outage* rather than a terminal's life; a socket that printed something worked. **A close the daemon gave a reason for is never retried** — text on this socket means the terminal could not be opened, and reopening a refusal refuses again.<br>**Proved in the browser against a real `ws` server and the real xterm.js** ([`terminal.test.tsx`](web/src/terminal.test.tsx)), copying Y-130's shape: a socket hung up with nothing said is reopened to the same URL and told the window again — because a pty opens with one — and bytes on the *second* socket reach the screen; a socket the daemon gave a reason for is not reopened, proved by waiting out the whole budget and finding one connection; the budget is exhausted rather than infinite, and the give-up message is not the refusal alert; and unmounting stops it. Three deliberate breaks, each red in a different place: `ATTEMPTS = 0` fails only the reopen; dropping the *refused* guard fails only the no-reopen; and dropping the unmount guard fails **five** tests at once, because `cleanup()` then reconnects one test into the next one's server — which is the clearest evidence that guard is load-bearing. React Compiler gate passed, sentinel count unchanged at five, bundle 592.73 → 592.90 kB.<br>**Two negative findings, and the first is a behaviour change nobody asked for.** **tmux's own detach key now reattaches.** Typing the prefix and `d` inside the terminal detaches the client, which closes the socket with nothing said, which is indistinguishable from a network that went away — so the page reopens it. The Close button is how you leave; there is no way to tell the two apart from this side without inspecting the stream, which is the thing Q5 says not to do. **And a drop nobody noticed makes two clients.** If a socket dies without either end seeing a close, the daemon holds its pty and its tmux client until TCP gives up while the browser opens a second — so reconnect is a new way to reach that state without a second device. [Y-131](#3-task-board) measured what it costs and the answer is bounded: with `window-size latest` the window follows whoever typed **last** (**I-54**), so the live client has its size back on the next keystroke. What is still missing is the reaping — there is no idle timeout and the daemon sends no ping (Y-129) — and that is deliberately not this row's.<br>**What this does not cover.** **Nothing has met a real daemon, a real browser and a real ssh at once** — the far half is proved against a real tmux and the near half against a real socket, and the join is still what needs the fleet, exactly as Y-129 and Y-130 left it. Whether a phone's `close` event fires at all when the screen wakes is unknown and needs a phone; if it does not, nothing reopens and this row buys nothing on the case it was opened for. **Scrollback is not replayed** — tmux redraws the screen, not the history above it, so what is above the fold after a reconnect is whatever xterm.js still holds; copy-mode is where the rest lives and is out of scope by the plan's §6. The fixture is tmux **3.5a** against **3.7b** on both real machines (I-42), which here is the one place the two were checked against each other: the redraw was measured on both. macOS is untouched, and **Y-118** is untouched, so this is still unreachable from the phone through `tailscale serve`.<br>**The routing decision is reopened and deliberately not taken.** This row said it should be, and half of Y-130's objection is now spent: a deep link promised a socket reopened on load, and a socket that reopens itself exists. The other half does not follow from it — reconnect happens inside a terminal already on the screen, while a URL has to survive a reload, which needs the workspace name to come from somewhere and the dashboard to have a router at all. That is a decision about the page rather than a patch to it, and it belongs to whoever takes it. |
| Y-133 | The container fixture is built over the network on every CI run, with no retry | ✅ done | claude | — | **Found by a red `main` that no commit caused.** The post-merge run at `4991998` failed the `test` job on 2026-08-04 while the identical tree had passed on its own branch six minutes earlier. Nothing under test failed: `ensure_image()` in [`tests/common/mod.rs`](crates/yantra-core/tests/common/mod.rs) shells out to `podman build`, and the `apk add openssh-server tmux ncurses` in [the Containerfile](crates/yantra-core/tests/fixture/Containerfile) could not reach `dl-cdn.alpinelinux.org` — `SSL routines::unexpected eof while reading`, which apk reports as `Permission denied` and then `unable to select packages`. One test died on the build failure and nextest cancelled the 45 still running. Re-running the same job unchanged went green.<br>**The fragility is structural rather than unlucky.** §B3 buys its honesty with a real sshd and a real tmux, and the price is that every fresh runner builds that image from Alpine's CDN before a single test runs — a local image store makes this free on a developer's machine and never in CI. So the whole suite sits behind one third-party TLS handshake, on a branch that is protected and on merges nobody is watching.<br>**One retry around the `podman build` is the small answer** and is what this row proposes; publishing the image to a registry and pulling a digest trades a build for a different network call and adds an artefact this repo does not have. **The acceptance criterion is that a failure still says so**: a retry that swallows the first error leaves a long pause and no evidence, so the second attempt must report both.<br>**Closed 2026-08-05: one retry, and the failure is louder than it was.** [`ensure_image()`](crates/yantra-core/tests/common/mod.rs) still short-circuits on `podman image exists`, so a developer who already has the image pays none of this; when it does build, a failed build waits two seconds and builds again, and only a second failure is an error. **The error carries both attempts** — each labelled, each with its exit status and its own captured stderr — because the whole point of the row was that a swallowed first error leaves a long pause and nothing to read. Two seconds is short enough that nobody reads it as a hang and long enough to outlast a reset connection; there is no third attempt, because this is a flake mitigation and a build that fails twice is a build that is broken.<br>**Verified by producing the failure, not by describing it.** The fixture's `Containerfile` was pointed at a repository host that does not resolve and the tag bumped so nothing could be reused, which reproduces the shape of the 2026-08-04 break — the `apk` fetch fails, then `unable to select packages`, then the build exits 3. The test went red naming `attempt 1/2 (exit status: 3)` and `attempt 2/2 (exit status: 3)` with the full `apk` output under each, so the log says two attempts were made and what each one said. **The recovery path was produced too**: the same broken `Containerfile` was repaired from outside while the test sat in its pause, the first attempt failed, the second built, and the test passed with nothing printed — a green run that would have been red before this row. **What is not covered**: only the build is retried. A `podman run` that fails, an sshd that never answers, or a `podman` that is missing behave exactly as they did, and a CDN outage outlasting one build plus two seconds still reddens `main` — with a log that now says so twice. |
| Y-134 | Nothing reaps a terminal socket — no ping from the daemon, no idle timeout | ✅ done | claude | — | **Named as not built by two rows and owned by neither, which is how it stayed unwritten.** [Y-129](#3-task-board) recorded *"there is no idle timeout and the daemon sends no ping, so a socket dropped by something in the middle is discovered when a write fails"*; [Y-132](#3-task-board), reaching the same gap from the other side, recorded that the reaping *"is deliberately not this row's"*. Both were right about their own scope and the result was a defect written down twice and scheduled never.<br>**What it held.** A [`pty::Terminal`](crates/yantra-core/src/pty.rs) owns the local `ssh` child, the pty master and the reader thread, and its `Drop` kills and waits — so the ssh connection, and therefore the tmux client on the far side, lived exactly as long as `bridge` did. `bridge` was a `select!` over `terminal.read()` and `socket.recv()` with **no third arm**, and nothing in the daemon was on a timer, so a socket that died with neither end seeing a close was discovered only when a `socket.send` failed — **and that send needs the far side to print something first**, which an agent thinking quietly never does.<br>**The instrument is a ping, and a traffic timer would have been wrong in both directions.** Output in progress is not idleness — an agent printing into a pane nobody is watching is the thing M6 exists for — and silence is not death, since an agent that runs quietly for an hour takes nothing and prints nothing and must survive. Only a pong separates them. [Q5](#6-open-questions) constrains the choice as much as it settles it: a ping is a protocol frame and not the stream, so the rule that no frame in either direction reaches a `tracing` call is untouched, and nothing added here reads a byte of what is on the socket.<br>**Twenty seconds, and two consecutive misses.** Neither number is a latency budget — a browser answers a ping below `WebSocket` in microseconds — so what the interval prices is one frame on a socket that is otherwise silent, against the ssh connection, pty, reader thread and tmux client an abandoned one keeps warm behind `ControlPersist=300`. One missed pong is a phone whose radio slept or a busy main thread, and is deliberately survivable; two in a row spanning forty seconds is a peer that is not there. So a dead peer is closed between forty and sixty seconds after its last pong, well inside the kernel's own retransmission budget, which was previously the only thing that would ever have noticed. `MissedTickBehavior::Delay` is load-bearing rather than tidy: `pty::open` awaits ssh with `ConnectTimeout=10` and can outlast an interval, and the default burst would deliver the catch-up ticks as misses the peer was never given the chance to answer.<br>**The ping starts at the upgrade rather than at the pty**, which is also what made the acceptance criterion testable. `first_size` is gone, folded into one loop carrying an `Option<Terminal>`, so the timer covers the whole socket — a caller that upgrades and then sends no size held a daemon task before this too. **Both halves are proved against Y-129's real `TcpListener`**, the interval short under `cfg(test)`: a peer that answers every ping with a masked pong is still being pinged and never closed after several intervals, and a peer that answers nothing receives exactly its tolerance of pings and then the socket ends. Each was made to fail first — deleting the arm that resets the counter on a pong closes the peer that was answering, and widening the tolerance leaves the silent one open.<br>**What those tests cannot say.** Neither holds a `Terminal`, because `pty::open` reaches a real workspace on a real machine; they prove the liveness machinery, which is the same code whether or not a pty exists, and not the drop it causes. **Proving the reap end to end against a real pty still needs the fleet**, exactly as Y-129, Y-130 and Y-132 each left it.<br>**Deliberately not built: anything that limits sockets per workspace.** Y-129 named that in the same breath and it is a different question — this row bounds how long an abandoned socket lives, not how many there may be at once. [`crates/yantrad/CLAUDE.md`](crates/yantrad/CLAUDE.md), its `llms.txt`, and the sentence in [`web/README.md`](web/README.md) saying there is still no ping on the daemon side were corrected in the same change. |
| Y-135 | `resume` refusing at the trust prompt arrives as 500 | ✅ done | claude | — | **Left alone deliberately by [Y-113](#what-closed-and-what-it-steered), and the reason it recorded is *slightly* wrong in a way that matters, because the wrong name points at the wrong file.** The variant is `resume::Error::AwaitingTrust`, not an `up::Error`. The outcome is identical: `again`'s `map_err` in [`write.rs`](crates/yantrad/src/write.rs) matches only `resume::Error::Workspace` and `resume::Error::Up(up::Error::Workspace)` and sends everything else into `from_workspace(None)`, whose `_` arm is `INTERNAL_SERVER_ERROR`. `open` has the same shape through `up_workspace`, and `stop` through an inline match — **so this is three handlers with one hole, not one bug in `resume`.**<br>**What the status says about the world.** [`status.rs`](crates/yantra-core/src/status.rs) documents `Verdict::AwaitingTrust` as *"Not running, and not a failure either"*, and **I-49** says an agent at the dialog is inert: *"Neither is a fault — there is nothing to report yet — but every Yantra command reads as though the launch failed."* A 500 is that failure mode reproduced one layer up, at the HTTP boundary, for a state the daemon has correctly identified and named. [`Act.tsx`](web/src/components/Act.tsx) then draws 500 as *"The verb ran and failed."* — so the dashboard reports a crash when what happened is that a human has not answered a dialog on their own machine, and ADR-0011 says that human is the only one who may.<br>**The right status is not the open part; the repo has already chosen it twice.** **409 means a refusal about state** here, with the reasoning written down both times: `from_create` — *"a name already taken is a 409, since the caller asked for something reasonable that the world already answers"* — and `from_edit`, Y-126's own sentence, *"the request is reasonable and the world already answers, and `yantra down` is what changes that."* `AwaitingTrust` is exactly that shape, and answering the dialog is the `yantra down` of it. **503 means nothing was decided** — `edit::Error::CannotTell`, `Refused::CannotAsk`, `Refused::Forwarded` — which is where `Ssh`, `Tmux`, `Terminfo` and `Status` belong, being a machine that could not be asked rather than a daemon that broke.<br>**The open part is the shape of the mapper, and `from_edit` is the one to copy** — it names every variant of `edit::Error` and has **no wildcard**, so a variant added later cannot default into 500. `from_workspace`, `from_create` and both inline matches all have one. `from_resume` and `from_up` written exhaustively are the change; `NoStateDir` and `agent::Error::Random` stay 500, being genuinely this daemon's fault.<br>**The commoner instance of the same bug is not the trust prompt.** `agent::Error::NotLoggedIn` takes the identical path, so **I-44** on the Mac — the whole of what [Y-122](#3-task-board) carries — arrives at the dashboard as *the verb ran and failed*. Y-113's clause names the narrower case; the mapper fixes both.<br>**One string travels with it.** `Act.tsx`'s 503 sentence currently reads *"The daemon could not ask Tailscale who is calling"*, which stops being true the moment an unreachable *workspace* machine also answers 503. Rewording it is part of this change, not tidying after it (§B5).<br>**Nothing asserts a status for `up`, `down` or `resume` today**, and no test in `yantrad` can reach those handlers for Y-126's reason. The pattern to copy is `a_session_open_on_the_machine_being_left_is_a_conflict_and_not_a_failure`: build the core error by hand, wrap it in `Refused::Verb { status: …, said: chain(&error) }`, call `.into_response()`, and assert the status **and** the body.<br>**Closed by fixing the mapper rather than the variant.** `from_up`, `from_down` and `from_resume` name every error they can carry and have no wildcard, and **`from_workspace` no longer takes an `Option`** — the `None` that meant *something else, so 500* was the hole itself, and removing it means an error that is not a workspace's can no longer arrive there at all. `agent::Error` and `status::Error` got mappers of the same shape, both being reached through more than one verb and both holding a variant that really is this daemon's.<br>**`NotLoggedIn` is a `409`**, judged against the two definitions rather than against how badly it bites. Nothing was left undecided — the machine was asked, it answered, and the answer is *not yet*, which is what disqualifies 503. Nothing broke either. What remains is a refusal about state whose remedy is a person at that machine's own keyboard, the same remedy `AwaitingTrust` has and the same shape `yantra down` is for the edit refusal. So the two errors this row is about arrive as one status, which is what lets the dashboard say one honest sentence about them.<br>**The rest.** `StartupConflict`, `NoRepo`, `NoAgent` and `Startup` are `409` for that reason too — the world already answers, and an edit, a clone or `up --agent` changes the answer. **`Unclear` is `503` and not `409`**: `resume` refuses there precisely because it could not tell what is in the pane, and R-23 says a check that cannot know must say so rather than name someone. `agent::Error::Unreadable` is `503` on the same argument, being `claude auth status` printing something that is not its own JSON. `Ssh`, `Tmux`, `Terminfo` and a status that could not be read are `503`; `NoStateDir` and `agent::Error::Random` stay `500`.<br>**Y-113's clause named the wrong file, and the code won.** `AwaitingTrust` is `resume::Error`'s: `up` cannot reach it, and the path that can reaches it directly rather than through `Up`.<br>**The string travelled with the change (§B5).** [`Act.tsx`](web/src/components/Act.tsx)'s `503` claimed the daemon could not ask Tailscale who is calling, which stopped being true the moment an unreachable *workspace* machine answered `503` as well; it now says nothing could be asked and nothing was decided, which is the wording [`EditWorkspace.tsx`](web/src/components/EditWorkspace.tsx) already uses for the same reason. The `409` needed a sentence of its own and is drawn with the plain alert rather than the destructive one, again as the edit form already does — a state the daemon named correctly may not be painted as a crash. **`NewWorkspace.tsx` keeps the narrow Tailscale wording deliberately**: `POST /api/workspaces` touches no machine, so `whois` really is the only thing that can leave it undecided there.<br>**What the tests pin.** No test in `yantrad` can reach these handlers (Y-126's reason), so each builds the core error by hand and asserts the status and the body: the trust prompt is a `409` whose body still names the workspace and says what it is holding at, and `NotLoggedIn` is a `409` through `up` and through both of `resume`'s paths. **Both directions are pinned**, so this cannot be read as *stop answering 500*: `NoStateDir` and a session id that could not be generated stay `500`, and an ssh failure and `Unclear` stay `503`. The dashboard's own test asserts the trust prompt draws the plain alert and never the words *the verb ran and failed*.<br>**Deliberately not built.** The status stays `write.rs`'s and does not move into the library — a refusal *kind* on the core errors would put an HTTP decision in `yantra-core`, which ADR-0005 does not want and the CLI cannot use. `from_workspace` keeps its own wildcard: with the `Option` gone it ranges over `workspace::Error` alone, whose remaining variants are all this daemon failing to read its own files, and `500` is the right answer for exactly that set. And nothing here touches I-44 itself — [Y-122](#3-task-board) owns that; this row only stops the dashboard calling it a crash. |
| Y-136 | The agents section still hands over `yantra up` and `yantra resume` as text to paste | ⬜ todo | — | — | **M5 existed to remove exactly this, and this section is where it survived.** [Y-113](#what-closed-and-what-it-steered) records it under *what was deliberately not built*: *"the agents section still hands over `yantra up` and `yantra resume` as pastes"*, deferred on the ground that folding them into buttons *"is its own change, not this one's"*.<br>**It is not cosmetic, and the milestone's own premise is why.** M5's definition of done is *start, stop and resume a session **from the dashboard***, and Y-113 proved that from a real phone for the workspaces table. An agent row on the same page answers the same verb with a string to select and copy — and **a paste is unusable on a phone**, there being no terminal on it to paste into. This is the one place on the page that still fails the sentence the milestone was written around.<br>**One cell.** `agentCommand` in [`columns.tsx`](web/src/columns.tsx) returns `yantra up {name}` for `no_session` and `yantra resume {name}` for `finished`/`stopped`/`crashed`/`killed`, and reaches the screen through `agentColumns`' `COMMAND` cell. Its other branches return `yantra attach`, and **those stay**: `attach` execs `ssh -t` and hands the terminal over (Y-078, ADR-0011), so it has no route to POST to. Only the `up` and `resume` branches have routes behind them.<br>**There is no missing data, which is what makes this small.** `AgentRow` is a `workspace: Workspace` beside a nullable `status: WorkspaceStatus` — it carries the whole workspace, which is precisely the one prop [`Act`](web/src/components/Act.tsx) takes. `Act` needs `startup` twice over, to choose the `{ agent: 'claude' }` body for `up` and to decide whether `resume` is offered at all, and it has it; it renders *waiting on {machine}* while in flight, and the agent row carries `machine` too. The workspaces table already writes `<Act workspace={…} />` into a cell, and `Act` exports the `button` class that `columns.tsx` and `EditWorkspace.tsx` already reuse.<br>**The one part that is a decision rather than a substitution.** `Act` offers all three verbs and lets the daemon refuse; this section's states are narrower, `agentCommand` choosing the verb from `status.status.state`. Dropping `Act` in whole would put a **Stop** button beside an agent that has already stopped — technically harmless, since `down` on nothing answers *there was nothing to stop*, and still a button that says the page does not know what it is looking at. Whether this section gets `Act` unchanged or a narrower use of the same component is the row's to settle, and it is the only thing here that is not mechanical.<br>**The interaction worth knowing, and it is deliberately not a dependency.** A Resume button here answers `awaiting_trust` — a state **this table renders by name** — with the 500 that [Y-135](#3-task-board) is about, and `Act`'s 500 string is *"The verb ran and failed."* The button works either way; it says the wrong thing until Y-135 lands. That makes this section the sharpest case for that row and is why the two sit beside each other, but neither blocks the other and ordering them would stall a loop on nothing.<br>**Two sentences go stale with it and §B5 makes them part of the change**: [`Command.tsx`](web/src/components/Command.tsx)'s doc comment claims *"Everything with a route behind it is a button now (Y-113)"*, which is what this row falsifies, and the Y-113 residue in [§3](#what-closed-and-what-it-steered) says the same thing from the other direction. |
| Y-137 | `workspace::load` accepts `startup = ""` from a hand-edited file | ⬜ todo | — | — | **[Y-119](#what-closed-and-what-it-steered) closed the writing path and said so loudly**, its last paragraph being this row: *"`load` still accepts `startup = ""` from a file on disk … refusing it at load is a different decision — it would make an existing file stop the whole of `list()`, which is the rule a malformed file already gets and a blank string may not deserve."*<br>**Where the asymmetry actually is.** `non_empty` in [`workspace.rs`](crates/yantra-core/src/workspace.rs) is called by `create_in` and `update_in` and by nothing else; `parse` builds a `Workspace` straight out of `OnDisk` with no check at all. So `load_from` — and with it `load`, `list`, every CLI verb and every route — accepts it. **And not only `startup`**: `machine = ""` and `repo = ""` load equally, which Y-119 did not say, and `machine` reaching `ssh` verbatim under ADR-0009 makes the empty one the more interesting of the three.<br>**What it costs downstream, named rather than implied.** `Some("")` is not `None`, and `None` is the state that means *just a shell* (ADR-0007; the dashboard draws it as a blank cell rather than the word "none"). With `Some("")`, `tmux::ensure` respawns the created pane with an empty command; `up --agent` is refused by `StartupConflict` reading *"already runs `` at startup"*; `resume` is refused by `Startup` reading *"runs `` at startup rather than an agent"*. Three sentences, each true, each with an empty pair of backticks where the command should be, and none of them readable enough to send the operator to the file.<br>**The design question is Y-119's and the row has to answer it rather than skip it.** `list()` maps `load_from` over every `.toml` in the directory and **one `Err` fails the whole listing**, deliberately: *"silently omitting a workspace would report 'no sessions on that machine' for a machine that was never queried, and a wrong answer is worse than a refusal."* That rule was written for a **malformed** file. A blank string is not malformed TOML, and paying for one with every other workspace vanishing from the dashboard is a large price for a small defect. Three answers exist and the row picks one, with its reason: refuse at load and accept that `list()` stops; refuse at load **and** change `list()` to carry per-workspace failures instead of failing whole — which is a change to its contract and to `GET /api/workspaces`, and is the honest fix if the all-or-nothing rule is what is wrong; or leave `load` alone and make the three readers say it, which buys least because there are three of them and they would each say it differently.<br>**What must not happen is the friendly reading.** Coercing `Some("")` to `None` at load is refused on the writing path in `non_empty`'s own comment — *"coercing here would have two callers disagree about what they wrote"* — and doing it at load is worse, because then the file and the loaded workspace disagree and neither the operator nor `yantra ls` can see it.<br>**R-23 is the frame.** A workspace that loads clean and then fails at `up` is a confident lie about a file the operator could fix in a second, so whatever the row chooses, the refusal has to name the file, the field and the workspace — and a refusal that takes the other workspaces down with it has to say that too. |
| Y-138 | §3 says four dashboard tables still overflow below 48rem; `DataTable` says all four get blocks | ⬜ todo | — | — | **Opened to make four tables fit a phone, and it stops being that the moment it is read against the code — which is why it is here with the doubt written into it rather than dropped.** The finding is worth a row either way; the framing that produced it is not.<br>**There are four tables, not five, and all four already get the labelled blocks.** `DataTable` is instantiated four times in [`App.tsx`](web/src/App.tsx) — Machines, Workspaces, Sessions, Agents — over `machineColumns`, `workspaceColumns`, `sessionColumns` and `agentColumns` in [`columns.tsx`](web/src/columns.tsx), and `Column<` appears nowhere else in `web/src`. The page has six `Section`s; the other two are the create and edit forms, which are not tables.<br>**The block rendering is global to the component and not opt-in.** [`DataTable`](web/src/components/DataTable.tsx) takes `{ columns, rows, rowKey, empty }` and no flag, reads `(width < 48rem)` through `matchMedia` under `useSyncExternalStore`, and returns one labelled `<dl>` per row ahead of the table branch. There is no way for a table to miss it. Y-121's archive row says the same twice — *"Shipped 2026-08-03 in `DataTable.tsx` and nowhere else"*, *"All four `Column<T>[]` arrays are untouched"* — and so does [`web/README.md`](web/README.md): *"All four tables share the component, so all four get it."*<br>**So what is left is one false sentence in this file**, on the Y-121 bullet of [§3](#what-closed-and-what-it-steered): *"Only the workspaces table becomes labelled blocks below `48rem`; the other four tables still overflow, which they always have."* It is wrong twice over — the fix was not workspaces-only, and *the other four* implies five where there are four. It reads like the pre-implementation framing, and it is: the archive row's own *"The other four tables have always overflowed"* was true when Y-121 was opened and about to stop being true when it shipped. **This is §B5's drift, in the file that owns the rule** — a restated fact nobody edited and nothing failed over.<br>**Two in-code comments drifted the same way and belong in the same change.** `DataTable.tsx` says *"two of the six are already wider than the screen"* while the workspaces table now has **seven** columns, Y-126's `EDIT` and Y-130's `TERMINAL` having arrived after Y-121; and `Command.tsx` says *"Everything with a route behind it is a button now (Y-113)"*, which is [Y-136](#3-task-board)'s.<br>**What would make this a code row instead, stated so the next reader does not have to re-derive it.** Nothing in the repo measures the machines, sessions or agents tables at any width — every number Y-121 recorded (924 px in 310 px, `ACT` at 310, the button at x 358–443, `48rem` chosen because 768 px is the narrowest width where it is on screen) is the *workspaces* table's. If the blocks are wrong for one of the other three, the evidence is not here. The likeliest candidate is the agents table's `DETAIL` cell, which renders free-form daemon prose in `whitespace-pre-wrap` and is the widest unbounded thing on the page. Measuring the other three at 390 px is the only thing that reopens this as code, and this row does not assume it will.<br>**Whoever takes it should also keep the harness**: jsdom implements no `matchMedia`, so every dashboard test stubs a width-aware one and names the width it draws at, defaulting to 1280 px — i.e. the existing suite runs in table mode unless a test narrows deliberately. |
| Y-139 | Q12: is R-1 retired, or only retired on Linux? | ⬜ todo | — | — | **[Q12](#6-open-questions) has been open since M3 and it is a measurement, not an argument.** Issue #63545 is a **macOS + tmux** report; Y-023's spike that retired **R-1** ran on `cachyos-g14`, which is Linux, because the MacBook was unreachable until Y-049 closed on 2026-07-30. R-1, Q7 and ADR-0011 were all retired or unblocked on that Linux-only evidence, for a bug filed against the other operating system in this fleet.<br>**The prior attempt narrowed the question rather than answering it, and the row starts from where it stopped.** Q12's own follow-up records a re-run on the Mac on 2026-07-30: Claude Code 2.1.220 booted in a fully detached tmux session, the transcript **was** created and written while fully detached — 57,841 bytes, `session_attached=0` throughout — and the agent answered *Not logged in · Please run /login*. So macOS's file-write path is **not** broken, which is most of what #63545 alleges. What has never been measured there is a transcript **advancing across a real conversation** while detached, because the agent never had one.<br>**What would reinstate R-1**, and the row should be run to settle exactly this: a detached tmux session on `bishwajeets-macbook-pro` in which an authenticated Claude Code takes a prompt and its transcript's mtime **stops advancing** while the session stays detached — **two readings**, seconds apart, with `session_attached=0` between them. Anything less is not R-1. A file that exists proves nothing, which is Q12's own prescription: *"make `yantra logs` assert the transcript mtime is advancing, not merely that the file exists, because #70632's failure mode looks healthy until you tail it."* [`logs.rs`](crates/yantra-core/src/logs.rs) already reports `modified` and derives an age from it, so the reading exists and nothing takes two of them. **What retires R-1 for good** is the same run with the mtime advancing, on the machine and the OS the issue was filed against.<br>**I-44 blocks one route and only one.** Over ssh the agent is not authenticated — the token is in the login keychain and ssh lands in launchd's `Background` domain — which is precisely what stopped the 2026-07-30 run, and it is [Y-122](#3-task-board)'s to fix. **From a GUI login on the Mac it is not blocked at all**, and that route is the more faithful reproduction anyway: #63545 is about macOS and tmux, not about macOS and ssh and tmux. So this is runnable today by hand and is not gated on Y-122 — but the two are **not the same experiment**, and whoever takes it must say which one they ran.<br>**The practical trap on that machine has been met already.** `tmux` is not on the ssh session's `PATH` (**I-34**): `ssh mac 'command -v tmux'` finds nothing while `tmux 3.7b` sits at `/opt/homebrew/bin/tmux`. Resolve it absolutely, as `Tmux::resolve` does. An unknown `TERM` also aborts `tmux attach` there (I-36, I-43).<br>**Where it goes**: [`crates/yantra-core/tests/manual_macbook.rs`](crates/yantra-core/tests/manual_macbook.rs), `#[ignore]`d rather than skipped (I-32) and run with `just test-mac <machine>` — where every check no container can make already lives.<br>**What it cannot settle.** **I-53** — `claude auth status` reports the credential it found and never that it works — so *authenticated* has to be proved by the agent answering a prompt, not by the gate passing. And a null result on one run is weak evidence for an intermittent bug; say how long it was watched. |
| Y-140 | Embed the dashboard in the binary, behind a cargo feature that is off by default | ⬜ todo | — | — | **[Y-073](#what-closed-and-what-it-steered) shipped the directory half and deferred this by name**: *"The cargo feature this row describes arrives with the M7 appliance that wants one file to copy — building it now would buy nothing and cost R-24 immediately."* **M7 is next, so it is due**, and what it buys is M7's own definition of done: a Pi 5 / N100 that runs 24/7 and survives reboot should be one binary to copy, not a binary plus a directory plus an environment variable that refuses at startup when it is wrong.<br>**[R-24](#7-risk-register) is the hazard and it is the centre of this row, not a caveat on it.** *The JS toolchain leaks into the Rust build. Starts as a convenient `build.rs`, ends with `cargo clippy` and the `aarch64-unknown-linux-musl` cross-build needing npm.* Its retire condition is `cargo build` green on a machine with no Node installed, and the repo holds that line in two places already: [`web.rs`](crates/yantrad/src/web.rs)'s module doc says assets are a directory *because* embedding makes every `fmt`, `clippy`, `test` and musl job depend on npm, and the [`justfile`](justfile) says of the landing recipes *"Deliberately absent from `ci` and `check` — R-24's retire condition is that the Rust gate stays green on a machine with no Node installed, so nothing here may become a dependency of either."*<br>**The asymmetry that makes this dangerous is in the workflows and is worth stating exactly.** [`ci.yml`](.github/workflows/ci.yml) has **no path filter** — `fmt`, `clippy`, `test`, `deny` and the aarch64 cross-build run on every push and every pull request. [`web.yml`](.github/workflows/web.yml) is filtered to `['web/**', '.github/workflows/web.yml']` and is the only job in the repo that runs `npm ci` and `npm run build`. **So on a Rust-only PR nothing builds `web/dist` and five Rust jobs run.** A `build.rs` or an `include_dir!` that wants those assets unconditionally is red on every Rust PR, and the obvious repair — teaching `ci.yml` to run npm — is R-24 arriving in full, on the first day, exactly as it is written.<br>**How it stays off.** The feature is absent from `default`, and nothing in `just check` (`fmt-check lint test deny`) or `just ci` (`check` + `appliance`) may enable it. **`--all-features` is the specific thing that must never appear**: `just lint` is `cargo clippy --workspace --all-targets` and `just test` is `cargo nextest run --workspace`, neither passes it today, and one `--all-features` added later for tidiness turns every Rust job into an npm job silently. That omission is load-bearing and reads like an oversight, so it wants a comment where the recipes are — the same treatment `landing-build` already has.<br>**How CI proves it, and the assertion is a negative one.** That the default build is green proves nothing; what has to be provable is that **no job turns the feature on**. Two halves. The five existing Rust jobs keep running with no Node step anywhere in them — R-24's retire condition already exercised on every PR, and this row's job is to keep it that way, not to establish it. And the one job that *does* build with the feature must build `web/dist` first, which puts it on the web side of the path filter or in a job of its own, never in `ci.yml`. Whichever, the check worth writing greps the workflows and the `justfile` for the feature name and for `--all-features`, because a build that succeeded says nothing about which jobs needed npm to succeed.<br>**What it must not change.** The directory half stays, and its two failure shapes are deliberate: **unset** serves the API alone and says how, **set but wrong** refuses at startup because a `ServeDir` over a missing directory answers 404 to everything and reads as a broken dashboard rather than a typo in one variable. An embedded fallback must not turn *set but wrong* into *quietly serves the embedded copy* — that is the confident lie R-23 is about. **Precedence between `YANTRA_WEB` and the embedded assets is the one real decision in this row**, and it is a decision about which of the two is allowed to be silently wrong.<br>**Two things travel with it.** Whatever carries the bytes (`rust-embed`, `include_dir`, or a `build.rs` of our own) goes through `just deny` in the same PR. And `just appliance-size` reports either side of the change: the appliance's size is a quality target carried over from ADR-0003's measurement discipline, and a whole dashboard is not a rounding error on a musl binary. |

### Landing site — out of milestone

Owner's ask, 2026-08-01: a *coming soon* page. **This is not M4.** The dashboard is a read-only
tool served over Tailscale to one person; this is a public single-page holding note. They share an
identity and nothing else — which is why what they share ships as
[`design/tokens.css`](design/tokens.css), plain CSS custom properties with no framework
dependency, and the reasoning as [`docs/design-system.md`](docs/design-system.md).

**It meets [ADR-0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md) cleanly.** That ADR
picked plain CSS + custom properties as the styling layer and left *what the incoming design system
is delivered as* open between CSS variables, a Tailwind preset and React components; this is the
first, which is the one that keeps its promise that the integration diff is `index.css` and nothing
else. **One collision is recorded rather than designed around**: shadcn's `cssVariables` mode
defines `--accent` as a muted hover surface and this file defines it as cinnabar, both on `:root`,
with no error either way — `docs/design-system.md` §7 carries the bridge.

Lives in `landing/`, leaving `web/` free for Y-072. Astro 7 + React 19 islands + Tailwind 4.

**Shipped as [GitHub stack #72](https://github.com/2002Bishwajeet/yantra/pull/67)**, five layers, using the native feature rather than five PRs that merely point at each other — which is what Y-064 widened the CI trigger for. The difference is not cosmetic: a registered stack retargets and rebases the layers above one that merges, and can be merged atomically, where hand-chained `base` branches leave every upper layer to be fixed by hand. `gh stack link` was used rather than `submit`, because the PRs already existed and `link` adopts them instead of recreating them.
The guardrail *"no UI until the CLI is good"* is not in play: this renders nothing about a machine,
reads no API, and has no way to reach `yantra-core`.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-200 | Landing scaffold + the design system | 🔵 review | claude | — | Astro 7.1.6 / React 19.2.8 / Tailwind 4.3.3, static output. Tokens and spec as above. **Three findings worth the research cost:** (1) **`astro dev` daemonizes itself under an AI-agent environment** — Astro 7 bundles `am-i-vibing`, so `npm run dev` forks and the foreground process exits 0, which kills Playwright's `webServer` with *"exited early"*; the fix is counter-intuitively `ASTRO_DEV_BACKGROUND=1`, which means *"I am already the background child"*. (2) **`@astrojs/tailwind` is deprecated** — Tailwind 4 is a Vite plugin with no `tailwind.config.js`, and tokens go in CSS via `@theme`; `@theme inline` is required when a token references a var defined elsewhere, or Tailwind emits the literal string. (3) **`create astro` has no `--typescript` flag** any more; strict is unconditional. Also `--no-ai`, to suppress generated agent files this repo already has conventions for. |
| Y-201 | Pattachitra motifs as generated SVG | 🔵 review | claude | Y-200 | Vine, rosette, cusped arch, hanging lamp, pearl band, sigil — **parameterised, never hand-authored path data**. Two rules earned by getting them wrong: the arch's lobe count must be **even** (`\|sin(N·θ)\|` then vanishes at `θ = 0, π/2, π`, cusping both springings and the apex; odd scoops the apex into a dome), and **only the inner rim is cusped** — cusping the silhouette turns a shrine into a sunburst. |
| Y-202 | The yantra shader | 🔵 review | claude | Y-200 | Nine interlocking triangles, a spoked chakra band, a bindu, in a WebGL2 fragment shader. Raw WebGL2, **no dependency** — a full-screen triangle from `gl_VertexID` needs no buffers, no VAO, no attributes. `client:only="react"` is mandatory: every other directive still server-renders, and `canvas` does not exist during SSR. Honours `prefers-reduced-motion` by drawing **one static frame** rather than nothing. Structure adapted from `canvas-ui` (**MIT + Commons Clause** — adaptable, *not* redistributable as a component library). |
| Y-203 | Header, footer, and the one sheet | 🔵 review | claude | Y-201, Y-202 | The whole page: painted frame, masthead, the shrine, one line of copy, coming-soon, footer. One screen, no scroll, no router. |
| Y-204 | Visual regression tests | 🔵 review | claude | Y-203 | Playwright `toHaveScreenshot`, light + dark × desktop + mobile. **Snapshots are OS-specific and Playwright's default path template no longer includes the platform**, so Linux and macOS collide at one path while rendering fonts differently — pin to one OS. |
| Y-205 | Deploy previews on Cloudflare | 🔵 review | claude | Y-204 | Every PR touching `landing/` or `design/` builds, runs the visual regression, and uploads a preview version, with one comment carrying the URL and edited in place on later pushes; a push to `main` deploys production. **Workers static assets, not Pages** — Pages is not deprecated, but Cloudflare has stopped recommending it, and the evidence is in wrangler itself, which prints *"Workers are the recommended way to deploy all new projects"* when a Pages project is missing; Astro's deploy guide no longer documents Pages at all. **No `@astrojs/cloudflare`**: the adapter is explicitly unnecessary for `output: 'static'`, which is also why `wrangler.jsonc` is hand-written. **A separate workflow, not a job in `ci.yml`** — that is R-24's own stated mitigation, and its retire condition (*`cargo build` green on a machine with no Node*) holds only if the two gates share no file; `landing-build` and `landing-visual` are justfile recipes per the CI convention but are unreachable from `just ci` and `just check`. **Two findings that would each have cost an afternoon**: `wrangler versions upload` *fails on a Worker that has never been deployed*, so a one-time `wrangler deploy` is a documented bootstrap rather than a surprise; and `wrangler-action` **silently drops the preview alias** — its output schema parses only `preview_url`, so `deployment-url` is the hash-versioned URL and the stable `pr-N-…` alias has to be recovered from stdout. **The deploy steps skip rather than fail without the two secrets**, so the build and the visual regression gate every PR before anyone has a Cloudflare account; the same branch covers a fork PR, and the answer there is the `workflow_run` split, never `pull_request_target`, which would run with secrets against unreviewed code. The token is a **custom** one scoped to `Workers Scripts: Edit` — Cloudflare's own guide recommends the `Edit Cloudflare Workers` template, which grants all-zone route write plus KV, R2 and Tail to a CI secret. Also swaps Astro's default favicon for the sigil. **Four setup steps the owner must do**, in `landing/README.md`. |
| Y-206 | The patta frame, redrawn from the reference | 🔵 review | claude | — | The first landing design was rejected outright — wrong concept, too folk, too busy, and it read as AI-made. The mechanism is worth recording: it landed exactly on a known cliché (warm ground + serif display + terracotta accent, centred, ornamental border), each choice defensible on its own. Rebuilt from a second reference, an Odisha Pattachitra of the **Nabagunjara**, with every value *measured* rather than eyeballed — the palette is the top-decile-saturation mean of each hue family across the image. Four framework-free drawing stacks in `landing/src/patta/`, pure functions returning SVG strings, colour only via custom properties. **Corrections that overturn earlier rows:** Y-201's two arch rules are *wrong for this arch* — both rims are cusped equally (a parallel offset; it is the creeper that stays calm), and an even lobe count is necessary but nowhere near sufficient, because `|sin|` gives a smooth maximum and the apex needs a **local tent** of half-width ~0.15 rad to make a real ~155° vertex. Motif rules are specific to the painting they were measured from, not general Pattachitra law. **Other findings:** a Bézier aimed at its own endpoint always arrives as a spike — that is why generated petals come out as thorns; tiles carry `preserveAspectRatio="none"`, so any caller making `tile ≠ band` stretches every floret, and density must scale both together; co-prime background sizes kill the combined super-period but **not a single layer's own lattice**, so the 60–250px band must be left empty; `feTurbulence` is unusable for a full-page ground because Chromium tiles the filter region. There is **no second border band** — the creeper belongs to the arch's spandrel, and only looks like a border if you crop the top edge where the arch runs near-horizontal. The frame wraps the **viewport**, redrawn in CSS pixels on resize rather than scaled from a fixed viewBox. Light ground only by default: a patta is a painted cloth, so the dark ground is an invention and does not get to be what a dark-mode visitor sees. Wired into the page by Y-207. |
| Y-207 | The landing page, rebuilt on the patta stack | 🔵 review | claude | Y-206 | `index.astro` is the frame now: cream band, cloth field, the border wrapping the viewport at 65% density, the torana standing in the field and the **Jai Prakash bowl inside it**, read against the real sun over the Jantar Mantar at Jaipur from the viewer's clock. That last part is the whole argument for the instrument being there rather than an ornament being there. **Deleted, not left dead:** `Frame`, `Header`, `Footer`, `Shrine`, `motifs/`, `YantraShader.tsx`, `shaders/`, `/preview/patta`, the React integration and its four dependencies, and the two font families the old lockup used — the page has no client framework left. **Three geometry findings.** The arch must not take the field's shape: a near-square panel puts the head's two radii within a few percent of each other, while spanning a 1.7:1 desktop field flattens it to a tent and a phone field draws a 3:1 lancet. A cusped opening's clearance is nowhere near its half-width — the two upper cusps bind first, and sizing the bowl off the half-width drew it over the band, so `toranaOpening` now hands back the boundary itself. And the instrument has to be drawn *before* the arch, because the lamps hang from the inner cusps into the opening the bowl occupies and cross it at any radius. **The baselines are rewritten** and the clock is pinned with `page.clock.setFixedTime` — the bead is a real shadow, so an unpinned clock moves it every run; one test moves the clock past sunset and asserts both the bead and the caption follow. `design/tokens.css` is untouched and the landing no longer reads it: see `landing/README.md` for why the two palettes have diverged and which one is measured. |

### What closed, and what it steered

One line per closed task that deferred something, dropped something, or left the plan somewhere other
than where it started — the state that would be lost if the row simply went away. Everything else
about them, the evidence included, is in [docs/archive/m4-m5.md](docs/archive/m4-m5.md); M5's own
re-scope is in [§2](#2-milestones).

- **[Y-037](docs/archive/m4-m5.md#carried-forward-from-m0m3-the-ones-that-closed-here)** — the release pipeline is rehearsed green on five targets and **nothing has ever been published**. Publishing needs a tag and a version worth tagging, so the README documents building from source and points at no binaries.
- **[Y-044](docs/archive/m4-m5.md#carried-forward-from-m0m3-the-ones-that-closed-here)** — the session state store was **dropped on 2026-08-02 without being built**: five candidate consumers audited, none needs one, because declared state is the workspace TOML, live state is tmux, and history is the agent's own transcript. **The daemon persists nothing.** What brings it back is the first question about the *past*.
- **[Y-073](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — asset serving shipped as the **directory half only** (`YANTRA_WEB`). Embedding the dashboard behind a cargo feature waits for the M7 appliance that wants one file to copy; building it now would buy nothing and cost R-24 immediately.
- **[Y-080](docs/archive/m4-m5.md#found-by-the-scope-audit-2026-07-31)** — a repo-wide relative-link check was written and **deliberately not kept**. It found 0 broken links across 53 files and would have caught neither drift, because both times the stale reference resolved to a *real* ADR that was about something else. The fix was to delete the duplicated fact, not to check it.
- **[Y-083](docs/archive/m4-m5.md#found-by-verifying-the-cli-2026-07-31)** — ADR-0003 and research notes 06a–06c were **deleted rather than superseded**, which bends §B5's *immutable once accepted* on the ground that the decision never governed shipped code. **Retired numbers survive as numbers and are never reused**: R-0, R-4, R-9, R-11, R-14…R-18, I-15…I-17 and ADR-0003.
- **[Y-101](docs/archive/m4-m5.md#m4--web-ui-closed-2026-08-02)** — [ADR-0004](docs/adr/0004-rust-for-the-daemon.md) was **amended in place** with a dated blockquote rather than superseded, because what stopped holding was a premise and not the decision; `CLAUDE.md` §B1 and `README.md`'s Stack line follow it.
- **[Y-110](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — closed the **macOS half only**. The Linux `sysfs_power` battery path is still `[D]` and reachable only through a fake tree, since `cachyos-g14` cannot be unplugged remotely; I-9's desktop half still has no desktop; and *asleep or off*, produced on the Mac later the same day once the owner closed the lid over this task's beat, does not survive a `yantrad` restart, because nothing is persisted.
- **[Y-113](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — `resume` refusing at the trust prompt still arrives as **500**, left alone deliberately because it is an `up::Error` reaching a `workspace::Error` mapping. Also not built: a plain-shell start for an agent workspace, and the agents section still hands over `yantra up`/`yantra resume` as pastes rather than buttons.
- **[Y-114](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — the PWA is proven in headless Chromium only. **The iOS install itself is unverified** — no iPhone or iPad was available — so *Add to Home Screen*, `display: standalone` and the `apple-touch-icon` are untested on the device the milestone is for.
- **[Y-115](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — **closed on evidence with no code written.** The row predated the three tasks that answered it, and each of its four clauses turned out to have an owner already: Y-109, Y-113, and a sessions section older than the row.
- **[Y-116](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — **editing did not ship and the row's title had promised it.** There is no `PUT`, no `PATCH` and no `yantra edit`; creating is all there is, and the file stays the operator's to edit meanwhile.
- **[Y-119](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — closes the **writing** path only. `workspace::load` still accepts `startup = ""` from a hand-edited file on disk, and refusing it at load is a different decision — it would stop the whole of `list()` over one blank string.
- **[Y-121](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — **renumbered from Y-120** on 2026-08-03, two parallel changes having reached for the same free number. Only the workspaces table becomes labelled blocks below `48rem`; the other four tables still overflow, which they always have.

---

## 4. Research index

| # | Topic | File | Status | Key question it must answer |
| --- | --- | --- | --- | --- |
| R1 | Tailscale / Headscale inventory | [01](docs/research/01-tailscale-inventory.md) | ✅ done | Does Tailscale give live CPU/RAM/battery, or must Yantra ship its own agent? → **Zero telemetry. Agent required.** Verified absent from `ipnstate.PeerStatus`, the full 235 KB API v2 OpenAPI spec, *and* `tailscale metrics`. |
| R2 | tmux sessions & terminal streaming | [02](docs/research/02-tmux-sessions.md) | ✅ done | How do we stream a live terminal to a browser with least work? → **Daemon-side PTY + xterm.js over WebSocket**, with the PTY on the daemon side rather than a native addon in the web stack. Control mode deferred to v2 as the scaling transport; ttyd rejected as primary. |
| R3 | AI agent CLIs | [03](docs/research/03-ai-agent-clis.md) | ✅ done | Is "resume exactly where it left off" actually possible across all 5 agents? → **Not uniformly.** Only Claude Code & Gemini let you *preset* a session id; Codex & OpenCode force capture-after-launch; Aider has no session concept at all. |
| R4 | Workspace prior art | [04](docs/research/04-workspace-prior-art.md) | ✅ done | Why should Yantra exist instead of Coder / DevPod? → **Justified, but narrowly.** Coder's workspace is a Terraform build artefact; an always-on laptop it must never destroy is unrepresentable. Floor is coderd + Postgres + provisioners, sized for 1,000 users. |
| R5 | Scheduling | [05](docs/research/05-scheduling.md) | ✅ done | What is the 100-line version of a scheduler for 5 machines? → **Filter→Score→Bind, ~100 lines.** 6 hard filters, 6 weighted signals summing to 100, deterministic tie-break, never queue. Everything else from Nomad/K8s explicitly rejected. |
| R6 | Pi 5 feasibility: SSH, PTY, SQLite, hardware | [06](docs/research/06-runtime-feasibility.md) | ✅ done | What does the appliance actually need a systems language for? → **The hardware layer, nothing.** Pi 5 device-tree overlays hand the encoder and the LED strip to userspace as an evdev node and a char device; the kernel and RP1's PIO are the real-time layer. |
| R7 | SSH exec over the system binary | [07](docs/research/07-ssh-transport.md) | ✅ done | How do you run one command over `ssh` and learn what actually happened? → **You cannot, unaided.** `ssh` reports a signal-killed command as 255 with empty stderr — `clientloop.c` has no `exit-signal` branch — so signal death, remote 255 and a dropped multiplexed connection are indistinguishable. Needs a sentinel. Separate argv buys nothing: `ssh` joins arguments and feeds the remote *login shell*. |
| R8 | React + the compiler | [08](docs/research/08-react-and-the-compiler.md) | ✅ done | What does "latest React" actually mean today, and how is the compiler wired? → **19.2.8 + `babel-plugin-react-compiler` 1.0.0, and the config every guide shows is dead** — `react({ babel })` was removed in plugin-react 6. No SWC path, no Babel-free path. **Compiler bail-outs are silent** and react.dev's claim that an ESLint error implies a skip is false, measured. |
| R9 | Component libraries | [09](docs/research/09-component-libraries.md) | ✅ done | Which choice keeps the look swappable when the design system arrives? → **shadcn in CSS-variable mode, and barely a dependency** — its table/alert/card/skeleton/empty/spinner are `dependencies: null`, identical across four presets. `oat.ink` is real (Oat UI, 10 KB, zero deps) and loses only because it is not React. |

---

## 5. Decisions

Architecture decisions live in [`docs/adr/`](docs/adr/). Index:

| ADR | Decision | Date | Status |
| --- | --- | --- | --- |
| [0001](docs/adr/0001-record-architecture-decisions.md) | Record architecture decisions | 2026-07-28 | accepted |
| [0002](docs/adr/0002-project-name.md) | Project is named **Yantra (यन्त्र)** | 2026-07-28 | accepted |
| 0003 | *withdrawn before it governed any code; the number is not reused* | 2026-07-28 | — |
| [0004](docs/adr/0004-rust-for-the-daemon.md) | **Rust for the daemon, CLI and agent; TypeScript for the web UI** | 2026-07-28 | accepted |
| [0005](docs/adr/0005-core-logic-in-a-library-crate.md) | Orchestration logic lives in a **library crate**; binaries stay thin | 2026-07-29 | accepted |
| [0006](docs/adr/0006-ssh-exec-transport.md) | The SSH exec primitive: **system `ssh` + `ControlMaster`**, sentinel trailer, base64 payload | 2026-07-29 | accepted, amended by [0008](docs/adr/0008-withdraw-the-stdin-watchdog.md) |
| [0007](docs/adr/0007-workspace-schema-v1.md) | Workspace schema v1 in TOML | 2026-07-29 | accepted, amended by [0010](docs/adr/0010-drop-branch-from-the-workspace-schema.md) |
| [0008](docs/adr/0008-withdraw-the-stdin-watchdog.md) | The stdin-EOF watchdog is withdrawn | 2026-07-29 | accepted |
| [0009](docs/adr/0009-machine-names-are-ssh-destinations.md) | A workspace's `machine` is an **ssh destination, not a Yantra identifier** — the Tailscale inventory observes, never resolves | 2026-07-30 | accepted |
| [0010](docs/adr/0010-drop-branch-from-the-workspace-schema.md) | **`branch` is dropped** — it was parsed and never acted on; branch selection returns in M3 as worktrees, not checkouts | 2026-07-30 | accepted |
| [0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md) | **Claude Code runs as a TUI in tmux; the transcript JSONL is the log** — no headless path in M3. Closes Q7/Y-024 | 2026-07-30 | accepted |
| [0012](docs/adr/0012-the-cli-and-the-daemon-are-two-callers-of-one-library.md) | **The CLI and the daemon are two callers of one library** — the CLI keeps calling `yantra_core` in-process | 2026-07-31 | accepted |
| [0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md) | **React with the compiler for the web UI**, and a token seam so the design system stays swappable | 2026-08-01 | accepted |
| [0015](docs/adr/0015-resume-forks-the-conversation.md) | **`resume` forks the conversation rather than resuming a session id** — `--continue --fork-session --session-id <uuid>`, measured on 2.1.220; extends [0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md)'s *Launch* bullet | 2026-08-02 | accepted |
| [0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md) | **The dashboard writes, and Tailscale identity authorises it** — a write is allowed only from a node owned by this daemon's own user and carrying no tags, resolved **live** rather than from the 30 s snapshot, and refused when `tailscale` cannot answer. **It rejects nothing on this tailnet today** — six nodes, one user, no tags, measured — and says so; the value is that a shared node, a tagged CI runner or a second invited user each arrive through an admin console that this repository never sees. | 2026-08-02 | accepted 2026-08-03 |
| [0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) | **The heartbeat carries only what placement scores** — seven fields, identity from the source address | 2026-07-31 | accepted 2026-08-03 |
| [0017](docs/adr/0017-the-forwarded-address-is-the-caller-when-the-hop-is-ours.md) | **The forwarded address is the caller, when the hop is ours** — the caller's address is the TCP peer, unless that peer is one of the daemon's own bind addresses, in which case it is `X-Forwarded-For`. **Amends [0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md)**'s decisions 2 and 4 in how the address is *obtained*; the predicate, the live lookup and the closed failure are untouched. Measured against `tailscale` 1.98.9: `serve` **replaces** the header with the connection's real source, and sets **no** `Tailscale-User-*` header at all for a tagged node — so the address is the only channel a tagged caller announces itself through. | 2026-08-03 | **proposed** |
| [0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) | **On macOS the tmux server carries the login session, and Yantra gains no second transport** — the Mac's tmux server is started by the user's login session, so the panes it forks are already in `gui/<uid>`; ssh keeps its monopoly and `yantra-agent` stays a reporter. Proposes to close Y-122 and R-21 | 2026-08-03 | ⬜ **proposed — the owner's to accept** |

**Still to write.** Numbers are assigned when an ADR is written, never reserved — this list had
drifted a full four rows out of step with `docs/adr/` before it was rebuilt on 2026-07-30, because
planned decisions were holding numbers that written ones then took.

| Decision | Blocked on |
| --- | --- |
| ~~Machine telemetry~~ | ✅ **written 2026-07-31 as [ADR-0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md)** and **accepted by the owner 2026-08-03** (Y-103). Nothing is left to write here. |

---

## 6. Open questions

| # | Question | Blocks | Owner |
| --- | --- | --- | --- |
| ~~Q1~~ | ~~Why not just run Coder / DevPod?~~ | — | ✅ **CLOSED 2026-07-31: signed off by the owner — "Coder is not useful for my case."** The kill criterion is cleared; Y-021 done. |
| ↳ | **R4's answer:** Coder models a workspace as a *Terraform build artefact* — `start`/`stop`/`delete` are `terraform apply` transitions. A machine that already exists, is always on, and must **never be destroyed** (my MacBook) is unrepresentable in that model. Its floor is coderd + Postgres 13+ + provisioners, with the smallest validated architecture sized for 1,000 users. Yantra's niche: **pre-existing, personally-owned, heterogeneous machines**. | | |
| Q2 | Which machines actually exist today, with what specs and OS? | M2 | biswa<br>**Partly answered by measurement, 2026-08-02, and left open because the specs half is still missing.** `tailscale status --json` reports **six nodes, one user, no tags**: `cachyos-g14` and `bishwajeets-macbook-pro` (both online and both running agents), an **iPhone and an iPad** — which is what makes M5's acceptance test physically possible rather than aspirational — and two offline `laptop-*` nodes that are the first concrete evidence bearing on [Q4](#6-open-questions). What is still owed here is specs and OS per machine, which Tailscale does not carry (R1). |
| Q3 | Is a per-machine Yantra agent required, or can we get by with SSH polling? | ADR-0007 | ✅ **CLOSED. Push-heartbeat agent, 10s interval, stale at 30s.** R1: Tailscale exposes zero telemetry, so an agent is required. R5 then killed my SSH-poll preference outright — **SSH cannot see a sleeping laptop**, and a scheduler whose main job is "is this machine usable right now" cannot be blind to sleep. node_exporter/Glances also rejected. |
| Q4 | Windows: first-class, or WSL2-only? | M2 scope | 🟡 **deliberately left OPEN.** Owner declined to commit to WSL2 (2026-07-28). Deferred to M2, when real remote machines first matter — it does not block M1.<br>**Binding constraint meanwhile: write no code that assumes either answer.** The transport trait must not hard-code POSIX-isms in its signature, and nothing may assume `tmux` or `ControlMaster` exist on a target. Windows genuinely lacks all three of tmux, `ControlMaster` and Tailscale-SSH-as-server (R-7), so whatever is decided, it is a second path — the job now is to keep adding that path cheap rather than to pick it early. <br>**Y-022 narrows it:** the tailnet's only Windows node is the second boot of a laptop that already runs Linux, so supporting Linux-only costs **zero machines**. Meanwhile macOS is one of just two online hosts, making it the more urgent portability question. Also: **Tailscale SSH can never serve Windows** — its server component is Linux and open-source-macOS only — so a Windows target needs OpenSSH-for-Windows and has no tmux, two separate problems rather than one. |
| ~~Q5~~ | ~~Secrets: reference-only or does Yantra hold them?~~ | — | ✅ **CLOSED: reference-only, always.** Workspaces store a pointer (`op://…`, `pass show …`, a sops path); Yantra resolves it at launch, hands it to the process, and never writes it to SQLite, logs, the API, or a terminal stream. Holding secrets would mean earning the right to — encryption at rest, key management, stream redaction, audit. That is a security product, not a workspace orchestrator. |
| ~~Q6~~ | ~~Open-source-for-others or personal-first?~~ | — | ✅ **CLOSED 2026-07-31: personal-first. Open source for others later.** So no plugin architecture, no multi-user model, no configuration surface added for a hypothetical second user — one fleet, one owner, and the second user is a later problem that a working tool earns the right to have. The repo stays public; that is a licence and a habit, not a promise of extensibility. **Binds M4 directly:** the web UI is a dashboard for its owner, not a product — single-tenant, no auth beyond Tailscale, no theming, no settings screen. |
| ~~Q7~~ | ~~Is the agent control path **TUI-in-tmux** or **headless**?~~ | — | ✅ **CLOSED 2026-07-30: TUI in tmux, one path** — [ADR-0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md). R3's "all five are safer driven headless" is knowingly overruled on evidence R3 did not have: two of its three hazards belong to agents Yantra does not ship, and the third (#63545) did not reproduce. Headless leaves nothing to attach to, which is the one thing Yantra is for. The transcript JSONL is the log, `claude agents --json` is the status source, and `claude auth status` gates the launch so I-44 cannot produce a healthy-looking useless session. |
| Q8 | Do we adopt **server mode** where it exists (OpenCode `serve` HTTP+SSE, Codex `app-server daemon`)? Strictly better than tmux-wrapping, but adds a second transport. | ADR-0006 | biswa |
| Q11 | **Should session-affinity be a score term at all?** R5 raises it against itself: the live-session short-circuit already delivers idempotent `yantra up X`, so a 20-point affinity weight may be re-solving a solved problem. Prior: **drop it**, and let the short-circuit do the work. | M10 scoring weights | biswa<br>**Moved to M10 with placement, 2026-08-02, and it no longer blocks anything.** The prior stands: drop it, and let the live-session short-circuit do the work. |
| Q10 | **Wake-on-LAN**: R1 says Tailscale never exposes MACs and has no `wake` command — WoL needs the Pi 5 as an always-on **L2 relay** plus a hand-maintained MAC table. Accept that manual table, or drop WoL from the scheduler until the appliance exists (M7)? Prior: **drop until M7**; a scheduler that can't wake a machine simply won't place work there. | M7 | ✅ **ANSWERED 2026-08-02 — defer.** The owner confirmed there is no always-on machine on the MacBook's LAN, so the relay this needs does not exist. Yantra will not ship a wake button that cannot wake anything (R-23, Y-115). Note for M7: research 01 §6 warns the pattern wakes *powered-off* machines but often **not S3 sleep**, which is exactly a closed laptop — so this is unproven, not merely deferred. |
| ~~Q9~~ | ~~Ship Aider or drop it?~~ | — | ✅ **CLOSED: dropped from v1.** No session id, no resume, no JSON, no MCP, ~1 release in 6 months. Yantra's core promise is "continue where you left off"; Aider has no session to continue, so supporting it would mean faking the product's central feature. Revisit only if it grows a session model. |
| ~~Q13~~ | ~~Does the CLI start talking HTTP to the daemon?~~ | — | ✅ **CLOSED 2026-07-31: (a), two callers of one library** — [ADR-0012](docs/adr/0012-the-cli-and-the-daemon-are-two-callers-of-one-library.md). The CLI keeps calling `yantra_core` in-process and keeps working with no daemon running; `yantrad` becomes a second caller of the same functions. (b) would have made the one working interface — four milestones of verified behaviour — depend on the fifteen-line skeleton, in the milestone that first proves the skeleton can serve a page, and it is the only one of the two that is hard to reverse. The ADR records what would justify revisiting: a client that is not on the operator's machine (M6/M8), state that must not be derived twice, or a placement decision the CLI must also see (M5). |
| ~~Q14~~ | **What is the web UI built with?** → **Answered 2026-08-01: React, latest, with the React Compiler**, built with Vite, components from shadcn/ui in CSS-variable mode, linted by oxlint. Recorded as [ADR-0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md), because M6's browser terminal builds on it. **The deciding constraint was not the framework but the seam**: a design system is arriving from elsewhere, so the question was which choice welds the look in place. [R9](docs/research/09-component-libraries.md) settled it by pulling the component source — for this page shadcn's tables are **dependency-free plain markup**, identical across four style presets, so the code is what we would have hand-written anyway and the only thing a re-skin touches is `index.css`. **`cssVariables` is a one-way door set at init.** [R8](docs/research/08-react-and-the-compiler.md) measured the toolchain rather than reading it, and found react.dev wrong on one point that matters: an ESLint error does **not** imply the compiler skipped a component, and bail-outs are silent — a green build proves nothing about whether optimisation happened. | Y-072 | biswa ✅ |
| Q12 | **Is R-1 actually retired, or only retired on Linux?** Issue #63545 is a **macOS + tmux** report; Y-023's spike ran on `cachyos-g14`, which is Linux, because the MacBook was unreachable until Y-049 closed on 2026-07-30. The spike's evidence is real (18,427 bytes written while fully detached on 2.1.220) and also argues against the open Linux issue #70632 — but R-1, Q7 and the Claude Code integration ADR were all retired or unblocked on Linux-only evidence, for a bug filed against macOS. Third-party support exists (open issue #79188, macOS 2.1.215, found plain tmux persists correctly) but that is someone else's machine, which §B3 says not to accept. Prior: **re-run the Y-023 spike against the MacBook before M3 builds on it** — cheap now that the machine is reachable — and make `yantra logs` assert the transcript mtime is *advancing*, not merely that the file exists, because #70632's failure mode looks healthy until you tail it. | M3, the Claude Code integration ADR | claude |
| ↳ | **Re-run on 2026-07-30. It did not get as far as R-1, because a prior problem stops it: on macOS an agent launched over SSH is not authenticated (I-44).** Claude Code 2.1.220 booted in a fully detached tmux session on the MacBook, took a prompt, and answered `Not logged in · Please run /login` — its token is in the login keychain, which the `Background` launchd domain an ssh session gets cannot read. So R-1 stays **untested on macOS**, and the transcript question is now behind Y-059 rather than beside it. Two things were learned anyway: the transcript file **is** created and written while fully detached on macOS (57,841 bytes, `session_attached=0` throughout), so the file-write path is not the problem; and Q12's own suspicion was right in kind — Linux evidence did not transfer — while wrong about which mechanism would break. | | |

---

## 7. Risk register

Risks that can invalidate a design, ordered by damage. A risk is retired only by evidence, not by optimism.

| # | Risk | Impact if real | Status | Retire by |
| --- | --- | --- | --- | --- |
| ~~R-1~~ | ~~Claude Code #63545: detached tmux stops writing transcript JSONL~~ | — | 🟢 **RETIRED by Y-023** — did not reproduce on 2.1.220; transcript written while detached within 5 s | — |
| R-2 | Gemini CLI self-terminates ~5s after TTY loss, **exit code 0**. | Silent, successful-looking death of an automated session. Exit code 0 means naive health checks report success. | 🟡 known, mitigable | Set `SANDBOX` env, or forbid non-tmux launches |
| R-3 | Agent CLIs churn fast — OpenCode ships hourly builds with channel-dependent DB filenames; Codex removed three documented flags this cycle. | Yantra's agent plugins break without any change on our side. | 🟡 accepted | Pin agent versions; treat R3 note as perishable |
| R-5 | Coder/DevPod may already solve this well enough for a personal setup. | Project's justification. | 🟢 **retired by R4** — Coder cannot represent a never-destroy, always-on personal machine. Sign-off still open (Y-021). | — |
| R-6 | **Scope creep into provisioning.** The gravitational pull from "adopt a machine" toward "create a machine" is strong and would make Yantra a worse Coder. | Project loses its only defensible niche. | 🟡 permanent | Written as a non-goal in [§1](#1-guardrails-read-before-writing-any-code) and CONTRIBUTING.md — **not** in an ADR, despite what this cell said until Y-080; re-check at every milestone |
| R-7 | **Windows breaks every standardisation.** No tmux; Windows OpenSSH lacks `ControlMaster`; and per R1, **Tailscale SSH cannot act as a server on Windows** — so one uniform SSH mechanism across the fleet is impossible. | Transport + session design silently assumes POSIX; a second code path appears late in M2. | 🟡 open | Q4 decision — declare Windows = WSL2 for M1–M3 |
| **R-22** | **The bind address is the entire security model.** Q6 removed auth, so a listener on the wrong interface is an unauthenticated read API over the fleet's shape — machine names, workspace names, repo paths. | Everything `yantrad` can read, readable by anything that can reach the port. | 🟢 **mitigated 2026-07-31 by Y-069, and measured** — but **[ADR-0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) widens what it covers**: a heartbeat `POST` is the first *write* into the daemon, so "read-only and unauthenticated" becomes "writable and unauthenticated". The retire condition is unchanged; the blast radius is not. Address comes from Tailscale rather than configuration; no flag can override it; the daemon refuses to start without one. Confirmed on the real tailnet: `ss -ltnp` shows the two tailnet addresses and **no `0.0.0.0`**, and `curl 127.0.0.1:7717` is refused. | ~~A test that asserts the daemon refuses~~ — **shipped**: `it_refuses_to_start_when_this_machine_holds_no_address`. Stays amber-to-green rather than retired while the API surface is still growing.<br>**Blast radius restated 2026-08-02 (Y-108), because the daemon is now writable.** The boundary is unchanged and so is its retire condition; what changed is what sits behind it. Anything that reaches the tailnet can now write a heartbeat — and the tailnet is not a set of trusted servers: it includes a phone and a tablet, and R1 found **two of five peers holding expired keys while still in the netmap**. A forged beat can make a machine look free when it is not, or overwrite the real agent's row by racing it. **What it cannot do is escalate**: the payload is data for a score and never a path, name, command or filename, so nothing in it reaches the layer where [ADR-0006](docs/adr/0006-ssh-exec-transport.md) turns a string into a remote shell command. The worst outcome is a session placed somewhere that then fails to start — a denial of usefulness, not an execution primitive. Three things bound it, in order of what they cost: identity from the **source address**, so a body cannot claim to be another machine; a **4 KB** limit and strict types with unknown keys rejected; and a write to **memory**, so a flood costs CPU and cannot fill a disk. **Still not done, deliberately**: no shared secret, no per-agent token, no mTLS, no request signing — a pre-shared key across five machines is a secret Yantra would have to store, and §B4 says it never stores secrets. The trigger to revisit is a node on the tailnet that is not the owner's, which is a Q6 change before it is a technical one. |
| **R-23** | **A cached dashboard tells confident lies.** Every failure mode of the read model looks like working software: a stale session list, a machine silently dropped from a snapshot, an empty page that actually means "the first refresh has not run". | Worse than no dashboard — a page that is wrong in a way nobody can see is trusted anyway. | 🟡 **new with M4.** Mitigated by the three rules in [the plan](docs/plans/m4-web-ui.md) §5.2, each a *display* requirement and not merely a data one. I-47 is the same mistake one layer down. | Age shown on every reading; unreachable rendered as unreachable; `Option` never defaulted to empty. |
| **R-24** | **The JS toolchain leaks into the Rust build.** Starts as a convenient `build.rs`, ends with `cargo clippy` and the `aarch64-unknown-linux-musl` cross-build needing npm. | Every CI job and the appliance build grow a dependency on a toolchain none of them need. | 🟡 **new with M4.** Mitigated by embedding behind a cargo feature that is off by default, and by a separate CI job for the UI. | `cargo build` green on a machine with no Node installed. |
| **R-21** | **macOS agents cannot authenticate over SSH.** Claude Code's account token lives in the login keychain; an ssh session runs in launchd's `Background` domain and cannot read it (**I-44**). The failure is silent at the keychain layer and only visible as *not logged in* in the agent's own TUI. | M3's whole demo — `yantra up demo --agent claude` — produces a running, healthy-looking, **useless** session on macOS, which is one of the two online machines in this fleet. Everything downstream (`logs`, status) reports success. | 🔴 **open, new 2026-07-30** — verified end to end, not inferred. **A proposal now exists: [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md), `proposed`, the owner's to accept (Y-122). Its untested premise was measured at the Mac's keyboard on 2026-08-05 and holds** — a window forked by a tmux server started from Terminal.app reads the keychain and answers `loggedIn: true`, where plain ssh on the same machine, same `$HOME`, does not. | **Y-059.** Candidates: launch the agent inside a tmux server started from the GUI session (measured on 2026-08-05 and it works — Y-122), a launchd LaunchAgent in `gui/<uid>` (what R-12's per-machine agent already has to be), or a credential *reference* resolved at launch per Q5. `security unlock-keychain` is not a candidate — it needs a password. **ADR-0018 picks the first and prices the second out**; [research 10](docs/research/10-claude-code-credentials-on-macos.md) killed the third, every credential path it found being a secret *value* §B4 refuses. |
| R-8 | **`Online: true` means "the control plane sees it", not "you can reach it".** Also `Expired` nodes need surfacing. | Scheduler happily places work on an unreachable machine; `yantra up` hangs instead of failing fast. | 🟡 known, mitigable | Probe with `tailscale ping` before placement; treat `Online` as a hint only |
| ~~R-19~~ | ~~**Cross-compiling Rust to macOS from Linux is genuinely hard** (needs the Apple SDK via osxcross). The per-machine push-agent (R-12) must run on macOS and Windows.~~ | Build tooling friction for the agent, not the daemon. Worst case, the appliance cannot build the Mac agent by itself. | 🟢 **RETIRED 2026-07-31 by Y-037**, on its own stated condition. Both Apple triples build on a **macOS runner** with Apple's own toolchain — one runner carries both slices — so nothing is cross-compiled to Apple from Linux and osxcross never enters the picture. Green on the first attempt, 155 KB and 161 KB archives. The risk was real; it is simply not on Yantra's path. | Build on native runners in CI rather than cross-compiling. `aarch64-unknown-linux-musl` for the appliance is unaffected and stays trivial. |
| R-20 | **Rust punishes design churn, and M0–M1 is exactly where the design is still unknown.** | Slower first month; a wrong early abstraction is more expensive to unpick than in TS. | 🟡 accepted knowingly | ADR-0004 records this as the consciously-paid cost. Mitigation: walking skeleton first, narrow traits, resist abstraction until the third use. |
| R-12 | **The push-agent has an install-and-update story on every machine.** Q3's answer converts Yantra from "pure orchestrator, zero footprint" into "ships software to five heterogeneous machines" — including macOS launchd, Windows service, and Linux systemd. | Real, permanent scope increase. Also weakens the "we only orchestrate" identity if it grows. | 🟡 accepted, must stay tiny | Keep the agent to heartbeat-only; no logic, no versioned protocol beyond a JSON blob |
| R-13 | **macOS and Windows telemetry commands are doc-sourced only.** R5 verified the Linux commands locally; the other two OSes were not run on real hardware. | The scheduler mis-scores exactly the machines it was built to choose between. | 🟡 unverified | Verify during Y-022, on the real Mac and Windows box |
| R-10 | **Hostname collisions are already real** — R1 found HostName colliding **twice in your existing 5-node tailnet**. | Keying anything on hostname corrupts inventory from day one. | 🟢 avoidable by design | Key on `Peer.ID`; treat HostName as a display label only |

---

## 8. Session log

**Moved to [docs/session-log.md](docs/session-log.md)** on 2026-07-31 (Y-068) — append there, not
here. It was 40 KB, a third of this file, and it is read approximately never, while this file is read
first by everyone. M0 and M1's entries are further back, in
[docs/archive/m0-m1.md](docs/archive/m0-m1.md).
