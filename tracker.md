# Yantra — Tracker

**This file is the single source of truth for project state.**
Read it first. Update it last. If it disagrees with your memory, the file wins.

| | |
| --- | --- |
| **Project** | Yantra (यन्त्र) — personal developer control plane |
| **Current milestone** | `M13 — The dashboard you can work in` (🟡 wip since 2026-08-11; M6 🟡 wip since 2026-08-04) |
| **Status** | ✅ **M0–M4 closed. M5's buildable work is finished.** The acceptance test runs end to end from a phone, through the browser over HTTPS against the live fleet: create a workspace, start it, Claude Code really running in tmux in the named repo, start again and it attaches rather than duplicating, resume, stop. **The Mac does it where a person has logged in at it, and nowhere else** — the token is in the login keychain and ssh lands in launchd's `Background` domain (I-44), which [research 10](docs/research/10-claude-code-credentials-on-macos.md) established is architecture and not a setting. [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) is **accepted** (Y-122), [Y-151](docs/archive/m6-m7.md) built its §1 and §5, and on **2026-08-05 both were measured on that machine** against a tmux server started at its own keyboard: the gate found a credential in that server where ssh in the same run found none, and [Y-139](docs/archive/m6-m7.md) then had Claude Code answer in it while the session stayed detached — which retires **R-1** on macOS and answers **Q12**. **[R-21](#7-risk-register) stays open regardless**: a Mac nobody has logged in at still has no such server, and ADR-0018 §7's launchd job that would put one there is M7's. **What is open is in [§3](#3-task-board)**: Y-117, deferred to M10. How M4 and M5 got here, and this cell's own rolling narrative up to 2026-08-03, are in [docs/archive/m4-m5.md](docs/archive/m4-m5.md). |
| **Runtime** | **Rust** (daemon, CLI, per-machine agent) + **TypeScript** (web UI only) — see [ADR-0004](docs/adr/0004-rust-for-the-daemon.md) |
| **Top risk** | **R-7** — Windows is a second path with no tmux and no Tailscale SSH server, and Q4 is still deliberately open. *R-21 (macOS agents cannot authenticate over ssh, I-44) is **downgraded**: it is real and unfixed, but Y-059 found a machine that works without touching it, so it costs a target rather than the milestone. R-1 refuted on Linux and, since Y-139 on 2026-08-05, on macOS too; R-2 is now handled in code — see I-47 and `status.rs`.* |
| **Last updated** | 2026-09-03 |
| **Updated by** | Claude (session: Y-314…Y-317 wired the attention band and the session kill; **Y-323 pruned 73 closed rows** into [m6-m7.md](docs/archive/m6-m7.md) and [m11-m13.md](docs/archive/m11-m13.md), taking this file from 342 kB to 114 kB) |

---

## 0. How to use this file

**Rules for every contributor, human or agent:**

1. **Before starting work** — read §2 (milestones) and §3 (task board). Pick a task that is `todo` and whose dependencies are `done`.
2. **When starting** — set the task to `doing` and put your name in Owner.
3. **When finishing** — set to `done`, add the artifact path in Notes, and append a line to §8 (Session log).
4. **When you learn something that changes the plan** — do NOT silently change course. Add an entry to §5 (Decisions) or §6 (Open questions) and flag it.
5. **Never delete a task.** Move it to `dropped` with a one-line reason. The graveyard is evidence.
6. **One task = one reviewable change.** If a task cannot be finished in a single sitting, split it.

**Status vocabulary:** `todo` · `doing` · `blocked` · `review` · `done` · `dropped`

### What the prefixes mean

Added 2026-08-02, because this file opens by calling itself the source of truth and then uses seven
undefined prefixes. **Every identifier in the repo is one of these:**

| Prefix | Is a | Lives in | Example |
| --- | --- | --- | --- |
| `Y-123` | **Task.** A single reviewable change. | §3 below | Y-109 — show heartbeat age on the dashboard |
| `M0`–`M10` | **Milestone.** A stage of the project. | §2 | M5 — control from the phone |
| `ADR-0013` | **Architecture Decision Record.** A dated document saying *we decided X, and here is why* — written so the reasoning survives, not just the outcome. Immutable once accepted; if one is wrong you write a superseding one rather than editing it. | [`docs/adr/`](docs/adr/) | ADR-0013 — what a heartbeat carries |
| `Q4` | **Open question**, usually one only the owner can answer. | §6 | Q4 — Windows: first-class or WSL2-only? |
| `I-51` | **Invariant.** A rule learned by getting it wrong once; violating it produces a bug that looks like something else. | the **crate** trackers, §1b says which | I-51 — tmux re-quotes multi-word commands |
| `R-22` | **Risk.** Something that could go badly, tracked so it is not rediscovered. | §7 | R-22 — the bind address is the entire security model |
| `R1` *(no hyphen)* | **Research note.** Dated evidence gathered before deciding. | [`docs/research/`](docs/research/) | R1 — Tailscale inventory |

> **`R-1` and `R1` are different things** and always have been — risk one versus research note one,
> told apart by a hyphen. Nothing else in this scheme collides. Read `R-` as *risk* and a bare `R`
> as *research*; when writing new prose, prefer naming the research note by file rather than by
> number.

---

## 1. Guardrails (read before writing any code)

These exist because this project's failure mode is obvious: it is a nine-phase mega-vision, and
mega-visions die in Phase 4 with a beautiful architecture and nothing that runs.

- **Walking skeleton first.** M1 is one thin end-to-end slice, not a layer. No layer gets built to completion before the whole path works badly.
- **One machine before many.** Everything must work against a single machine before it works against the tailnet.
- **Localhost is not a special case.** Every machine — including the one the daemon runs on — is reached over SSH. One code path, forever. The tempting shortcut (spawn `tmux` directly when local) buys a little convenience now and costs two divergent code paths for the life of the project, where the well-tested one is the one that matters least. Decided 2026-07-28; goes into ADR-0005.
- **Orchestrate, don't reinvent.** If a task's description starts to look like "implement SSH/terminal multiplexer/scheduler", stop and re-read the brainstorm.
- **Provisioning is a permanent non-goal.** Yantra adopts machines that already exist; it never creates, images, or destroys them. This is precisely the line that separates it from Coder — and R4's verdict is blunt: *if Yantra ever grows a provisioning layer it becomes a worse Coder and should be deleted.* **This guardrail is where that is written down**, together with [CONTRIBUTING.md](CONTRIBUTING.md); no ADR carries it, and Y-080 found three places that claimed one did.
- **One agent first. Do not build the universal adapter.** The vision doc says every agent should behave identically; that is the *destination*, not the starting point. Yantra supports **Claude Code only** in M3 — the one actually installed, with the strongest resume story (`--session-id` presets the UUID, verified in Y-023). A second agent gets added when a second agent is genuinely wanted, and the shared interface is extracted **from two working implementations**, never guessed ahead of one. Owner's framing, 2026-07-28: *"use the cli that we want, not to join everything. If needed we can do it later."*
- **No UI until the CLI is good.** The CLI is the API's first client and its honesty check.
- **No hardware until the software is boring.** Phases 7–9 are a reward, not a shortcut.
- **Test hard.** Every orchestration primitive gets an integration test against a real sshd + real tmux. Mocks lie about SSH. **The fixture is a disposable podman container**, not the host — a separate filesystem, user and network hop make it a more honest stand-in for a remote machine than `ssh localhost`, and it changes nothing about the host's security posture.
- **Rust for the daemon, TypeScript for the web UI** — [ADR-0004](docs/adr/0004-rust-for-the-daemon.md). Chosen for quality and appliance fit over iteration speed, with that trade made explicitly. Rust punishes design churn, so **think before writing**: this is the milestone where the shape is still unknown.
- **Orchestration lives in `yantra-core`, a library crate** — [ADR-0005](docs/adr/0005-core-logic-in-a-library-crate.md). The binaries are thin. The library never prints and never exits, which forces a typed error from the first fallible call instead of an M2 cleanup; M2's daemon changes *where* the code is called from, not *what* it does.
- **Keep SSH, tmux, telemetry and hardware behind narrow traits.** Not to enable a runtime swap any more — to keep the four seams that touch an unreliable outside world fakeable in tests.

---

## 1b. Invariants

Non-obvious rules that research proved the hard way. Violating one produces a bug that looks like
something else entirely — which is why they are worth more than the code they constrain.

**They live with the crate they bind**, because that is the file open on the screen of whoever is
about to break one. Moved there on 2026-07-31 (Y-068).

| Where | What, and how load-bearing |
| --- | --- |
| [crates/yantra-core/tracker.md](crates/yantra-core/tracker.md) | I-1…I-6, I-8, I-20…I-30, I-33…I-37, I-39…I-49, I-51…I-57 — ssh, tmux, agent, terminfo, logs, inventory, workspace. Every one earned by execution against a real machine. [`CLAUDE.md`](crates/yantra-core/CLAUDE.md) maps them to modules. |
| [crates/yantrad/tracker.md](crates/yantrad/tracker.md) | I-7, I-10…I-14, I-18, I-58…I-60 — SQLite, scheduling, PTY, the bind address the daemon never looks at twice, the notification a restart swallows, and the dashboard directory that can vanish after the startup check has passed. **Only I-60 is exercised by shipped code**, and two were measured against runtimes ADR-0004 removed. |
| [crates/yantra-agent/tracker.md](crates/yantra-agent/tracker.md) | I-9, I-19 — telemetry and hardware. Neither exercised by shipped code. |
| [crates/yantra/tracker.md](crates/yantra/tracker.md) | None, and that is the finding: the CLI talks to nobody else's program, so it has nothing to be surprised by. Its contract is its exit codes. |

The four below bind no crate. They are about how this project *works* — what counts as evidence —
rather than what it talks to, so they stay here where the guardrails are.

| # | Invariant | Why | Src |
| --- | --- | --- | --- |
| I-31 **[D]** | **A test suite of instantaneous commands proves nothing about duration.** Any primitive that wraps a remote process needs at least one test with a command that takes seconds. | Y-041 shipped a watchdog that killed everything slower than ~200 ms and passed CI, because every command in its suite finished in under a millisecond. Found only when Y-042 ran `tmux`, which is merely slow enough to notice. | Y-042 |
| I-32 **[V]** | **A green CI run is not evidence unless it ran on the environment that failed.** GitHub rolls runner images out gradually, so a fix can go green five times on machines that were never broken. Re-run until the job lands on the failing image, and print the versions that matter so you can tell which one you got. | Y-048's fix passed five consecutive runs on image `20260720.247`, which had the working podman. Only the sixth landed on `20260726.254` and actually tested anything. Without the version printout, all six looked identical. | Y-048 |
| I-38 **[V]** | **A machine's own MagicDNS short name is not its tailnet address.** | `getent hosts cachyos-g14` returns `127.0.1.1`: `/etc/hosts` shadows MagicDNS for the local hostname. Self-dialling also never traverses the WireGuard path, so `tailscaled`'s port-22 interception is never reached and a self-directed `ssh` gets `Connection refused`. A workspace that names the local machine therefore fails looking like a network problem. | Y-049 |
| I-50 **[V]** | **A host *can* reach a normally-bound listener on its own tailnet address — it is the MagicDNS *name* that fails, not the address.** Anything dialling this machine must use the address. | Sharpens **I-38**, which generalised too far. I-38's "self-directed traffic never traverses the WireGuard path" is about `tailscaled` **intercepting** port 22 — interception needs that path. A real listener bound to the tailnet address needs no interception, because the address is on a local interface. Measured against a running `yantrad` on `cachyos-g14`: `curl` to its own tailnet address returned `ok`, while the same request to its own short name failed, `getent hosts` giving `127.0.1.1`. So the machine running the daemon is **not** a special case for a heartbeat, and ADR-0013 no longer has to assume it might be. | Y-020 |

### Retired

| # | Invariant | Why | Src |
| --- | --- | --- | --- |
| ~~I-15~~, ~~I-16~~, ~~I-17~~ | **Retired by [ADR-0004](docs/adr/0004-rust-for-the-daemon.md).** All three constrained a runtime Yantra does not use, and nothing replaced them. The numbers are not reused. | | — |

---

## 2. Milestones

Renumbered from the original 9-phase roadmap in [docs/vision.md](docs/vision.md) to front-load a working
end-to-end path. The original phases still map: M1 covers old Phases 1–3, M2 = Phase 5, M3 = Phase 4, etc.

| ID | Milestone | Definition of done (the demo that proves it) | Status |
| --- | --- | --- | --- |
| **M0** | Foundations | Research notes landed, ADRs written, repo skeleton + test harness runs green. | ✅ **done**, [archived](docs/archive/m0-m1.md) — 9 research notes, 4 ADRs, cargo workspace, CI + release pipelines, repo scaffolding. 29 commits. |
| **M1** | Walking skeleton | `yantra up demo` on my Linux box: reads one workspace file → opens tmux session → cd's to repo → runs startup cmd → prints attach hint. Idempotent (running twice attaches, doesn't duplicate). Integration-tested against a real sshd + tmux in a disposable podman container (§B3 — stricter than the `ssh localhost` this originally said). | ✅ **done**, [archived](docs/archive/m0-m1.md) — PRs #6–#11. `yantra_core::{workspace,ssh,tmux,up}` + the CLI, 4 ADRs (0005–0008), 8 invariants (I-24…I-31), 33 tests. |
| **M2** | Real machines | Same command targets a *remote* tailnet machine chosen by name. Machine inventory from Tailscale. `yantra ls machines` / `yantra ls sessions` work. | ✅ **done** 2026-07-30 — Y-050..Y-058, 9 tasks. `up` opens a session on the MacBook by name and attaches on the second run, as a committed test; `ls machines` and `ls sessions` both work. 10 invariants came out of it (I-34…I-43). Q4 (Windows) stays open by choice and blocks nothing. |
| **M3** | Agent orchestration | `yantra up demo --agent claude` launches Claude Code in the session; `yantra logs demo` tails its transcript; `yantra down demo` stops cleanly. **Claude Code only** — no plugin abstraction until a second agent is actually wanted. | ✅ **done** 2026-07-31 — Y-059..Y-065. All four commands verified end to end against a **real** Claude Code 2.1.220, not a stub: the agent launched under the id Yantra chose, `status` read it back from `claude agents --json`, `logs` rendered its transcript, and `down` produced `stopped cleanly (exit 143)`. Six invariants, I-44…I-49. |
| **M4** | Web UI | Read-only dashboard over the same HTTP API the CLI uses: machines, workspaces, sessions, live status. Served over Tailscale. | ✅ **done** 2026-08-02, [archived](docs/archive/m4-m5.md) — [docs/plans/m4-web-ui.md](docs/plans/m4-web-ui.md) then [m4-dashboard-next.md](docs/plans/m4-dashboard-next.md), Y-068..Y-102. `yantrad` serves the read model at `/api` and the dashboard draws its four sections, each stamping its own age. Q13 and Q14 both answered before code started. |
| **M5** | Control from the phone | Start, stop and resume a session **from the dashboard**, choosing the machine from a list. Served over HTTPS and installable as a PWA. | ✅ **buildable work done** 2026-08-03, [archived](docs/archive/m4-m5.md) — [docs/plans/m5-control-from-the-phone.md](docs/plans/m5-control-from-the-phone.md), Y-103..Y-121. The acceptance test runs from a phone over HTTPS against the live fleet; **both rows it was left waiting on have closed** — Y-118 in code, Y-122 in a decision — and the *on the Mac* clause is decided and written and not yet demonstrated: [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) says how, [Y-151](docs/archive/m6-m7.md) built §1 and §5, and what it waits on is one run against a GUI-login tmux server on that machine. **Re-scoped by the owner on 2026-08-02**: `Placement` moves to M10. Two machines, one of which is usually the right answer, does not need a scheduler — it needs a list to tap. Nothing built for the old scope is wasted: ADR-0013's seven fields are what a *person* reads when choosing a machine by hand, so the heartbeat's consumer changed and its payload did not. |
| **M6** | Terminal in browser | Live interactive terminal attached to a session from the web UI. | 🟡 wip — **planned 2026-08-04**, [docs/plans/m6-terminal-in-browser.md](docs/plans/m6-terminal-in-browser.md), Y-127..Y-132. The architecture was settled in advance by [R2](docs/research/02-tmux-sessions.md) §3 — daemon-side PTY + xterm.js over WebSocket, control mode deferred to v2, ttyd rejected — so planning measured the repo against that note rather than re-opening it. Two things it found: `attach.rs` already assembles the exact remote command a browser terminal has to run, and `Exec` cannot carry it, being one-shot with `RequestTTY=no` and no stdin. **Y-118 became a dependency** rather than a neighbour. The pty (Y-128), the socket (Y-129) and the browser (Y-130) are in, and **every layer is proved against something real except the join between them**: a real tmux for the pty, a real listener for the authoriser, a real socket and the real xterm.js for the page — and nothing yet that has carried a session's bytes end to end, which needs the fleet and a browser rather than another test. **Y-118 closed on 2026-08-05** ([ADR-0017](docs/adr/0017-the-forwarded-address-is-the-caller-when-the-hop-is-ours.md)), so the socket is now authorised through `tailscale serve` on 8443 as it already was on 7717, and the last dependency this milestone was waiting on is gone. **What the acceptance test waits on now is nobody's code.** It is a real browser on the phone, against a real daemon on this machine, against a real tmux session on a real machine of the fleet — the join no layer's test can stand in for, and it is the **owner's to run**. M6 is not done until it has been. **Y-132 added reconnect and no buffer with it**: tmux draws the pane for whichever client attaches, so the screen comes back from the far side and the browser's only job is to open another socket — which leaves the daemon holding nothing across a closed one.<br>**2026-08-06: the join was carried end to end, and not on the topology this milestone names.** Claude Code's TUI drew in a real Brave tab over `wss://…:8443` through `tailscale serve`, took typed input into its own prompt, and cleared that prompt on `^C` — so bytes crossed the pty, the ssh, the socket and the browser in both directions, at thirty rows, with the tmux status line drawn and no CRLF corruption. **The far side was this machine reaching itself** over an unprivileged `sshd`, not `bishwajeets-macbook-pro`, which is why the milestone **stays open**: the Mac is named in [the M6 plan](docs/plans/m6-terminal-in-browser.md) §1 for three specific reasons and every one of them is still untested — **I-34**, `tmux` absent from the PATH a non-interactive ssh gets there, and **I-36**/**I-43**, the two terminfo databases on one machine. **The Mac half is not merely unrun, it is unrunnable from here**: that machine has no tmux server, and [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) §1 has `up` refuse to start one rather than create a `Background` one, so this closes at that keyboard or not at all. Two things found on the way, neither of them code: a `web/dist` three days stale predates [Y-130](docs/archive/m6-m7.md) and serves the M5 dashboard with no button to open a terminal at all, and `YANTRA_WEB` pointing at a deleted worktree left the daemon answering `/` with an **empty-bodied 404** — the deep link's own status, so a browser drew nothing and the journal said nothing — because the refusal `docs/development.md` promises is made at start and a directory that vanishes afterwards was a case nothing checked. **The second of those is fixed and rowed as [Y-155](docs/archive/m6-m7.md)**, which is now a `503` naming the path, said once in the journal, and [I-60](crates/yantrad/tracker.md) written down; **the stale `web/dist` was checked and did not hold** — a rebuild from current `web/src` emitted `index-DaDVdyVL.js`, the content-hashed name already on disk, so the bundle was current and there is nothing to row. |
| **M7** | Appliance | Runs 24/7 on the Pi 5 / N100. Survives reboot. Notifications via ntfy. | 🟡 wip — **planned 2026-08-05**, [docs/plans/m7-appliance.md](docs/plans/m7-appliance.md). **The demo is hardware-blocked and the preparation is not**: no Pi 5 and no N100 is on this tailnet, so the reboot cannot be run and cannot be faked, while supervision, the install path, the notifier and R-24's enforcement can all be built against machines that exist. The plan proposes rows and questions and opens neither — that is the owner's, per [§0](#0-how-to-use-this-file). **The owner opened them on 2026-08-05**: Y-142…Y-150 are in [§3](#3-task-board) and Q15…Q19 in [§6](#6-open-questions). |
| **M8** | Hardware prototype | Display + rotary encoder + LEDs driving real workspace actions. **Simpler than planned:** R6 found `dtoverlay=rotary-encoder` (evdev, IRQ-decoded in kernel) and `dtoverlay=ws2812-pio` (`/dev/leds0`, RP1 PIO) — **no Rust, no microcontroller**. Panel stays a second **Rust** process for lifecycle isolation only. | ⬜ todo — **that "no Rust, no microcontroller" is conditional on the box being a Pi 5**, which [Q15](#6-open-questions) has not answered: both overlays are RP1's, and an N100 has neither. [Y-152](docs/archive/m6-m7.md) and [research 11](docs/research/11-appliance-hardware.md) also add the level-shifting constraint R6's table is silent on, and the first thing to close here is R6's own gap — nobody has checked `/dev/leds0`'s byte order against a real strip. |
| **M9** | Enclosure & PCB | University workshop build. | ⬜ todo |
| **M10** | Placement | `preferred` and `automatic` modes with an explainable decision record (`yantra why demo`). | ⬜ todo — **was M5 until 2026-08-02.** Deferred by the owner, not dropped: the telemetry it would score already arrives every 10 s, so what is missing is the scorer and the explanation, not the facts. **Q11** (is session-affinity a score term at all) belongs to this milestone and no longer blocks anything. |
| **M11** | The work inbox | The dashboard draws what needs attention — issues assigned to me, pull requests waiting on my review, unread notifications — each stamped with its own age, and `yantra ls attention` prints the same list. | ⬜ todo — opened 2026-08-10 by the owner. Promotes GitHub integration out of *Future Possibilities* ([`brainstorm.md:535`](docs/brainstorm.md)), which [R13](docs/research/13-dashboard-revamp-and-github.md) §6 said was the owner's call and nobody else's. **The daemon holds no token.** [R13](docs/research/13-dashboard-revamp-and-github.md) §2.2 row 1 is the mechanism: `gh` already holds an OAuth token in the keyring, so §B4 is met by construction rather than by policy — the same argument [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) §4 used for `claude`'s credential. Measured on `cachyos-g14` 2026-08-10, all four answering with no credential reaching Yantra: review-requested pull requests, assigned issues, **27** notifications, and private repositories. **Where this fails is the appliance, not the feature** — [Q20](#6-open-questions). |
| **M12** | Sessions you can act on, and what they cost | Every tmux session on the fleet is on the page — including the ones no workspace names — and can be attached to, closed or deleted. Each shows what it spent. | ⬜ todo — opened 2026-08-10 by the owner, **lifecycle first, history after**. Today `ls sessions` lists a session but only a workspace-backed one can be stopped, and **nothing anywhere deletes a workspace**: there is no `rm` in the CLI's thirteen verbs and no `DELETE` among the daemon's fifteen routes. Cost is nearer than it looks — Claude Code's transcript carries `usage` per message and [`logs.rs`](crates/yantra-core/src/logs.rs) already reads that file. It carries **no dollar figure**, which is what [Y-182](docs/archive/m11-m13.md) is about. |
| **M13** | The dashboard you can work in | `/` opens on what needs you and what is running, not on an inventory. Every state a surface can be in is drawn, including the ones it cannot answer. A phone reaches the fleet in under three screens and a workspace in one tap. | 🟡 wip — **opened 2026-08-11 by the owner**, and ten of its thirteen rows landed the same day, after [D3](docs/design/03-dashboard-surface.md) settled the surface in a 27-question interview. **Y-187…Y-199**, plus the four dashboard rows M12 opened and D3 redefined — [Y-174](#3-task-board), [Y-180](#3-task-board), [Y-183](#3-task-board), [Y-185](docs/archive/m11-m13.md) — each of which got *smaller*. D3's twenty-nine work units are thirteen rows because Y-200 upward is the landing page's and Y-187–Y-199 was what remained; the units stay in [D3](docs/design/03-dashboard-surface.md) §16 as the detail each row links to. **2026-09-03: nine more rows, and no more scope.** [D5](docs/design/05-workspace-page.md) detailed [Y-198](#3-task-board) and the owner minted **Y-305**…**Y-313** from it — the same split D3 took, where one row that pointed at a hundred lines of design became the work those lines describe. Y-198 stays as their parent and closes when they do. **2026-09-03, the same day: eight more.** [D6](docs/design/06-sessions-attention-spend.md) detailed [Y-174](#3-task-board), [Y-179](#3-task-board) and [Y-180](#3-task-board), and the owner minted **Y-314**…**Y-321** from it. Most of it is wiring — the daemon has served two of those readings since Y-173 and no file in the browser asks for either. **Pigment, type and motif are not in it** — the owner holds that direction, and [ADR-0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md) makes the eventual re-ground a change to `index.css` rather than to any of this. |

---

## 3. Task board

Closed tasks live in the archive — [M0 and M1](docs/archive/m0-m1.md),
[M2 and M3](docs/archive/m2-m3.md), [M4 and M5](docs/archive/m4-m5.md),
[M6 and M7](docs/archive/m6-m7.md), [M11 to M13](docs/archive/m11-m13.md). **This section is only
what is open**, plus one line per closed task that steered. A short section is the goal rather than a
sign of a slow week.

**Task numbers: `Y-0xx`/`Y-1xx` here, `Y-2xx` for the landing page.** The landing-page effort keeps
its own tracker on its own branch and took a number this file had already used **three times** —
Y-084, which it renumbered itself out of; Y-091, while a different Y-091 was merged here; and then
Y-100 and Y-101, taken while this file was allocating those same two numbers on the same day. Three
collisions from one source is a pattern rather than an accident, and the cost is not a merge
conflict: it is two rows in two files claiming the same identifier, so a commit message or a PR title
stops saying which piece of work it belongs to. **Y-200 upward is reserved for the landing page**,
and nothing in that range is ever allocated from this file. **Adopted 2026-08-02**: the landing rows
became Y-200..Y-207 and PR #80 merged renumbered.

**2026-08-11: `Y-1xx` ran out, so this file continues at `Y-3xx`.** M13 allocated Y-187..Y-199 and
Y-186 was already taken, which leaves nothing below the landing page's reservation. `Y-2xx` stays
the landing page's — it is at Y-208 and still allocating — so the next number here is **Y-300**. The
rule is unchanged in substance: one range per source, and neither ever reaches into the other.

### Open

**What is here is what is open.** Y-323 moved 73 closed rows to the archive on 2026-09-03; the
narrative that used to introduce them went with the rows it was about, into
[m6-m7.md](docs/archive/m6-m7.md). Two of its paragraphs stated rules rather than history and those
rules still bind: **Y-117 waits on M10**, and **this preamble does not say how many rows are open**,
because a count restated above a table that already carries it is the drift [Y-120](docs/archive/m6-m7.md)
deleted everywhere else.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-117 | `up --machine`, the override the picker needs | 🔵 deferred to M10 | — | — | **Found while building Y-112's routes, and it is a gap in M5's acceptance test rather than a refinement.** `up(name, term, agent)` takes no machine: the target comes from `workspace.machine`, a single required field, so *choose the Mac from a list* has nothing to send. [`docs/brainstorm.md`](docs/brainstorm.md)'s Scheduling Philosophy describes exactly this as **Manual** — *"I explicitly choose. Mac. Windows. Linux. Run there."* — and it is the first of its three modes, so the override is the vision's own starting point rather than an extra. **The CLI gets the flag first** ([`crates/yantrad/CLAUDE.md`](crates/yantrad/CLAUDE.md): anything the web UI can do must be expressible in `yantra`), then the route carries it in the body. **Deferred on 2026-08-02, the day after it was opened, because building it as scoped would break three verbs.** A session lives in tmux *on a machine*, and `down`, `resume`, `status` and `logs` all find it by reading `workspace.machine`. A **transient** override therefore places a session where nothing afterwards knows to look — `yantra up` on the Mac followed by `yantra down` searching the laptop, silently finding nothing, and reporting the absence as success (I-30). Three ways out, and the cheap two are wrong: persisting the override into the TOML makes a launch flag rewrite the operator's file, and searching every machine for the session needs the read model to survey the whole tailnet rather than *the machines your workspaces name*.<br>**What replaces it is smaller and already planned: the machine is chosen when the workspace is written, which is [Y-116](#3-task-board).** That keeps one source of truth, needs no new concept, and is what `brainstorm.md`'s UI Philosophy already describes — *the interface should generate them automatically*. Manual mode is satisfied by choosing the machine in the form rather than at the moment of launch.<br>**What brings it back** is the read model surveying the tailnet instead of the workspace list, which is the same change **Preferred** and **Automatic** modes need — so this belongs with them in M10, not ahead of them. |
| Y-143 | What a tagged enrolment does to `allowed()` | ⬜ todo | — | — | **A measurement, not a feature** — [the M7 plan](docs/plans/m7-appliance.md) §3.2. `owner()` is `Self.UserID` on the machine running the daemon and [ADR-0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md) authorises callers against it, so enrolling the appliance the standard headless way — with a tagged auth key — may make it refuse every write from the phone that installed it, on a box with no other way in. One ephemeral tagged node on this tailnet settles what `owner()` and `whois` then return, and M7 needs to know before the box arrives rather than after; if the answer is *refuses everything*, the install document has an owner action in it (Q17). **Not hardware-blocked.**<br>**2026-08-06: the enrolment is two owner actions rather than one, and the tailnet was read to find out.** `tailscale status --json` here returns **no `Tags` on any node, on this one or on any peer**, and a single `UserID` — `<id>`, `<owner>@github` — owning every machine in the netmap. So there is nothing tagged to measure against and **a key cannot apply a tag that the policy file does not define first**: the ACL needs a `tagOwners` entry (`"tag:yantra-appliance": ["autogroup:admin"]`) before Settings → Keys can mint against it, and the key itself wants **ephemeral, pre-approved and single-use** so the node deletes itself on disconnect and the credential cannot be replayed. Both are admin-console actions and neither is code, which is why this row is the owner's to unblock. **The measurement does not need the appliance or any hardware**: `tailscaled` in userspace mode in a disposable container (§B3) enrols with that key, and what is read off it is `Self.UserID` and `Self.Tags` there against `whois` from here. **The key is a secret and stays one** (§B4) — it reaches the container through the environment, never a repo file, never a workspace field, and the ephemeral flag bounds what a leak would be worth.<br>**2026-08-09: [Q17](#6-open-questions) is answered *tagged*, which promotes this row from a follow-up to the condition on that answer.** A result of *refuses every write* does not merely add an owner action to the install document — it sends the ruling back, to an ADR-0016 amendment or to untagged with expiry disabled. |
| Y-144 | The appliance's ssh identity: a key, a config, and a `known_hosts` nobody typed | ⬜ todo | — | — | **The largest under-priced cost in the milestone** — [the M7 plan](docs/plans/m7-appliance.md) §3.3. Every machine name is an ssh destination resolved by the local `~/.ssh/config` and never by Yantra ([ADR-0009](docs/adr/0009-machine-names-are-ssh-destinations.md)), and on first boot the appliance has no config, no key any fleet machine has authorised, and an empty `known_hosts`. Provable today against the container fixture (§B3) from an identity that is not the developer's, and against a real fleet machine from a second account. **[Q18](#6-open-questions) is answered against `tailscale ssh`**, so this row writes the config rather than choosing between two transports. **Partly hardware-blocked**: the shape is testable, the appliance's own config is not.<br>**2026-08-09 — `yantra ssh-identity` landed**, proved against the fixture from a keypair made at test time ([tests/ssh_identity.rs](crates/yantra-core/tests/ssh_identity.rs)); the `known_hosts` half needed no code. **Left**: [D2.10](docs/design/02-setup.md)'s confirmation that generating the key is Yantra's at all, and a real fleet machine from a second account. |
| Y-150 | The appliance runs 24/7 and survives a power cycle | ⬜ todo | — | Y-142, Y-143, Y-144, Y-145, Y-146, Y-147, Y-148, Y-149 | **The milestone** — enrol, install, pull the plug, and watch the dashboard come back and the phone buzz ([the M7 plan](docs/plans/m7-appliance.md) §1, §5). It also executes **I-9**'s desktop half for the first time: every machine this fleet has ever had is a laptop with a battery, which is why that invariant's founding case has been `[D]` since Y-106. **Hardware-blocked entirely**, and a purchase before it is a task — which is why nothing above it is written as *when the Pi arrives*. Depends on everything above. |
| Y-159 | Host the script on a name that resolves off the tailnet, and rewrite the install document around it | ⬜ todo | — | Y-157 | **Done:** the pinned script is served from the landing site — [`landing.yml`](.github/workflows/landing.yml) already deploys `landing/` to Cloudflare whenever it changes — and [`docs/appliance.md`](docs/appliance.md) is rewritten in the same PR ([§B5](CLAUDE.md)) around fetch-and-run, keeping the from-source path and what the script deliberately cannot do. **The host has to resolve publicly and the dashboard's name does not**: the dashboard stays on `.ts.net` ([Y-154](docs/archive/m6-m7.md), 2026-08-09), and the box being installed is not on the tailnet yet. Why: [#190](https://github.com/2002Bishwajeet/yantra/pull/190). |
| Y-169 | An unknown `/api` path falls through to the SPA and answers `200 text/html` | ⬜ todo | — | — | **Done** when a miss under `/api` is a JSON 404 and the SPA fallback still serves `/`, `/m/*` and `/w/*`. Found 2026-08-10 restarting a stale daemon: `/api/readiness` on the pre-[Y-168](docs/archive/m11-m13.md) binary returned `index.html`, so an absent route was indistinguishable from a served page. Sibling of [I-60](crates/yantrad/tracker.md) — a wrong answer that reads as a right one — and a different bug. |
| Y-174 | The dashboard draws what needs attention | ⬜ todo | — | Y-173 | **Done** when `/` lists the three kinds, each linking to GitHub, drawing the same three freshness states as every other section. It is a band inside *Needs you* rather than an eighth card, under its own heading because its verbs open GitHub ([D3](docs/design/03-dashboard-surface.md) §14). [D6](docs/design/06-sessions-attention-spend.md) §3 settles it: a block under `Needs you`'s own `h3`, its own 300 s stamp, and four sentences where `gh` cannot answer. **The daemon has served `/api/attention` since Y-173 and no file in `web/src` asks for it** — this row is wiring. Its units were minted 2026-09-03 as **Y-314**…**Y-316**. |
| Y-179 | Attach the browser to any session, not only a workspace's | ⬜ todo | — | an unwritten ADR | **Done** when the terminal opens on a machine-and-session pair over `terminal.rs`'s protocol. **Blocked on an ADR nobody has written**, and deliberately: this is R13's decision **D** and D1.9's blocker — it widens what a terminal may reach from *a pane in a workspace's session* to *any session on any machine*, and [ADR-0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md)'s authoriser is all that stands between that and the tailnet. [D6](docs/design/06-sessions-attention-spend.md) §6 settles the design and gives the ADR its direction (owner, 2026-09-03: any session on the owner's own fleet is reachable). **`attach::plan` reads a workspace for two facts only**, so this is a second `plan` taking machine and session, not a second terminal. The ADR is **Y-318**; the work behind it is **Y-319** and **Y-320**. |
| Y-180 | The dashboard draws a session as something to act on | ⬜ todo | — | Y-177, Y-178 | **Done** when each session carries its verbs, and a session with no workspace is drawn as such rather than hidden. Nothing new lands on `/`: a claimed session **is** its workspace row, and an unclaimed one is a row on `/machines`, counted in the fleet footer ([D3](docs/design/03-dashboard-surface.md) §14). [D6](docs/design/06-sessions-attention-spend.md) §4 settles it: an ACT column on `sessionColumns`. **`Act.tsx` already exports `Kill` and nothing renders it.** Adoption is refused — `tmux::Summary` carries no repo, so it would guess the one field a workspace cannot do without. Minted 2026-09-03 as **Y-317**. |
| Y-183 | The usage view | ⬜ todo | — | Y-182 | **Done** when a page shows spend per session and per workspace. Layout follows T3 Code's usage page rebuilt on this project's tokens — the same take [Y-164](docs/archive/m11-m13.md) made of its primitives at pinned commit `963ebf5b`, so **the MIT attribution that took applies here too**. The page is `/usage`, a nav item reading **one machine at a time on request** ([D3](docs/design/03-dashboard-surface.md) §11.4) so no fan-out happens on open, and `AS_OF` sits beside the figure as [`price.rs`](crates/yantra-core/src/price.rs) already requires of the CLI. [D6](docs/design/06-sessions-attention-spend.md) §5 settles it, and settles it as a refusal: **no fleet total**, because one would cost an ssh transcript read per workspace on open. A total goes through the CLI first or not at all. |
| Y-198 | `/w/{name}`: three tabs, the transcript, and the trust prompt in place | ⬜ todo | — | Y-187 | **Done** when `?view=` round-trips, a desktop opens the terminal and a phone the transcript, the transcript shows turns with tool calls collapsed, and a workspace at `awaiting_trust` opens on the pane holding the question. **It sends the conversation over the wire**, which [D3](docs/design/03-dashboard-surface.md) §11.3 records against [Y-181](docs/archive/m11-m13.md). [D5](docs/design/05-workspace-page.md) settles it, and its nine units were minted on 2026-09-03 as **Y-305**…**Y-313** — this row closes when they do. Its §5.2 puts D3 §4.5's twelve rows on the fleet row rather than here. §11. |
| Y-199 | `/usage`, `/settings`, and the push the open page suppresses | 🟡 wip | — | Y-183, **§12.2** | **Done** when `/usage` reads one machine at a time on request with `AS_OF` beside the figure, `/settings` writes the ntfy relay, and one event produces one notification while a tab is visible. The beacon is in-memory daemon state that a restart forgets, which is what *persists nothing* means (Y-044). **Blocked in part**: where a daemon that persists nothing keeps the relay URL is [D3](docs/design/03-dashboard-surface.md) §12.2 and is undecided. §11.4, §13. **Built**: `POST /api/workspaces/{name}/tokens`. **D3 §11.4 is wrong about the picker** — spend is per workspace, not per machine, because `tokens` loads a workspace to find its transcript. **Still blocked**: `/settings` on §12.2, and the presence beacon with it. Building `/usage` found a **priced zero** in shipped code — a session the table carries no model of published `cost: 0.0`, and the CLI printed `$0.00` with its own test asserting it under a name that forbids it. Fixed here. |
| Y-305 | A tool call carries what it acted on | ⬜ todo | — | — | **Done** when `Entry.tools` is `Vec<Call>`, the target is the first key present of `command`, `file_path`, `path`, `pattern`, `url`, `query`, `description`, `prompt`, it is capped at 120 characters and a cut one says so, `yantra logs` prints it, and a call with no target prints its name alone. Measured: the input already crosses the wire and the parse discards it. [D5](docs/design/05-workspace-page.md) §2.1, §4.2. |
| Y-306 | The transcript read takes a window and returns a count | ⬜ todo | — | — | **Done** when the probe takes `before` as `tail -n {lines + before} \| head -n {lines}`, returns `total` from `grep -c` on the same pipeline, and a repo path or session id holding a quote still cannot reach the remote shell. Both far-side lines measured free over 15 MB. [D5](docs/design/05-workspace-page.md) §2.2, §4.4. |
| Y-307 | `POST /api/workspaces/{name}/logs` | ⬜ todo | — | Y-305, Y-306 | **Done** when the daemon answers the projection, sits on `allowed()`, returns 409 for a transcript that is absent or has no turn, and 503 naming the machine, the command and what ssh said. `tokens`'s shape. Nothing serves the transcript today. [D5](docs/design/05-workspace-page.md) §9. |
| Y-308 | Three tabs, and the URL carries which | ⬜ todo | — | — | **Done** when `terminal · transcript · spend` are three `Link`s, an explicit `?view=` always wins, the width decides once at 768 px, a resize moves nothing, an unknown value falls back rather than 404s, and a press replaces the history entry. There is no `ui/tabs` and ADR-0014's first rule forbids editing one in. [D5](docs/design/05-workspace-page.md) §3. |
| Y-309 | The transcript view | ⬜ todo | — | Y-307, Y-308 | **Done** when turns show who spoke and when, a tool call is one line expanding to its target, `Older` walks back, the stamp moves on a clock that fetches nothing, and the four empty states read in §4.5's words. Text renders as text, not Markdown. [D5](docs/design/05-workspace-page.md) §4. |
| Y-310 | A conversation that moved while you paged back | ⬜ todo | — | Y-309 | **Done** when a grown `total` says the conversation moved on and offers a re-read rather than stitching a shifted window, asserted against a file that gained records between two reads. [D5](docs/design/05-workspace-page.md) §4.4. |
| Y-311 | The spend tab | ⬜ todo | — | Y-308 | **Done** when `Answer` and `Figure` are shared with `/usage`, this page passes no picker, and a session with any unpriced model shows tokens as the headline and no dollar line. [D5](docs/design/05-workspace-page.md) §6. |
| Y-312 | Three tabs against a machine that is not there | ⬜ todo | — | Y-308, Y-309, Y-311 | **Done** when each tab draws its own refusal naming the machine, the machine stays a link to `/m/{machine}`, and no page-level banner replaces the tabs. [D5](docs/design/05-workspace-page.md) §7. |
| Y-313 | `Terminal` takes a height | ⬜ todo | — | — | **Done** when the prop defaults to `60vh` and `/w/{name}` passes nothing — which is what gives [D3](docs/design/03-dashboard-surface.md) §4.5's twelve rows somewhere to come from. [D5](docs/design/05-workspace-page.md) §5.1. |
| Y-314 | The attention band draws | ✅ done | claude | Y-173 | **Done:** [`Attention.tsx`](web/src/components/Attention.tsx) under `Needs you`'s rows, fetched by [`Fleet.tsx`](web/src/routes/Fleet.tsx); `speaks()` in [`work.ts`](web/src/work.ts) keeps an empty queue out. Types in [`api.ts`](web/src/api.ts), tests in [`attention.test.tsx`](web/src/attention.test.tsx). The contract fixture Y-173 deferred is [Y-322](#3-task-board). |
| Y-315 | The band carries its own age | ✅ done | claude | Y-314 | **Done:** the band stamps `/api/attention`'s own age and names its 300 s clock. It does not use `Age`, whose stale thresholds are the 30 s sweep's. |
| Y-316 | `gh` that cannot answer says which | ✅ done | claude | Y-314 | **Done:** a failed look draws the daemon's sentence verbatim, a pending one a skeleton, and `never` says nobody has looked. |
| Y-317 | An unclaimed session can be killed | ✅ done | claude | Y-177, Y-178 | **Done** when `sessionColumns` gains an ACT column holding `Kill`, it confirms first, and an already-gone session reads as nothing to kill rather than as a failure. `Act.tsx` exports `Kill` today and nothing renders it. Adoption is refused, not deferred. [D6](docs/design/06-sessions-attention-spend.md) §4.2, §4.4.<br>**Built 2026-09-03**: the ACT column in [`columns.tsx`](web/src/columns.tsx), tested in [`dashboard.test.tsx`](web/src/dashboard.test.tsx). The daemon needed nothing — `DELETE /api/machines/{machine}/sessions/{session}` already answers 200 with `killed: false` for a session that is gone. |
| Y-318 | ADR: a socket may address a session rather than a workspace | ⬜ todo | — | — | **Done** when it is written and accepted, stating the boundary, the refused alternative and what a mistyped address reaches. Owner's direction, 2026-09-03: any session on the owner's own fleet is reachable. This is the ADR [Y-179](#3-task-board) has been blocked on. [D6](docs/design/06-sessions-attention-spend.md) §6.1. |
| Y-319 | A session-addressed `plan` | ⬜ todo | — | Y-318 | **Done** when it takes a machine and a session, `remote_command` and `Tmux::pane` are unchanged, and a missing pane names the session rather than a workspace. `attach::plan` reads a workspace for two facts only. [D6](docs/design/06-sessions-attention-spend.md) §6.2. |
| Y-320 | `GET /api/machines/{machine}/sessions/{session}/terminal` | ⬜ todo | — | Y-319 | **Done** when it bridges on the same `allowed()` as the workspace socket, and §4.2's ACT column gains a Terminal verb naming what it will open. [D6](docs/design/06-sessions-attention-spend.md) §6.2. |
| Y-321 | The first load is still under budget | ⬜ todo | — | Y-314, Y-317 | **Done** when it is measured after the band and the ACT column land, against [D3](docs/design/03-dashboard-surface.md) §9.1's 145 KiB, and recorded. [D6](docs/design/06-sessions-attention-spend.md) §8. |
| Y-322 | The attention contract is machine-checked | ⬜ todo | — | Y-314 | **Done** when `contract.rs`'s fixture carries an `attention` entry, `contract.gen.ts` regenerates with it, and `web/src/api.ts`'s hand-written `Attention` and `Item` are the generated ones. Y-173 deferred this until the dashboard read the route; [Y-314](#3-task-board) made that true. |
| Y-323 | The task board is pruned into the archive | ✅ done | claude | — | **Done:** 73 closed rows moved to [m6-m7.md](docs/archive/m6-m7.md) and [m11-m13.md](docs/archive/m11-m13.md). §3 went from 268 kB to 42 kB, the file from 342 kB to 114 kB. The pre-rule rows average 5.6 kB and the post-§B5 ones 612 B, which is the case for the rule. |
### Landing site — out of milestone

Owner's ask, 2026-08-01: a *coming soon* page. **This is not M4.** The dashboard is a read-only
tool served over Tailscale to one person; this is a public single-page holding note. They share an
identity and nothing else — which is why what they share ships as
[`design/tokens.css`](design/tokens.css), plain CSS custom properties with no framework
dependency, and the reasoning as [`docs/design-system.md`](docs/design-system.md).

**It meets [ADR-0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md) cleanly.** That ADR
picked plain CSS + custom properties as the styling layer and left *what the incoming design system
is delivered as* open between CSS variables, a Tailwind preset and React components; this is the
first, which is the one that keeps its promise that the integration diff is `index.css` and nothing
else. **One collision is recorded rather than designed around**: shadcn's `cssVariables` mode
defines `--accent` as a muted hover surface and this file defines it as cinnabar, both on `:root`,
with no error either way — `docs/design-system.md` §7 carries the bridge.

Lives in `landing/`, leaving `web/` free for Y-072. Astro 7 + React 19 islands + Tailwind 4.

**Shipped as [GitHub stack #72](https://github.com/2002Bishwajeet/yantra/pull/67)**, five layers, using the native feature rather than five PRs that merely point at each other — which is what Y-064 widened the CI trigger for. The difference is not cosmetic: a registered stack retargets and rebases the layers above one that merges, and can be merged atomically, where hand-chained `base` branches leave every upper layer to be fixed by hand. `gh stack link` was used rather than `submit`, because the PRs already existed and `link` adopts them instead of recreating them.
The guardrail *"no UI until the CLI is good"* is not in play: this renders nothing about a machine,
reads no API, and has no way to reach `yantra-core`.

| ID | Task | Status | Owner | Depends | Notes |
| --- | --- | --- | --- | --- | --- |
| Y-200 | Landing scaffold + the design system | 🔵 review | claude | — | Astro 7.1.6 / React 19.2.8 / Tailwind 4.3.3, static output. Tokens and spec as above. **Three findings worth the research cost:** (1) **`astro dev` daemonizes itself under an AI-agent environment** — Astro 7 bundles `am-i-vibing`, so `npm run dev` forks and the foreground process exits 0, which kills Playwright's `webServer` with *"exited early"*; the fix is counter-intuitively `ASTRO_DEV_BACKGROUND=1`, which means *"I am already the background child"*. (2) **`@astrojs/tailwind` is deprecated** — Tailwind 4 is a Vite plugin with no `tailwind.config.js`, and tokens go in CSS via `@theme`; `@theme inline` is required when a token references a var defined elsewhere, or Tailwind emits the literal string. (3) **`create astro` has no `--typescript` flag** any more; strict is unconditional. Also `--no-ai`, to suppress generated agent files this repo already has conventions for. |
| Y-201 | Pattachitra motifs as generated SVG | 🔵 review | claude | Y-200 | Vine, rosette, cusped arch, hanging lamp, pearl band, sigil — **parameterised, never hand-authored path data**. Two rules earned by getting them wrong: the arch's lobe count must be **even** (`\|sin(N·θ)\|` then vanishes at `θ = 0, π/2, π`, cusping both springings and the apex; odd scoops the apex into a dome), and **only the inner rim is cusped** — cusping the silhouette turns a shrine into a sunburst. |
| Y-202 | The yantra shader | 🔵 review | claude | Y-200 | Nine interlocking triangles, a spoked chakra band, a bindu, in a WebGL2 fragment shader. Raw WebGL2, **no dependency** — a full-screen triangle from `gl_VertexID` needs no buffers, no VAO, no attributes. `client:only="react"` is mandatory: every other directive still server-renders, and `canvas` does not exist during SSR. Honours `prefers-reduced-motion` by drawing **one static frame** rather than nothing. Structure adapted from `canvas-ui` (**MIT + Commons Clause** — adaptable, *not* redistributable as a component library). |
| Y-203 | Header, footer, and the one sheet | 🔵 review | claude | Y-201, Y-202 | The whole page: painted frame, masthead, the shrine, one line of copy, coming-soon, footer. One screen, no scroll, no router. |
| Y-204 | Visual regression tests | 🔵 review | claude | Y-203 | Playwright `toHaveScreenshot`, light + dark × desktop + mobile. **Snapshots are OS-specific and Playwright's default path template no longer includes the platform**, so Linux and macOS collide at one path while rendering fonts differently — pin to one OS. |
| Y-205 | Deploy previews on Cloudflare | 🔵 review | claude | Y-204 | Every PR touching `landing/` or `design/` builds, runs the visual regression, and uploads a preview version, with one comment carrying the URL and edited in place on later pushes; a push to `main` deploys production. **Workers static assets, not Pages** — Pages is not deprecated, but Cloudflare has stopped recommending it, and the evidence is in wrangler itself, which prints *"Workers are the recommended way to deploy all new projects"* when a Pages project is missing; Astro's deploy guide no longer documents Pages at all. **No `@astrojs/cloudflare`**: the adapter is explicitly unnecessary for `output: 'static'`, which is also why `wrangler.jsonc` is hand-written. **A separate workflow, not a job in `ci.yml`** — that is R-24's own stated mitigation, and its retire condition (*`cargo build` green on a machine with no Node*) holds only if the two gates share no file; `landing-build` and `landing-visual` are justfile recipes per the CI convention but are unreachable from `just ci` and `just check`. **Two findings that would each have cost an afternoon**: `wrangler versions upload` *fails on a Worker that has never been deployed*, so a one-time `wrangler deploy` is a documented bootstrap rather than a surprise; and `wrangler-action` **silently drops the preview alias** — its output schema parses only `preview_url`, so `deployment-url` is the hash-versioned URL and the stable `pr-N-…` alias has to be recovered from stdout. **The deploy steps skip rather than fail without the two secrets**, so the build and the visual regression gate every PR before anyone has a Cloudflare account; the same branch covers a fork PR, and the answer there is the `workflow_run` split, never `pull_request_target`, which would run with secrets against unreviewed code. The token is a **custom** one scoped to `Workers Scripts: Edit` — Cloudflare's own guide recommends the `Edit Cloudflare Workers` template, which grants all-zone route write plus KV, R2 and Tail to a CI secret. Also swaps Astro's default favicon for the sigil. **Four setup steps the owner must do**, in `landing/README.md`. |
| Y-206 | The patta frame, redrawn from the reference | ⚫ dropped | claude | — | The first landing design was rejected outright — wrong concept, too folk, too busy, and it read as AI-made. The mechanism is worth recording: it landed exactly on a known cliché (warm ground + serif display + terracotta accent, centred, ornamental border), each choice defensible on its own. Rebuilt from a second reference, an Odisha Pattachitra of the **Nabagunjara**, with every value *measured* rather than eyeballed — the palette is the top-decile-saturation mean of each hue family across the image. Four framework-free drawing stacks in `landing/src/patta/`, pure functions returning SVG strings, colour only via custom properties. **Corrections that overturn earlier rows:** Y-201's two arch rules are *wrong for this arch* — both rims are cusped equally (a parallel offset; it is the creeper that stays calm), and an even lobe count is necessary but nowhere near sufficient, because `|sin|` gives a smooth maximum and the apex needs a **local tent** of half-width ~0.15 rad to make a real ~155° vertex. Motif rules are specific to the painting they were measured from, not general Pattachitra law. **Other findings:** a Bézier aimed at its own endpoint always arrives as a spike — that is why generated petals come out as thorns; tiles carry `preserveAspectRatio="none"`, so any caller making `tile ≠ band` stretches every floret, and density must scale both together; co-prime background sizes kill the combined super-period but **not a single layer's own lattice**, so the 60–250px band must be left empty; `feTurbulence` is unusable for a full-page ground because Chromium tiles the filter region. There is **no second border band** — the creeper belongs to the arch's spandrel, and only looks like a border if you crop the top edge where the arch runs near-horizontal. The frame wraps the **viewport**, redrawn in CSS pixels on resize rather than scaled from a fixed viewBox. Light ground only by default: a patta is a painted cloth, so the dark ground is an invention and does not get to be what a dark-mode visitor sees. Wired into the page by Y-207. **Dropped by Y-208** — the owner rejected this design too. The stacks are deleted, not deprecated: recover them from `827d300` if a later direction wants them. |
| Y-207 | The landing page, rebuilt on the patta stack | ⚫ dropped | claude | Y-206 | `index.astro` is the frame now: cream band, cloth field, the border wrapping the viewport at 65% density, the torana standing in the field and the **Jai Prakash bowl inside it**, read against the real sun over the Jantar Mantar at Jaipur from the viewer's clock. That last part is the whole argument for the instrument being there rather than an ornament being there. **Deleted, not left dead:** `Frame`, `Header`, `Footer`, `Shrine`, `motifs/`, `YantraShader.tsx`, `shaders/`, `/preview/patta`, the React integration and its four dependencies, and the two font families the old lockup used — the page has no client framework left. **Three geometry findings.** The arch must not take the field's shape: a near-square panel puts the head's two radii within a few percent of each other, while spanning a 1.7:1 desktop field flattens it to a tent and a phone field draws a 3:1 lancet. A cusped opening's clearance is nowhere near its half-width — the two upper cusps bind first, and sizing the bowl off the half-width drew it over the band, so `toranaOpening` now hands back the boundary itself. And the instrument has to be drawn *before* the arch, because the lamps hang from the inner cusps into the opening the bowl occupies and cross it at any radius. **The baselines are rewritten** and the clock is pinned with `page.clock.setFixedTime` — the bead is a real shadow, so an unpinned clock moves it every run; one test moves the clock past sunset and asserts both the bead and the caption follow. `design/tokens.css` is untouched and the landing no longer reads it: see `landing/README.md` for why the two palettes have diverged and which one is measured. **Dropped by Y-208**, with Y-206. |
| Y-208 | Strip the design, keep the plumbing | 🔵 review | claude | — | Two landing designs rejected in a row. The owner is taking the direction back — *"I will try to draw some prototype and do prompt enhancement first"* — so the page becomes a placeholder rather than a third guess at it. **Deleted:** `src/patta/` entire (border, torana, instrument, tokens, ground and their two READMEs), the four baselines, and both of the old webfont families; `index.astro` is now three elements — name, line, coming-soon — and the page ships **no JavaScript at all**. **A system font stack does not survive CI**: it was the first thing tried, and `system-ui` resolved to a different face on the runner than on the dev box, failing both mobile baselines at 2% of pixels while desktop slipped under `maxDiffPixelRatio`. One self-hosted face (IBM Plex Sans 400/600) is the fix — a reproducibility requirement rather than a design choice, and the reason the *previous* build's baselines were stable without anyone noticing why. **Kept, and that is the whole point of doing this as a strip rather than a revert:** the Astro scaffold, the Playwright harness and the Cloudflare deploy (Y-200, Y-204, Y-205). None of what those cost was design research, so the redraw lands on a working pipeline instead of rebuilding one. **The light-only argument goes with the cloth** — a neutral placeholder has no reason to override a viewer's OS preference, so `prefers-color-scheme` is honoured again, asserted by a test because a reversal like that is what a later edit silently undoes. The favicon sigil is **left in place**: it is a mark rather than the rejected page, and swapping it would be one more unilateral design call of exactly the kind that got us here. **Nothing is lost** — the whole patta build is `827d300` on `main`. |

### What closed, and what it steered

One line per closed task that deferred something, dropped something, or left the plan somewhere other
than where it started — the state that would be lost if the row simply went away. Everything else
about them, the evidence included, is in [docs/archive/m4-m5.md](docs/archive/m4-m5.md); M5's own
re-scope is in [§2](#2-milestones).

- **[Y-037](docs/archive/m4-m5.md#carried-forward-from-m0m3-the-ones-that-closed-here)** — the release pipeline was rehearsed green on five targets and had never run at a tag. **[Y-156](docs/archive/m6-m7.md) cut v0.1.0 on 2026-08-09**, so the README points at binaries.
- **[Y-044](docs/archive/m4-m5.md#carried-forward-from-m0m3-the-ones-that-closed-here)** — the session state store was **dropped on 2026-08-02 without being built**: five candidate consumers audited, none needs one, because declared state is the workspace TOML, live state is tmux, and history is the agent's own transcript. **The daemon persists nothing.** What brings it back is the first question about the *past*.
- **[Y-073](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — asset serving shipped as the **directory half only** (`YANTRA_WEB`). Embedding the dashboard behind a cargo feature waited for the M7 appliance that wants one file to copy; building it then would have bought nothing and cost R-24 immediately. **[Y-140](docs/archive/m6-m7.md) built it on 2026-08-05**, and the wait was the right call — the feature is absent from `default` and the directory half is unchanged, so what M7 bought is a build the Rust gate never makes.
- **[Y-080](docs/archive/m4-m5.md#found-by-the-scope-audit-2026-07-31)** — a repo-wide relative-link check was written and **deliberately not kept**. It found 0 broken links across 53 files and would have caught neither drift, because both times the stale reference resolved to a *real* ADR that was about something else. The fix was to delete the duplicated fact, not to check it.
- **[Y-083](docs/archive/m4-m5.md#found-by-verifying-the-cli-2026-07-31)** — ADR-0003 and research notes 06a–06c were **deleted rather than superseded**, which bends §B5's *immutable once accepted* on the ground that the decision never governed shipped code. **Retired numbers survive as numbers and are never reused**: R-0, R-4, R-9, R-11, R-14…R-18, I-15…I-17 and ADR-0003.
- **[Y-101](docs/archive/m4-m5.md#m4--web-ui-closed-2026-08-02)** — [ADR-0004](docs/adr/0004-rust-for-the-daemon.md) was **amended in place** with a dated blockquote rather than superseded, because what stopped holding was a premise and not the decision; `CLAUDE.md` §B1 and `README.md`'s Stack line follow it.
- **[Y-110](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — closed the **macOS half only**. The Linux `sysfs_power` battery path is still `[D]` and reachable only through a fake tree, since `cachyos-g14` cannot be unplugged remotely; I-9's desktop half still has no desktop; and *asleep or off*, produced on the Mac later the same day once the owner closed the lid over this task's beat, does not survive a `yantrad` restart, because nothing is persisted.
- **[Y-113](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — still not built: **a plain-shell start for an agent workspace**, there being one button, labelled `Start claude` or `Start`, that does what it says. Its other two deferrals are spent — the trust prompt's **500** is [Y-135](docs/archive/m6-m7.md)'s `409`, and it was `resume::Error`'s rather than `up::Error`'s all along; and the agents section's `yantra up`/`yantra resume` pastes are [Y-136](docs/archive/m6-m7.md)'s buttons.
- **[Y-114](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — the PWA is proven in headless Chromium only. **The iOS install itself is unverified** — no iPhone or iPad was available — so *Add to Home Screen*, `display: standalone` and the `apple-touch-icon` are untested on the device the milestone is for.
- **[Y-115](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — **closed on evidence with no code written.** The row predated the three tasks that answered it, and each of its four clauses turned out to have an owner already: Y-109, Y-113, and a sessions section older than the row.
- **[Y-116](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — **editing did not ship and the row's title had promised it.** There is no `PUT`, no `PATCH` and no `yantra edit`; creating is all there is, and the file stays the operator's to edit meanwhile.
- **[Y-119](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — closed the **writing** path only, and [Y-137](docs/archive/m6-m7.md) closed the reading one: `workspace::load` refuses a blank `machine`, `repo` or `startup` from a hand-edited file, naming the file, the field and the workspace. The decision Y-119 left open went the way it feared — one such file did stop the whole of `list()`, exactly as malformed TOML did — because the alternative makes the listing's contract depend on which way a file is unusable. **[Y-141](docs/archive/m6-m7.md) then changed the rule for the whole class**: `list` carries per-workspace failures, so a file that does not load is named with its reason and costs the others nothing.
- **[Y-121](docs/archive/m4-m5.md#m5--control-from-the-phone-buildable-work-finished-2026-08-03)** — **renumbered from Y-120** on 2026-08-03, two parallel changes having reached for the same free number. **Every table on the page becomes labelled blocks below `48rem`** — the shape belongs to [`DataTable`](web/src/components/DataTable.tsx) and no table can opt out of it. This bullet used to say *only the workspaces table*, which was the framing the row was opened with, restated here after the row had already falsified it ([Y-138](docs/archive/m6-m7.md)). **What is unmeasured is everything except workspaces**: every pixel Y-121 recorded is the workspaces table's, so machines, sessions and agents have the blocks by construction and by nobody's measurement.

---

## 4. Research index

| # | Topic | File | Status | Key question it must answer |
| --- | --- | --- | --- | --- |
| R1 | Tailscale / Headscale inventory | [01](docs/research/01-tailscale-inventory.md) | ✅ done | Does Tailscale give live CPU/RAM/battery, or must Yantra ship its own agent? → **Zero telemetry. Agent required.** Verified absent from `ipnstate.PeerStatus`, the full 235 KB API v2 OpenAPI spec, *and* `tailscale metrics`. |
| R2 | tmux sessions & terminal streaming | [02](docs/research/02-tmux-sessions.md) | ✅ done | How do we stream a live terminal to a browser with least work? → **Daemon-side PTY + xterm.js over WebSocket**, with the PTY on the daemon side rather than a native addon in the web stack. Control mode deferred to v2 as the scaling transport; ttyd rejected as primary. |
| R3 | AI agent CLIs | [03](docs/research/03-ai-agent-clis.md) | ✅ done | Is "resume exactly where it left off" actually possible across all 5 agents? → **Not uniformly.** Only Claude Code & Gemini let you *preset* a session id; Codex & OpenCode force capture-after-launch; Aider has no session concept at all. |
| R4 | Workspace prior art | [04](docs/research/04-workspace-prior-art.md) | ✅ done | Why should Yantra exist instead of Coder / DevPod? → **Justified, but narrowly.** Coder's workspace is a Terraform build artefact; an always-on laptop it must never destroy is unrepresentable. Floor is coderd + Postgres + provisioners, sized for 1,000 users. |
| R5 | Scheduling | [05](docs/research/05-scheduling.md) | ✅ done | What is the 100-line version of a scheduler for 5 machines? → **Filter→Score→Bind, ~100 lines.** 6 hard filters, 6 weighted signals summing to 100, deterministic tie-break, never queue. Everything else from Nomad/K8s explicitly rejected. |
| R6 | Pi 5 feasibility: SSH, PTY, SQLite, hardware | [06](docs/research/06-runtime-feasibility.md) | ✅ done | What does the appliance actually need a systems language for? → **The hardware layer, nothing.** Pi 5 device-tree overlays hand the encoder and the LED strip to userspace as an evdev node and a char device; the kernel and RP1's PIO are the real-time layer. |
| R7 | SSH exec over the system binary | [07](docs/research/07-ssh-transport.md) | ✅ done | How do you run one command over `ssh` and learn what actually happened? → **You cannot, unaided.** `ssh` reports a signal-killed command as 255 with empty stderr — `clientloop.c` has no `exit-signal` branch — so signal death, remote 255 and a dropped multiplexed connection are indistinguishable. Needs a sentinel. Separate argv buys nothing: `ssh` joins arguments and feeds the remote *login shell*. |
| R8 | React + the compiler | [08](docs/research/08-react-and-the-compiler.md) | ✅ done | What does "latest React" actually mean today, and how is the compiler wired? → **19.2.8 + `babel-plugin-react-compiler` 1.0.0, and the config every guide shows is dead** — `react({ babel })` was removed in plugin-react 6. No SWC path, no Babel-free path. **Compiler bail-outs are silent** and react.dev's claim that an ESLint error implies a skip is false, measured. |
| R9 | Component libraries | [09](docs/research/09-component-libraries.md) | ✅ done | Which choice keeps the look swappable when the design system arrives? → **shadcn in CSS-variable mode, and barely a dependency** — its table/alert/card/skeleton/empty/spinner are `dependencies: null`, identical across four presets. `oat.ink` is real (Oat UI, 10 KB, zero deps) and loses only because it is not React. |

---

## 5. Decisions

Architecture decisions live in [`docs/adr/`](docs/adr/). Index:

| ADR | Decision | Date | Status |
| --- | --- | --- | --- |
| [0001](docs/adr/0001-record-architecture-decisions.md) | Record architecture decisions | 2026-07-28 | accepted |
| [0002](docs/adr/0002-project-name.md) | Project is named **Yantra (यन्त्र)** | 2026-07-28 | accepted |
| 0003 | *withdrawn before it governed any code; the number is not reused* | 2026-07-28 | — |
| [0004](docs/adr/0004-rust-for-the-daemon.md) | **Rust for the daemon, CLI and agent; TypeScript for the web UI** | 2026-07-28 | accepted |
| [0005](docs/adr/0005-core-logic-in-a-library-crate.md) | Orchestration logic lives in a **library crate**; binaries stay thin | 2026-07-29 | accepted |
| [0006](docs/adr/0006-ssh-exec-transport.md) | The SSH exec primitive: **system `ssh` + `ControlMaster`**, sentinel trailer, base64 payload | 2026-07-29 | accepted, amended by [0008](docs/adr/0008-withdraw-the-stdin-watchdog.md) |
| [0007](docs/adr/0007-workspace-schema-v1.md) | Workspace schema v1 in TOML | 2026-07-29 | accepted, amended by [0010](docs/adr/0010-drop-branch-from-the-workspace-schema.md) |
| [0008](docs/adr/0008-withdraw-the-stdin-watchdog.md) | The stdin-EOF watchdog is withdrawn | 2026-07-29 | accepted |
| [0009](docs/adr/0009-machine-names-are-ssh-destinations.md) | A workspace's `machine` is an **ssh destination, not a Yantra identifier** — the Tailscale inventory observes, never resolves | 2026-07-30 | accepted |
| [0010](docs/adr/0010-drop-branch-from-the-workspace-schema.md) | **`branch` is dropped** — it was parsed and never acted on; branch selection returns in M3 as worktrees, not checkouts | 2026-07-30 | accepted |
| [0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md) | **Claude Code runs as a TUI in tmux; the transcript JSONL is the log** — no headless path in M3. Closes Q7/Y-024 | 2026-07-30 | accepted |
| [0012](docs/adr/0012-the-cli-and-the-daemon-are-two-callers-of-one-library.md) | **The CLI and the daemon are two callers of one library** — the CLI keeps calling `yantra_core` in-process | 2026-07-31 | accepted |
| [0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md) | **React with the compiler for the web UI**, and a token seam so the design system stays swappable | 2026-08-01 | accepted |
| [0015](docs/adr/0015-resume-forks-the-conversation.md) | **`resume` forks the conversation rather than resuming a session id** — `--continue --fork-session --session-id <uuid>`, measured on 2.1.220; extends [0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md)'s *Launch* bullet | 2026-08-02 | accepted |
| [0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md) | **The dashboard writes, and Tailscale identity authorises it** — a write is allowed only from a node owned by this daemon's own user and carrying no tags, resolved **live** rather than from the 30 s snapshot, and refused when `tailscale` cannot answer. **It rejects nothing on this tailnet today** — six nodes, one user, no tags, measured — and says so; the value is that a shared node, a tagged CI runner or a second invited user each arrive through an admin console that this repository never sees. | 2026-08-02 | accepted 2026-08-03 |
| [0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) | **The heartbeat carries only what placement scores** — seven fields, identity from the source address | 2026-07-31 | accepted 2026-08-03 |
| [0017](docs/adr/0017-the-forwarded-address-is-the-caller-when-the-hop-is-ours.md) | **The forwarded address is the caller, when the hop is ours** — the caller's address is the TCP peer, unless that peer is one of the daemon's own bind addresses, in which case it is `X-Forwarded-For`. **Amends [0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md)**'s decisions 2 and 4 in how the address is *obtained*; the predicate, the live lookup and the closed failure are untouched. Measured against `tailscale` 1.98.9: `serve` **replaces** the header with the connection's real source, and sets **no** `Tailscale-User-*` header at all for a tagged node — so the address is the only channel a tagged caller announces itself through. | 2026-08-03 | **proposed** |
| [0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) | **On macOS the tmux server carries the login session, and Yantra gains no second transport** — the Mac's tmux server is started by the user's login session, so the panes it forks are already in `gui/<uid>`; ssh keeps its monopoly and `yantra-agent` stays a reporter. **Closes [Y-122](docs/archive/m6-m7.md), whose deliverable was the decision; R-21 stays open until the code ships** — [Y-151](docs/archive/m6-m7.md) for §1 and §5, M7 for §7's launchd job | 2026-08-03 | accepted 2026-08-05 |
| [0019](docs/adr/0019-a-probe-that-asks-a-machine-is-a-post.md) | A probe that asks a machine on demand is a `POST` | 2026-08-11 | accepted |

**Still to write.** Numbers are assigned when an ADR is written, never reserved — this list had
drifted a full four rows out of step with `docs/adr/` before it was rebuilt on 2026-07-30, because
planned decisions were holding numbers that written ones then took.

| Decision | Blocked on |
| --- | --- |
| ~~Machine telemetry~~ | ✅ **written 2026-07-31 as [ADR-0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md)** and **accepted by the owner 2026-08-03** (Y-103). Nothing is left to write here. |

---

## 6. Open questions

| # | Question | Blocks | Owner |
| --- | --- | --- | --- |
| ~~Q1~~ | ~~Why not just run Coder / DevPod?~~ | — | ✅ **CLOSED 2026-07-31: signed off by the owner — "Coder is not useful for my case."** The kill criterion is cleared; Y-021 done. |
| ↳ | **R4's answer:** Coder models a workspace as a *Terraform build artefact* — `start`/`stop`/`delete` are `terraform apply` transitions. A machine that already exists, is always on, and must **never be destroyed** (my MacBook) is unrepresentable in that model. Its floor is coderd + Postgres 13+ + provisioners, with the smallest validated architecture sized for 1,000 users. Yantra's niche: **pre-existing, personally-owned, heterogeneous machines**. | | |
| Q2 | Which machines actually exist today, with what specs and OS? | M2 | biswa<br>**Partly answered by measurement, 2026-08-02, and left open because the specs half is still missing.** `tailscale status --json` reports **six nodes, one user, no tags**: `cachyos-g14` and `bishwajeets-macbook-pro` (both online and both running agents), an **iPhone and an iPad** — which is what makes M5's acceptance test physically possible rather than aspirational — and two offline `laptop-*` nodes that are the first concrete evidence bearing on [Q4](#6-open-questions). What is still owed here is specs and OS per machine, which Tailscale does not carry (R1). |
| Q3 | Is a per-machine Yantra agent required, or can we get by with SSH polling? | ADR-0007 | ✅ **CLOSED. Push-heartbeat agent, 10s interval, stale at 30s.** R1: Tailscale exposes zero telemetry, so an agent is required. R5 then killed my SSH-poll preference outright — **SSH cannot see a sleeping laptop**, and a scheduler whose main job is "is this machine usable right now" cannot be blind to sleep. node_exporter/Glances also rejected. |
| Q4 | Windows: first-class, or WSL2-only? | M2 scope | 🟡 **deliberately left OPEN.** **2026-08-09: it now costs a release artifact** — [`release.yml`](.github/workflows/release.yml) shipped no Windows `yantra-agent` in v0.1.0, because [`probes.rs`](crates/yantra-agent/src/probes.rs)'s `compile_error!` is this question standing in the way of a build. Owner declined to commit to WSL2 (2026-07-28). Deferred to M2, when real remote machines first matter — it does not block M1.<br>**Binding constraint meanwhile: write no code that assumes either answer.** The transport trait must not hard-code POSIX-isms in its signature, and nothing may assume `tmux` or `ControlMaster` exist on a target. Windows genuinely lacks all three of tmux, `ControlMaster` and Tailscale-SSH-as-server (R-7), so whatever is decided, it is a second path — the job now is to keep adding that path cheap rather than to pick it early. <br>**Y-022 narrows it:** the tailnet's only Windows node is the second boot of a laptop that already runs Linux, so supporting Linux-only costs **zero machines**. Meanwhile macOS is one of just two online hosts, making it the more urgent portability question. Also: **Tailscale SSH can never serve Windows** — its server component is Linux and open-source-macOS only — so a Windows target needs OpenSSH-for-Windows and has no tmux, two separate problems rather than one. |
| ~~Q5~~ | ~~Secrets: reference-only or does Yantra hold them?~~ | — | ✅ **CLOSED: reference-only, always.** Workspaces store a pointer (`op://…`, `pass show …`, a sops path); Yantra resolves it at launch, hands it to the process, and never writes it to SQLite, logs, the API, or a terminal stream. Holding secrets would mean earning the right to — encryption at rest, key management, stream redaction, audit. That is a security product, not a workspace orchestrator. |
| ~~Q6~~ | ~~Open-source-for-others or personal-first?~~ | — | ✅ **CLOSED 2026-07-31: personal-first. Open source for others later.** So no plugin architecture, no multi-user model, no configuration surface added for a hypothetical second user — one fleet, one owner, and the second user is a later problem that a working tool earns the right to have. The repo stays public; that is a licence and a habit, not a promise of extensibility. **Binds M4 directly:** the web UI is a dashboard for its owner, not a product — single-tenant, no auth beyond Tailscale, no theming, no settings screen.<br>**Amended 2026-08-11 by the owner: what this refused is preferences, not configuration.** No theme switcher, no density control, no layout choice — and `/settings` exists for the values a person must set before the product works, which today is the ntfy relay's URL and token ([D1](docs/design/01-dashboard.md) §6). They are environment variables and nothing in the product can write them. The premise that changed is that Q6 was answered before Yantra had a setting of its own; *no settings screen* was written against a preferences panel, and a relay URL is not one. **Where the value is kept is not decided** — [D3](docs/design/03-dashboard-surface.md) §12.2 names three answers with three different §B4 consequences. |
| ~~Q7~~ | ~~Is the agent control path **TUI-in-tmux** or **headless**?~~ | — | ✅ **CLOSED 2026-07-30: TUI in tmux, one path** — [ADR-0011](docs/adr/0011-claude-code-runs-as-a-tui-in-tmux.md). R3's "all five are safer driven headless" is knowingly overruled on evidence R3 did not have: two of its three hazards belong to agents Yantra does not ship, and the third (#63545) did not reproduce. Headless leaves nothing to attach to, which is the one thing Yantra is for. The transcript JSONL is the log, `claude agents --json` is the status source, and `claude auth status` gates the launch so I-44 cannot produce a healthy-looking useless session. |
| Q8 | Do we adopt **server mode** where it exists (OpenCode `serve` HTTP+SSE, Codex `app-server daemon`)? Strictly better than tmux-wrapping, but adds a second transport. | ADR-0006 | biswa |
| Q11 | **Should session-affinity be a score term at all?** R5 raises it against itself: the live-session short-circuit already delivers idempotent `yantra up X`, so a 20-point affinity weight may be re-solving a solved problem. Prior: **drop it**, and let the short-circuit do the work. | M10 scoring weights | biswa<br>**Moved to M10 with placement, 2026-08-02, and it no longer blocks anything.** The prior stands: drop it, and let the live-session short-circuit do the work. |
| Q10 | **Wake-on-LAN**: R1 says Tailscale never exposes MACs and has no `wake` command — WoL needs the Pi 5 as an always-on **L2 relay** plus a hand-maintained MAC table. Accept that manual table, or drop WoL from the scheduler until the appliance exists (M7)? Prior: **drop until M7**; a scheduler that can't wake a machine simply won't place work there. | M7 | ✅ **ANSWERED 2026-08-02 — defer.** The owner confirmed there is no always-on machine on the MacBook's LAN, so the relay this needs does not exist. Yantra will not ship a wake button that cannot wake anything (R-23, Y-115). Note for M7: research 01 §6 warns the pattern wakes *powered-off* machines but often **not S3 sleep**, which is exactly a closed laptop — so this is unproven, not merely deferred. |
| ~~Q9~~ | ~~Ship Aider or drop it?~~ | — | ✅ **CLOSED: dropped from v1.** No session id, no resume, no JSON, no MCP, ~1 release in 6 months. Yantra's core promise is "continue where you left off"; Aider has no session to continue, so supporting it would mean faking the product's central feature. Revisit only if it grows a session model. |
| ~~Q13~~ | ~~Does the CLI start talking HTTP to the daemon?~~ | — | ✅ **CLOSED 2026-07-31: (a), two callers of one library** — [ADR-0012](docs/adr/0012-the-cli-and-the-daemon-are-two-callers-of-one-library.md). The CLI keeps calling `yantra_core` in-process and keeps working with no daemon running; `yantrad` becomes a second caller of the same functions. (b) would have made the one working interface — four milestones of verified behaviour — depend on the fifteen-line skeleton, in the milestone that first proves the skeleton can serve a page, and it is the only one of the two that is hard to reverse. The ADR records what would justify revisiting: a client that is not on the operator's machine (M6/M8), state that must not be derived twice, or a placement decision the CLI must also see (M5). |
| ~~Q14~~ | **What is the web UI built with?** → **Answered 2026-08-01: React, latest, with the React Compiler**, built with Vite, components from shadcn/ui in CSS-variable mode, linted by oxlint. Recorded as [ADR-0014](docs/adr/0014-react-with-the-compiler-for-the-web-ui.md), because M6's browser terminal builds on it. **The deciding constraint was not the framework but the seam**: a design system is arriving from elsewhere, so the question was which choice welds the look in place. [R9](docs/research/09-component-libraries.md) settled it by pulling the component source — for this page shadcn's tables are **dependency-free plain markup**, identical across four style presets, so the code is what we would have hand-written anyway and the only thing a re-skin touches is `index.css`. **`cssVariables` is a one-way door set at init.** [R8](docs/research/08-react-and-the-compiler.md) measured the toolchain rather than reading it, and found react.dev wrong on one point that matters: an ESLint error does **not** imply the compiler skipped a component, and bail-outs are silent — a green build proves nothing about whether optimisation happened. | Y-072 | biswa ✅ |
| ~~Q12~~ | **Is R-1 actually retired, or only retired on Linux?** Issue #63545 is a **macOS + tmux** report; Y-023's spike ran on `cachyos-g14`, which is Linux, because the MacBook was unreachable until Y-049 closed on 2026-07-30. The spike's evidence is real (18,427 bytes written while fully detached on 2.1.220) and also argues against the open Linux issue #70632 — but R-1, Q7 and the Claude Code integration ADR were all retired or unblocked on Linux-only evidence, for a bug filed against macOS. Third-party support exists (open issue #79188, macOS 2.1.215, found plain tmux persists correctly) but that is someone else's machine, which §B3 says not to accept. Prior: **re-run the Y-023 spike against the MacBook before M3 builds on it** — cheap now that the machine is reachable — and make `yantra logs` assert the transcript mtime is *advancing*, not merely that the file exists, because #70632's failure mode looks healthy until you tail it. | M3, the Claude Code integration ADR | ✅ **ANSWERED 2026-08-05: retired on macOS too, on macOS evidence.** [Y-139](docs/archive/m6-m7.md) ran the measurement this question specified, on the machine and the OS #63545 names; the readings are in the row below. |
| ↳ | **Re-run on 2026-07-30. It did not get as far as R-1, because a prior problem stops it: on macOS an agent launched over SSH is not authenticated (I-44).** Claude Code 2.1.220 booted in a fully detached tmux session on the MacBook, took a prompt, and answered `Not logged in · Please run /login` — its token is in the login keychain, which the `Background` launchd domain an ssh session gets cannot read. So R-1 stays **untested on macOS**, and the transcript question is now behind Y-059 rather than beside it. Two things were learned anyway: the transcript file **is** created and written while fully detached on macOS (57,841 bytes, `session_attached=0` throughout), so the file-write path is not the problem; and Q12's own suspicion was right in kind — Linux evidence did not transfer — while wrong about which mechanism would break. | | |
| ↳ | **2026-08-05: the measurement has a harness and still has no result** — [Y-139](docs/archive/m6-m7.md), `the_transcript_advances_while_the_session_stays_detached` in [`crates/yantra-core/tests/manual_macbook.rs`](crates/yantra-core/tests/manual_macbook.rs). It takes the two readings this question asked for, with `session_attached` checked before each, and tells *advancing* from *stopped* from *the agent never started*. It cannot run itself: the MacBook had no tmux server on 2026-08-05 and one started over ssh is still `loggedIn: false` (I-44), so the run needs a server started from a GUI login at that keyboard. **This question closes when someone runs it and records which of the two experiments they ran, and for how long.** | | |
| ↳ | **2026-08-05: it was run, and the answer is that R-1 does not reproduce on macOS.** The experiment was the **ssh + tmux** one, into a `yantra-gui` server the owner started at the Mac's own keyboard in Terminal.app — not a GUI-login tmux — and it watched for **90 s**, the default window. The mtime moved through `1785938331`, `1785938359` and `1785938384` while the turn count climbed `2` → `4` → `6`, `session_attached=0` before every reading, and the agent answered, which is what carries this past **I-53**. What stopped an earlier attempt is the reusable finding rather than a result: an agent holding at the trust dialog is inert and swallows the harness's keystrokes (I-23, I-49), so the harness refuses and names the directory instead of reporting a false *stopped*. **The honest limit**: 90 s of an active conversation showing the failure mode did not occur is not proof that an intermittent one cannot. | | |
| Q15 | **Which box, and does it change the target?** *Pi 5 / N100* names two architectures and `just appliance*` builds only `aarch64`. An N100 is x86_64, is faster, takes NVMe rather than an SD card (R6: *"SD cards, not SQLite, are the durability risk"*) and costs more idle power; a Pi 5 is the board M8's device-tree overlays are written for, and M8 follows this milestone. **Prior: the Pi 5, because M8 is that board's work and two boxes is one too many.** [the M7 plan](docs/plans/m7-appliance.md) §6.<br>**[Y-152](docs/archive/m6-m7.md) found this question is larger than the three arguments above, and they are the smaller half.** [R6](docs/research/06-runtime-feasibility.md)'s *hardware layer is file descriptors* is a property of **RP1**, not of Linux: an N100 has no header, no RP1 and no device tree, so there is no overlay to reach for and a microcontroller stops being an alternative and becomes the only path. **This question therefore decides M8's architecture, not only M7's build target** — Pi 5 makes M8 roughly 150 lines of Rust over three file descriptors, an N100 makes it a firmware project with a serial protocol and a flashing story. [Research 11](docs/research/11-appliance-hardware.md) §1.<br>**2026-08-06: four requirements this question was never asked against, and they change what the argument is about** — [research 11 §8](docs/research/11-appliance-hardware.md#8-what-to-buy). A **€120 ceiling** covering the board, its storage and whatever it takes to boot; **sourced in the EU**, or from a Chinese seller if genuinely cheaper, with **second-hand acceptable**; **24/7 operation, so idle draw is the figure and peak is not**; and a **printed enclosure**, so no case is budgeted. **Three of this question's own clauses move.** *"An N100 costs more idle power"* is confirmed and much larger than implied — 10–14 W from the three sources that state a method, against a Pi 5 at ~2.7 W — but an N100 is now out on **price** before power reaches it, with a €164 floor for a barebone and €229.99 for the cheapest complete machine. *"Takes NVMe rather than an SD card"* rests on R6's *"put the database on NVMe"*, and [ADR-0004](docs/adr/0004-rust-for-the-daemon.md)'s 2026-08-02 amendment (Y-044) deleted the database — what writes to the card is the OS, which is a tunable rather than a requirement, so **NVMe stops being something the design needs**. And *"a Pi 5 is the board M8's overlays are written for"* survives but was overstated in §1: an MCU on USB costs about €5, a USB-HID encoder reaches the host as **evdev — the same interface the overlay would have given** — so the real cost is not difficulty but a **second deployable artifact against an install path that covers exactly one** — plus a recovery story that needs the dead firmware's cooperation, since `picotool -f` only works while it is still running.<br>**What this does to the prior: it survives, on different reasoning and with a variant attached.** Memory prices tripled the large boards and Raspberry Pi has explicitly held the 1 GB and 2 GB ones, so the cheapest Pi 5 is now the only one whose price held — **2 GB at €68.90, or 1 GB at €46.90**, both BerryBase 2026-08-06. A complete Pi 5 2 GB box is **€115.10** and fits; the runner-up, a used Futro S740 with a Pico 2, is **€50.49** and loses on the second artifact rather than on price or power, where §8.4's arithmetic gives it nine years to repay. **Still the owner's ruling ([§B0](CLAUDE.md)), and this row does not make it.** | Y-145, Y-149, Y-150, **M8** | ✅ **ANSWERED 2026-08-09 — the Pi 5, 2 GB**, on [R11 §8.7](docs/research/11-appliance-hardware.md)'s basket: board, card, supply and cooler, €115.10. **Nothing about the build target changes** — `just appliance*` stays `aarch64` — and **M8 stays the RP1 path** rather than a firmware project. The runner-up lost on a second deployable artifact, never on price or power; R11's flip conditions stand and are checkable. [Y-150](#3-task-board) and M7's reboot demo now wait on the order arriving rather than on a decision. |
| Q16 | **How much may a notification say, given that ntfy.sh is a public relay whose topic is its only password?** Naming a workspace and a machine publishes the fleet's shape — R-22's own list — to a third party. Self-hosting keeps the body on the tailnet and still sends a content-free trigger through ntfy.sh for the iPhone ([the M7 plan](docs/plans/m7-appliance.md) §3.4). **Prior: name the workspace and the verdict, never the machine or the repo, and revisit if self-hosting proves cheap.** | Y-146, Y-147 | ✅ **ANSWERED 2026-08-06 — the relay is a channel, and the question was narrower than the answer** ([Y-147](docs/archive/m6-m7.md)). **It is a general publish channel, not the notifier's private wire**: any session or agent can push a message — session status, *needs attention*, a workflow dispatch, a critical reminder, context used, limits remaining — and the fleet notifier is simply its first caller. The owner's words: *"each session gets something to use, and we can use it later for something else."* **The design consequence is that there is no message taxonomy to build**: every one of those is a body string, so there is no enum of kinds, no category field and no routing layer, and *one channel per session* is a second URL rather than a second feature — the topic lives in the URL, which makes it configuration. **The disclosure posture is that Yantra ships the mechanism and never invents content**: the body is whatever the caller passed, the operator chooses where it goes, and the documented default is a self-hosted instance or a high-entropy topic, because on ntfy.sh the topic is the only password there is. **This is broader than the prior this row recorded and supersedes it** — *name the workspace and the verdict, never the machine or the repo* now describes what [`yantrad`](crates/yantrad/CLAUDE.md)'s notifier chooses to send rather than what the channel permits, and it is unchanged as behaviour: `Notification` still has no field for a machine or a repo. |
| Q17 | **Is the appliance enrolled tagged or untagged?** Tagged is the standard headless answer and may make the daemon refuse every write ([the M7 plan](docs/plans/m7-appliance.md) §3.2); untagged means a key that expires and a human who has to notice. Disabling key expiry for one node is the third answer, and it is an admin-console action rather than a code change. | Y-143, Y-150 | ✅ **ANSWERED 2026-08-09 — tagged**, with [Y-143](#3-task-board) as a **condition on the ruling rather than a follow-up to it**. The hazard is `[D]`: it is read from `inventory.rs`'s `owner()` against [ADR-0016](docs/adr/0016-the-dashboard-writes-and-tailscale-identity-authorises-it.md) and has never been measured, because **no node on this tailnet carries a tag**. **If the measurement returns *refuses every write*, this answer is what changes** — either ADR-0016 gains an amendment or the enrolment falls back to untagged with expiry disabled for that node. Y-143 needs two owner actions first, neither of them code: a `tagOwners` entry in the policy file, then an **ephemeral, pre-approved, single-use** key minted against it. |
| Q18 | **Does the appliance keep its own `~/.ssh/config`, or does the fleet move to `tailscale ssh`?** The first is a file to maintain twice; the second removes key distribution, stays inside I-20, and is ACL work — and it cannot cover Windows (R-7), which Q4 has left open anyway. [the M7 plan](docs/plans/m7-appliance.md) §3.3. | Y-144 | ✅ **ANSWERED 2026-08-08 — the appliance keeps its own `~/.ssh/config`.** Tailscale SSH serves only from Linux and from the open-source `tailscaled` build on macOS, never the standard macOS app (R-7), so the Mac in this fleet could not answer it without being rebuilt. One transport that reaches every machine beats a better one that reaches most of them: [ADR-0006](docs/adr/0006-ssh-exec-transport.md) and [ADR-0009](docs/adr/0009-machine-names-are-ssh-destinations.md) stand unchanged, and the cost accepted is a host list written down a second time. |
| Q19 | **Does the container fixture extend to a systemd unit at all?** §B3 says verification is a real thing in a disposable podman container, and this repo's fixture runs a real sshd and a real tmux. A real `systemd` in a container is a different proposition: it can show that a unit parses, starts, restarts and is enabled, and it cannot show a boot ordering against a real `tailscaled`. **If the answer is no, the honest fallback is a `--user` unit on this machine and a `[D]` that stands until the box exists** — a worse test and a better admission than a passing one that proves nothing. [the M7 plan](docs/plans/m7-appliance.md) §6. | Y-142 | ✅ **ANSWERED 2026-08-05 — yes, up to boot ordering** ([Y-142](docs/archive/m6-m7.md)). A real systemd as PID 1 in a disposable podman container took the real unit and the real `yantrad`, and showed the property the whole design rests on: the refusal `listen_on` makes at boot is *retried* — four restarts, still `activating (auto-restart)` at 45 s — where the same unit at systemd's default `RestartSec` reached the start limit and sat in `failed` in under eight seconds. Fedora rather than the sshd fixture's Alpine, which ships no systemd at all. **The boundary is exactly the one this question predicted**: nothing in a container is `tailscaled`, so `After=tailscaled.service` is asserted by nothing. The fallback's `[D]` was not needed for the restart half and is still owed for the ordering half, which closes with the box. |
| Q21 | **Does the phone reach a session through Yantra's own terminal, or through `claude --remote-control`?** The flag is real — measured on Claude Code 2.1.226 — and hands over Anthropic's own mobile UI for nothing. | [Y-185](docs/archive/m11-m13.md); any *open this on my phone* affordance | ⬜ open — it also **routes the session through claude.ai**, which is outside the tailnet, so it spends the one property this project is built on: no public ingress, and nothing leaving the network the owner controls. Yantra's own path already carries bytes end to end (M6). Recommendation: keep ours as the default and offer the flag as a per-session opt-in, never as the way the product works. |
| Q20 | **Where does a GitHub credential live when `yantrad` runs on the appliance?** [M11](#2-milestones) works because `gh` holds the token on the machine the daemon runs on. `docs/appliance.md` creates that account as `useradd --system --shell /usr/sbin/nologin`, so it has no keyring, and [R13](docs/research/13-dashboard-revamp-and-github.md) §2.6 records `gh`'s documented fallback there as a plaintext file — a stored secret with a different name. | M11 on the appliance; any repo browser | ⬜ open — answering *the daemon holds a token* supersedes [ADR-0004](docs/adr/0004-rust-for-the-daemon.md)'s 2026-08-02 amendment, rewrites §B1's *"The daemon persists nothing"* and re-opens Q5. R13 §2.1 also found the inversion: a GitHub App's 8-hour token plus rotating refresh is **worse** here than an OAuth App's non-expiring one, so "use the App, it is more secure" is the wrong instinct. Not an implementation PR's call. |

---

## 7. Risk register

Risks that can invalidate a design, ordered by damage. A risk is retired only by evidence, not by optimism.

| # | Risk | Impact if real | Status | Retire by |
| --- | --- | --- | --- | --- |
| ~~R-1~~ | ~~Claude Code #63545: detached tmux stops writing transcript JSONL~~ | — | 🟢 **RETIRED by Y-023** — did not reproduce on 2.1.220; transcript written while detached within 5 s. **The macOS evidence Q12 said was missing exists since 2026-08-05** ([Y-139](docs/archive/m6-m7.md)): on `bishwajeets-macbook-pro`, the OS #63545 was filed against, the mtime moved through three values and the turn count climbed `2` → `4` → `6` across 90 s with `session_attached=0` before every reading. One run says the failure mode did not occur, not that an intermittent one cannot. | — |
| R-2 | Gemini CLI self-terminates ~5s after TTY loss, **exit code 0**. | Silent, successful-looking death of an automated session. Exit code 0 means naive health checks report success. | 🟡 known, mitigable | Set `SANDBOX` env, or forbid non-tmux launches |
| R-3 | Agent CLIs churn fast — OpenCode ships hourly builds with channel-dependent DB filenames; Codex removed three documented flags this cycle. | Yantra's agent plugins break without any change on our side. | 🟡 accepted | Pin agent versions; treat R3 note as perishable |
| R-5 | Coder/DevPod may already solve this well enough for a personal setup. | Project's justification. | 🟢 **retired by R4** — Coder cannot represent a never-destroy, always-on personal machine. Sign-off still open (Y-021). | — |
| R-6 | **Scope creep into provisioning.** The gravitational pull from "adopt a machine" toward "create a machine" is strong and would make Yantra a worse Coder. | Project loses its only defensible niche. | 🟡 permanent | Written as a non-goal in [§1](#1-guardrails-read-before-writing-any-code) and CONTRIBUTING.md — **not** in an ADR, despite what this cell said until Y-080; re-check at every milestone |
| R-7 | **Windows breaks every standardisation.** No tmux; Windows OpenSSH lacks `ControlMaster`; and per R1, **Tailscale SSH cannot act as a server on Windows** — so one uniform SSH mechanism across the fleet is impossible. | Transport + session design silently assumes POSIX; a second code path appears late in M2. | 🟡 open | Q4 decision — declare Windows = WSL2 for M1–M3 |
| **R-22** | **The bind address is the entire security model.** Q6 removed auth, so a listener on the wrong interface is an unauthenticated read API over the fleet's shape — machine names, workspace names, repo paths. | Everything `yantrad` can read, readable by anything that can reach the port. | 🟢 **mitigated 2026-07-31 by Y-069, and measured** — but **[ADR-0013](docs/adr/0013-the-heartbeat-carries-only-what-placement-scores.md) widens what it covers**: a heartbeat `POST` is the first *write* into the daemon, so "read-only and unauthenticated" becomes "writable and unauthenticated". The retire condition is unchanged; the blast radius is not. Address comes from Tailscale rather than configuration; no flag can override it; the daemon refuses to start without one. Confirmed on the real tailnet: `ss -ltnp` shows the two tailnet addresses and **no `0.0.0.0`**, and `curl 127.0.0.1:7717` is refused. | ~~A test that asserts the daemon refuses~~ — **shipped**: `it_refuses_to_start_when_this_machine_holds_no_address`. Stays amber-to-green rather than retired while the API surface is still growing.<br>**Blast radius restated 2026-08-02 (Y-108), because the daemon is now writable.** The boundary is unchanged and so is its retire condition; what changed is what sits behind it. Anything that reaches the tailnet can now write a heartbeat — and the tailnet is not a set of trusted servers: it includes a phone and a tablet, and R1 found **two of five peers holding expired keys while still in the netmap**. A forged beat can make a machine look free when it is not, or overwrite the real agent's row by racing it. **What it cannot do is escalate**: the payload is data for a score and never a path, name, command or filename, so nothing in it reaches the layer where [ADR-0006](docs/adr/0006-ssh-exec-transport.md) turns a string into a remote shell command. The worst outcome is a session placed somewhere that then fails to start — a denial of usefulness, not an execution primitive. Three things bound it, in order of what they cost: identity from the **source address**, so a body cannot claim to be another machine; a **4 KB** limit and strict types with unknown keys rejected; and a write to **memory**, so a flood costs CPU and cannot fill a disk. **Still not done, deliberately**: no shared secret, no per-agent token, no mTLS, no request signing — a pre-shared key across five machines is a secret Yantra would have to store, and §B4 says it never stores secrets. The trigger to revisit is a node on the tailnet that is not the owner's, which is a Q6 change before it is a technical one. |
| **R-23** | **A cached dashboard tells confident lies.** Every failure mode of the read model looks like working software: a stale session list, a machine silently dropped from a snapshot, an empty page that actually means "the first refresh has not run". | Worse than no dashboard — a page that is wrong in a way nobody can see is trusted anyway. | 🟡 **new with M4.** Mitigated by the three rules in [the plan](docs/plans/m4-web-ui.md) §5.2, each a *display* requirement and not merely a data one. I-47 is the same mistake one layer down. | Age shown on every reading; unreachable rendered as unreachable; `Option` never defaulted to empty. |
| **R-24** | **The JS toolchain leaks into the Rust build.** Starts as a convenient `build.rs`, ends with `cargo clippy` and the `aarch64-unknown-linux-musl` cross-build needing npm. | Every CI job and the appliance build grow a dependency on a toolchain none of them need. | 🟢 **RETIRED by [Y-148](docs/archive/m6-m7.md) on 2026-08-06**, by enforcement rather than by observation. [Y-140](docs/archive/m6-m7.md) built the mitigation this row named — embedding behind `yantrad`'s `embed-dashboard`, absent from `default`, with the build that turns it on in [`embed.yml`](.github/workflows/embed.yml) rather than `ci.yml` — and added `just no-node` to `check` so the four *structural* ways it was most likely to arrive are a red build rather than a reviewer's job. Y-148 added the behavioural half, `just build-without-node` in `ci.yml`: the gate's own `build` and `lint` run with `node`, `npm` and `npx` shadowed by stubs that exit non-zero, so a `build.rs` that shells out by name is red on the pull request that adds it. **Retired does not mean unenforced, and every rule this row wrote stays a rule** — `--all-features` in no recipe and no workflow, the feature out of `default`, npm nowhere in `ci.yml`. What changed is that both halves are asserted on every pull request instead of resting on nobody having done it yet. | **Retired, and the residual is named rather than dropped.** The condition as written — `cargo build` green on a machine with no Node, measured 2026-08-05 — was **already true before Y-140** and was therefore the wrong test. The replacement was run *and made to fail*: with a temporary `build.rs` calling `Command::new("npm")` in `yantra-agent`, the whole of `just check` was green and `just build-without-node` was red at `cargo build`. **What it does not catch, measured on the same probe**: an absolute path to the `node` binary itself, and a JS runtime under another name. `npm` and `npx` by absolute path *are* caught, their shebang being `#!/usr/bin/env node`. The aarch64 cross-build is not run under the stubs, because build scripts run for the host whatever `--target` says. Each of those needs the workspace's first `build.rs` — there is still none — written to reach a toolchain by absolute path, which is a different risk from the convenience this row describes and arrives in a diff that says so. |
| **R-21** | **macOS agents cannot authenticate over SSH.** Claude Code's account token lives in the login keychain; an ssh session runs in launchd's `Background` domain and cannot read it (**I-44**). The failure is silent at the keychain layer and only visible as *not logged in* in the agent's own TUI. | M3's whole demo — `yantra up demo --agent claude` — produces a running, healthy-looking, **useless** session on macOS, which is one of the two online machines in this fleet. Everything downstream (`logs`, status) reports success. | 🔴 **open, new 2026-07-30** — verified end to end, not inferred. **The decision is made rather than proposed: [ADR-0018](docs/adr/0018-the-tmux-server-carries-the-macos-login-session.md) was accepted 2026-08-05 (Y-122), on a premise measured at the Mac's keyboard that day and holding** — a window forked by a tmux server started from Terminal.app reads the keychain and answers `loggedIn: true`, where plain ssh on the same machine, same `$HOME`, does not. **Deciding is not fixing**: this retires when ADR-0018 §1 and §5 are shown to work on that machine and M7 installs §7's launchd job. [Y-151](docs/archive/m6-m7.md) wrote §1 and §5 on 2026-08-05 — `up` refuses to start a tmux server there, and the gate now runs through `tmux run-shell` in the server that will fork the agent — and **both were measured on that machine later the same day**: against a tmux server started at the Mac's own keyboard in Terminal.app, `just test-mac bishwajeets-macbook-pro` found a credential in that server — `loggedIn true, authMethod claude.ai` — where ssh in the same run found none, and [Y-139](docs/archive/m6-m7.md) then had Claude Code answer in it. **This row stays open because the other half is M7's**: ADR-0018 §7's launchd job is what puts such a server there with nobody at the keyboard, and until it is installed a Mac that has not been logged in at fails exactly as this row says. | **Y-059.** Candidates: launch the agent inside a tmux server started from the GUI session (measured on 2026-08-05 and it works — Y-122), a launchd LaunchAgent in `gui/<uid>` (what R-12's per-machine agent already has to be), or a credential *reference* resolved at launch per Q5. `security unlock-keychain` is not a candidate — it needs a password. **ADR-0018 picks the first and prices the second out**; [research 10](docs/research/10-claude-code-credentials-on-macos.md) killed the third, every credential path it found being a secret *value* §B4 refuses. |
| R-8 | **`Online: true` means "the control plane sees it", not "you can reach it".** Also `Expired` nodes need surfacing. | Scheduler happily places work on an unreachable machine; `yantra up` hangs instead of failing fast. | 🟡 known, mitigable | Probe with `tailscale ping` before placement; treat `Online` as a hint only |
| ~~R-19~~ | ~~**Cross-compiling Rust to macOS from Linux is genuinely hard** (needs the Apple SDK via osxcross). The per-machine push-agent (R-12) must run on macOS and Windows.~~ | Build tooling friction for the agent, not the daemon. Worst case, the appliance cannot build the Mac agent by itself. | 🟢 **RETIRED 2026-07-31 by Y-037**, on its own stated condition. Both Apple triples build on a **macOS runner** with Apple's own toolchain — one runner carries both slices — so nothing is cross-compiled to Apple from Linux and osxcross never enters the picture. Green on the first attempt, 155 KB and 161 KB archives. The risk was real; it is simply not on Yantra's path. | Build on native runners in CI rather than cross-compiling. `aarch64-unknown-linux-musl` for the appliance is unaffected and stays trivial. |
| R-20 | **Rust punishes design churn, and M0–M1 is exactly where the design is still unknown.** | Slower first month; a wrong early abstraction is more expensive to unpick than in TS. | 🟡 accepted knowingly | ADR-0004 records this as the consciously-paid cost. Mitigation: walking skeleton first, narrow traits, resist abstraction until the third use. |
| R-12 | **The push-agent has an install-and-update story on every machine.** Q3's answer converts Yantra from "pure orchestrator, zero footprint" into "ships software to five heterogeneous machines" — including macOS launchd, Windows service, and Linux systemd. | Real, permanent scope increase. Also weakens the "we only orchestrate" identity if it grows. | 🟡 accepted, must stay tiny | Keep the agent to heartbeat-only; no logic, no versioned protocol beyond a JSON blob |
| R-13 | **macOS and Windows telemetry commands are doc-sourced only.** R5 verified the Linux commands locally; the other two OSes were not run on real hardware. | The scheduler mis-scores exactly the machines it was built to choose between. | 🟡 unverified | Verify during Y-022, on the real Mac and Windows box |
| R-10 | **Hostname collisions are already real** — R1 found HostName colliding **twice in your existing 5-node tailnet**. | Keying anything on hostname corrupts inventory from day one. | 🟢 avoidable by design | Key on `Peer.ID`; treat HostName as a display label only |
| R-24 | **`web/src/terminal.test.tsx` is flaky, and it is the only suite that drives a real WebSocket server.** Two cases fail intermittently — *does not reopen a socket the daemon gave a reason for* and *gives up after a bounded number of attempts* — roughly two runs in three. | A gate that fails at random is a gate people learn to re-run, and this is the one suite standing between a regression and D1 §4.5's interactivity guarantees. It also hides the next real failure in the file. | 🟡 **open, and pre-existing** — reproduced three times on `main` at `ddb6708` in a clean worktree, so [Y-190](docs/archive/m11-m13.md)'s two new states did not cause it. Found 2026-08-11 while verifying M13. | A run that fails zero times in fifty, and a named cause — the two cases both turn on `ATTEMPTS` and a 500 ms real timer, so a fake clock is the first thing to try. |

---

## 8. Session log

**Moved to [docs/session-log.md](docs/session-log.md)** on 2026-07-31 (Y-068) — append there, not
here. It was 40 KB, a third of this file, and it is read approximately never, while this file is read
first by everyone. M0 and M1's entries are further back, in
[docs/archive/m0-m1.md](docs/archive/m0-m1.md).
